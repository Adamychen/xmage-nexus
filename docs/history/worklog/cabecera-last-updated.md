# Cabecera "Last updated" archivada

> Línea 3 de `PROJECT.md` tal cual estaba el 2026-09-21, cuando pesaba ~26 KB: había acumulado un segundo changelog dentro de la cabecera. Copiada sin resumir. El detalle por fecha está en `PROJECT.md` §9 y en los archivos de este directorio.

> Last updated: 2026-09-21 (**Pulido UI**: flechas del stack sobre el drawer, `select` unificado, `DropdownMenu` y pie de Mazos con menús; ver Work Log); (**Panel derecho eliminado**: sin cabecera ni columna lateral; turno, fases, selector e iconos (pila, log, chat, mazo, comandante, mecánicas) viven en una franja sobre el divisor del tablero con un único desplegable con scroll, y el botón Pasar flota abajo a la derecha; ver Work Log); (**Rediseño UI fases 18–21**: `<button>` sueltos de 259 a 91 con `Button` (variantes `soft`/`soft-danger`/`link`), `IconButton`, `ChipButton`, `MenuItem` y `Tabs` (≈ −1.900 líneas netas, guarda de ratchet); **Rediseño UI fases 12–17**: `EmptyState`, `Checkbox`/`Switch`, `CloseButton` danger, `Chip` con tamaños + `ChipButton`, base de inputs en capa `base`, `useEscape`, `TranslationSchema` derivado de `en.ts`, claves i18n y CSS muertos fuera (≈ −4.500 líneas netas); **Rediseño UI fase 11**: `CloseButton` único con `aria-label` siempre (25 botones de cerrar migrados, ~−380 líneas netas); **Rediseño UI fase 10**: componente `Tabs` con roles ARIA y teclado, 8 grupos de pestañas migrados; **Rediseño UI fase 9**: `Button` como sistema único de botón en la capa `components` (50 botones migrados, −306 líneas netas); **Rediseño UI fase 8**: colores literales 468→0 (guarda en 0), paleta única de maná/rareza y corrección de colores categóricos aplanados por las fases 2–3; **Rediseño UI fase 7**: colores literales 2.702→468 con tokens de overlay `--veil-*`/`--shade-*`; **Rediseño UI fase 6**: `TournamentPanel` pasa a la primitiva `Modal` y `--dlg-*` a tokens; **Rediseño UI fase 5**: escalas `--fs-*` y `--r-*` aplicadas a todas las hojas (fuentes literales 941→44, radios 579→6); **Rediseño UI fase 1**: tokens `ui/tokens.css`, primitivas `Button`/`Chip` y acento azur+latón aplicado a Login y Lobby; **Presupuesto de peticiones a Scryfall**: cliente único (`scryfallClient.ts`) con cola de 10 req/s, tope de 4 simultáneas, pausa global en 429 y caché IndexedDB; el editor de mazos pasa de 64 peticiones/s a ≤10 (e2e `scryfall-budget`) ; **Galería de mazos**: el badge deja de mostrar ✓ en Commander con pareja de comandantes ilegal (`DeckBox` valida con metadatos de los comandantes) ; **Diálogo de problemas del mazo sin salida corregido**: botón `Aceptar y jugar` para cartas que el servidor carga como otra (Andúril→Anduril) y `sameName` insensible a diacríticos; ver Work Log) (**Handshake de beta desmitificado**: el fallo "intermitente" de login anónimo a `beta.xmage.today` documentado desde Fase 0 como bug de handshake del servidor remoto **no existe** — era el rechazo del servidor a usernames de más de 14 caracteres (`maxUserNameLength`), y `Can't receive server state before other data` es solo el síntoma de *cualquier* login fallido (el cliente pide el server state tras el login). Medido: 13/14 chars ✅, 15+ ❌, y 17/17 logins correctos contra beta. Corregidos los 5 documentos que propagaban el mito, `FixtureServer` valida ahora el username como el servidor real (red anti-drift) y 13 usernames generados en 7 specs e2e acortados a ≤14; sin cambios Java. Ver Work Log) (**U4-2 mesas revertido**: el disable de la contraseña de **mesa** en `beta.xmage.today` partía de una premisa falsa para mesas — el motor comprueba la password de mesa siempre (`TableController.joinTable`/`joinTournament`, `Wrong password.`), independiente de `authenticationActivated=false` (que solo afecta al login de cuenta); verificado en vivo con 2 cuentas contra beta (incorrecta → rechazada, correcta → join) y en UI (mesa privada creada con `passworded:true` + diálogo de unirse con campo activo y pre-relleno). `JoinTableDialog`/`useCreateTableForm`/`SecurityTab` ya no deshabilitan ni limpian la password; el login/Setup conservan el disable (ahí sí se ignora la de cuenta). Claves i18n huérfanas + CSS eliminados, tests reescritos; unit 1872/1872 + typecheck + build) (**PLAN-7**: CI remoto rojo desde 09-13 diagnosticado y arreglado — sello de commit del fork para los artefactos `org.mage` (caché Maven), bug real de preview huérfano en hover, 4 e2e solo-CI endurecidos; fase 4 launcher marcada publicada; plan4/5/6 cerrados → lista viva en `docs/history/plan7.md` §4; suite local 9/9. **release v0.2.0 preparado**: workflows `modules.yml`/`release.yml` restaurados y adaptados al fork (`workflow_call`, checkout rama `nexus` vía `NEXUS_FORK_DIR`), versión 0.1.0→0.2.0 en web/mcp/launcher y dashboard (`site/content.json` + `status.json`); **release v0.2.0 PUBLICADA** — run `release.yml` 35432670389 verde (34 min: módulos ×3 SOs, bundles firmados y `latest.json`), publicada y marcada latest el 2026-09-19) (**PLAN-5 implementado y verificado (orquestación multiagente)**: Familia A — `sanitizePromptText` en el embudo `prompt()` de `parse.ts` (0 `{this}`/`ICON_*`/tags; corpus real de Slicer + pipeline + galería; unit 1848); Familia B — oráculo `scripts/hint-schema.mjs` + baseline `hint-schema.json` + `hintRegistry` (100 claves) + guarda `hintCoverage` verificada roja en ambos sentidos; D2 badges ×5 (CaseSolved/Harnessed/Evidence/Prepared/Protector + i18n ×9); D3 subconjunto real de 5 specs cableado en `integration-report.mjs` y verificado en vivo (5 passed/2 skipped); D4 opt-in visual; D5 P5 cerrado; D6 seed pospuesto. Suite: unit/coverage/typecheck/build/java/human-test/i18n verdes; e2e 260 passed con 1 flake de hover (`printing-preview`, verde en aislado); `self-test` WATCHGAME era flaky por partidas IA-vs-IA (15/15 en caliente) y el triaje posterior encontró/corrigió un bug real del proxy (`NoClassDefFoundError org/jboss/mx` en `publishLobby`, log de 648 MB): shim `ObjectNameFactory` + throttle de `publishLobby`, verificado con caída controlada del server (recuperación automática, 0 stacks tras el fix)) (**frame real `slicer`** — Slicer, Hired Muscle: ceder un permanente al rival en SU upkeep (trigger OPPONENT + `GAME_ASK`, texto con `{this}` sin resolver), control repintado en la zona del SIM con untap y goad en `rules`/`cardIcons`; manifest 114→115, invariante `hasSlicerCeded`) (**plan4 §5 cierre autónomo**: prompts 5.1 sin ❌ de UI (contexto/reloj, cancelar `required`, `sourceName`, UserRequest, voto) tras cerrar en vivo las celdas ❓ (el servidor no emite `"Waiting for"`; el rival ve pila+prioridad y el reloj sigue visible); heurística 5.2 con evaluador 2 (0 hallazgos S3–4; los 12 hallazgos de las 2 pasadas corregidos, incl. localización de fin de partida/lobby/torneo y `.replace()` de standings); latencia 5.4 instrumentada y medida (`@latency` 7/7, acuse <100 ms con eco de 1,2 s); sensación de juego 5.7 (27 CSS con `prefers-reduced-motion` + `clearFlights` en `GAME_INIT` + `game-feel.spec.ts`); unit 1824/1824, e2e fake 259/4/0. **E2E fake en paralelo**: puerto dinámico por test (`FakeServer.start(0)` + `setFakePort` en `fixtures.ts`/`withFakeServer`/`chat`/`finished-matches`) + `fullyParallel` con 4 workers en fake (real sigue `workers:1` serial, `E2E_WORKERS` para override): suite completa **249 passed / 4 skipped / 0 failed en ~8,1 min** vs 246/4 y 31,6 min del baseline serial; una corrida intermedia destapó 1 flake preexistente por carga (clic hover-dependiente de `decks-gallery`), endurecido con hover+visible+`dispatchEvent` y validado con estrés 27/27 (`--repeat-each=3`) + 3 corridas completas; smoke dirigido 7/7 en 11,3 s y real `priority-stop-real` 1/1 con 1 worker. `autoShot` intacto). (**priority-stop-real verde — el flaky del plan4 §3.5 queda R**: la partida se atascaba en el `GAME_TARGET` de descarte del cleanup porque el locator `.first()` del test apuntaba a la carta más a la izquierda del abanico, tapada por la siguiente (~65% de solape real) → hit-test de Playwright fallido en bucle (443 reintentos medidos en el trace; con `actionTimeout=0` nunca salía) y el descarte jamás se respondía; en corridas previas con la rama de fallback el server re-preguntaba 611 veces. Fix en el spec: `.last()` + timeout de clic acotado (5 s) + espera del cierre del prompt en vez de sleep fijo; **3/3 corridas reales verdes (~18 s)** contando las 3 paradas, sin cambios de producto (el gap real de `maybeAutoPass`/`phaseStops` ya se cerró el 2026-09-17 en `actions.ts:315`). Diagnóstico multiagente (2 exploradores + revisor adversarial sobre trace/logs; ver Work Log). Unit 1780/1780 + typecheck). (**Decks-gallery sin drift**: los 3 tests fake que fallaban en HEAD quedan verdes — el de `precon/Editar` era un solape DEV-only del pill de debug sobre `.decks-edit-btn`, resuelto bajando el pill a `z-index:4` sin mover píxeles (0 baselines tocadas) y los otros 2 eran drift del spec tras el diálogo unificado de importación (`7dca1a7fd85`), alineados a `.decks-import-cta`/`.deck-import-modal`; decks-gallery 9/9, unit 1780/1780, typecheck y matriz visual 6/6). (**Cierre §3 del plan4**: P4 104→**114 frames**; **Suizo R** con `scripts/verify-swiss.mjs` (Constructed Swiss real de 4 `COMPUTER_MAD` × 2 rondas: `Starting→Dueling→Finished`, 2×2 emparejamientos en `rounds[].games`, standings 6/3/3/0 — TODO PASS 20/20) y **espectar R**: parche aditivo en el fork (`GameSessionWatcher.endGameInfo(Table)` + llamada en `GameController.endGameInfo()`, GameSessionWatcher.java:125-129 / GameController.java:899-901, sin cambios de contrato/proxy) verificado con `scripts/verify-spectator-end.mjs` (el espectador que entra a partida en curso recibe `END_GAME_INFO` con `GameEndView` no nulo — TODO PASS 17/17); 2HG fuera de alcance con causa (gameType inexistente en el config del servidor) y "casos" fuera de alcance con corrección de diagnóstico (el motor SÍ implementa Case), ver plan4 §3.9/§3.11. Unit web 1780/1780 + typecheck). (**Space = atajo global en la mesa**: en la región `.game-body` `Space` siempre pasa prioridad/confirma y las cartas se activan con Enter; el conflicto venía de `clickableProps` (role=button con Space) + el guard del atajo — fix por región `data-space-passes-priority` + test e2e rojo→verde en `control.spec.ts`; con un visor abierto `data-space-shortcut-off` bloquea el atajo; unit 1769/1769). (**P3 — galería de estados cerrada**: 124→**151 entradas** con la matriz §4 (cargando/error/vacío/desbordado/idioma largo/CJK/zoom/reconexión) y tablero Pod 2×2/Arena/Pod Commander; `VISUAL_ENTRIES` 21→48 y matriz navegador×resolución con `scripts/gallery-visual.mjs` (chromium+webkit × 1366×768/1920×1080/2560×1440 + 1600×900) y **336 baselines macOS**; 2 causas de no-determinismo cerradas — carrera de medición en `CombatArrowsOverlay` (geometría entera + recompute tras asentar) y animaciones infinitas no congelables en WebKit (`@media prefers-reduced-motion` en 19 CSS + `reducedMotion: 'reduce'`; cubre §5.7) — y bajo movimiento reducido los trazos de flechas/targeting pasan a sólidos (`stroke-dasharray: none`) para eliminar la fase de guion (el jitter de dash llegaba a 2 820 px en `board:arena-4`@2560), quedando `maxDiffPixels: 1200` como margen para el AA del marcador; pendiente CI con baselines Linux). (**Mindslaver/control total del turno ajeno (G12-1)** — el cliente web ya actúa el turno del jugador controlado: helper `web/src/state/control.ts` (`opponentHands` + `priorityPlayerName`/`activePlayerName`), `canPass` y `ActionButton` habilitados con "Controlas el turno de {name}" (`game.controlling_turn` ×9), Switch Hands elevado al store (`switchedHandKey` + poda en `eventHandler`) alimentando la `handBar` del shell en standard/arena/pod (`switchedHandCards`), zona del controlado interactiva (playables + combate vía `handleCardClick`) y guarda anti auto-pass. **Verificado en vivo 2026-09-17** (MCP + navegador, partida real vs SIM local con Mindslaver activado de verdad y la web attachada a la misma sesión): el pase desde el botón habilitado avanza el prompt del servidor (seq 40→41), el Switch Hands muestra la mano del controlado en la barra propia (8 cartas) y el clic en su Isla jugable la gira (`tapped:true` + `manaPool blue:1`, seq 42→43). Sin cambios Java (el server enruta `sendPlayerBoolean`/`sendPlayerUUID` del controlador). Tests: unit `control.test.ts`/`ActionButton.control.test.tsx`/`GameBoard.control.test.tsx`/`BoardZone.handSwitch.test.tsx`/`store.test.ts` + e2e fake `control.spec.ts` (`@control`, incluye la ventana de combate del controlado). Docs `COMPONENT_PARITY`/`INTERACTION_COVERAGE`/`plan4` §3.10 y §9.2 revertido). (**Mulligan gratis de multijugador/Brawl (CR 103.5c)**: el "Gap de motor" del plan4 era un falso diagnóstico — la exención es por jugador, no del que empieza; el wizard ya preselecciona 1 en FFA/Brawl/Commander multijugador, `mage_create_table`/`rec-lib` exponen `freeMulligans` y el frame `commander-free-mulligan` está re-grabado con 2 mulligans ⇒ mano 6, no 5). (**Cierre post-campaña de frames**: **P2 fidelidad v3** — adjuntos y tamaños de mano/biblioteca/cementerio/exilio con data-attrs nuevos y regresión e2e ×7 frames reales, 0 discrepancias; **fix de `SimPlayer`**: reparte Fact or Fiction de verdad (`targets=null` + `options.possibleTargets` + "Done") y `fof.json` re-grabado; **Conceder en el `MulliganDialog`** (el server ya lo honraba; i18n ×9 + tests); regresión detectada y corregida del escenario fake del Ring-bearer; suite fake 231 passed [3 fallos `decks-gallery` preexistentes del WIP de `web/src/decks/*` + 1 `draft.spec` flaky: 5/5 en solitario]). (**P6 v1 fuzzer self-play**: `scripts/fuzz.mjs` — 2 sesiones WS independientes [HUMAN política ingenua + SIM ya probado] con mazos reales al azar del pool de 1172 `.dck` del fork; vigila bloqueo/callback desconocido/error fatal por partida [P2-discrepancia vía navegador queda pendiente]. Verificado en vivo: encontró y corrigió un bug real del propio fuzzer — bucle de `GAME_SELECT` disparaba el anti-flood del Gateway [`1008`, 100 msg/s]; el primer fix ingenuo [descartar respuestas] rompió el mulligan y colgó 8/8 partidas en turno 1; fix correcto: cola a ritmo seguro sin descartar. Con el fix: 8/8 sin cierres espurios, 1 stall real candidato sin confirmar reproducibilidad por falta de seed determinista). (**P5 auditoría JsonUtil**: en vez del cliente de referencia Java completo [innecesario: `ProxyClient` no tiene switch por callback y `callbackCoverage.test.ts` ya cubre "prompt saltado"], se auditó la fidelidad de contenido de `JsonUtil.toJson()` y se encontró/corrigió un bug real — `ExileView`/`MutateView` [extienden `CardsView`→`LinkedHashMap`] perdían sus campos propios `name`/`id` en silencio porque `writeValue()` tomaba la rama `instanceof Map` antes de la reflexión de campos; `boardShared.ts`/`crossZone.ts` ya leían `exile.cards`/`.name` [clave que nunca existía] y `fidelity.ts` tenía el mismo punto ciego con cartas exiliadas; `BoardZone.tsx`/`deckTracker.ts` funcionaban por coincidencia leyendo `mutateView` como mapa plano. Fix + esquema + consumidores + migración estructural de los 101 frames de P4 [`name`/`id` opcionales en frames viejos, irrecuperables; `cards` se recupera en todos]; bonus NaN/Infinity→null. Cero cambios en `../xmage-fork`. `Mage.Proxy` 100% verde, typecheck limpio, `vitest run` 1692/1692). (**P4 14ª tanda**: 3 frames más — emblem, commander-zone, karn-restart; manifest 100→103. El §3 queda sin filas grabables pendientes: solo multijugador §3.11 [harnés multi-sesión], mulligan gratuito de Commander y los gaps de cliente G12-1/G12-2. Hallazgos: los emblemas van en `players[].commandList` como EmblemView [no en `myHelperEmblems`]; el recorder gana `driver.tableGameType` [mesa Commander de verdad, sin parches] y el impuesto viaja en `rules` del comandante; el reinicio de Karn llega solo como `GAME_UPDATE` [sin GAME_INIT/GAME_OVER]). (**P4 13ª tanda**: 6 frames más — dungeon, the-ring, concede-mulligan, initiative, radiation, starting-player; manifest 94→100. Hallazgos: dungeon es **estructurado** (`players[].commandList` DungeonView, sala en `rules[0]`); The Ring no tiene campo de nivel/portador (emblem `rules.length` + `cardIcons RINGBEARER`) y se corrigió `MechanicsTray.findRingBearer`; **el server sí honra CONCEDE en el mulligan** (la carencia es de UI) y ese fin de partida emitió `phase`/`step`/`activePlayerId` a `null` → contrato ampliado a nullable + regenerados; `players[].initiative` existe; el contador de radiación se llama **`rad`** y no es un upkeep; `skipStartingPlayerChoice:false` nuevo en `rec-lib` [GAME_TARGET solo al ganador del sorteo]). (**P4 12ª tanda**: 6 frames más — plot, floating-mana, attack-battle, always-attack, day-night, cloak; manifest 88→94. Hallazgos: el plot exilia con `faceDown:false`; el aviso de maná flotante es un `GAME_ASK` con `confirmEmptyManaPool`; **la batalla a atacar debe ser propia** (`ProtectedByOpponentPredicate`); "ataca cada combate" viaja como icono `hint:"Must attack (…)"`; **day/night no tiene campo en `GameView`** (hint en `rules` del DFC + chat); el trigger modal de cloak llega como `GAME_CHOOSE_ABILITY` y deja `cloaked:true` + ward {2} en `rules`; morph/manifest/disguise/cloak quedan completos). (**P4 11ª tanda**: 6 frames más — stun-oil, colorless-c, coin-dice, fail-to-find, disguise, win-effect; manifest 82→88. Hallazgos: stun/oil viven en `permanent.counters`; `{C}` trocea el pago (`Pay {1}{C}` → `Pay {1}`) con Wastes; dado/moneda solo viajan como texto al chat (sin campos en el view); el "fail to find" SÍ existe — `flag:false` en el TARGET cuando el filtro tiene predicados (básicas) y `sendPlayerBoolean(false)` lo resuelve; disguise = `disguised:true` + ward {2} solo en `rules`; y `rec-lib` ampliado para capturar `GAME_UPDATE` plano y `GAME_OVER` [fin de partida]). (**P4 10ª tanda**: 6 frames más — hexproof, choice-color, goad, attack-pw, switcheroo, monarch; manifest 76→82. Hallazgos: hexproof no es objetivo legal vs ward; `GAME_CHOOSE_COLOR`/`_NUMBER` no existen en 1.4.61 [color = CHOICE string, número = GET_AMOUNT]; goad solo por `rules`/`cardIcons`; atacar PW elige defensor con `GAME_TARGET` [nunca `special` con 2+]; Switcheroo autoelige con 2 legales; Monarch es `players[].monarch`, no `designationNames`). (**P4 8ª+9ª tanda**: 12 frames más — ponder, fof, manifest-mechanic, mass-tokens, class-level, poison, twincast, ward-no-pay, improvise, discover, trample-deathtouch, cant-block; manifest 64→76. Hallazgos: el SIM no reparte FoF [targets=null → cancela], ward real es `"Pay {1}?"` y Sythis no tenía Ward, el orden de Ponder son TARGETs secuenciales, Lure vence a la IA con deathtouch, el nivel de clase llega en `rules`, veneno en `PlayerView.counters`, y `GAME_CHOOSE_PILE` estrena hook en rec-lib). (**P4 7ª tanda paralela**: emerge, reduce-cost, double-strike, phasing, brainstorm-order y energy; manifest 58→64; hallazgos: emerge lanza sin chooser y reduce por MV, el prompt de maná ya viene reducido, el orden de Brainstorm son 2 `GAME_TARGET` y no `GAME_CHOOSE_CARDS_ORDER`, phasing no cambia de zona). (**P4 6ª tanda paralela**: escape, foretell, ninjutsu, must-block, pithing-needle y cavern; manifest 52→58; hallazgos: el exilio del coste de escape se auto-elige, foretell pregunta "Look at…" al clicar la carta boca abajo, ninjutsu vive en `canPlayObjects.other`, Lure fuerza el bloqueo por regla, la Needle manda ~25k nombres en `choice.choices` (string mode) y Cavern usa 396 `keyChoices`; `cheatSetup` al campo con decisión "as it enters" revienta el WorkerThread del proxy). (**P4 5ª tanda paralela**: 6 frames nuevos — dash, suspend, vigilance, first-strike, surveil y jump-start — con los drivers ya en módulos `scripts/drivers/*.mjs` + `record-sync.mjs`; manifest 46→52; hallazgos: dash usa `GAME_CHOOSE_CHOICE`, suspend es una `SpecialAction` fuera de `GameView.special`, los permanentes cheateados no tienen mareo y el SIM ataca con todo; bug de sonido de daño primero corregido). (**U4-2 login**: el campo Contraseña del LoginScreen/SetupWizard también se deshabilita con aviso cuando el destino es `beta.xmage.today` — el servidor público no comprueba contraseñas). (**U4-2 ampliado**: el campo Contraseña de Crear Mesa también se deshabilita en `beta.xmage.today` — antes solo el de unirse — con aviso ×9 y sin enviar password nunca). (**P4 segunda tanda (5 frames reales)**: drivers `hybrid` (Kitchen Finks híbrido), `anycolor` (Birds paga el {U} de Opt; hallazgo: el motor auto-resuelve el color cuando solo uno es pagable), `planeswalker` (Teferi +1, lealtad 4→5; hallazgo del harnés: las habilidades no se activan enviando su UUID — hay que clicar el permanente y responder `GAME_CHOOSE_ABILITY`; `playAbility` corregido), `saga` (History of Benalia lore:1 + ficha Caballero) y `morph` (Den Protector boca abajo); manifest 35→40, unit 41/41 y replay e2e 39/39). **P3 galería de estados v1**: ruta dev `#/gallery` que monta 44 estados reales (33 frames grabados + 10 prompts sobre el frame gang-block + login) sin stack; spec e2e que recorre todas las entradas sin errores de página y regresión visual opt-in `E2E_VISUAL=1` con 12 baselines deterministas. **P2 fidelidad de render v2**: el comprobador servidor-vs-DOM pasa de permanentes/pila/turno/fase a P/T, contadores, girado, daño, vidas, lado del controlador y prompt visible; data-attrs en `CardSlot`/`PlayerInfoBar` + marcador en `FeedbackDialog`, última ocurrencia del DOM canónica (mutate), regresión `e2e/fidelity.spec.ts` ×5 frames reales con 0 discrepancias. **MCP `mage_cheat_setup`**: la siembra determinista del modo test (P1) ya se opera por MCP — test hermético con FakeServer + verificado en vivo (2×Counterspell/2×Island, la partida sigue y la mesa se limpia); cierra el pendiente de P4. El proceso MCP de una sesión abierta no la tiene hasta reiniciar opencode). **Draft jugable**: la web ignoraba la respuesta de `sendCardPick` (el proxy devuelve el `DraftPickView` con `picking:false`, como el cliente desktop) y no hay evento hasta que todos pickean → «Tu turno» fantasma con el contador a 0 y cartas clicables sin acuse; ahora acuse inmediato («Has elegido X» + espera sin contador), guarda anti-respuesta-obsoleta, `DRAFT_UPDATE`→`picking:false`, anti-doble-pick, progreso sobre/carta, dirección de paso, ring del pick nuevo y nombre por Scryfall cuando el contrato real no trae `name` (UUIDs visibles). Unit 1516 + `@draft` 4/4; **verificado en vivo con draft real 2×HUMAN** (espera larga + re-habilitación), en local y en `beta.xmage.today`). **Maná del Deck Builder**: los repartos de maná pasan de `conic-gradient` de 38px a donuts SVG reales (`ManaPie`) con hueco entre sectores, total central y leyenda color/cantidad/%; unit 1506). (**Live QA de la estela**: mutate apilando verificado en vivo (Under/Over, pilas + badge en el espectador); replay viewer bloqueado por el server (`saveGameActivated="false"` → `games:[]`/`replayAvailable:false`; cliente sin controles ni salida); rendimiento real medido (GAME_INIT sealed de espectador 4,2KB → primer pintado ~27ms; el frame más grande observado, `GAME_UPDATE_AND_INFORM` de jugador, 47KB — sin necesidad de virtualizar); hallazgo: no se puede espectar un match de torneo en vivo desde el lobby). (**Self-play PvP + oleada paralela + harness MCP**: un solo agente jugando 2 cuentas 6 turnos con Bolt con objetivo real, bloqueo en grupo y concesión; 2 subagentes en paralelo (gang-block con reparto de daño multi-cantidad sin prompt de orden, Londres a 5 sin scry por engine, chat invisible vía MCP); pin `session?` en 19 tools + `mage_combat` gang-block aware con tests (suite mcp 33 pass); feed: sufijo `(1/1)`, `1 creature` y `has won` corregidos; workflow navegador con screenshot-por-defecto; beta leído con 19 mesas reales. Fichas ✅ + auras ✅ + 4 fixes de feed/i18n (attacker P/T, vida singular, winner/wants-to-concede, feed espectado). Siguiente: contadores (Ballista X)/PW/equipos/evasión. **Reiniciar opencode para usar el harness nuevo en vivo.** **MCP A+C1+C2+UI Playwright** en `mcp/`: tools DevOps + jugador LLM (estado compacto, prompts, acciones, auto-pass) verificado con una partida real vs Sim, `mage_e2e` + MCP `playwright` para tocar la UI real, registrado en `opencode.json`; **C3 completada** (arnés FixtureServer en CI sin Java, maná/interacciones complejas con auto-pass anti-flood, reconexión/resync con replay de estado en el proxy y multi-sesión); CI integration + e2e fake arreglados: warmup del proxy espera la BD de cartas (`NEXUS_PROXY_WARMUP_MS`), caché de `Mage.Proxy/db` en CI, causa raíz de los 77 fallos e2e fake = SetupWizard pisando `?proxyPort=` (fix en `skip()` + `applySetupConn`); solo quedan 2 tests de deep-links en known-broken. **QA jugada real (Playwright MCP) + fixes**: ping del `SimPlayer` (el bot ya no muere a los ~4 min), manifest `Build-Time` del proxy (NPE de `JarVersion`), localización de mensajes del servidor (target/maná/mulligan/fin), banner de turno con `for`/pronombre `they`, plural `1 jugador en línea`/`1 evento` y `staging_title` genérico. **Auditoría deck builder**: precons que no cargaban (clonado al editar), listas con claves duplicadas (`aggregateCards`), panel del mazo a 1px en ventanas bajas, **comandante explícito** (corona + elegibilidad + identidad de color + orden al equipar) e imports sin impresión (ya no inventan `M10/1`; el validador resuelve por nombre). **Auto-conexión al recargar**: `doConnect` deduplica el doble montaje de StrictMode y descarta intentos obsoletos (antes: 2 WS y vuelta al login a los ~5 s; ahora: 1 WS estable). **QA navegando la UI real + 8 fixes**: recargar en partida ya la restaura (latch `resumingGameId` para el replay del proxy), "Ir a la partida" reincorpora como jugador en vez de espectar, mazos homónimos ya no se pisan (refs por id), ficha `Mazo activo::`, feed por partida, localización del prompt de maná con HTML, `¿Mulligan down to 6 cards??` y pulso targetable con clic estable/reduced-motion. **Deck builder**: el slot de comandante ya es zona de arrastre propia (antes el drop burbujeaba al mazo general y duplicaba); designa sin duplicar, mueve desde banquillo/búsqueda y rechaza cartas no elegibles. **Dos comandantes reales**: `DeckV2.partnerCard` + parejas legales data-driven, `commanders[]` en el DeckJson (el proxy los baja al banquillo que XMage espera) y fix de metas localizadas (canónicas vs `printed_*`), verificado en partida real con la zona de mando mostrando 2 comandantes.
