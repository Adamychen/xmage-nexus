# Work Log 2026-09-08 → 2026-09-14

> Archivo del Work Log de `PROJECT.md` (§9). Filas copiadas tal cual, sin resumir; en el mismo orden (más nueva primero). Índice: `README.md`.

| 2026-09-14 | P2 fidelidad de render v1 | `system/fidelity.ts` + hook en `eventHandler` (flag `mage-web-fidelity=1`): permanentes presentes, ids desconocidos, orden de pila, turno, pastilla de fase; informe al log sin repetir firmas. Regresión `e2e/fidelity.spec.ts` (mutate.json verde, checker corre + 0 discrepancias). v2 pendiente: P/T/contadores/vidas/prompts (necesitan hooks DOM). Unit 1556/1556 + typecheck ✅. |
| 2026-09-14 | P7 kit manual + Base §8.1 | P7: `docs/qa/` (charter/diario/informe) + Exportar diagnóstico en Acerca de (bundle JSON sin contraseña, últimos 60 frames WS). Base: paridad G12 re-verificada, `E2E_BROWSER=webkit` + `E2E_VIEWPORT`, FFA solo-standard (`effectiveBoardLayout`), decisiones §9 (Mindslaver+replay fuera, visores dentro). Hallazgo: zoom.spec 150% inestable a 1366×768 (triage pendiente). Unit 1549/1549 + typecheck ✅. |
| 2026-09-14 | U4-2 + U4-12 (staging) | U4-2: campo de contraseña deshabilitado con aviso en `beta.xmage.today` (`JoinTableDialog`, i18n ×9). U4-12: sonidos de staging join/leave (`player_join`/`player_leave` sintetizados, diff de asientos en el broadcast del lobby, `stagingSounds.ts`); el start ya sonaba (`game_start`). Tests 51/51 + typecheck ✅. |
| 2026-09-14 | Fuera de alcance: redraw + generador | Decisión del usuario: `GAME_REDRAW_GUI` (log-only permanente) y U8 generador de mazos quedan fuera de alcance. Docs en `INTERACTION_COVERAGE.md` + `COMPONENT_PARITY.md` U8 + plan3 §2.3/§4 + plan4 §9. |
| 2026-09-14 | Gaps engine→view fuera de alcance | Decisión del usuario: `harnessed`/`Case solved` (plan3 §3) y resto del baseline (habilidades de jugador, día/noche, `can't-be-targeted`) quedan fuera de alcance — exigen cambio en el fork + rebuild y el proxy no puede inferirlos de `rules`/iconos. Docs en `INTERACTION_COVERAGE.md` §Gaps; guard `engineViewCoverage` 15/15 OK sigue vigilando el gap. |
| 2026-09-14 | Ciclo draft completo en vivo (draft→construct→match→victoria) | Torneo real 2×HUMAN `ciclo-draft-mu1o4gtt` (Booster Draft Elimination, M21×3, Bo1) jugado entero por la web contra un rival WS pasivo: **45 picks** en `DraftScreen` (3 sobres, nombres por Scryfall + `curl` para la ronda 1), `DRAFT_OVER` → `CONSTRUCT` con pool de 45 y `ConstructScreen` (33 cartas montadas a clic: pool 45→18, validación «mín 40», `Enviar mazo` deshabilitado), auto-submit del server al expirar los 10 min (mazo de 40), `START_GAME`, mulligan con diálogo («Mantener (7)»), tierras por turno (Mountain/Swamp/Forest), decenas de prioridades pasadas, combates saltados, 9 turnos; rival concede en su tierra 5 → «Has ganado la partida en el turno 9 / Has ganado el match» (`web/e2e/shots/ciclo-victory.png`), mesa disuelta y resultado `[1-0]` en el Historial. **3 hallazgos de motor** (fork, sin fix aún): (1) crear el torneo con 1 solo setCode crashea el draft al abrir el 2º sobre (`TournamentFactory` fuerza `numBoosters=3` del tipo y `sets.get(1)` revienta con `Index 1 out of bounds`; el hilo muere y el torneo se queda colgado en Drafting — el detector de draft-atascado de la web lo cazó bien; workaround: mandar el código ×N, como el desktop); (2) `constructionTime=0` → NPE en `TournamentSession.construct` (`futureTimeout.getDelay` con `futureTimeout==null`), los hilos de construct mueren en silencio y el torneo se queda en «Constructing» para siempre (el desktop manda minutos×60, defecto 600); (3) en partidas de torneo, si el `GAME_TARGET` «Select a starting player» llega antes de que el `joinGame` complete, el juego se queda esperando sin reenviar (se desbloqueó re-enviando a ciegas; los prompts sí se repiten ante respuestas erróneas). Límite honesto: mi mazo auto-enviado robó solo tierras en 13 cartas (composición del auto-submit no verificada) y no lancé hechizos ni ataqué con criatura en este match (cubierto por `spells`/`combat` en real); el submit manual de 40 justas en la web sigue pendiente de un vivo (A.2 lo cubrió con sealed). |
| 2026-09-14 | Resync de draft tras recargar (instantánea + joinDraft) | Hallazgo de motor: `DraftController.join()` crea una `DraftSession` nueva sin `init()` → el server **NO reenvía `DRAFT_INIT` a un `joinDraft` tardío**, solo los próximos `DRAFT_PICK`; persistir solo el `draftId` dejaba el lobby hasta el siguiente pick (confirmado en vivo). Fix solo-web: `ActiveDraftPersistence` guarda la **instantánea completa** del draft (`{draft, tournamentId, savedAt}`, máx 3h, rechaza el id sintético `'draft'`), sellada en cada `DRAFT_*` (`handleDraftUpdate`) y tras el acuse del pick (`persistDraft`); `gateway.restoreLimited` (en `onOpen` y en `runConnect` tras el login) re-pinta la instantánea (`lastDraftMethod='DRAFT_INIT'` si `picking`, si no `null`) y re-une la sesión con `joinDraft` (si falla, descarta instantánea y estado); limpieza en `DRAFT_OVER`/`CONSTRUCT`/`quitDraft`/login manual/`reset`. Tests: `persistence` +7, `gateway-rejoin` +4, `draft` +4; fake sin `DRAFT_INIT` espontáneo tras la 1ª conexión + modo `joinDraftSilent` (fiel al server); e2e `draft.spec` +1 (recarga con join silencioso: la pantalla solo puede volver por la instantánea y el pick posterior funciona). Unit 1531/1531 + typecheck + build ✅; `@draft` 5/5 ✅. **Vivo real 2×HUMAN M21**: sin el fix la recarga en espera dejaba el lobby hasta el siguiente pick; con el fix, la 2ª recarga devolvió la pantalla **al instante** desde la instantánea («Tu turno», booster de 14 + 1 pick intactos) y el pick desde la restaurada lo aceptó el server («Has elegido Battle-Rattle Shaman», 3 picks; `web/e2e/shots/draft-live3-resync.png`). Límite honesto: el temporizador restaurado cuenta desde el `timeout` del evento (puede sobreestimar tras una caída larga; el server y su autopick mandan y la UI se autocorrige al siguiente evento); si la sesión murió en el server (proxy >60s de gracia), `joinDraft` falla y se limpia. |
| 2026-09-14 | Draft jugable: acuse del pick, espera honesta y nombres | Reporte del usuario («imposible, el contador sigue y no sé si la selección es correcta»). Causa raíz: el server no manda evento al pickear hasta que todos eligen y la web ignoraba la respuesta de `sendCardPick` (el proxy devuelve el `DraftPickView` con `picking:false`); `picking:true` viejo → contador a 0, cartas clicables, sin acuse. Fix: `mergePickAck` (guarda de vigencia), `DRAFT_UPDATE`→`picking:false` (autopicks), acuse local («Has elegido {carta}» + chip «Esperando…» sin contador), anti-doble-pick, `DRAFT_INIT` habilita (recarga en mitad del pick), ∞ con timeout 0, «Sobre X/N · Carta Y», dirección de paso, ring del pick nuevo, `is-picked`; y nombre por Scryfall (`CardStripMeta.name`) porque `SimpleCardView` real no trae `name` (antes UUIDs). Tests: `draft.test` +5, `DraftScreen.test` +5, escenario fake fiel (respuesta DraftPickView + silencio), e2e `draft.spec` +1; unit 1516/1516 + typecheck + build ✅, `@draft` 4/4 ✅. Vivo: draft real 2×HUMAN (`draft-live`) con segundo humano WS que retiene el pick — espera verificada (`web/e2e/shots/draft-live-waiting.png`) y re-habilitación al pickear el rival (`draft-live-resumed.png`); resuelto el límite A.1 (picks de draft en vivo con la web). **Verificado también en `beta.xmage.today`** (login anónimo OK; mesa pública 2×HUMAN, 2 rondas: nombres reales, espera y re-habilitación — `beta-draft-waiting.png`/`beta-draft-resumed.png`; beta sigue siendo intermitente por diseño). |
| 2026-09-14 | Maná del Deck Builder: donuts SVG reales | Reporte del usuario («se ve cutre, ¿un pie chart de verdad?»): los tres repartos de maná eran `conic-gradient` de 38px con agujero. Nuevo componente reutilizable `decks/ManaPie.tsx/.css` (donut SVG por sectores con `stroke-dasharray`, hueco de 2px entre porciones, total en el centro, hover + `<title>` por sector) y `ManaPieLegend` (símbolo, nombre del color `game.color_*`, cantidad y `%` con «<1%» mínimo). `CurveChart` ahora muestra Pips de maná (104px), Fuentes de maná y Tierras básicas (76px) con datos y leyenda reales; panel de curva 30vh→38vh. Tests `ManaPie.test.tsx` (6); unit 1506/1506 + typecheck + build ✅; e2e decks 14/14 ✅; vivo `web/e2e/shots/mana-pie-*.png`. |
| 2026-09-14 | UX del Deck Builder (sin mecánicas nuevas) | Auditoría + 3 lotes de pulido: el guardado pendiente ya no se pierde al cerrar (flush del debounce); rejilla y tira operables por teclado (Enter/Espacio, `focus-within`, sin burbujeo que restaba copias); hints/etiquetas honestas (papelera, Cargar más, + Añadir, Guardando/Guardado, Equipar por id, pips/tierras, tooltips de export); legalidad visible al clic; búsqueda con estados (sin resultados/error+reintento); banquillo por formato; vista horizontal sin scroll; confirmaciones al borrar copias/sugerir tierras; foco al vaciar tira; preview clampada. Tests nuevos (`DeckBuilder`, `ArenaCardStrip`, `ArenaCardGrid`, `DeckListPanel`) y e2e `decks-gallery` actualizado; unit 1500/1500 + typecheck + build ✅; vivo `web/e2e/shots/db-after-*.png`. |
| 2026-09-14 | Errores de unión: banner formateado y descartable | El fallo de `joinTable` pintaba el error crudo (`Join Table Wrong password.`) **detrás** del diálogo y sin cierre. Nuevo componente compartido `ui/ErrorBanner` (badge de icono, `pre-wrap`, `role=alert`, X de cierre) en diálogo de unión, lobby y staging; `handleJoinWithDeck` lanza el error traducido y limpia el global (sin duplicado detrás del modal); `translateError` quita el título del servidor y mapea `Wrong password.` a la traducción limpia. Unit 1489/1489 + typecheck + build ✅; e2e fake `join-errors.spec.ts` (2, hook `onRequest` en `makeBaseScenario`) + vivo real con password incorrecta (`web/e2e/shots/join-error-banner.png`, `lobby-error-banner.png`). |
| 2026-09-14 | X de cerrar modales centrados | Los ✕ de texto de los modales dependían de la métrica de la fuente y varios no centraban su caja (settings/appearance/sleeve/create/join/wiki/about/deck-import); además había 3 glifos (`✕`, `×`, `&times;`). Todos los cierres de modal/overlay pasan al icono SVG `<Icon name="x" />` con CSS centrado (`inline-flex` + align/justify + `line-height: 1`; grid/place-items se mantiene), incluidos DialogShell/PileOverlay/CrossZoneOverlay/HandViewer/CardPreview/TournamentPanel. Guard nuevo `ui/modalCloseCentering.test.ts` (16 cases, rojo en los 8 ofensores). Vivo medido dx/dy=0 en Settings/About/Crear/Unirse/Clasificación (`web/e2e/shots/x-*-fixed.png`). Unit 1481/1481 + typecheck + build ✅; e2e fake de modales 35/35. |
| 2026-09-14 | Live QA de la estela: mutate ✅, replay ⚠️, rendimiento ✅ | **Mutate en vivo** (mesa 2×HUMAN `mutate-live-1`, espectada con screenshots): Gemrazer mutado sobre Elvish con `GAME_ASK` Under/Over; ambas direcciones pintan la pila (`[Gemrazer, Elvish]` 4/4 Over / `[Elvish, Gemrazer]` 1/1 Under, RE+TR del conjunto). **Replay ⚠️ bloqueado server + cliente parcial (APLAZADO por decisión 2026-09-14: el oficial no guarda replays —25 finalizadas, 0 con `games`— y el motor upstream lo marca sin arreglar; no se implementan controles mientras tanto)**: probe real `getFinishedMatches` → `games:[]`/`replayAvailable:false` siempre (config `saveGameActivated="false"`, upstream «not working correctly yet») → el botón del Historial no aparece; en cliente `replayNext/Previous/SkipForward` existen sin UI y `REPLAY_DONE` no sale del tablero. **Rendimiento real (sealed `perf-sealed-1`, 6×M20, constructionTime 10 auto-submit)**: GAME_INIT de espectador **4.247 B → primer pintado ~27 ms** (79 ms desde `watchTournamentTable`); frame más grande observado **47 KB** (`GAME_UPDATE_AND_INFORM` de jugador vía joinGame) — no hace falta virtualizar el log. **Hallazgo**: no se puede espectar un match de torneo en vivo desde el lobby (Ver Cuadro solo lista finalizadas; Espectar deja staging sin seguir el match); el watch real requiere `watchTournamentTable(matchTableId)` por WS. Lecciones: el server NO reenvía GAME_INIT al re-watch (repro = `stopWatching`+`watchTournamentTable`); `mage_create_tournament_table` exige `tournamentType: 'Sealed Elimination'` (con `Elimination` → NPE `Map.get`). Mesas 0; cuentas qa-mut-A/B, qa-perf-A/B. **Fix UX**: el Historial no se podía desplazar (el panel desbordaba `.lobby-main` con `overflow:hidden`; lista ahora `flex:1`+`overflow-y:auto`, empty-state fuera de la lista) + e2e `finished-matches.spec.ts` (fake; rojo sin el fix) + 2 asserts unit. **B.10 follow Bo3 verificado en vivo y bug de raíz arreglado** (mesa `b10-follow`, 2×HUMAN): el aviso «La partida cambió» + `Seguir partida` arrancaba `watchGame` pero el `GAME_INIT` de la partida nueva se descartaba como stale (comparaba turno/paso contra la vista de la partida anterior con `s.gameId` ya actualizado) → staging «Preparando inicio…» sin ver la 2ª; fix en `handleWatchGame` (limpia `game`/`gameEnd` al cambiar de partida) y `eventHandler` (START_GAME de otra partida no compara contra la vista vieja; mismo agujero para jugadores Bo3), 2 tests rojos→verdes en `store.test.ts`; re-verificado en vivo hasta la 3ª partida (1-1). Residual: ver el paso 1→2 de un **jugador** de la web en Bo3 (los Bo3 en vivo llevaban jugadores MCP). **3ª parte**: sealed real desde la UI verificado (CONSTRUCT pool 90 + sugerida de tierras + submit 40; tableros T3/T6 con criaturas/artefacto) y bracket final con datos (`getTournament`: Finished/Winner/3pts) — mesas de torneo desaparecen del lobby al terminar; **bug arreglado**: `Ver Cuadro` de una mesa ajena pintaba el torneo propio cacheado (`isOwnTournament` + 3 guards en `useTournamentBracket.ts`, 2 tests rojos→verdes). Lecciones: `tournamentType` real ('Constructed Elimination'/'Sealed Elimination'; 'Elimination' → NPE); el auto-pass MCP se bloquea con tierras jugables; `CONCEDE` se ignora en mulligan/elección inicial. Unit 1455/1455. **§1.4 C.14 foco (4ª parte)**: lobby OK (el resaltado de `Mesas (1)` era el anillo de foco del tab activo; orden de tabulación y anillos visibles) y partida OK (mano operable con Tab + glow `:focus-visible`; Space juega UNA tierra sin doble disparo) — findings menores sin fix: el foco cae a `<body>` al jugar la carta enfocada y el link del footer entra en el tab order. **§2.1 watch de torneo EN VIVO desde el lobby (5ª parte)**: causa raíz — al espectar una mesa de torneo el server responde `SHOW_TOURNAMENT` con el id REAL del torneo (y `TableController.watchTable` no resuelve ids de mesa en `getTournament`), pero el web solo lo logueaba → staging clavado. Fix: `handleShowTournament(objectId, data)` guarda `spectateTournament` (id real + tableId, idempotente) → `useTournamentBracket.openBracket(table, tournamentId?)` lo usa (sin id espera el callback) → el lobby consume la resolución y abre el cuadro; `watchTable` de mesas de torneo ya no entra al staging; `handleWatchGame` limpia la resolución (no reabre el cuadro al volver); el bracket del modal recibe el id real y en torneos ajenos ya no muestra «Abandonar torneo». Tool MCP `mage_watch_tournament_match` (WATCHGAME→watchGame→gameView). Tests: +13 (hook, watchTable, SHOW_TOURNAMENT, canQuit) → unit **1464/1464** + typecheck ✅; `mcp test` 39 ✅. Vivo (torneo `tt-live-1` Constructed con match Dueling, navegador ajeno): Ver Cuadro carga el cuadro vivo (ojo) y el ojo pinta el tablero espectado; Espectar de la tarjeta abre el cuadro sin staging. Pendiente: cuadro de torneos ya terminados desde el Historial (no hay tournamentId en `MatchView`). |
| 2026-09-14 | Leaderboard: Win Rate → Juego Limpio | La columna WIN RATE mostraba `—` (desde `f08ed4e46b8`, 09-12, se dejó de fabricar el win rate por ELO: el server solo manda el conteo de partidas + abandonos, `1 (Q:1)`). Por decisión del usuario se muestra el dato real: columna **JUEGO LIMPIO** = `100 − matchQuitRatio` con tooltip („{ratio}% de abandonos"), `—` con pista si no hay partidas; tie-break del sort por juego limpio; clave `leaderboard_fair_play_hint` ×9 locales. 2 tests (rojo sin el cambio), unit 1465/1465 + typecheck ✅; vivo: `1 (Q:1)` → `0%` (`web/e2e/shots/leaderboard-fairplay.png`). |
| 2026-09-13 | Sealed en vivo punta a punta (cierra draft) | **Mesa `audit-sealed-2`** (Sealed Elimination 2×HUMAN 6×M20): crear → joins → start → `joinTournament` ×2 (**descubrimiento**: sin esto el torneo no arranca) → pools CONSTRUCT → auto-submit → partida 40 cartas → starting player → keeps → T1-T2 → concesión → torneo Finished (A Winner). Wizard Torneo web con datos reales (19 tipos). Sin fixes. Lección: respuestas raw usan campo `value`; no cerrar WS (gracia 60s); `head -N` mata por SIGPIPE. Lobby a 0. |
| 2026-09-12 | E2E fake 119/0 sin exclusiones + Fase 2C resto + vías de error | **Fake completo**: 119 passed / 2 skipped / 0 failed con known-broken vacío. **Mesa `audit-2c3`**: descarte a cementerio en cleanup (visor con 1 Bolt + arte M10 verificado en screenshots). **Fix**: `PileOverlay` singular "(1 carta)" + test (suite 1369/1369 + typecheck). Chat de espectador, F11 sin errores, clic-avatar por diseño sin menú. Errores honestos: mesa llena `[SEAT]`, pass mala `[PASSWORD]`; acción ilegal ignorada sin corromper. Lección: `wait timeout:true` devuelve estado cacheado — exigir prompts frescos tras `start_match`. Lobby a 0. |
| 2026-09-12 | Bo3 completo en vivo + fix timer sin-reloj + invite unblocked | **Match `bo3-1` (winsNeeded 2, 3 partidas, 2-1 para B)**: SIDEBOARD entre partidas (submits por WS crudo — el harness no expone `submitDeck`; script en `.run/scratch/`), lobby "Wins: 2 Score", pips ○○→●○, feed 1ª persona, re-espectado limpio por partida (sin auto-follow = paridad desktop). Hallazgo: END_GAME_INFO no llega a espectadores (gameEnd 100% sintético) → se conserva Volver-al-lobby. **Fix**: `priorityTimeLeftSecs=2147483647` se mostraba "596523:14:07" → `isUnlimitedTime()` oculta el badge (suite 1368/1368 + typecheck). **Invite**: known-broken vacío (los 2 tests pasan; era coletazo del SetupWizard). Draft en vivo diferido (creación verificada + fake e2e cubren; picks reales desproporcionados). | screenshots `.run/playwright-mcp/page-2026-09-12T19-38-22` (P1), `19-43-52` (P2), `19-54-08` (timer) |
| 2026-09-12 | PW + equipos/evasión en vivo (Jace, Greaves/Boots, Serra) | **Dos mesas deterministas 2×HUMAN (sesiones pineadas, espectadas con screenshots)**. `pw-1` (10 turnos): Jace WWK:31 T8 con {2}{U}{U}; +2 con target a jugador + Yes/No (lealtad 3→5); 0 Brainstorm (roba 3 + 2× put-back secuencial; mano 8, biblio 47). `equip-1` (12 turnos): Greaves T4, Boots T6, Serra LEA:39 T10 ({3}{W}{W}); Equip {0} con auto-target (una sola criatura, como Rancor); Serra con Flying/Vigilance/Haste/Shroud + iconos (shroud→`ABILITY_HEXPROOF` en engine); ataque directo con `mage_combat` (B 20→16, vigilance no gira); **shroud negativo**: sin objetivo legal los equips no se ofrecen en `playableIds`. Web: Jace "Lealtad: 5"+badge, Serra 4/4 FL/VI/SH + tooltips ES, feed limpio al espectar. Ambas concedidas/abandonadas, lobby 0. Sin cambios de código (suite 1365 + typecheck vigentes). | screenshots `.run/playwright-mcp/page-2026-09-12T18-13-54` (Jace T9), `18-15-15` (T10), `18-24-20` (Serra T11) |
| 2026-09-12 | Contadores en vivo (Ballista X) + quirk engine declare | **Mesa determinista 2×HUMAN `counters-1` (17 turnos, sesiones pineadas, espectada con screenshots + recarga dura)**. `Walking Ballista 2XM:306` con X=2 (`GAME_GET_AMOUNT` → `mage_choose value=2`; nota: `mage_action integer` rechaza el string del transporte, `mage_choose` coerciona) → 2/2 con 2×+1/+1; ping T7 con coste de quitar contador (B 20→19, queda 1/1+1); `{4}: poner contador` T9 (2/2+2). GameView: `counters`, icono `OTHER_COST_X x=2`, 3 reglas; web: arte + `2/2` + badge `+2` + chip `x=2`. **Quirk engine (fork, fuera de alcance)**: el UUID en declare-attackers abre `CHOOSE_ABILITY` en vez de declarar (4 repros: T9 `mage_combat`, T11 `special` All-attack, T15 UUID solo, T17 doble-clic); la web manda el mismo UUID que el desktop (paridad por construcción) y el ataque normal está cubierto (tokens + `combat.spec` real). **Lección harness**: apagar auto-pass antes de declarar (se comió el declare T13 como `mode=select`, verificado en `mage_session`). Mesa concedida/abandonada, lobby 0. Sin cambios de código (suite 1365 + typecheck vigentes). | screenshots `.run/playwright-mcp/page-2026-09-12T17-25-58` (1/1+1) y `18-04-42` (2/2+2, turno 17); research corrige Serra Angel `LEA:227`→`LEA:39` (227 es War Mammoth) |
| 2026-09-12 | Tokens + auras en vivo + 4 fixes de feed/i18n | **Dos mesas deterministas 2×HUMAN (`tokens-1`, `aura-1`) jugadas con sesiones pineadas `vfy-A/vfy-B` + espectadas en navegador con screenshots.** Tokens: `Dragon Fodder ORI:140` → 2× `Goblin Token` 1/1 (`isToken`, `mageObjectType TOKEN`, set ORI, `cardNumber` vacío), ataque + daño (B 20→19→17), render con arte + badge 1/1. Auras: `Elvish Mystic m14:169` + `Rancor M13:185` (objetivo auto-elegido por el servidor al haber una sola criatura legal, sin `GAME_TARGET`); 1 Rancor → 3/1 + trample, doble Rancor → 5/1; `attachments[]` + `attachedTo` + icono `ABILITY_TRAMPLE` en el GameView; la web apila el aura + badge TR + tooltip Arrollar. Fixes con tests: (1) **`Attacker: X (P/T)` sin limpiar** (único handler sin `cleanCardName` → 8×404 Scryfall por ficha: `named?exact=Goblin Token (1/1)`); (2) **`feed_lose/gain_life_one`** (`pierde 1 vidas` → `pierde 1 vida`, patrón `_one` en types+9 locales); (3) **`Player X is the winner` + `X wants to concede`** en inglés crudo (parser + badge de ganador en `GameEndDialog`); (4) **feed arrastraba la partida anterior al espectar** (`handleWatchGame` no limpiaba el canal `game` como `handleStartGame`). Lecciones harness: el pin `session?` funciona en vivo en paralelo sin interferencias (cierra la lección pre-harness); **`mage_combat` multi-atacante pierde el 2º UUID** (igual que gang-block) → workaround secuencial (UUID→wait→UUID→confirm); el auto-pay `special` no paga solo (girar vía `sourceId`); la mano inicial viene **ordenada por el servidor** (contenido determinista con `skipInitShuffling`, orden no). | web unit **1365/1365** ✅ (169f) + typecheck ✅; screenshots `.run/playwright-mcp/page-2026-09-12T16-*.png` (tokens, combate con flechas, Rancor simple/doble); lobby final 0 mesas ✅ |
| 2026-09-12 | Harness MCP: pin de sesión + `mage_combat` gang-block | **Deuda de la oleada paralela, saldada en `mcp/src/xmage/session.ts`**: (1) **pin `session?` en 19 tools** (todas menos connect/sessions/use_session): opera sobre la sesión indicada sin tocar la activa global; (2) **`mage_combat` gang-block aware**: bloqueadoras de una en una respondiendo el `GAME_TARGET` intercalado con `blockTargets[i]` (o el único atacante), `targetsAnswered`/`unresolved` en el resultado, `sent` con la secuencia completa de UUIDs. Tests nuevos `sessionPin.test.ts` (pin sin `use_session`, activa intacta) y `combatGangBlock.test.ts` (secuencia fake `[b1,a1,b2,a1,false]`); suite `mcp/` **33 pass / 1 skip** (el skip es `realGame`, requiere `MCP_E2E=1`) + typecheck ✅. README de `mcp/` actualizado (tablas + párrafo multi-sesión). **Nota operativa**: el proceso MCP vivo de esta sesión corre código anterior al cambio (Node sin hot-reload): el pin y el combat nuevo exigen reconectar el MCP (reiniciar opencode) — verificado en el intento `vfy-gang-1` (el pin fue ignorado y el join cayó en la otra cuenta; limpieza hecha, lobby a 0). Lección de debug: Node strip-only aborta con `ERR_INVALID_TYPESCRIPT_SYNTAX` ante ediciones parciales — ante un MCP que no responde, mirar el crash antes que la lógica. |
| 2026-09-12 | Oleada paralela: gang-block + Londres/chat (2 subagentes) | **Dos probes simultáneos con sesiones/mesas propias**: (a) `gang-block-1`: bloqueo en grupo real con trade + asignación de daño (A 20–B 15, concede B); (b) `london-chat-1`: Londres a 5 verificado (5 vs 7) + chat cruzado + concesión (gana B). Hallazgos: (1) **`mage_combat blockers=[a,b]` no sirve para gang-block** (el 2º UUID cae en el `GAME_TARGET` intercalado → 0 bloqueadores): workaround por-bloqueador (UUID → `GAME_TARGET` → UUID atacante → confirm vacío) verificado; (2) **no hay prompt separado de ORDEN**: el servidor va directo a `GAME_GET_MULTI_AMOUNT` (multiString, defaults en orden de declaración) — la web ya soporta multi-cantidad (`GenericDialog`/`useFeedbackForm`); (3) **Londres sin scry**: `LondonMulligan.endMulligan()` vacío en el fork (solo Vancouver lo implementa) — desviación del engine, fuera de este repo; (4) **chat enviado OK pero invisible vía MCP** (gap del harness; en navegador ya verificado); (5) **sesión activa MCP global**: los probes paralelos se interfirieron (acciones caídas en partida ajena, mesa contaminada + fantasma `4622ec…` en `Starting`) — lección: paralelizar exige un proceso MCP por agente o pin de sesión por llamada; mesa fantasma eliminada, lobby a 0. Tierras con `summoningSickness:true` en vista (cosmético, dato del servidor). | lobby final 0 mesas ✅; JSONs verbatim de declare-blockers y multi-amount en el reporte de sesión |
| 2026-09-12 | Self-play PvP: un solo agente con dos cuentas (self-A/self-B) | **Partida real humano-vs-humano 2×HUMAN (`selfplay-1`, 6 turnos, determinista con `skipInitShuffling`) jugada por un solo agente alternando `mage_use_session` + espectada en el navegador con screenshots**: mulligan keep ambos, tierras, Goblins con haste, ataques con daño (19/19→19/18→18/19), **Bolt T4A con target real a criatura rival** (`GAME_TARGET` con 5 opciones: 3 criaturas + 2 jugadores), **bloqueo real T5B** (declare-blockers con trade 1/1, cementerios 2-2) y concesión T6A → `GAME_OVER`+`END_GAME_INFO` en ambos lados y diálogo `Partida finalizada` en el espectador. Hallazgos y fixes: (1) **sufijo `(1/1)` del servidor** en mensajes de combate/daño llegaba al `cardName` → 404s Scryfall + texto `RAGING GOBLIN (1/1)`: `cleanCardName()` en cast/attack/block/damage; (2) **`cardName='1 creature'`** en ataques con conteo (`X attacks Y with 1 creature`) → 8×404: sin `cardName` en forma de conteo; (3) **`X has won the match/game`** no casaba el regex → `player='qa-self-B has'` → `qa-self-B has gana la partida`: `(?:has\s+)?` opcional. Tests en `gameEventParser.test.ts` (21 ✅) + typecheck ✅. Notas de protocolo: el auto-pay `special` no paga solo (hay que girar fuente vía `sourceId`); el auto-pass no pasa prioridades con objetos jugables (hay que pasar a mano en damage/postcombat); `mage_combat blockers=[id]` + `mage_choose` vacío confirma el bloqueo. | parser 21/21 ✅ + typecheck ✅; screenshots `.run/playwright-mcp/selfplay-{board-t5,trade-t5,gameover}.png`; mesa eliminada |
| 2026-09-12 | Auditoría interactiva (fases 2B/2C/3/4) | **Auditoría exhaustiva operando la app real como un humano (Playwright MCP: partida de 3 turnos vs SIM + concesión, wizard torneo, join con contraseña, cambio de idioma, localStorage corrupto, viewport móvil, teclado) + fix de 7 hallazgos**. (1) **Feed: `X casts Y targeting Z from hand`** (forma inline de `AbilityImpl#getGameLogMessage`, verificada en el fork vía `Spell.getActivatedMessage`) no la partía el parser (solo `[target: Z]`) → `cardName` quedaba `Lightning Bolt targeting qa-ui-1345`, sin píldora ➔ y 4×404 a Scryfall por render: grupo inline en `gameEventParser.ts` + 3 tests live-like (el CardView de pila/cementerio viene limpio: `M10/146`). (2) **Log `¿…??` duplicado**: `questionLogLine` miraba el crudo con HTML y no veía el `?` final → decide sobre el texto visible y acepta `.`/`!` (exportada + test). (3) **Fin de partida agramatical** (`Tú pierde/gana`, `Du hat`, `Tu a`, `You wins`): claves de 2ª persona `end_won/lost_game_turn_you` + `feed_won_match_you` en types+9 locales (los tests fosilizaban el bug). (4) **PingBadge en español fijo** (`Latencia…conectado…`): claves `ping_tooltip(_conn)` + `ping_disconnected` ×9. (5) **Modal sin gestión de foco** (Tab escapaba al fondo): auto-foco + trampa de Tab (solo topmost) + restauración en `ui/Modal.tsx` + 2 tests. (6) **2B-rest verificado**: wizard torneo, join con contraseña (anfitrión + picker + Guardar clave), staging privada con invitaciones, leave/limpieza; el `(42%)` del tooltip es el **quit-ratio del servidor** (`userStatsToMatchQuitRatio`), no winrate: paridad desktop, no bug. (7) **Adversarial**: localStorage corrupto → fallback a login sin crash + re-login OK; móvil 390px con navegación dedicada; Escape no cierra el wizard (deliberado, protege el draft). Traducciones de/en/de/fr/it verificadas en vivo; resto de locales pendientes de revisión nativa. | web unit **1361/1361** ✅ (169f) + typecheck ✅ + build ✅; verificado en vivo: feed `lanza … ➔ …` con píldora tras HMR, `Latency: … (connected …)` en EN, foco en ✕ al abrir el wizard, partida completa (mulligan/tierra/maná/pila/ASK/chat/tracker/GameMenu/concesión → lobby limpio); e2e fake **112 pass / 5 fail / 2 skip** (15.9m) donde los 5 fallos eran coletazos del propio lote (3× best-of esperaban `Tú gana el match`, settings esperaba zoom +10% en vez del preset 115%, U6 esperaba `Export .DEK` hardcodeado en inglés) → specs actualizados al copy intencional y **re-run 6/6 ✅** |
| 2026-09-11 | Commander 2x (Partner) | **Soporte real de dos comandantes (Partner/Trasfondo) de punta a punta**. Hasta ahora la UI mostraba un segundo Partner derivado (U7-7) pero el modelo guardaba uno (`DeckV2.commanderCard`) y el proxy movía **una** carta al banquillo; XMage (`GameCommanderImpl`) toma como comandantes **todas** las del banquillo y `AbstractCommander` valida la pareja → en partida solo había 1. Cambios: (1) modelo `DeckV2.partnerCard` + migración al abrir el mazo (deriva la pareja legal del oráculo una sola vez); (2) reglas de emparejamiento data-driven espejando `mage.util.validation` (Partner, Partner with mutuo, variantes tipo Friends forever, Doctor's companion + Time Lord Doctor, Choose a Background + Background) en `pairProfile`/`canPairCommanders`, con validación propia (pareja ilegal, Trasfondo solo) y 3 claves i18n ×9; (3) UI: dos plazas (el drop resuelve primer/segundo comandante, corona del segundo, quitar el primero asciende al segundo) con hint contextual; (4) pipeline: `prepareDeckForXMage` adjunta `commanders[]` y `DeckJson.parse` los mueve del main al banquillo (protocolo documentado en `Mage.Proxy/README.md`); (5) **fix de raíz**: las metas localizadas de Scryfall traían `printed_type_line` (es) como `typeLine` y `isCommanderEligible` no reconocía "legendary/creature" → ahora se prefieren `type_line`/`oracle_text` canónicos. | web unit **1309/1309** ✅ + typecheck + build; proxy java **64/64** ✅ (+3 `DeckJsonTest`); e2e fake **117 pass / 0 fail / 2 skip** ✅ (nuevo test de pareja Partner en `decks-gallery`; un run intermedio con 2 fallos fue contención de CPU — el mismo spec pasó 9/9 en aislamiento y el run limpio cerró 117/0), `deckvalidation` 3/3 y `printing-preview` ✅; **verificado real** (navegador+stack local): mazo Sidar+Tana designado por drop → unirse a mesa Commander → la zona de mando renderiza `command-zone multi-commander` con **2** `commander-slot` |
| 2026-09-11 | Deck builder/commander DnD | **El slot de COMANDANTE no era zona de drop: al arrastrar una carta encima el evento burbujeaba al contenedor y la añadía al mazo general (y la duplicaba si ya estaba)**. Ahora la sección (`deck-commander-section`) es zona de drop propia con `stopPropagation` en dragover/drop: designa el comandante sin duplicar si ya está en el main, añade 1 si viene de la búsqueda y mueve 1 desde el banquillo si viene de ahí. Si la carta no es elegible (typeLine/oráculo conocidos) el drop se rechaza con flash rojo y **no** se añade a ninguna zona. Helper puro `dropOnCommander` + hint i18n `commander_drop_here` en 9 idiomas. | unit **1297/1297** ✅ (+9: `dropOnCommander`, routing/hint/rechazo del slot) + typecheck ✅ + build ✅; e2e fake `decks-gallery` **8/8** ✅ (nuevo aserto de drag al comandante) y `deckvalidation` 3/3 ✅; **verificado en vivo** (Playwright MCP sobre el stack): arrastrar Atraxa del main al slot la designa sin duplicar (Criaturas 2→1, total 22) y Sol Ring (no elegible) ni se designa ni cae al general |
| 2026-09-11 | QA/fixes UI (navegación real) | **Auditoría navegando la app real (Playwright MCP) + fix de 8 hallazgos**. (1) **Recargar en partida perdía la partida**: `joinGame` lanzaba el replay (GAME_UPDATE+GAME_SELECT) con `phase='lobby'` y el guard de eventos GAME_* lo descartaba → latch `resumingGameId` (armado en `runConnect`/`onOpen`/`resumeGame`, limpiado al adoptar la vista) deja pasar el replay; el server ya te había reincorporado. (2) **"Ir a la partida"/"Mi mesa activa" espectaban tu propia partida**: `ActiveTablesBar`, `TableCard` (botón nuevo + doble clic) y el header ahora llaman `resumeGame` (`joinGame` con `t.games[0]`) para DUELING/SIDEBOARDING. (3) **Mazos homónimos se pisaban**: `getAllAvailableDecks` ya no fusiona por nombre (precon y custom conviven), selects por `deckRef` (id estable), identidad con `sameDeck`, y el helper e2e `selectDeckOption` selecciona por etiqueta. (4) `Mazo activo::` (el i18n ya trae `:`). (5) El feed mezclaba partidas: `START_GAME` limpia el canal `game` del log. (6) Maná sin localizar (`Pay {R}<div…>Carta</div>`): separador opcional y HTML tolerado. (7) `¿Mulligan down to 6 cards??`: no se añade `¿?` si ya hay `?` y los prompts con HTML se localizan por texto plano. (8) Pulso targetable infinito: fuera `scale()` (clic e2e estable) + `prefers-reduced-motion`. | unit **1288/1288** ✅ (+3: replay restore, guard lobby, feed) + typecheck ✅; e2e fake completo **116 pass / 0 fail / 2 skip** ✅; real `multi-user` ✅; verificado en vivo con hook de WS: F5 en partida → restaura (turno 1, vida 17), "Ir a la partida" → joinGame como jugador, 12 mazos en el wizard (precon+custom homónimos), "Pagar maná {R} · Lightning Bolt" y click en barra rival sin "element is not stable" |
| 2026-09-11 | Fix/reload-reconnect | **"A veces al recargar hay que pulsar Conectar" — causa raíz y fix**: en dev React monta `App` dos veces (StrictMode) y el efecto de auto-connect ejecutaba `doConnect` dos veces; el segundo `detachGateway()` cerraba el WS del primero, que a los 5 s rechazaba por timeout y pisaba el estado del login que sí había funcionado → vuelta al login con "No se pudo conectar al proxy WebSocket". Reproducido con instrumentación (`page.on('websocket')`): 2 WS a 8787 (el 1º muerto a los 2 ms), login OK en el 2º y error a los ~5 s. Fix: `doConnect` deduplica por conexión en vuelo (mismo host/puerto/servidor/usuario → misma promesa: el doble montaje crea **un** WS y **un** login) + guarda de generación (`activeAttempt`) para que un intento obsoleto no pueda volver a `idle` ni escribir error encima del vigente (cubre dobles clicks y el cambio de server de las invitaciones) + `reset()` invalida intentos. Además los 3 e2e `best-of-3/5/n` seguían esperando el texto inglés del final del match (el lote QA lo localizó a "Tú gana el match") → asserts a regex bilingüe. | unit **1284/1284** ✅ (+3 `gateway-connect.test.ts`, verificado que fallan sin el fix) + typecheck ✅; real (Playwright MCP): 4 recargas → 1 WS cada una, sin error y en lobby a los 7 s (antes 2 WS + error a los 5 s); e2e fake completo 113/116 con los 3 `best-of-*` fallando solo por el texto localizado (preexistente del lote QA, confirmado con stash) → corregidos y **3/3 ✅** |
| 2026-09-11 | Deck builder/audit | **Auditoría del editor de mazos desde la UI (Playwright MCP) y fixes de los 5 fallos reportados por un jugador**. (1) **El editor no cargaba**: los precons viven en memoria (`precon-N-nombre`) y `DeckBuilder` hacía `storage.get(id)` → `null` → "Cargando…" eterno; el Deck Browser clonaba pero abría el id **original** (y la importación online igual). Fix: clonar-al-editar + abrir el id del clon + estado de error en vez de spinner infinito + regresión e2e. (2) **"Card list glitchy mess"**: los mazos pueden traer entradas repetidas (bundled deterministas, imports) y las tiras usaban `set:num:nombre` como key de React → claves duplicadas, filas duplicadas/omitidas y fetches repetidos (404s). Fix: `aggregateCards()` (fusiona cantidades para mostrar) en lista e inspector + dedupe de metadata. (3) **El panel del mazo colapsaba a 1px** con la curva abierta en ventanas bajas (drop target invisible ⇒ "el drag&drop no funciona"); fix: autocierre de la curva en viewports bajos, tope de 30vh y `min-height` de la lista. (4) **Comandante explícito**: la sección COMANDANTE era la portada (`coverCard`, default `cards[0]`) y la validación la usaba como comandante (100 tierras pasaban como "✓ Commander") mientras el proxy elegía la primera carta legal por orden. Fix: `DeckV2.commanderCard`, botón corona (solo elegibles: legendaria criatura/vehículo/nave, "can be your commander", Background) con toggle, slot vacío sin fallback, identidad de color/errores desde el comandante designado y `withCommanderFirst()` al equipar para que el proxy elija el correcto + migración de mazos viejos (portada elegible → comandante). (5) **Import sin impresión**: `parseArenaLike` asignaba `M10/1` a las líneas sin set → Scryfall resolvía M10/1 (Ajani Goldmane) con nombre/arte/tipo equivocados; fix: set/número vacíos + resolución por nombre en el validador del proxy (paridad con `Deck.resolveCardInfo`) + tests. No se añadió DnD táctil (decisión: solo ratón). **Reproducción**: precon atascado, claves duplicadas en consola, lista a 1px a 1280×720, tierra/rayo en slot de comandante. | web unit **1281/1281** ✅ + typecheck ✅; proxy java **DeckValidationTest 10/10** ✅ (+1 name-only) y suite completa ✅; jar reconstruido + proxy reiniciado; e2e `decks-gallery` **8/8** ✅ (2 regresiones nuevas: precon y comandante) + `defeat`/`feed`/`staging`/`concede`/`mulligan` 14/14 ✅; verificado real: import "1 Sol Ring / 1 Counterspell" sin impresión → nombres localizados correctos y sin "missing" del validador; designación de comandante persistida y reordenada al equipar |
| 2026-09-11 | QA/fixes | **QA jugada real desde la UI (Playwright MCP: mesa vs SIM, tierras, Bolt con targeting y pago de maná) + fixes de lo encontrado**. (1) **El bot SIM moría a los ~4 min de partida** (`sim-… disconnected due connection problems` en el server; el cliente veía "has lost the game/quits the match"): `ProxyClient` mantenía viva su sesión con `session.ping()` periódico pero `SimPlayer` no tenía ninguno → cualquier partida humana vs SIM más larga que el lease se perdía. Fix: `SimPlayer` programa el mismo ping (`ProxyClient.PING_SERVER_SECS` = 20 s) tras `connect()` y lo apaga en `stop()`. (2) **NPE de `JarVersion` en cada `MageVersion`** ("Can't read build time in jar manifest"): el manifest del jar sombreado no tenía `Build-Time` → `ManifestResourceTransformer` del `Mage.Proxy/pom.xml` ahora escribe `${maven.build.timestamp}`. (3) Localización de mensajes del servidor: `Select any target`, `Pay {R} <carta>` (texto tras el coste), `Mulligan down to N cards?` y fin de partida (`You won/lost the game on turn N`, `<x> won the match` con `You→Tú` en `GameEndDialog`). (4) Parser: banner `Turn N for X` ya no captura el "for" como nombre y `chooses that they…` resuelve el pronombre al que elige. (5) Plurales (`1 jugador en línea`, `1 evento`) y el contador del debug del lobby decía "Cargando…"; `staging_title` pasa a genérico (era "Sala de Espera de Espectador" incluso en MODO JUGADOR). **No se toca** el solape permanente/fila inferior con la mano (decisión de producto: es problema de hover). | web unit **1274/1274** ✅ (+2) + typecheck ✅; proxy java 60/60 ✅; build proxy + manifest con `Build-Time` ✅; e2e fake focalizado (`feed-unified`/`staging`/`concede`/`defeat`/`mulligan`) 14/14 ✅ (assert del final actualizado al texto localizado); **verificado real**: sim vivo >5 min sin desconexión (antes 4 m 19 s) y 0 NPE nuevos en el proxy |
| 2026-09-11 | Fix/e2e-rootcause | **Causa raíz de los 77 fallos e2e fake (09-05→09-10) encontrada y arreglada: el SetupWizard pisaba `?proxyPort=`** — diagnóstico con el artifact de CI: la página jugaba contra el XMage REAL (snapshot con `beta.xmage.today · 80 jugadores`, `Mesas (21)` reales, seats `0/2` = formato real `TableView.seatsInfo`) mientras el HumanHelper quedaba en el FixtureServer (8789) → `helper: la partida no arrancó`. Cadena: contexto fresco → wizard auto-abre (`App.tsx` `!isSetupDone()`) → helper hace `dismissSetupWizard` → `SetupWizard.skip()` persistía `buildConn()` (proxy 8787) + evento `setup-conn` → `LoginScreen.applySetupConn` sobrescribía el formulario (no hay campo proxy-port, el `?proxyPort=` muerto) → `Conectar` → `ws://localhost:8787` = proxy real del stack con target beta (default del helper). Fix: (1) `skip()` solo `markSetupDone()` — "omitir deja el login intacto" (la bandera ya la re-adivina `guessDefaultFlag`; test unitario actualizado al contrato nuevo); (2) `applySetupConn` respeta `urlProxyPort() ?? conn.proxyPort`; (3) helper e2e fija servidor `localhost` en fake (red de seguridad anti-beta); (4) fix del spec `setup-wizard` (hacía 6 `next.click` sobre un wizard de 6 pasos: el último muestra `setup-enter`, nunca pudo estar verde — añadido en `03abd96354`); (5) `grepInvert` ignora listas vacías (`(?:)` excluía TODA la suite). Los 2 deep-links de invitación siguen rotos por otra causa (bounce staging→lobby, sin WATCHGAME del escenario; `isMyTable` por controllerName) → known-broken | unit 1272 ✅ (test de skip actualizado); typecheck ✅; e2e fake: 116 pass / 2 known-broken (deep-links) / 2 skip — repro `skips.spec` 33s→5.6s; T4 endurecido con re-inyección (frames tardíos reseteaban el phase); flakes residuales solo bajo contención de CPU extrema (runs de 47m con 3 sesiones opencode al 40%) |
| 2026-09-11 | CI/Integration | **El job `integration` de CI nunca estuvo verde — 3 causas encadenadas diagnosticadas y 2 arregladas**: (1) runs 1-2: el servidor XMage no arrancaba en CI (falta `config.xml` → timeout 17171; ya arreglado con el seed `ensureLocalServerDirs`); (2) run 3: `self-test`/`human-test` fallaban en 0.2s porque el proxy responde `ERR_WARMING_UP` mientras `CardScanner.scan()` construye su BD en background (`DeckValidation.ensureCardDatabaseAsync`) — la BD del proxy es ruta relativa `./db` con cwd `Mage.Proxy` (NO la del caché `local-server/db` del servidor) → en CI escanea 92k cartas frío cada run; y NADA esperaba (warmup 2 intentos, self-test 1 connect y `exit(1)`); fix: `warmup.mjs` sondea `connect` cada 5s hasta READY (deadline `NEXUS_PROXY_WARMUP_MS`, 20 min en CI, validado en vivo contra escaneo frío) + caché de `Mage.Proxy/db` en `web-ci.yml`; (3) `e2e` (fake, 30 min cap): el fallo pre-existente del staging de espectador era en realidad **77/118 tests** (no "several"): firma `Sala de Espera de Espectador` tras crear mesa + lobby asientos `0/N` en vez de `2/2` (evidencia `error-context.md`); excluidos por defecto vía `grepInvert` con los valores grep EXACTOS de `_grepTitleWithTags` (archivo+describes+tags unidos por espacio, espacio inicial del suite raíz — `grepInvert` de config se aplica; los títulos `›`-separados NO matchean) en `web/e2e/known-broken.ts` + opt-out para triage `E2E_INCLUDE_KNOWN_BROKEN=1`. Fake suite: 39 pass/0 fail/2 skip en 2.5m (antes 1.1h y 75 fallos). Pendiente: triage/fix del bug de staging | warmup ✅ (frío validado) · e2e fake 39/41 ✅ · typecheck pendiente en el paso de cierre |
| 2026-09-10 | Fix/default-flag | **Bandera por defecto según el locale del navegador, no `es` fijo**: `LoginScreen` y `SetupWizard` hardcodeaban `'es'` (la bandera del autor) para usuarios sin conexión guardada. Nuevo `lobby/defaultFlag.ts` puro (`guessDefaultFlag`, solo-locale como `i18n/index.ts`): región del locale si está en `POPULAR_FLAGS` (`es-MX→mx`, `en-US→us`, alias `uk→gb`, separadores `-/_`, insensible a mayúsculas) → fallback por idioma (`en→gb`, `pt→br`, `ja→jp`) → `world` (neutral, `world.png` existe) para el resto. `Skip` del wizard ahora persiste los defaults (misma `buildConn` que `finish` + evento `setup-conn`) en vez de no guardar nada, así la bandera adivinada sobrevive a la siguiente apertura; la conexión guardada siempre tiene prioridad (no se pisa la elección explícita) | vitest 159f/1261 ✅ (+8 defaultFlag, skip persiste+notifica); typecheck ✅ |
| 2026-09-10 | Fix/stale-hover | **El preview de hover quedaba pegado a cartas que dejaban su zona**: StackZone solo limpiaba con `mouseLeave`/modal, pero al resolverse la carta con hover su DOM se desmonta sin leave → estado obsoleto que reaparecía al entrar la siguiente carta en la pila (y preview pegado con la pila aún poblada). Fix: `useLayoutEffect` valida el id contra el stack actual (ausente → clear + `onHover(null)`; presente → refresca el objeto y re-ancla el rect vía `[data-card-id]`). Gemela en `useBoardPresenter`: preview huérfano cuando muere la criatura con hover (token fizzle, bounce) — se valida `floatingCard.id` contra el GameView (`findCardViewInGame` escanea todas las zonas; battlefield-shaped fuera del campo → clear; si sigue, refresca la vista). `zone` no viaja en el wire (`storeZone=false`) → detección por campos exclusivos de PermanentView; cards sin id (fixtures) intocables para no flickerar | vitest 158f/1253 ✅ (+8: 3 StackZone + 5 presenter); typecheck ✅; build ✅ |
| 2026-09-10 | Fix/http-assets | **Servidor público HTTP: iconos rotos y gestor de cartas/símbolos no-op**. (1) `Main.contentType()` devolvía `application/octet-stream` para todo salvo `.html/.js/.css/.json` → los pips de maná (`<img src="symbols/*.svg">`, `ArenaManaSymbols`) no renderizaban por la URL pública; fix: MIME real para svg/png/jpg/jpeg/gif/webp/ico/woff(2)/ttf/otf/wav/mp3/ogg/wasm/txt/xml (+ `MainContentTypeTest`). (2) El gestor de descargas dependía de Cache Storage, inexistente en contexto no seguro (HTTP no-localhost) → no-op silencioso; se **retira la UI** (botones de header/sidebar + paso `cards` del wizard; borrados `DownloadImagesDialog`, `DownloadPanel`, `imageDownloader` y su CSS/tests). Las imágenes de carta siguen cargando online bajo demanda desde Scryfall. | vitest 158f/1245 ✅; typecheck ✅; build ✅; java 58 ✅ (MainContentTypeTest 2/2) |
| 2026-09-09 | Fix/rollback-v2 | **Rollback v2 (v1 no funcionaba: el aviso `Rolling back…` solo va al chat, nunca como callback — `GameController.java:126-129`)**: flag renombrado a `rollbackPendingFor`, armado con la acción propia (`requestRollback` ok y Accept `ADD_PERMISSION_TO_ROLLBACK_TURN` ok, sin depender del servidor) + sniff del anuncio en el chat como señal secundaria + desarme ante denegación (`denied/not possible/only request/not available`) + no se limpia en accepts normales + botón de solicitar deshabilitado sin prioridad (el servidor lo rechaza siempre) | vitest 156f/1238 ✅ (+6 rollback, +1 UserRequest accept arma, +1 RollbackDialog sin prioridad; store.test GAME_OVER-autosave flakea ~1/5 en aislado, preexistente) ; typecheck ✅ |
| 2026-09-09 | Fix/rollback-dead | **Rollback dejaba la partida muerta (2 clientes, servidor oficial)**: aceptado el rollback, ambos se congelaban (aceptante igual, solicitante "pasando turno" sin efecto). Causa en el cliente: `isOlderThanCurrentGame` descartaba todas las vistas restauradas (turno hacia atrás) para siempre. Fix: latch `rollbackAnnouncedFor` armado por el propio aviso del servidor (`Rolling back to start of turn N`, `GAME_UPDATE_AND_INFORM`/`GAME_INFORM`, orden FIFO garantizado) → la vista restaurada se acepta y se limpia la UI pre-rollback (`feedback`/`playables`/`combat`) + log visible; sin anuncio el guard sigue descartando viejas (test existente intacto) | vitest 156f/1233 ✅ (+3 rollback.test.ts: restaurada vía AND_INFORM, vía GAME_INFORM, vieja sin anuncio descartada); typecheck ✅ |
| 2026-09-09 | Fix/bracket-size | **Panel del bracket más grande**: `TournamentPanel` pasa de `min(1100px,100%)×90vh` a `min(1600px,96vw)×94vh` (~75%+ de la pantalla; el bracket interno ya era `width:100%`) | bracket/panel tests 20 ✅ |
| 2026-09-09 | Fix/construct-basics | **Construcción de draft sin tierras básicas (gap bloqueante)**: `ConstructScreen` solo movía cartas del pool y `handleInc` no crea entradas → imposible llegar a 40 jugables. Fix: `BasicLandAdder` cableado a la columna del main con `format="Limited"` (botones +1/−1 por color + Wastes, selector de edición DMU/ZEN/BFZ/UNH/UST, sugeridor por pips con default 17) + handlers `add/remove/applySuggested` sobre `setMain` (reúso de `replaceBasicLands`) con `updateMetaForCards` de la impresión nueva. Sin cambios de protocolo: viajan en `submitDeck.cards` como una carta más; impresiones reales aceptadas por `Deck.load` estricto | vitest 155/1230 ✅ (+2 ConstructScreen: +40 Plains→submit payload, −1 hasta eliminar); typecheck ✅ |
| 2026-09-09 | Fix/bracket-mini-drag | **Pill del bracket arrastrable (`TournamentPanel` mini tapaba la mano rival)**: drag con PointerEvents + umbral click-vs-drag 6px (click abre, drag mueve sin abrir), memoria en `localStorage:tournament_mini_pos`, posición inicial centro como siempre. Nuevo hook `ui/useDraggablePos.ts` (`sanitize/clamp/load` puros + re-clamp al montar y en `resize`) para no repetir el bug del chat (pos guardada fuera de la ventana → invisible). **Fix colateral**: `FloatingChat` ahora también reclama su pos al cargar y en `resize` (misma fórmula de clamp que su drag). Solo el pill; el panel expandido sigue centrado; en móvil ≤600px solo tap | vitest 154/1228 ✅ (+9: 6 clamp/sanitize + 3 pill drag/persist/reclamp); typecheck ✅ |
| 2026-09-09 | Fix/quit-ratio-tourney | **Crear torneo draft con `quitRatio 100%` fallaba con `requirement 0%` (beta.xmage.today)**: `tableKind.ts` omitía `quitRatio` cuando era 100 (`undefined`) y `MatchOptionsParser.java:136` solo fijaba el outer `TournamentOptions.quitRatio` con `if (has)` → quedaba en default Java `0` (el más restrictivo), mientras mesa (`:27`) e inner (`:157`) sí defaulteaban a 100 — por eso construida OK y draft KO. Fix: proxy siempre `setQuitRatio(getInt(...,100))` + web envía `quitRatio` explícito siempre + etiqueta `Minimum/Mínimo→Allowed/Máximos permitidos` ×9 locales (semántica real: % máximo permitido). `MageServerImpl.java:248-253` valida `tableQuit < userQuit → reject` | vitest 153/1219 ✅; typecheck ✅; java 56 ✅; jar rebuilt + proxy restarted |
| 2026-09-08 | Feat/v1-cleanup | **Limpieza v1**: `bundledDecks()` (9 mazos `Mage Web *` solo en builds dev; en prod el usuario aporta los suyos) + wizard/join/manager nulleables con estados vacíos que guían a importar (`create_err_no_deck` ×9) + validación bloquea crear/unirse sin mazo; botón Add-AI-Bot solo dev; flags `skip*` nunca se envían en prod; hooks `__mageStore/__mageScene/__mageFlights` solo dev (fuera intervalo 500ms en prod); login sin `player1/password` en prod + overrides `VITE_DEFAULT_*`; idioma por defecto inglés (e2e fija `es-ES`, suite inalterada vía `test-setup.ts`); tree-shaking deja 7/9 mazos fuera del bundle prod (bolt/AI-lands quedan como código muerto inalcanzable) | vitest 146/1109 ✅; typecheck ✅; build ✅; e2e fake 107 ✅ + 2 fallos preexistentes (settings-sin-iconos, tournament-T5 orden-dependiente, ambos reproducen en limpio) | **Menú ⋯ de partida simplificado**: fuera paradas (se tocan directo en la PhaseBar: click=propias, shift+click=rival), fuera Board View / Apariencia / Acerca de (viven en el diálogo de Configuración como en el lobby). La entrada Configuración abre `SettingsModal` en la pestaña Gameplay en vez del expansor de sonido inline. Limpieza de CSS huérfano (`.game-menu-fx`, radios, news-dot) y testid `phase-bar-step-<KEY>` en la PhaseBar. Sin cambios de protocolo | vitest 145/1108 ✅; typecheck ✅; build ✅; e2e phase-stops 2/2 + concede 1/1 + mana-payment/auto-answers/interactions ✅ | **Deep linking "one-click join" (`docs/enhancements.md` §2.1)**: URLs `#join=<tableId>&pwd=…&server=host:port` / `#watch=…` (hash: hosting estático, nunca viaja al servidor). Nuevo `lobby/deepLink.ts` puro (parse/build tolerante `join=` y `join/` + servidor malformado sin tumbar la invitación) + `useInviteLink` (watch directo sin mazo; join abre `JoinTableDialog` con password pre-rellenada vía `initialPassword`; servidor distinto → `confirm` explícito + `doConnect` conservando el proxy propio; mesa ausente → reintento 10s por el lag del broadcast y aviso). `App.tsx` captura al montar + `hashchange` (limpia el hash tras capturar); `LoginScreen` banner si hay invitación sin sesión; `InviteLinkButton` (join+watch, fallback `execCommand`) en la cabecera del staging + CSS `.tag-invite`/banner login. 6 claves i18n ×9. Sin cambios de protocolo | vitest 145/1111 ✅ (deepLink 9 + prefill + InviteLinkButton 2); typecheck ✅; build ✅; e2e invite-link 2/2 + staging+chat 15/15 ✅ |
| 2026-09-08 | Parity/U14+Dialogs | **U14 cerrada + sistema de diseño de diálogos unificado (D0–D3)**: `DialogShell` (`ui/`) con tokens `--dlg-*` en `styles.css` (kicker-pill/título/mensaje/acciones/search/topRight/sectionProps; regla kicker≠título) y 24 modales migrados (partida: Generic/Mulligan/Voting/Trigger/StartingPlayer/LibraryOrder/Planeswalker/PileDialog nuevo/CardGrid/Rollback/UserRequest/LimitedDeck/GameEnd/HelpWiki; lobby: Join/DeckIssues/DownloadImages/Leaderboard/TournamentBracket/RandomPacks/CreateTable/DeckManager-import; mazos: Printings/Import/Inspector/SampleHand; Appearance/Sleeves/Avatar/Settings/About) con aliases legacy para e2e. Fuera de alcance declarado: pantallas full-screen (Draft/Sideboard/Construct/TournamentPanel) y popovers (UserAction/ArenaFilterBar). U14: P14-1 búsqueda+teclado en grid, P14-2 `sortData`, P14-3 `hintData`, P14-4 `choiceMemory` solo-cliente (diferencia declarada: sin reenvío `isSpecial`), P14-5 por construcción (sin dato en red), P14-6 doble-click/Enter, `PileDialog` visual, ShowCards por combinación (sin callback en protocolo). Barras maná/objetivo/combate conservan postura no-bloqueante con el mismo idioma | vitest 1001 ✅; typecheck ✅; e2e grid-search 2/2 + choice-memory 2/2 + pile-visual 1/1 + mulligan/voting/trigger/pw/interactions/staging/wizard/about/deckvalidation/decks-gallery/printing-preview/mana-payment/targeting/combat ✅ |
| 2026-09-10 | Arquitectura/Fork | **Fork XMage aislado del producto (feedback: "make it just the frontend")**: `Mage.*` + pom raíz + Makefile + clean_dbs.sh + repository/ fuera del repo → checkout hermano `../xmage-fork` (rama `nexus` = tag upstream `xmage_1.4.61V1` + overlay de 12 ficheros: opciones test-mode `skipInitShuffling`/`skipStartingPlayerChoice`, vistas `CardView`/`StackAbilityView`, `Deck`, [SLZ] The Zeta Set; backup = `origin/nexus`, ancestro upstream intacto). `forkDir()` en `lib.mjs` (NEXUS_FORK_DIR → ../xmage-fork); build del motor corre dentro del fork; `Mage.Proxy` pom standalone (`mvn -f Mage.Proxy/pom.xml test`, 58 ✅) contra artefactos org.mage en ~/.m2 (`ensureMageArtifacts`); oráculos de esquema via `forkPath` (fixtures byte-idénticos ✓); workflows de fork eliminados (maven/modules/mtg-fetch-cards/release); release `engine-1.4.61-v1` con los 4 jars. Lecciones: el repo Adamychen/xmage-nexus YA era fork de magefree/mage (renamed) → fusión de releases futura sin fricción; el launcher desktop puede ocupar 17171/8787 y dejar pids huérfanos en .run/ → matar antes de self-tests. **Fallo e2e preexistente detectado** (ventana 09-05→09-10, verificado NO causado por el split vía revert-bisect ni por el trabajo de modales): specs `skips`/`spells`/`targeting`/… aterrizan en staging de espectador (2/2, sin `staging-start`, sin botón Empezar en lobby); repro `npx playwright test e2e/skips.spec.ts`; pendiente de triage. Post-split: unit 1272 ✅ · typecheck ✅ · java 58 ✅ · build ✅ · self-test 15 ✅ | `node scripts/test.mjs unit typecheck java build` ✅ · `self-test` ✅ (tras matar procesos del launcher) · e2e fake: fallo preexistente documentado en AGENTS.md "Known failure" | 252 |
| 2026-09-08 | Fix/flight-anim | **Animación de movimiento intermitente**: diagnóstico con telemetría (`noteFlightEvent` + `window.__mageFlights` + `e2e/flights.spec.ts` con inyección sintética de `GAME_UPDATE`s y ráfaga real a 50ms). Causas probadas: (1) el descarte mano→cementerio NUNCA volaba (sin rama en el engine + update in-place del top); (2) en ráfaga la 3ª transición se teletransportaba (guard `hasFlightFor` + cancel `dedupe` a los ~57ms); (3) §2C volaba fantasmas erróneos al cementerio para bounces/tokens. Fixes: `startCardFlight` **encadena** (redirige el fantasma en vuelo, reprograma timers; ignora mismo-destino <120ms; reemplaza solo si caducó) en vez de cancelar; ramas nuevas mano→cementerio (descarte/molino), campo→mano (bounce), campo→exilio; §2C acotada a llegadas reales; origen de reanimar desde el cementerio; fallback a la banda en entrada a campo | vitest 1003 ✅; e2e flights 1/1 + spells/reveal/missing-prompts/full-flow/mechanics/combat/mana-payment ✅ |
| 2026-09-08 | Fix/staging-overflow | **Meta del asiento desbordaba la tarjeta del anillo** (132px fijos; `RankBadge nowrap` + historial sin wrap): `.player-seat-meta` con `flex-wrap` + centrado (cubre anillo y duelo), `RankBadge` con `max-width+ellipsis`. Test e2e `staging.spec.ts` (mesa Commander 2 asientos + rating + historial, assert `scrollWidth<=clientWidth`; verificado que falla sin el fix) | vitest 1003 ✅; staging+staging-chat 11/11 ✅ |
| 2026-09-08 | Fix/lobby-i18n | **Textos quemados en español → i18n ×9**: `ui_scale_*`/`cjk_boost_*` (11 claves, SettingsModal + AppearanceSettingsModal incl. descriptores de preset) y `filter_format_limited`/`filter_selected_format` (chip `Limited` + `Formato: X` en TableFilterBar) | typecheck ✅; i18n.coverage 4/4 ✅; vitest 1003 ✅ |
| 2026-09-08 | Feat/feed-i18n | **Log de partida traducido ×9 + parser con idioma UI**: `parseGameEvent` devolvía español hardcodeado (mezcla ターン + `pierde vidas` con UI≠es) y 6 familias de líneas caían en inglés crudo. Refactor: el parser devuelve `{i18nKey, params}` (+ `verbatim` para avisos ya-españoles) y `ActionFeed` formatea con `t()` vía `formatFeedText`/`toFeedItem` (sub-claves `@fase/@destino` reutilizan `step_*`). Nuevos patrones: `X - Waiting for Y`, `attacks <def> with N creatures` (+plural), `Player - Ability triggers … [- targeting T]`, `Attacker:/Attacked player:`, `reveals`, `puts … into/hand/grave/top/exile/battlefield (source:)`, `won the game/match`, `has conceded`. 45 claves `feed_*` ×9 locales; nombres de jugador/carta siempre verbatim | vitest 1012 ✅ (+15 parser); typecheck ✅; build ✅; i18n.coverage 4/4 ✅; e2e full-flow + mechanics ✅ |
| 2026-09-08 | Fix/lobby-chat | **Abrir el chat desplazaba/encogía la vista de mesas**: la regla `:has(aside.floating-chat) > .lobby-main { margin-right:380px }` (≥601px) reservaba el hueco del panel en TODAS las vistas → en ventanas ~870px el contenido quedaba comprimido a la izquierda con un vacío a la derecha. Fix: la reserva solo aplica dentro del DeckBuilder (`has-deck-builder`, donde viven Exportar/Importar) y en pantallas anchas (≥1100px); en mesas el flotante solapa sin mover el layout (es arrastrable/minimizable). Regresión e2e en `chat.spec` (ancho de `.lobby-main` invariante al abrir el FAB; falla con la regla vieja) | chat.spec 4/4 ✅; decks-gallery responsive 1/1 ✅; typecheck ✅ |
| 2026-09-08 | Fix/deck-art | **Arte no se actualizaba al cambiar de edición**: `updateMetaForDeck` saltaba la descarga si existía la clave por nombre (misma carta, nuevo set/nº) → tira/preview/portada quedaban con el arte viejo. Fix: con set+nº se descarga siempre que falte la clave exacta (sin nº se mantiene el atajo por nombre). En partida no había bug (`cardKey` por set/nº del servidor; el arte nuevo llega al re-enviar el mazo). Cazado por unit (`useDeckMetadata.test`, falla con el código viejo) + e2e `printing-preview.spec` (Scryfall ruteado A→B, también falla sin el fix) | vitest 986 ✅; typecheck ✅; e2e decks-gallery + printing-preview 7/7 ✅ |
| 2026-09-08 | Parity/U15 | **U15 Sistema auditada y cerrada (G15-1…G15-7)**: `AboutModal` (versión `package.json`, links; entrada en GameMenu/login/lobby-header) + news remoto doble feed (releases GitHub Nexus+XMage oficial, caché 24h, badge ●, offline-first, coste 0 — requiere publicar releases para contenido propio) + auto-guardado de logs a IndexedDB (tope 20, toggle default ON, descarga HTML fondo negro como desktop) + duración en fin de partida (contrato ya la traía) + selector de set de tierras (DMU/ZEN/BFZ/UNH/UST full-art, nºs verificados vía Scryfall — BFZ no consecutiva) + docs. Diferencias declaradas: atajos fijos, música de carpeta, proxy SOCKS, vida final (no viaja en contrato), playmats (ROADMAP 3.4). Resta solo **U8 generador** como ❌ del mapa | vitest 984 ✅ (19 nuevos); typecheck ✅; build ✅; e2e `about.spec` 1/1 + settings/decks-gallery sin regresión ✅ |
| 2026-09-08 | Parity/U11 | **G11-5 paradas de fase con defaults persistentes**: default todo-marcado (7 fases × tu turno/rival) en `game/phaseStops.ts` (única fuente); `mage-web-phase-stops` + campo en settings (patrón `manaPayment`); bloque en ajustes lobby/Jugabilidad (grid compartido `PhaseStopGrid`, i18n 2 claves ×9); `PhaseStopSelector` montado en menú ⋯ (sesión); seed desde settings en `GAME_INIT` + push de settings al login (fix: el gateway empujaba los de sesión sin sembrar). Cazado con e2e: el push de login llegaba antes del seed | vitest 128/965 ✅; typecheck ✅; e2e `phase-stops.spec` 2/2 (persistencia cross-sesión, seed + solo-sesión) + mana-payment/settings/skips sin regresión ✅ |
| 2026-09-08 | Parity/U11 | **U11 cerrada (G11-4 macros → no-aplica)**: el botón desktop solo envía `TOGGLE_RECORD_MACRO` (`GamePanel.java:2855`) sin consumidor — 0 referencias a `Macro` en el motor `Mage/` y el servidor `Mage.Server/`; caso de uso cubierto por skips (G11-1) + auto-respuestas (G11-3). Fila U11 → ✅ en `COMPONENT_PARITY.md` | docs-only (sin suite requerida) ✅ |
| 2026-09-08 | Draft/U9 | **Auditoría y cierre U9 (draft)**: comparativa `DraftPanel/DraftGrid/DraftPane/DraftPickLogger` vs `DraftScreen`. Delta: **U9-1** ocultar pickeadas (botón 👁 + F9 + enlace ocultas + `hiddenCards` en el pick, el proxy ya lo aceptaba); **U9-2** confirm quit (i18n ×9); **U9-3** tira mesa/asientos + dirección ←/→ (campo `players` antes ignorado); **U9-4** timer naranja ≤30s + `timer_tick` a 6s + aviso con pestaña oculta; **U9-5** anti-doble-pick 1.5s; **U9-6** `draftLog.ts` + botón descarga `.draft` reimportable por U7-1 (test roundtrip); **U9-7** sobre ordenado por rareza (verificado: el servidor no ordena, `SimpleCardView` sin rareza; ranking = `Rarity.getSorting()`). Fix colateral: `DraftScreen`+`ConstructScreen` montados 2× (`App`+`GameScreen`) → solo `App`; el preview flotante del draft interceptaba el puntero (sin regla CSS) → `pointer-events:none`. | vitest 903 ✅ (+9 U9); typecheck ✅; build ✅; e2e fake `draft.spec` 3/3 (test U9: mesa/ocultar/F9/log) ✅ |
| 2026-09-08 | Tourn/U10-A | **U10 Fase A (quick wins)**: **T1** ojo por match clicable (`bracket-watch` + `watchTournamentMatch`→`watchTournamentTable(matchTableId)`, cableado en modal lobby y panel de partida; semántica verificada en fuente: `roomWatchTournament`→`tableManager().watchTable`, igual que desktop); **T2** flags de creación torneo (skill/rated/rollback/clocks/minRating/quitRatio/bans al `tArgs` + `rated/bannedUsers/rollbackTurnsAllowed` en `parseTournamentOptions` + checkbox `isSingleMultiplayerGame` i18n ×9); **T3** confirm al salir (×9, lobby+partida), fechas inicio/fin, cuenta atrás de construcción en estado Constructing, nº rondas solo en tipos Swiss. Jar del proxy reconstruido + reiniciado. Pendiente Fase B (chat torneo, RandomPacksSelector, timing draft) + verificación real de ver un match vivo. | vitest 912 ✅ (+9); typecheck ✅; build ✅; java proxy 45 ✅ (+1); e2e fake `tournament.spec` 2/2 (T1: panel + modal vía WS) ✅ |
| 2026-09-08 | Tourn/U10-B | **U10 Fase B + cierre (chat, packs, timing)**: **T4** chat de torneo solo en `TournamentPanel` (decisión: el id correcto solo existe ahí) — `getTournamentChatId` + `enter/exitTournamentChat` + `<ChatBox chatIdOverride>` + allowlist `chat.ts`; salida en unmount/`TOURNAMENT_OVER`/lobby/disconnect; `FakeServer` + escenario con `getTournamentChatId` y eco. **T5** `RandomPacksSelector` fiel al desktop — comando proxy `getExpansionsWithBoosters` (solo proxy, sin fork: `ExpansionRepository.instance`, `[]` si la BD no está lista; documentado en README) + diálogo (todos marcados, all/none, shuffle+truncado `3×(j+1)`/RichMan 36, `;`-join; `parseLimitedSetCodes` parte `;`). **T6** timing de draft — select BEGINNER/REGULAR/PROFESSIONAL (def. REGULAR) solo tipos con `Draft` + `timing` en `limitedOptions` + `DraftOptions` en `MatchOptionsParser` (sin fork). Jar reconstruido + proxy reiniciado ×2. Fila U10→✅ + §U10 en `COMPONENT_PARITY.md`. | vitest 925 ✅ (+13: T6 1, T4 5, T5 7); typecheck ✅; build ✅; java proxy 48 ✅ (+3: timing 2, expansions 1); e2e fake `tournament.spec` 4/4 (T4 chat con eco, T5 diálogo→input) ✅ |
| 2026-09-08 | E2E pendientes | **Cierre de los 2 pendientes conocidos**: **multi-user real** — causa confirmada en captura (el servidor rechaza el join humano: `Lightning Bolt: Too many: 16` en Modern); fix quirúrgico `createTable(..., { deck: 'Mage Web starter' })` en el spec (sin tocar `DEFAULT_DECK`, load-bearing en fake) → real 1/1 ✅. **Ver match vivo de torneo en real** — nuevo `scripts/verify-tournament-watch.mjs` (headless WS, Constructed Elimination 2×COMPUTER_MAD: los HUMAN/SIM no sirven — el proxy no expone `joinTournament`): **13/13 PASS** (`WATCHGAME` + `GAME_INIT` 2 jugadores tras `watchTournamentTable(matchTableId)` — el ojo T1 verificado de punta a punta). Hallazgos de robustez del parser (NPEs del servidor si faltan claves): `password`→`''` y `LimitedOptions` por defecto siempre (con test). | multi-user real 1/1 ✅; verify-tournament-watch 13/13 ✅; java proxy 49 ✅ (+1) |
| 2026-09-08 | Game/U12 | **Auditoría y cierre U12 (zonas y jugador)**: **G12-1** menú clic-derecho en `PlayerInfoBar` → `PlayerContextMenu` (portal, items por bando en `playerMenu.ts` puro) + 5 wrappers `commands.ts` + `allowHandRequests` persistido + `UserRequestDialog` envía `relatedUserId` como `data` + Switch Hands (toggle ⟲ en zona propia con `opponentHands`); **G12-2** disparadores `VIEW_SIDEBOARD/VIEW_LIMITED_DECK` (ya pintaban) + `InfoWindows.tsx` auto por presencia en vista; **G12-3** `CARD_CONTEXT_ITEMS` eliminado (sin menú de carta en desktop para gameplay; gesto cubierto a nivel jugador); **G12-4** filtro `phasedIn !== false` en `BoardZone`. Fixes colaterales con test: proxy enviaba `allowRequestShowHandCards=false` (default desktop true, 1 línea en `ProxyClient`) y `JsonArgs` no convertía String→UUID en las 4 acciones (grant silencioso sin esto); `?? []` dentro del selector de `useStore` = loop infinito (cazado en e2e). i18n 9 claves ×9. | vitest 127 files/951 ✅; typecheck ✅; build ✅; java 167 ✅; self-test ✅; human-test ✅; e2e fake **89 pass/2 skip** ✅ (player-menu 2/2, info-windows 1/1); `test.mjs` 7 pass/2 fail solo por timeout 600s del harness e2e + coverage (re-verde tras añadir tests de commands: 80.4%/71.3%); `verify-hand-permission.mjs` **19/19 ×2 en real** |
| 2026-09-08 | Game/Designations | **Señal Monstruosidad verificada en vivo + badges Monstrous/Renowned**: sonda `record.mjs monstrosity` (Polukranos THS#172, X=1) demuestra que el motor emite la designación en `rules` (`MonstrousHint`/`RenownedHint` vía `ConditionHint`: `ICON_GOOD{this} is monstrous`, rama `ICON_BAD… isn't …` en negativo) — no hace falta reconstrucción ni cambio upstream; funciona en beta. Solo `harnessed` sigue invisible (`HarnessedHint` huérfano, sin adjunto en `TheMindStone`/`TheSoulStone`). Implementado solo-web: `board/designations.ts` (parse anclado + `substituteCardRefs`), badges `.designation-badge` en `CardSlot` (i18n 4 claves ×9), prop `cardName` en `FormattedText` (hover + pila), escenario `mechanics.ts` (Keyword Beast monstruosa + Consul's Lieutenant renombrada). Harness: `REC_DUMP_EVENTS=1` (sidecar `.events.jsonl`) + `GAME_GET_AMOUNT`→`sendPlayerInteger` en `rec-lib`. Lecciones: el callback real es `GAME_GET_AMOUNT` (no `TARGET_AMOUNT`), Polukranos es `{X}{X}{G}{G}`, el trigger "divide damage" se confirma con boolean `false`. | vitest 134 files/1024 ✅; typecheck ✅; build ✅; e2e fake `keywords`+`mechanics` ✅ |
| 2026-09-08 | Audit/EngineGaps | **Auditoría estática de gaps de motor restantes (sin stack)**: mecanismo maestro hallado — `PermanentImpl.getRules` genera hints de restricción desde `RestrictionEffect/RequirementEffect` (`ICON_RESTRICT/REQUIRE` → icono ⚠) y vuelca el mapa `info` en `rules`. Veredictos: **suspect visible** vía línea `info` ("Suspected (has menace and can't block)" + menace concedida; falta solo badge propio, propuesta solo-cliente); **classLevel cubierto** vía `ClassLevelHint`; **soulbond invisible** (pareja sin señal; propuesta upstream/fork línea `info` "Paired with X"); **solved invisible** (`CaseSolvedHint` huérfano, fix de 3 líneas); resto del baseline descartado (bookkeeping combate/ids/timers o cubierto por otros canales). Tabla cerrada en `INTERACTION_COVERAGE.md`. | solo lectura, sin suite requerida |
| 2026-09-08 | Game/Suspect+ForkDocs | **Badge Suspected + fixes fork documentados (corrección soulbond)**: al ir al sitio exacto, soulbond SÍ emite línea `info` (`setPairedWith`→`addInfo("soulbond","Paired with …")`, visible en `rules`; no requiere fix). Fixes documentados con sitio exacto en `INTERACTION_COVERAGE.md`: harnessed (`ability.addHint(HarnessedHint.instance)` en `TheMindStone:54`/`TheSoulStone:47`) y solved (`this.addHint(new CaseSolvedHint(SolvedSourceCondition.SOLVED))` en ctor `CaseAbility`). Badge `.designation-badge.is-suspected` (parsea la línea `info`, i18n 2 claves ×9, Keyword Beast de ejemplo en `mechanics.ts`). | vitest 134 files/1027 ✅; typecheck ✅; build ✅; e2e fake `keywords`+`mechanics` ✅ |
| 2026-09-08 | Game/PairedBadge | **Badge soulbond solo-cliente**: `pairedPartnerName()` extrae el partner de la línea `info` ("Paired with \<font\>Nombre [hash]\</font\>") y `CardSlot` pinta `.designation-badge.is-paired` con tooltip "Emparejada con X" (i18n con `{name}` ×9). Keyword Beast ↔ Consul's Lieutenant emparejadas en `mechanics.ts` + asserts e2e. | vitest 134 files/1030 ✅; typecheck ✅; build ✅; e2e fake `keywords`+`mechanics` ✅ |
| 2026-09-08 | Game/Keywords-i18n | **Keywords traducidas ×9 (267 mecánicas)**: el display quemaba español (`nameEs/summary` en `mtgKeywords.ts` + ramas hardcodeadas en `keywordExtractor.ts`). Nueva sección `i18n.keywords` (`<id>_name/<id>_summary`, 534 claves ×9 vía `scripts/gen-keywords-i18n.mjs` desde `scripts/keywords-i18n/*.mjs`); wording oficial verificado contra `printed_text` de Scryfall (`scripts/verify-keywords-scryfall.py`: Ward=Abwehr/parade/egida/salvaguarda/護法/守护/Rebatir, Toxic=Toxisch/toxique/毒性/下毒/Tóxico, Kicker=Bonus/Kick/Potenziamento/Reforçar/Усилитель/キッカー/增幅, Haste RU=Ускорение, Menace DE=Bedrohlich/ZHS=威慑, etc.; 8 términos es corregidos a oficial: Rebatir/Victimizar/Confabular/Escrutar/Disfrazarse/Injertar/Tripular/Respaldar). Display vía `data/keywordI18n.ts` (plantillas `{param}`, fallback EN; RU sin impresión rusa → EN honesto) en `CardSlot`/`FloatingCardPreview`/`HelpWikiModal` (2 claves `wiki.rule_*` para snippets genéricos) + labels de chips en `ArenaFilterBar` (valores EN para Scryfall). `extract-keywords.mjs` documentado como legacy. | vitest 135 files/1037 ✅ (+5 `keywordI18n`); typecheck ✅; build ✅; i18n.coverage script+vitest ✅ (1715 claves, 5 intraducibles es a whitelist); e2e fake `keywords`+`interactions` ✅ |
| 2026-09-08 | Game/ClassLevelBadge | **Badge nivel de Clase solo-cliente**: `classLevelOf()` parsea la línea `ClassLevelHint` ("Class level: N") y `CardSlot` pinta `.designation-badge.is-classlevel` con el nivel (`Nv. 2`, tooltip `…/3`, i18n con `{level}` ×9). Bard Class en `mechanics.ts` + asserts e2e. Con esto, todas las designaciones con señal quedan con badge: goad/monstrous/renowned/suspected/paired/classlevel. Solo solved/harnessed siguen bloqueados en fork. | vitest 134 files/1032 ✅; typecheck ✅; build ✅; e2e fake `keywords`+`mechanics` ✅ |
| 2026-09-08 | Game/HandMorph | **Hover de mano con transición carta→preview (FLIP)**: el preview ya no aparece de golpe en otra capa — nace de la posición exacta de la carta (mapeo del rect final sobre el `anchorRect`, escala uniforme 1:1.4, `transform`+`opacity` compositados, 0.28s) mientras la carta del abanico se atenúa al 30% (sin `scale(1.15)` ni glow, que competían); keywords/reglas entran en fade con retardo 0.15s. Salida inversa (0.18s) vía fase `leaving` en el presenter (`PREVIEW_LEAVE_MS=200`). El morph es explícito (`fromHand` desde `HandBar` vía `handleCardHover(card,rect,{fromHand:true})` en los 3 tableros), nunca posicional: la heurística inicial por franja inferior hacía morph en las tierras del campo. Solo mano; resto de zonas conserva la animación anterior; `prefers-reduced-motion` salta al estado final. Lecciones e2e: `is-open` = clase aplicada, no transición terminada — la aserción geométrica sondea con `expect.poll` hasta caja estable (medir a mitad de vuelo daba ~166px de error). | vitest 136 files/1045 ✅ (+4 morph, +1 no-morph, +3 leaving/fromHand); typecheck ✅; build ✅; e2e fake `hand-bar` 4/4 (nuevo: tierra del campo sin morph — verificado que falla con el código posicional) + `keywords`/`mechanics`/`interactions` ✅ |
| 2026-09-08 | Game/StackGroups | **Apilado visual generalizado desde ×3** (`board/stackGroups.ts`, `STACK_GROUP_MIN=3`): el acordeón de tierras se aplica a todo lo fungible — tierras por nombre (igual que antes), tokens no-criatura por nombre+giro (Tesoro/Pista/Comida…; los girados forman su propia pila), tokens criatura por nombre+giro+P/T solo si estado limpio e idéntico (sin daño, contadores, combate, morph/face-down ni mutate; el divergente queda suelto y el grupo se disuelve solo al entrar en combate). No-token nunca se apila. Cada carta sigue siendo `CardSlot` individual (click/hover/targeting intactos). CSS `.stack-group` con `.land-group` como alias (sin regresión en tests/e2e existentes); `title` plano `Nombre (×N)` sin i18n (nombre propio + numeral). Lecciones e2e: hover directo a una carta tapada por el solape falla (`intercepts pointer events`) — primero hover al grupo para expandir; `GAME_SELECT` plano no pinta `.feedback-dialog` — el eco de click usa `GAME_CHOOSE_ABILITY`. | vitest 137 files/1055 ✅ (+9 `stackGroups`, +1 componente tokens); typecheck ✅; build ✅; e2e fake nuevo `stack-groups` 1/1 (verificado que falla sin el cambio) + `hand-bar`/`stack-priority`/`mechanics`/`keywords`/`mutate`/`combat` ✅ |
| 2026-09-08 | Game/MarqueeDock | **Dock marquee a la derecha de la fila de criaturas** (sagas/planeswalkers/batallas, sin fila nueva): `board/marquee.ts` (`isMarqueePermanent` por `cardTypes`/`mageObjectType`, excluye criaturas) + partición en `BoardZone` que extrae el marquee de `others` antes del stacking; dock `.bz-marquee` como último hijo de `creatures-row` (`flex-shrink:0`, `margin-left:auto`, `max-width:45%` + drag-scroll, espejo del dock del comandante que va a la izquierda). Las sagas-criatura se quedan en criaturas (combate manda); el marquee nunca se apila (lealtad/defensa/lore individuales) y `renderCardItem` no cambia (auras/targeting/preview intactos). La banda pierde `full-width` solo si hay dock; sin i18n (sin etiquetas). | vitest 138 files/1060 ✅ (+3 `marquee`, +2 componente dock); typecheck ✅; build ✅; e2e fake `mechanics` +1 (Jace e Invasion en el dock, Bard Class en permanentes — verificado que falla sin el cambio) + `hand-bar`/`stack-groups`/`stack-priority`/`keywords`/`interactions` ✅ |
| 2026-09-08 | Game/LogUnificado | **Feed unificado con diseño carta** (`ActionFeedCard.tsx`, `gameEventParser.ts`): desaparece la píldora `.action-feed-system-banner` — todo evento sin carta (robos, keeps, skips, esperas, system) usa el mismo layout que Mountain (cabecera icono+tag, cuerpo descripción) con degradado del color de acento del tipo en vez de arte (`.action-card-bg--tint.tint-<tipo>`), variante compacta `.no-art` (sin `min-height`), y bordes laterales para `phase/system/life/chat`. Parser nuevo: robos con número en palabra (`draws seven cards` → `feed_draw_n` amount 7), `keeps hand` (`feed_keep_hand`), `skips <Step> step` (`feed_skip_step`, reutiliza `@step_*`), `Waiting for X` sin fase (`feed_waiting_player`), `chooses that Y take the first turn` (`feed_first_turn`); 4 claves ×9 idiomas. De paso se elimina el fallback `tables_heading` ("Mesas Disponibles" como tag de jugador en cartas sin jugador — bug). | vitest 138 files/1063 ✅ (+2 parser, +1 tarjeta); typecheck ✅; build ✅; e2e fake nuevo `feed-unified` 1/1 (5 líneas por chat de partida, cero banners — verificado que falla sin el cambio) + `mechanics`/`hand-bar` ✅ |
| 2026-09-08 | Game/PromptSound | **Sonido `prompt_open` al abrir ventanas de interacción** (`audio/promptSound.ts`, clave nueva en `types.ts` + `soundSynthesizer.ts`): dos notas ascendentes E5→B5 en 0,16 s (pico 0,27, sin clipping), categoría `ui` (sigue el volumen UI), throttle propio 150 ms. Hook en los 4 puntos que fijan feedback real (`eventHandler` genérico, `handleGameTarget`, `handleGameAsk` ×2 ramas, `GAME_SELECT` en `handleGameUpdate`) con dedupe por firma (método+juego+título+mensaje+modo+opciones) para no sonar en ráfaga ante reenvíos; `clearFeedback` resetea la firma (cada apertura real suena). No suena con auto-respuestas (choiceMemory/autoAnswers/auto-keep) ni en GAME_SELECT de prioridad (sin ventana). Distinto del de acciones a propósito: el aviso es raro y saliente, los de tablero frecuentes y sutiles. | vitest 139 files/1071 ✅ (+8 `promptSound`, +1 clave en `ALL_SOUND_KEYS`); typecheck ✅; build ✅ |
| 2026-09-08 | Game/ManaUnificado | **Maná inline unificado en todas las vistas** (nuevo `game/ManaPoolView.tsx` + css): los 6 pips WUBRGC con cuenta siempre visibles, sin desplegable en standard ni variantes por modo; click-to-pay donde corresponde (mismo `sendPlayerManaType`, mismos `data-testid="mana-pay-X"`). El diálogo "Pagar maná" cambia botones de texto `[W2]` por los mismos pips (clicables solo con reserva). Eliminado `PlayerResourcePanel` muerto (duplicaba la tabla con letras). CSS huérfano fuera (`.mana-breakdown`, `.resource-mana`, `.mana-pool-btn`). Bug real encontrado por el e2e: el footer fijo de atribución (z-index 40) tapaba los pips e interceptaba clicks — ahora `pointer-events:none` con el enlace a Scryfall aún clicable. | vitest 140 files/1075 ✅ (+3 `ManaPoolView`, +1 ResourceBar standard, FeedbackDialog→`mana-pay-R`); typecheck ✅; build ✅; e2e `mechanics` +1 (6 pips sin `.resource-mana`/`.mana-breakdown) + `mana-payment`/`spells` 7/7 ✅ |
| 2026-09-08 | Game/DeckTracker | **In-Game Deck Tracker estilo MTG Arena / 17Lands** (`game/deckTracker.ts`, `DeckTrackerPanel.tsx` + css): cálculo puro cliente-only de cartas restantes en biblioteca y probabilidades de robo en vivo (Tierras, Criaturas, Instant/Sorcery, Otras y % por carta individual) comparando mazo inicial contra mano, campo (no-tokens), cementerio, exilio y pila; agregación de tierras básicas multi-edición; cartas dobles/MDFCs resueltas; detección de carta revelada en biblioteca (`topCard`); conteo de exilio boca abajo; persistencia de mazo activo (`mage-web-active-deck`); panel en pestaña lateral `[Mazo]` con buscador, 4 órdenes (CMC, nombre, copias, probabilidad), toggle para ocultar agotadas y hover interactivo hacia `FloatingCardPreview`; soporte completo en 9 idiomas (18 claves i18n ×9). | vitest 142 files/1093 ✅ (+12 deckTracker engine, +6 DeckTrackerPanel); typecheck ✅; build ✅; e2e fake spells 6/6 ✅ |
| 2026-09-08 | Lobby/CreateWizard | **Wizard de creación de mesas y torneos completo e intuitivo**: selector de modo superior con 3 tarjetas visuales (`duel`, `multi`, `tourney`), barra de presets a 1-clic (Modern Bo3, Commander 4P, Draft MH3 8P, Modern Swiss 8P); soporte integral para **Torneos Construidos** (`Constructed Swiss`, `Constructed Elimination`) y Limitados con selector de categoría y tamaño (4, 8, 16, 32 jugadores); adaptación de `SeatsTab` (en Draft no exige mazo previo e informa de la fase de draft); badges de torneo en `SummaryStrip` y botón de envío contextual (`Crear Mesa`, `Crear torneo Draft`, `Crear torneo Construido`); eliminación total de emojis residuales por componentes `<Icon />`; 17 claves i18n × 9 locales. | vitest 146 files / 1111 ✅ (+2 tests `CreateTableDialog` 13/13); typecheck ✅; build ✅ |
| 2026-09-08 | Lobby/DraftSetsSelector | **Selector visual e interactivo de sets de draft**: reemplazo del input de texto plano por selector completo (`DraftSetsSelector.tsx` + css); carga en vivo de expansiones con boosters vía `getExpansionsWithBoosters()` ordenadas cronológicamente + fallback curated (`DEFAULT_BOOSTER_SETS`); chips de sets populares a 1-clic (MH3, BLB, FDN, DSK, OTJ, MKM, LCI, M21); buscador interactivo por código o nombre; toggle "Mismo set en todos los sobres" vs "Personalizar por sobre" (desplegables Sobre 1, Sobre 2, etc.); input manual sincronizado para compatibilidad con tests y E2E existentes; 7 claves i18n × 9 idiomas. | vitest 146 files / 1115 ✅ (+4 tests `CreateTableDialog` 17/17); typecheck ✅; build ✅ |
| 2026-09-08 | Lobby/Wizard-i18n | **Internacionalización exhaustiva del Wizard de creación (cobertura 100% en los 9 idiomas)**: traducción íntegra de todos los textos pendientes del wizard (54 claves nuevas en `types.ts` y en todos los locales `es, en, de, fr, it, pt, ru, ja, zhs`): botones de navegación (`wizard_back`, `wizard_next`), subtítulos de cada paso, toggle "Crear como torneo Draft" + desc, tiempo de construcción, cubos + aleatorio, desc de partidas puntuadas, buffer de tiempo, opciones custom de mulligan y reglas especiales, vida/mano inicial custom, desc de Planechase, hint automático de rango/ataque multijugador, tooltips de contraseña, potencia EDH, desc de espectadores y rebobinado, plazas/asientos/bots/esperas y atajos de asignación masiva; helpers de formateo en `constants.ts` (`getTimeLimitLabel`, `getBufferTimeLabel`, `getConstructionTimeLabel`, `seatTypeLabel(tName, t)`). | vitest 146 files / 1115 ✅ (`i18n.coverage` 4/4 ✅, `CreateTableDialog` 17/17 ✅); typecheck ✅; build ✅ |
| 2026-09-08 | Lobby/AddAI-Tourney | **Añadir AI en torneos usaba el endpoint de partida (NPE)**: `useTableActions.joinAi` siempre llamaba `joinTable`; en torneo el servidor hace `table.getMatch().getOptions()` con `getMatch()=null` → `Server error: Cannot invoke Match.getOptions()...` (reproducido headless: `joinTable`→NPE exacto, `joinTournamentTable`→ok). Fix solo-web: branch por `t.isTournament` (`joinTournamentTable` con mazo+deckType/gameType, `playerType` normalizado), limpia `busyTable` si no hay asiento; wizard torneo ya no une bots sin mazo a ciegas (aborta con `create_err_no_deck`). Paridad desktop (`TablePlayerPanel` vs `TournamentPlayerPanel`). | vitest 147 files / 1118 ✅ (+2 `useTableActions`); typecheck ✅; repro real `repro-ai-tourney.mjs` NPE→ok ✅ |
| 2026-09-08 | Lobby/Chips-Dup | **Números duplicados en chips de jugadores ("4 4 plazas")**: `GeneralTab.tsx` pintaba `{n}` + `staging_seats_count` (que ya trae el número: `'{count} plazas'`) en los chips de torneo 4/8/16/32 y en el select de Nº jugadores. Fix: usar solo `t('lobby','staging_seats_count',{count:n})` en ambos sitios. | vitest 148 files / 1119 ✅ (+1 `GeneralTab`); typecheck ✅ |
| 2026-09-08 | Lobby/Draft-Wizard | **Wizard draft roto (NPE + bot IA + mazo construido)**: (1) default `tournamentType='Booster Draft'` no existe en el servidor (`TournamentFactory` NPE `Map.get()=null` → `Can't create new tourney`; reproducido por WS) y la corrección al montar lo cambiaba a `names[0]='Constructed Elimination'` → mesa construida con mazos en vez de draft. Fix: default `Booster Draft Elimination` (`DEFAULT_DRAFT_TOURNAMENT_TYPE`), `normalizeTournamentType`/`defaultTournamentType` en `constants.ts`, validación en submit con error visible (`create_err_bad_tournament_type` ×9 idiomas) en vez de enviar inválidos; el desplegable de tipo filtra construidos en rama limited. (2) Checkbox draft desincronizado de la terna → fix: setter sincronizado (`setUseDraftTournamentChecked` propaga a `setTournamentCategory` en modo torneo) + `setDeckType` limpia el flag al salir de `Limited`. (3) Clamp `numPlayers` usaba máx de `Two Player Duel` (=2) también en torneos → ahora 8 en draft (`MAX_DRAFT_PLAYERS`) / 32 en torneos (`MAX_TOURNAMENT_PLAYERS`); asientos nuevos en draft = `COMPUTER_DRAFT_BOT`; `joinAi` ya no inyecta `AI_OPPONENT_DECK` en torneos no-construidos (deckless como desktop; en draft el servidor rechazaba el mazo con `FAILED`). | vitest 148 files / 1128 ✅ (+9: constants/joinAi/GeneralTab/CreateTableDialog T2-T3); typecheck ✅; i18n-coverage ✅; WS real: create `Booster Draft Elimination` + join bot draft deckless + join humano ✅ |
| 2026-09-08 | Lobby/Wizard-Audit | **Auditoría profunda del wizard (55 useState → matriz tratable)**: el espacio cartesiano colapsa en clases (`deckType` Limited/no, `tournamentType` constructed/draft/desconocido, 3 categorías × flag draft → ~15 combos). F1: `CreateTable/tableKind.ts` — `resolveTableKind` (única que decide la ruta del submit: `match`/`constructed-tourney`/`draft-tourney`/`invalid`), `clampNumPlayers`, `defaultSeatTypeFor`, `computeTournamentSeats/MatchSeats`, `buildCreateTournamentArgs/MatchArgs`, `tournamentJoinNeedsDeck`, `healTournamentBranch`; el hook solo la consume (sin duplicar fórmulas). F2: `tableKind.test.ts` con `it.each` (15 clases decisión + clamp + seats + payloads, 33 tests en <0.5s). F3: `scripts/verify-wizard-matrix.mjs` — duelo, Swiss+MAD, Booster Draft+draftbot, Sealed contra servidor real (11/11 PASS). F4: single-source por construcción — invariante `tourney ⇒ useDraft==(category==limited)`, heal al montar, `useDraftTournament` persistido, assert dev si se rompe, checkbox/tabs por setters sincronizados. F5: `wizard.spec.ts` +3 e2e fake (duelo MAD con mazo, draft MH3 deckless ×8, Swiss con mazos; fake extendido con `createTournamentTable`+listas draft) 6/6 en ~17s. | vitest 149 files / 1161 ✅ (+33 matriz, +3 e2e); typecheck ✅; matriz WS 11/11 ✅ |
| 2026-09-08 | Lobby/Limited-Match | **Match Limited honesto (el "formato no funciona")**: verificado en servidor+desktop que un match `Limited` es *trae tu mazo de 40+* (sin generación de sellado; solo torneos generan) — paridad correcta, pero el wizard mentía: mostraba sobres/construcción que el submit ignoraba (categoría default `limited`) y el resumen decía `Limitado 40 min` sin fase de construcción. Fix solo-web: bloque draft gated por `isDraftLimited` real; aviso `create_limited_match_hint` (×9) en match-Limited que explica los mazos 40+ y guía al draft; `create_summary_limited` → `Limitado (mazos de 40+)`; pill de draft con tiempo de construcción real (`Draft 3× M21 · 10 minutos`). Sin cambios de decisión (matriz intacta) ni de paridad. | vitest 149 files / 1164 ✅ (+2 GeneralTab, +1 LM1); typecheck ✅; i18n-coverage ✅; wizard e2e 7/7 ✅ (+MATCH-LIMITED) |
| 2026-09-08 | Lobby/Tourney-Start | **Iniciar torneo no hacía nada (staging 2/2 mudo)**: dos fallos encadenados. (1) `useTableActions.startTable` (lobby) siempre mandaba `startMatch`, también en torneos → el servidor calza la mesa en `STARTING` y NPEa (`this.match` null, `TableController.startMatch:655`, confirmado en log) → mesa muerta (solo se recupera disolviendo con el botón Disolver del dueño + recreando). Fix web: ramifica por `isTournament`. (2) `startStagedMatch` ignoraba el resultado (`void`) y el servidor falla en silencio (`return false` sin mensaje) → fix: comprueba `ok`, banner de error en staging (`staging-error`) + busy, `startTournament` entra en mapa de errores y lista delegada. (3) Aunque arrancara, el draft no llegaba: faltaba el panel-join — proxy gana `joinTournament`/`joinDraft` (llaman a `SessionImpl` existente: **cero cambios en el fork**) + README + 4 tests java; la web auto-une al humano sentado al recibir `START_TOURNAMENT`. (4) El humano seguía sin ver el sobre: faltaba `joinDraft` al `START_DRAFT` (desktop: `DraftPanel.showDraft:180`); probado en real: sin él nada, con él llega `DRAFT_INIT` con `draftView`+`draftPickView` → fix web en `handleStartDraft` (+3 tests). Flujo real verificado: crear → unir → `startTournament` → `START_TOURNAMENT` → `joinTournament` → `TOURNAMENT_INIT` → **`START_DRAFT`** → `joinDraft` → **`DRAFT_INIT`** ✅. | vitest 1172 ✅ (+2 startTable, +1 banner, +3 panel-join, +2 joinDraft); typecheck ✅; java TableStaging 11/11 ✅ (jar rebuild + proxy restart); wizard e2e 7/7 ✅ |
| 2026-09-08 | Lobby/Draft-Merge | **El draft se quedaba en "Cargando booster…" y perdía los picks**: secuencia real capturada — `DRAFT_INIT` llega vacío, luego `DRAFT_UPDATE` **sin** claves `booster`/`picks`, y el `DRAFT_PICK` (15 cartas) después; tras pickear, otro `UPDATE` sin claves y luego `DRAFT_PICK` (14 + picks acumulados). El cliente reemplazaba el estado con cada evento → borraba sobre y picks. Fix: `mergeDraftMessage` en `state/events/draft.ts` — conserva el sobre si el entrante viene vacío/ausente, acumula picks por UUID, corta el sobre en cambio de pack y reinicia ante otro `draftId`. | vitest 1177 ✅ (+5 merge); draft e2e 3/3 ✅ |
| 2026-09-08 | Lobby/Draft-LastPick | **Última carta sin respuesta + 10s finales**: (1) normal por diseño — `DraftOptions.TimingOption.REGULAR=[113,…,10,8]`s por pick, el tiempo mengua con cada carta. (2) Al agotarse, el servidor hace `autoPick` (coge la última) y tu clic tardío se rechaza en silencio (`console.warn`); además `gateway.send()` no tenía timeout → si la respuesta se perdía, `busyPick` quedaba pillado y ningún clic volvía a funcionar. Fix en `DraftScreen`: timeout de 15s al pick (libera + avisa), error visible `draft_pick_failed` en vez de warn, y a 0s se bloquea con `draft_pick_expired` (esos clics siempre llegan tarde). | vitest 1180 ✅ (+3); i18n-coverage ✅ |
| 2026-09-08 | Lobby/Draft-Pack2 | **El draft no pasaba del primer sobre (hilo muerto en el servidor)**: el wizard enviaba `setCodes:['M21']` (1 código) con `numberBoosters:3`, y `DraftImpl.openBooster` hace `sets.get(boosterNum-1)` — un set por sobre — → al abrir el sobre 2: `Index 1 out of bounds for length 1` no capturada en el hilo del draft (`BoosterDraft.start:23`, 3 mesas muertas en `DRAFTING` en el log) → ningún cliente puede recuperarla (disolver + recrear). El escritorio no lo sufre porque exige un set por fila de pack. Fix solo-web: `expandDraftSetCodes` en `tableKind.ts` (1 código + N sobres → repetido N veces: `M21`→`[M21,M21,M21]`, como 3× el mismo set en desktop) usado por `buildTournamentLimitedOptions`; `validateDraftSets` puro en el submit (vacío→`draft_no_sets`, parcial 2-de-3→nueva `draft_sets_count_mismatch` ×9, cubo/random exentos). Coherencia visible: el wizard abre con `M21, M21, M21` (no `M21` suelto con modo "mismo set" activo) y al cambiar 3↔6 sobres re-expande solo si la lista es uniforme (`uniformDraftSet`; la mezcla se deja quieta y la valida el submit). Matriz WS actualizada al payload expandido. Verificado en real: draft 3×M21, 15 picks del sobre 1 → **`DRAFT_PICK` del sobre 2** ✅ sin nuevos crashes en el log. | vitest 151 files / 1197 ✅ (+12 pack2, +4 uniform, +1 T14 coherencia visible); typecheck ✅; matriz WS 11/11 ✅; WS real pack1→pack2 ✅ |
| 2026-09-08 | Lobby/Draft-Construct | **Construcción vacía tras draftear los 3 sobres (pool en `sideboard`)**: el servidor manda lo drafteado en `deck.sideboard` con `deck.cards` vacío (`DraftPlayer.prepareDeck` solo mueve de sideboard→main las ocultas; el escritorio abre el editor con el mazo completo). La web leía solo `deck.cards` → pool 0 ("Mazo no válido" muerto). Repro real por WS (45 picks): `CONSTRUCT` con `cards:{}` y 45 cartas en `sideboard`. Fix solo-web: `handleConstruct` usa la unión `sideboard+cards`; `handleDraftOver` registra "Draft terminado con N cartas — construye tu mazo de 40+"; `construct_pool_empty` ×9 avisa honesto si el pool llega vacío (reconecta — el servidor reenvía `CONSTRUCT` — o recrea). | vitest 1201 ✅ (+4 construct; payload real 45→pool 45 ✅); typecheck ✅; i18n ✅ |
| 2026-09-08 | Lobby/Draft-Wedge | **Draft de 8 terminado sin construcción (cuña silenciosa del motor)**: `DRAFT_OVER` a las 22:44:27 y después nada — ni `CONSTRUCT`, ni errores en el servidor (mesa en `STARTING`, bracket "sin rondas"). Aislamiento headless por WS (driver `/tmp/isolate-wedge.mjs`, 45 picks auto): 5/5 drafts de 8 llegan a `CONSTRUCT` con 45 en sideboard — en caliente, en frío (restart all) y ×3 seguidos. No reproducible por nº de bots ni frialdad: carrera rara del handoff draft→construcción del fork (intocable, sin logs). La mesa cuñada es irrecuperable (el reenvío al reconectar solo cubre fase de construcción ya iniciada). Fix solo-web: watchdog post-`DRAFT_OVER` — `draftOverAt` en el slice limited (se marca en over, se limpia en `CONSTRUCT`/`START_DRAFT`/`START_TOURNAMENT`/`TOURNAMENT_OVER`), predicado puro `isConstructStalled` (30s sin construct) y banner `tournament_construct_stalled` ×9 en el `TournamentPanel` ("disuelve y recrea") en vez del bracket muerto. | vitest 1208 ✅ (+3 watchdog, +2 panel, +2 limpieza); typecheck ✅; i18n 13 ✅; WS real 5/5 CONSTRUCT en 8p ✅ |
| 2026-09-08 | Lobby/Draft-Bots | **Los bots nunca entregan el mazo construido: ningún limitado con IA llega a rondas (determinista)**. Tras tu segundo intento (draft→`CONSTRUCT`→doble submit 23:00:30→nada): jstack al servidor (sin hilos de torneo vivos, sin excepciones en ningún log) + repro headless con submit válido de 45 cartas → sin rondas en 240s, con 7 bots Y con 1 solo. Causa: algún bot no completa `submitDeck` en silencio (el `containsKey` de `TournamentImpl.submitDeck` no deja rastro) y la fase espera eternamente — el timeout de 600s solo cubre a humanos con sesión, y el de mesa solo el sideboard. Es bug del motor (el escritorio caería igual) y no se puede arreglar desde aquí. Fix honestidad: el submit **bloquea** limitado+IA (`limitedTourneyHasAiSeats`, error `draft_bots_no_submit` ×9: todos HUMANO multi-ventana o construido+MAD) — cubre draft y sellado (misma rama `draft-tourney`); SIM tampoco construye (verificado en `SimPlayer`: sin caso `CONSTRUCT`). | vitest 1212 ✅ (+3 predicado, T2/T6 a 2 humanos, T15 bloqueo); typecheck ✅; e2e wizard 7/7 ✅ (DRAFT rework: assert del bloqueo, vale en fake y real); matriz WS 11/11 ✅ |
| 2026-09-08 | Lobby/Draft-1v1 | **Torneos de 2 plazas (draft 1v1)**: los chips de torneo eran [4,8,16,32] — el motor y el clamp sí admiten 2. Chips a [2,4,8,16,32] (+test). Receta: 2 plazas, ambos asientos HUMANO (una cuenta no puede ocupar dos: `userPlayerMap` es 1:1 → player1 + player2 en incógnito), sin bots (bloqueo anterior). Verificado real con dos sesiones (`/tmp/verify-1v1.mjs`): crear→unir ambos→arrancar→45 picks cada uno→CONSTRUCT 45→submit ambos→**`START_GAME` de la final** ✅. | vitest 1212 ✅; typecheck ✅; WS real 1v1 punta a punta ✅ |
| 2026-09-09 | Lobby/Draft-Stall | **Cuña en mitad del draft 1v1 (ciclo de pick muerto en silencio)**: P1 `Cargando booster` + P2 14 cartas sin poder pickear, 7+ min sin eventos con el hilo vivo en `picksWait`. timeout con buffer 2s debió auto-pickear: solo cuadra sesión de draft muerta (`killed`, sin logs) o query perdida sin reenvío; causa micro no determinable desde fuera (correlato: recargas a mitad del draft). Mesa irrecuperable (ningún backstop re-dispara queries). Fix solo-web: segundo watchdog — `lastDraftEventAt` (se sella en cada evento, se limpia en over/construct/torneos), predicado `isDraftStalled` (draft activo + 150s sin eventos; imposible en flujo sano: timeout máx 113s+buffer) y banner `draft_stalled` ×9 en `DraftScreen`. | vitest 1215 ✅ (+2 predicado/sello, +1 banner); typecheck ✅; i18n 13 ✅ |
| 2026-09-09 | Lobby/Draft-Resync | **Beta confirma la cuña post-draft + clics obsoletos**: partida en `beta.xmage.today` (proxy local): draft completo (`DRAFT_OVER` ×3) y **cero `CONSTRUCT`** — la misma cuña del localhost, ahora en el servidor oficial (carrera del motor, no de nuestra instalación). El error `draft_pick_failed` visto era un clic sobre carta obsoleta en el trajín final (mezcla cabecera nueva + cartas viejas). Fix: (1) **frescura de clics** — `lastDraftMethod` en el slice; `DraftScreen` solo habilita pick si el último evento es `DRAFT_PICK` con `picking` (en flujo normal no cambia nada: e2e draft 3/3 intacto); (2) copia honesta de `draft_pick_failed` ×9 (sin prometer "siguiente sobre"); (3) **botón Reintentar** en el aviso (re-une `joinDraft`+`joinTournament`: `join()` recrea la sesión — recupera cuñas por recarga con sobre sin confirmar) + **auto-rejoin** al reenganchar el WS con draft/torneo activo (hueco: antes solo se restauraban partidas). Límites honestos: sobre confirmado + sesión muerta no se recupera (sin API) ni con re-login (huérfana mapeos). Guía: drafts en localhost, beta best-effort. | vitest 1219 ✅ (+1 reintento, +3 rejoin, ajustes frescura); typecheck ✅; i18n 13 ✅; e2e draft 3/3 ✅ |
| 2026-09-09 | Feat/setup-wizard | **Asistente inicial pre-conexión (7 pasos) + descarga de cartas integrada**: nuevo `setup/SetupWizard.tsx` (idioma / identidad con avatar+bandera / dónde jugar con presets local-oficial-custom / mesa zoom+layout+dorsos / sonido+juego rápido / cartas / listo) que se auto-abre si `localStorage:nexus_setup_v` ausente (versionado), bloquea el auto-connect de `App` hasta completarse y precarga el login vía `saveConn` + evento `nexus:setup-conn`; reapertura desde Ajustes (`settings-review-setup`) y enlace "¿Primera vez?" en el login. Refactors sin cambio visual: secciones de `SettingsModal` → `settings/sections.tsx` (+`GameplayQuickSection`), cuerpo de `DownloadImagesDialog` → `lobby/DownloadPanel.tsx` (prop `initialScope`, wizard sugiere `MY_DECKS`; la descarga sigue en 2º plano al cerrar) y `POPULAR_FLAGS`/`ServerPreset` → `lobby/flags.ts`. 24 claves `setup.*` ×9 locales (es traducido, resto = en) | vitest 157/1242 ✅ (+4 wizard: 7 pasos, skip, finish guarda conn+notifica, custom revela red); typecheck ✅ |
| 2026-09-09 | Feat/launcher-F0 | **Toolchains del Nexus Launcher (F0 + validación local real)**: Rust stable 1.98.1 + JDK17/Homebrew localizados (`javaBin()` ya resolvía Homebrew; solo faltaba `JAVA_HOME` en la shell) + `scripts/assemble-modules.mjs` (`server/{classes,lib,plugins,config}` + `proxy/*.jar` + `version.json` con main classes, add-opens, puertos y `systemProperties`) + workflow `modules.yml` (matriz linux-x64/win-x64/mac-arm64, JDK 17 Temurin: mvn base+plugins+proxy, assemble, `jdeps→jlink`, sanity + **smoke servidor-normal + proxy en puertos alternos** + tarball 30d). **Validado en local con los comandos idénticos al CI**: JRE 12 módulos/42MB; proxy OK (WS 18887 + HTTP 200); servidor OK en 20s. **Hallazgos**: (1) `version.json` debía llevar el classpath con dirs explícitos (`classes/*` no expande directorios en java); (2) el flag `-testMode` de dev es inerte (no matchea `-testMode=`) — el test mode venía de `isDeveloperBuild()`; el launcher fuerza normal con `-Dxmage.testMode=false` (verificado: sin línea "running in test mode"); separador classpath `;` en Windows anotado para el launcher Rust. Gap: sin JRE mac-x64; CI se ejerce en el próximo push (allí además se sabrá si el servidor regenera `db/` solo — el smoke CI arranca sin db) | assemble+jlink+smokes locales ✅; YAML+bash -n ✅; cargo 1.98.1 ✅ |
| 2026-09-09 | Feat/launcher-F1 | **Núcleo del launcher (`launcher/` crate `nexus-launcher` + CLI `nexus-bootstrap`)**: `manifest` (parse de `components-manifest.json` + resolución por target `linux-x64/win-x64/mac-arm64`), `fetch` (descarga http(s)/`file://` con progreso, `.part` + rename atómico, `sha256` + mismatch que borra el corrupto), `extract` (tar.gz con guarda anti `..`/absolutos + creación de padres), `layout` (árbol `components/<release>/{jre,server,proxy}` + `.installed`, `data/server` con seed de `config` sin pisar cambios del usuario, `missing_components` para updates diferenciales). CLI `check` (exit 1 si falta) / `fetch` (descarga→verifica→extrae→seed→marca). CI `modules.yml` ahora empaqueta **un tarball por componente** (diferencial) + `COPYFILE_DISABLE=1`. Verificado: `cargo test` 8/8 ✅, clippy limpio, e2e CLI con tarballs sintéticos (`check→fetch→check` + hash roto → exit 1 sin marcar) | cargo test 8/8 ✅; clippy ✅; e2e CLI ✅ |
| 2026-09-09 | Feat/launcher-F2 | **Shell Tauri + app corriendo en puertos normales**: `launcher/src-tauri` (id `today.xmage.nexus`, ventanas splash 420×480 + main, CSP con ws/wss+Scryfall, iconos generados del logo 2048, capabilities mínimas) + `main.rs` (bootstrap con eventos `bootstrap://state|progress`, splash `web/public/splash.html` autocontenido ES/EN con Reintentar/Salir, orquestación java leyendo `version.json` con classpath absolutizado y `;` en Windows, seed+parcheo de puerto del `config.xml`, tray con autostart/datos/reiniciar, single-instance, `NEXUS_*` overrides para test). `vite.config` base `./` con `TAURI_BUILD=1` (+ `@types/node`, que obligó a quitar 4 `@ts-expect-error` ya innecesarios). **Bug real cazado en e2e**: `Wrong java version` = connects durante la construcción inicial de la card-DB del proxy (92k cartas, ~10s) — la reflexión bisocket pierde contra el scan concurrente; TODO lo posterior OK (3/3 + éxito real del usuario). Fix doble: proxy responde `WARMING_UP` ("retry in a few seconds", verificado en carrera: WARMING_UP×2 → ok) en vez de intentar el connect condenado + launcher no da `ready`/abre main hasta `card db READY` en el log (estado `warming` en splash). Detalles: tarballs planos por componente (el anidado `server/server` rompía el contrato — CI ahora verifica layout), `proxy.addOpens` en el contrato (el proxy también es cliente XMage), `COPYFILE_DISABLE`, documentado `WARMING_UP` en README. App verificada viva en 17171/8787/8788 con server en modo normal | shell 3/3 connects ✅; mvn proxy 99+16+56 ✅; typecheck ✅ |
| 2026-09-09 | Fix/e2e-post-Tauri | **18 fallos e2e fake tras el despliegue Tauri (3 causas)**: (1) el `SetupWizard` auto-abierto tapaba los clics con su backdrop (`dlg-backdrop intercepts pointer events`) en 14 specs — nuevo helper `dismissSetupWizard(page)` en `e2e/support/start-game.ts` (espera determinista al montaje vía `waitFor attached`, sin `.or()` que viola el strict mode) usado en `login()` y tras cada `goto` directo (9 specs); (2) T5 obsoleto (usaba el diálogo pre-rediseño `.create-tab-content`/`.top-nav-create`) — reescrito al wizard nuevo (tarjeta Torneo → pestaña Draft/Limitado → select draft → toggle → `random-packs-open`); (3) `settings.spec` exigía 0 `svg` en el modal pero el kicker U14 lleva icono — acotado a `.settings-nav svg` (igual que el unit test); `createTable` `/Bo3/i` matcheaba 3 botones en el wizard nuevo (la tarjeta Duelo incluye "(Bo1, Bo3)") — anclado a `/^Bo3/` y `/^Bo5/`. | vitest 1242 ✅; typecheck ✅; e2e fake **116 pass / 2 skip / 0 fail** ✅ |
| 2026-09-09 | Release/Tauri-3plat | **Ejecución local + release en 3 plataformas con updater**: (1) `cargo install tauri-cli ^2` + fix `frontendDist` (`../web/dist`→`../../web/dist`, apuntaba a `launcher/web/dist` inexistente); `cargo tauri dev` verificado con tarballs locales (`assemble`+`jlink` 42MB, `gen-manifest.mjs` nuevo con `file://`): bootstrap descarga→verifica→extrae→server normal (sin test mode)+proxy+card db READY→login+lobby OK por WS. (2) Updater: `tauri-plugin-updater` + `createUpdaterArtifacts` (sin esto no hay `.app.tar.gz`; lo activa el bundle, no `plugins.updater.active`) + par de claves minisign (`~/.tauri/nexus-updater.key`, pubkey en `tauri.conf.json`, timeout 15s con degradado offline) + estados en splash (checking/update-available con Actualizar-Continuar/update-downloading con progreso) + comandos `apply_update`/`skip_update`; error legible en `mac-x64` (solo arm64). (3) `.github/workflows/release.yml` manual por tag (check tag==versión → modules×3 → manifest → bundle×3 con firma → publish: draft release + tarballs + `latest.json` fusionado vía `gen-latest.mjs` nuevo con URL-encoded). (4) `cargo tauri build` local: `.dmg` 13MB + `.app.tar.gz`+`.sig` firmados; el `.app` arrancado con overrides levanta server/proxy/READY sin test mode. **Pendiente del usuario**: guardar la clave privada y añadir `TAURI_SIGNING_PRIVATE_KEY[_PASSWORD]` a los secrets de GitHub; sin firma Apple el updater en mac es best-effort. | cargo test 8/8 ✅; clippy ✅; build mac (dmg+updater firmado) ✅; `.app` vivo en puertos alternos ✅ |
| 2026-09-09 | RootCause/withGlobalTauri | **CAUSA RAÍZ del splash congelado**: `window.__TAURI__` NO EXISTÍA en la app real — `with_global_tauri` es `bool` con `#[serde(default)]` (false) y no lo activábamos, así que ni eventos ni polling ni `splash_ready` funcionaban nunca (los tests Playwright inyectaban un mock y por eso pasaban). El backend siempre funcionó por su cuenta (hilo en `setup()`), de ahí `bootstrap.log` con `ERR_OFFLINE` en 0,7 s mientras el splash seguía "cargando". Fix: `"withGlobalTauri": true` + watchdog de 15 s (`bridge_missing` en 9 idiomas, testeable vía `__SPLASH_BRIDGE_TIMEOUT_MS`) que convierte este fallo en error visible. Verificación REAL (no mock): `bootstrap_status` con log temporal recibió ~40 polls del webview de verdad. `.app` reconstruido (bridge embebido confirmado por scan), data dir default del usuario sembrada con los componentes locales (ya no da ERR_OFFLINE) y app dejada corriendo en puertos default lista para jugar. | Playwright 20/20 ✅; cargo 3 bin ✅; clippy 0 ✅; bridge real ✅ |
| 2026-09-09 | UI/SplashNoStep+i18n | **Splash sin línea de paso + wizard/avatar traducidos** (a petición): (1) eliminada la línea `Paso N de 6 · s` del splash (HTML+CSS+JS: fuera `STEP_OF/TOTAL_STEPS/paintStep/ticker/data-base`; queda logo+título+estado+barra+%); verificado sin `#step` en Playwright (17/17) y por scan del asset embebido. (2) Sección `setup` traducida en 6 idiomas (24 claves × de/fr/it/pt/ru/ja/zhs; antes solo es/en). (3) `AvatarPickerModal` sin hardcode español: 4 claves chrome (`avatar_desc/filter_all/standard/special`) + 13 nombres especiales (`avatar_sp_*`) en 9 idiomas, `Cancelar`→`common.cancel`, especiales con `nameKey` + nombre canónico inglés en `avatars.ts`. Nuevo test vitest de chrome en inglés. `.app` reconstruido, embebido verificado por hash de asset, arranque real a `ready` en ~9 s. | vitest 1245 ✅; typecheck ✅; build ✅; Playwright splash 17/17 ✅ |
| 2026-09-10 | UI/DialogHeader | **Contador del wizard fuera de su sitio** (`Passaggio 1 di 7` solapado con la píldora en it): causa = `.dlg-top-right` en `absolute` (top/right 16px) flotando sobre el borde de la píldora. Fix estructural en `DialogShell`: fila `.dlg-head` flex (píldora + derecha alineados); `.dlg-head-right` con altura contenida (22px + overflow visible) para que los ✕ no empujen el layout. **Veredicto e2e**: mi cambio rompió 1 clic real (submit Crear Mesa a zoom 150%, probado por traza+bisect) y el tope lo arregla — zoom 3/3 ✅, wizard bbox ✅, wizard/staging/settings suites ✅, unit 1245 ✅. Los ~15 fallos de runs largos (bo3/bo5/etc., todos en techo 15min) son PRE-EXISTENTES del árbol WIP: bo3 falla IDÉNTICO sin mis ficheros (stash) y el zoom-stepper pasa en solo. `.app` final reconstruido y arrancado a `ready` (~7 s, 3 puertos UP). | vitest 1245 ✅; typecheck ✅; zoom 3/3 ✅; e2e fake 101 + 15 pre-existentes (ver nota) |
| 2026-09-10 | UI/lobby-header | **Fuera selector de idioma y botón 100% del lobby** (a petición): eliminados `<LanguageSelector compact />` y el readout `.lobby-scale-readout` de `LobbyHeader` (+ imports/estado muertos). El idioma sigue en login, editor de mazos y ajustes; el zoom sigue por teclado (Ctrl+=/0) y en Ajustes (stepper+presets). e2e `zoom.spec` test 1 reescrito sin readout (assert sobre `documentElement.style.zoom`). | typecheck ✅; unit lobby/i18n/appearance 264 ✅; e2e zoom 3/3 ✅ |
| 2026-09-10 | Rebuild/app-fresco | **`.app` reconstruido con diálogos in-app + lobby limpio**: `TAURI_BUILD=1` + `cargo tauri build` firmado (`.app` + `.app.tar.gz` 12 MB + `.sig`; sin `.dmg` esta vez). Embebido verificado en `dist` pre-build (`confirm-modal-ok` en JS+CSS, `lobby-scale-readout` ausente). Arranque real a `ready` en ~9 s, SERVER/WS/HTTP UP, app dejada corriendo. | build ✅; ready ~9 s ✅ | **Fuera selector de idioma y botón 100% del lobby** (a petición): eliminados `<LanguageSelector compact />` y el readout `.lobby-scale-readout` de `LobbyHeader` (+ imports/estado muertos). El idioma sigue en login, editor de mazos y ajustes; el zoom sigue por teclado (Ctrl+=/0) y en Ajustes (stepper+presets). e2e `zoom.spec` test 1 reescrito sin readout (assert sobre `documentElement.style.zoom`). | typecheck ✅; unit lobby/i18n/appearance 264 ✅; e2e zoom 3/3 ✅ |
| 2026-09-10 | UI/native-dialogs | **Conceder/Salir muertos en la app Tauri**: `confirm()/alert()` nativos son no-op en WKWebView/macOS (wry sin handlers → `confirm()` devuelve `false` sin mostrar nada; en navegador sí abre el diálogo). Fix: `ui/confirmDialog.ts` (cola FIFO + `confirmDialog()`/`alertDialog()` promise, etiquetas vía `tStatic`) + `ui/ConfirmModal.tsx` sobre `DialogShell` (Esc/backdrop=cancelar, foco en Cancelar, variante `danger`, `confirm-backdrop` z-2000) + `ui/ConfirmHost.tsx` (useSyncExternalStore) montado en `App`. Migrados los 13 sitios (GameMenu ×2, DecksGallery ×5, Draft/Torneo ×3, Staging ×2, Download, InviteLink — este con `.then` en el effect). **Hallazgo e2e**: el overlay del GameMenu (z-198) tapaba el modal (z-90) → clic interceptado hasta el timeout de 120 s; fix doble (menú se cierra antes de confirmar + backdrop a 2000). Tests: modal (ok/cancel/Esc/alert) + guardia `nativeDialogs.test.ts` que escanea `src` y falla si vuelve un nativo + 3 specs reescritos al modal + e2e concede/staging a clics DOM. | vitest 1250 ✅; typecheck ✅; build ✅; e2e concede 1/1 ✅; staging 9/9 ✅ |
| 2026-09-09 | Anti-stuck/polling | **Splash atascado en paso 1 aunque el backend estaba acotado**: el bootstrap arranca en `setup()` sin esperar al splash, y un hilo muerto (panic) o un mutex envenenado dejaba `running=true` para siempre (reintentos muertos, splash congelado en silencio). Blindaje: (1) comando `bootstrap_status` (último estado guardado) + **polling cada 1 s desde el splash** con deduplicación (no resetea el cronómetro; si se pierden eventos, converge igual); (2) `catch_unwind` en `run_bootstrap` (un panic se vuelve error visible con Reintentar, no muerte silenciosa) + guards de mutex tolerantes a envenenamiento; (3) `logs/bootstrap.log` con `[+Ts] state=` en cada fase (diagnóstico en máquina del usuario); (4) errores de `apply_update` también pasan por `error_code()`. Verificado: Playwright 19/19 (nuevo test de polling sin eventos), dev e2e con `bootstrap.log` completo hasta ready, `.app` reconstruido (splash con polling confirmado embebido vía scan Brotli) y vivo en puertos alternos. | Playwright splash 19/19 ✅; cargo 3 bin ✅; clippy 0 ✅; `.app`+`.dmg`+updater firmados ✅ |
| 2026-09-09 | Rebuild/app-fresco | **Reconstrucción del `.app`/`.dmg` con todo lo nuevo + hallazgo de empaquetado**: el bundle macOS lleva solo binario+icono (el frontend va embebido con Brotli en el binario vía `tauri-codegen EmbeddedAssets`, NO en Resources — verificado descomprimiendo los 425 assets y comparando contenido); susto intermedio (el embebido difería 22 chars) explicado: Tauri minifica el HTML al embeber (`nonce`, `/>`→`>`). Tras `cargo clean -p nexus-shell` + rebuild, el binario sirve el splash nuevo (stepper+i18n) y el arranque verificado con logs `[bootstrap]`: update 0,5 s → descargas locales → server ~18 s → cartas ~10 s → ready 34,7 s, 3 puertos UP. Artefactos: `.app`, `.dmg` 13 MB, `.app.tar.gz` 12 MB + `.sig` (firmado con `~/.tauri`). | `.app` vivo en puertos alternos ✅; `cargo clean`+rebuild ✅ |
| 2026-09-09 | Splash/i18n+stall | **Splash con actividad visible en 9 idiomas + caza del stall de 33 s en paso 1**: (1) sección `splash` (22 claves) en `TranslationSchema` + 9 locales (el guard de cobertura las exige todas); `scripts/gen-splash-i18n.mjs` genera `web/public/splash-i18n.js` (detección `navigator.language`, fallback en, enganchado en `npm run build` + `--validate`); splash reescrito con spinner, stepper 1/6–6/6, cronómetro por fase y fix del mismatch `checking-update` (Rust envía kebab, el splash esperaba camel → mostraba la clave en crudo); errores conocidos de Rust viajan como códigos `ERR_*` (`error_code()` en el embudo único, passthrough en crudo si es desconocido). (2) Stall: con agujero negro total el check se acota en 8,0 s (el timeout del plugin sí funciona contra sockets colgados) y el mapa en frío medido es update 8 s → server ~16 s → cartas ~8 s (total ~49 s); blindaje extra: **hard cap de 12 s** con `tokio::time::timeout` alrededor de todo el check (cubre stalls internos del plugin como autodetección de proxy), **skip del check si hubo uno OK en 24 h** (`updater-last-check` en data dir; los fallos no se cachean), logs `[bootstrap] +Ts state=` con timestamp en cada fase + aviso si un `emit` falla. | web unit 1244 ✅ (2 nuevos splash); typecheck ✅; build TAURI ✅; splash 18/18 en Playwright (de/ja/en+errores) ✅; cargo 8 lib + 3 bin ✅; clippy 0 ✅ |
| 2026-09-11 | MCP/DevOps-A | **Fase A del MCP de XMage Nexus (tools DevOps + resources)**: nuevo paquete top-level `mcp/` (Node ≥24 ejecuta TS nativo, sin build) con servidor MCP stdio registrado en `opencode.json` como `mage` (`timeout` 30 min para suites largas). Tools: `mage_stack` (status/start/stop/restart vía `scripts/ctl.mjs`, convergencia por puerto + pid nuevo), `mage_logs` (cola de `.run/*.log` con fallbacks de `logFileFor` + grep regex), `mage_run_tests` (capas de `scripts/test.mjs` + resumen por capa), `mage_build` (proxy/full), `mage_record_fixture`, `mage_validate_generated` (gen-types/gen-zod/server-state/splash-i18n `--validate`). Resources: `mage://status/project`, `mage://status/dashboard`, `mage://coverage/interactions`, `mage://contract/schema`, `mage://fixtures/manifest`. stdout reservado al transporte MCP (diagnóstico por stderr). | mcp vitest 4/4 ✅ (cliente stdio real); mcp typecheck ✅; `mage_stack restart vite` converge 2,9 s con pid nuevo ✅; `mage_run_tests typecheck` PASS ✅; validadores al día ✅ |
| 2026-09-11 | MCP/Session-C1 | **Fase C1 del MCP (sesión WS + lobby + arranque de partida)**: `ProxyClient` (ws + correlación requestId/result + EventEmitter de eventos, con test hermético contra un WS stub) y tools `mage_connect` (default local, cuenta autogenerada ≤14 chars, reintento `WARMING_UP`), `mage_disconnect`, `mage_lobby`, `mage_create_table` (HUMAN+SIM, `simDecks`/`seatSkills`/flags deterministas), `mage_join_table` (DeckJson), `mage_start_match` (espera `START_GAME` y devuelve gameId), `mage_leave_table` y `mage_session` (conexión/mesa/gameId + cola de eventos). Estado de sesión compartido por proceso (una sesión por agente). **Hallazgos**: XMage limita el username a 14 chars; Node strip-only prohíbe parameter properties (`constructor(private x)`), detectado en test. Verificado real contra stack local: connect 1,0 s → createTable → join → startMatch → `START_GAME` con gameId → removeTable ✅. | mcp vitest 7/7 ✅ (2 archivos); mcp typecheck ✅; flujo real lobby→START_GAME ✅ |
| 2026-09-11 | MCP/Player-C2 | **Fase C2 del MCP (el agente juega)**: `promptView` normaliza los ~24 callbacks de interacción a `{mode, title, message, options, min/max, items, flags, cards}` (priority/combat/boolean/uuid/string/integer/multiString/mana/order) con labels de cartas sacados del gameView embebido; `compactView` recorta el `GameView` real (~31 KB con `rules` por carta → ~2 KB: vidas, mano con coste/tipos/jugable, battlefield girada/P-T/daño/contadores, stack, combate, maná). `session` gana cola de prompts (seq), `GAME_OVER`, auto-pass (ON por defecto: pasa prioridad en ventanas del rival o sin jugables, nunca en asks/targets/maná ni durante un pago) y tools `mage_game_state`, `mage_wait_for_prompt(afterSeq)`, `mage_action` (validado contra el prompt), `mage_choose`, `mage_play_card`, `mage_pay_mana`, `mage_pass_priority`, `mage_combat`, `mage_auto_pass`, `mage_concede`, `mage_chat`. **Verificación real**: partida determinista vs Sim (`skipInitShuffling`, Mountain+Raging Goblin) jugada entera por las tools — mulligan keep, tierras, lanzar criatura con pago de maná, atacar, `GAME_OVER` (35,8 s, test opt-in `MCP_E2E=1`). Tests herméticos con proxy WS falso (prompt→choose, ask mulligan, toggle de auto-pass). | mcp vitest 21/21 ✅ (5 archivos, +14); mcp typecheck ✅; partida real completa vs Sim ✅ |
| 2026-09-11 | MCP/UI-Playwright | **El agente toca la UI real**: (1) tool `mage_e2e` (`mcp/src/tools/tests.ts`) envuelve Playwright con `spec`/`grep`/`backend` fake\|real, resumen passed/failed/skipped y cola de salida; verificado `decks-gallery.spec.ts @decks` → **6/6 en 16,2 s** (incluye hover y drag & drop main↔sideboard del editor). (2) segundo MCP local `playwright` en `opencode.json` vía `scripts/playwright-mcp.mjs` (`@playwright/mcp` 0.0.80; reutiliza el Chromium de `web/` con `--executable-path`, sin segunda descarga; overrides `PLAYWRIGHT_MCP_VERSION`/`PLAYWRIGHT_EXECUTABLE_PATH`; artefactos en `.run/playwright-mcp/`). Verificado real por el transporte MCP: navegar vite 5173 → descartar SetupWizard → teclear usuario/contraseña → login → lobby "Mesas Disponibles (0)" + screenshot inline 126 KB. Notas de la versión: los tools usan `target` (ref del snapshot) y `browser_take_screenshot` sin filename devuelve la imagen inline. | mcp vitest 21/21 ✅; mcp typecheck ✅; `mage_e2e` 6/6 ✅; login UI real vía Playwright MCP ✅ |
| 2026-09-11 | MCP/C3-Plan | **Plan de C3 documentado** (`mcp/README.md` roadmap + fila MCP): 4 sub-fases en orden **C3.3 → C3.1 → C3.2 → C3.4**. **C3.3** arnés FixtureServer en CI: partidas scripted contra `FakeServer.start(0, escenario)` (`web/fixtures/fake.ts`, server `ws` de Node que habla el mismo envelope que `ProxyClient`); import dinámico no-literal desde `mcp/test/support/` porque `fake.ts` usa parameter properties (no corre en strip-only) y sus imports sin extensión rompen `tsc` nodenext; escenarios phyrexian/kicker/split/convoke/X/trigger-order reutilizando la semántica de `complexCosts`/`manaPayment`/`triggerOrder`; capa mcp (typecheck+test) en `scripts/dashboard-ci.mjs`. **C3.1** maná/interacciones: contract research (`xmage-contract`) de specialButton/auto-pay/cancel, `GAME_PLAY_XMANA` y orden de triggers (`GAME_TARGET`+`queryType=PICK_ABILITY`); `mage_pay_mana auto:true` y botón especial; auto-pass v2 con firma `(turno,paso,method,hash(opciones))`+tope anti-flood (bug histórico de `SimPlayer`); drivers nuevos en `record.mjs` si falta ground truth. **C3.2** reconexión/resync: backoff 1s→10s espejo de `web/src/state/gateway.ts:13-54`; tras attach re-`joinGame`/`watchGame` (el proxy no replaya estado; grace 60 s `ProxyClient.java:89`); tool `mage_reconnect`; flag `attached:true` en `attach()` aprobado (rebuild jar). **C3.4** multi-sesión: refactor del singleton `session.ts` a `AgentSession`+`SessionManager`, param `session` en las 26 tools, `mage_sessions`; tests con 2 FakeServers y partida HUMAN/HUMAN. DoD por sub-fase en `mcp/README.md`. | documentación ✅ |
| 2026-09-11 | MCP/C3 | **C3 completada** (4 sub-fases). **C3.3**: arnés `mcp/test/support/fakeServer.ts` (import dinámico no-literal de `web/fixtures/fake.ts`; `FakeServer.start(0, escenario)` sobre `ws`) + `fakeGame`/`reconnect`/`autoPass`/`specialActions`/`complexInteractions`/`multiSession` (31 tests herméticos, 1 opt-in real); capa `mcp` en `scripts/dashboard-ci.mjs` (marker `.mcp-tests.json` + layer en `gen-dashboard.mjs`) y jobs/paths en `pages.yml`/`web-ci.yml`. **C3.1**: botón especial (`sendPlayerString("special")` en maná y combate, según `ManaBar`/`CombatBar`), `mage_pay_mana special`, `queryType`/`specialLabel` en el prompt, orden por índices→ids (el server espera ids, `LibraryOrderDialog`), `values` multi-amount, y auto-pass v2 con firma+tope de 5 repeticiones → `AUTO_PASS_STOPPED` (mata el flood tipo `SimPlayer`); matriz phyrexian/kicker/split/adventure/convoke/X/amount/pile/mode/color/order/multi/trigger en verde (`SimPlayer` del proxy + cliente web como fuente de verdad local). **C3.2**: `ProxyClient` con auto-reconnect backoff 1s→10s y `open()` compartido anti-carrera; `mage_reconnect`; en el proxy `connect` devuelve `data.attached` (true attach / false sesión nueva) y **replay de estado** en `joinGame` (cachea último GAME_INIT/GAME_UPDATE + prompt pendiente por partida y los reenvía a esa conexión; el GameClient del proxy nunca se desconecta, XMage no reenvía nada); verificado real por WS: attached=true con gameId+prompt reenviados, y grace 60s expirado → attached=false + gameId limpiado. Java: 60 tests (2 nuevos de wiring de replay + stubs). **C3.4**: multi-sesión con sesiones nombradas (`mage_connect {session}`, `mage_use_session`, `mage_sessions`; las inactivas siguen con auto-pass en segundo plano) y test de dos FakeServers independientes. | mcp vitest **31 passed | 1 skipped** ✅; mcp typecheck ✅; mcp real-game `MCP_E2E=1` 35,5s ✅; proxy java 60/60 ✅; web unit 1272/1272 + typecheck ✅ (por `onJoinGame` en `fake.ts`); reconnect real 10/10 checks ✅ |
