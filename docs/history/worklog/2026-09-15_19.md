# Work Log 2026-09-15 → 2026-09-19

> Archivo del Work Log de `PROJECT.md` (§9). Filas copiadas tal cual, sin resumir; en el mismo orden (más nueva primero). Índice: `README.md`.

| 2026-09-19 | PLAN-7 — CI remoto en verde, fork publicado y cierre de planes | **Hallazgo**: `Web client CI` en rojo en `master` desde el 2026-09-13 con la suite local 9/9. (1) Job `proxy`: `cannot find symbol cheatSetup` — `ensureMageArtifacts()` solo comprobaba que existiera el jar y `setup-java cache: maven` restauraba artefactos `org.mage` previos al parche P1 ⇒ sello `~/.m2/repository/org/mage/.nexus-fork-commit` (`forkCommit`/`stampMageArtifacts`), reinstalación si difiere. (2) e2e solo-CI: **bug real** de preview huérfano (slot con hover re-renderizado sin `mouseleave`) ⇒ ancla `:hover` + guarda `mouseover` en `useBoardPresenter` (3 unit, rojo sin fix); `hand-bar` espera prioridad + `toPass`; `latency` tierra descarta intentos contaminados por eventos del helper (+ `waitEventsQuiet`); `best-of-5` 300 s; `gallery` presupuesto proporcional. (3) Fork `nexus` 1 commit sin publicar (`2fbfc7d021`, END_GAME_INFO a espectadores; la v0.2.0 salió sin él) — el usuario decide conservarlo. (4) Fase 4 launcher → publicada en PROJECT/ROADMAP/`site/content.json`. Plan4/5/6 cerrados; V5–V7 a `docs/history/plan7.md` §4; regla de CI remoto en `AGENTS.md`. | e2e fake 6 workers 263 passed/4 skipped; estrés 5 specs ×3 verde; `node scripts/test.mjs` 9/9 (stack reiniciado) |
| 2026-09-19 | Release v0.2.0 ejecutado y publicado (workflow `release.yml` + updater + dashboard) | Commit `87a7bea4b8a` y tag `v0.2.0` pusheados (15 ficheros: bump web/mcp/launcher + workflows restaurados); dispatch manual `release.yml` con `tag=v0.2.0` → run [35432670389](https://github.com/Adamychen/xmage-nexus/actions/runs/35432670389) **verde en 34 min**: `check-version` OK (tag = web/launcher/tauri), `modules.yml` ×3 SOs (Maven engine+plugins+proxy, jlink, smoke servidor normal + proxy, tarballs y verificación de layout; 5–12 min por SO), `manifest` (`components-manifest.json`), bundles Tauri firmados (Win exe+msi, macOS dmg+app.tar.gz, Linux deb+rpm+AppImage, todos con `.sig`), `publish` (draft con 24 assets) → `gh release edit --draft=false --latest` (**published + latest**; `latest.json` con darwin-aarch64/linux-x86_64/windows-x86_64 verificado). `site/content.json` release.status → `published` y `gen-dashboard` regenerado. |
| 2026-09-19 | Release v0.2.0 preparado — pipeline del launcher restaurada/adaptada al fork + bump de versión | **Contexto**: al preparar el cierre se comprobó que `.github/workflows/release.yml` y `modules.yml` (los que publicaron v0.1.0 con bundles Win/macOS/Linux y `latest.json`) se **borraron** en `aa6a8de39c7` al aislar el fork: `gh api .../actions/workflows` solo lista 4 hoy (labeler ×2, web-ci, pages) y los runs de release que se ven en Actions son históricos del 2026-09-10. **Restauración**: recuperados de `git show v0.1.0:.github/workflows/...` y adaptados al layout actual — `modules.yml` ahora es reutilizable (`workflow_dispatch` + `workflow_call`, sin trigger por push), checkoutea la rama `nexus` como `xmage-fork/` (`NEXUS_FORK_DIR`, mismo patrón que `web-ci.yml`) y construye con los scripts canónicos (`node scripts/build.mjs` + `assemble-modules.mjs`, jlink, smoke servidor normal + proxy, tarballs por SO); `release.yml` llama al reutilizable desde su job `modules`, conserva manifest→bundle×3→publish draft con `latest.json` y su `check-version` ahora exige también `web/package.json`. **Bump 0.2.0**: `web/package.json`+lock, `mcp/package.json`+lock+`src/index.ts`, `launcher/Cargo.toml`+lock (`nexus-launcher`/`nexus-shell`) y `tauri.conf.json`; `site/content.json` release → 0.2.0 (tag v0.2.0, status draft, notas/highlights nuevas) y `gen-dashboard` regenerado. Los secrets de firma (`TAURI_SIGNING_PRIVATE_KEY[_PASSWORD]`) ya estaban subidos desde 2026-09-10. **Validación**: YAML OK (ruby), `unit`+`typecheck` verdes, `mcp` 41 passed/1 skip (un flake de timing, verde al reintentar), `cargo metadata` coherente y validadores de generados al día. **Pendiente del usuario**: commit+push, tag `v0.2.0`, dispatch manual del workflow y, al publicar el draft, pasar `release.status` a `published` en `site/content.json` + marcar el release como latest (`gh release edit v0.2.0 --latest`; hoy `releases/latest` apuntaba a `engine-1.4.61-v1` y el endpoint del updater daba 404 — corregido marcando v0.1.0). |
| 2026-09-18 | PLAN-6 ejecutado con orquestación multiagente (5 WPs ∥ + validación en vivo) — 2 bugs reales cazados y corregidos; suite completa 9/9 | Criterio rector fijado por el usuario: **funcionar contra upstream `beta.xmage.today`; sin parches de vista nuevos en el fork**. **WP-D (docs)**: `AGENTS.md` deja de afirmar que `invite-link` sigue excluido (known-broken vacío desde 09-12), `ROADMAP.md` sincronizado con `site/content.json` (prioridad/Space, sideboard, multi-bloqueador, Commander, draft → Completed), `web/AGENTS.md` sin el número fijo de galería (160+), `INTERACTION_COVERAGE.md` corrige "casos fuera de alcance" (badge `CaseSolved` ya renderizado; hint huérfano en motor = gap upstream aceptado), "mazmorra interna" → **Room de Duskmourn** y añade fila Speed (presencia vía `designationNames`; **nivel 1–4 no emitido upstream**). **WP-B1 (fail-to-find)**: el botón "Terminar" **ya existía** (`TargetBar.tsx:40-42`, `CardGrid.tsx:142-148`, `useFeedbackForm.ts:99-101`); lo que faltaba era cobertura — escenario fake con `required:false`/`flag:false`, `web/e2e/fail-to-find.spec.ts` (@failtofind; rojo con mutación del `flag`, verde restaurado) y caso `flag:'false'` en `feedback.test.ts`; filas duplicadas del doc corregidas. **WP-B2 (day/night)**: se retira la heurística muerta de `designationNames` y el indicador usa el hint real de `rules`/`myHelperEmblems` (`web/src/board/dayNight.ts`, con `secondCardFace` y fallback para espectador); `MechanicsTray`/`PlayerInfoBar`/`GameScreen` adaptados, `hintRegistry` re-apuntado, tests con los strings reales de los frames (rojo→verde) y escenario `mechanics` con el permanente Nightbound. **WP-G (guarda de causas engine→view)**: `web/src/state/engineViewRegistry.ts` con **140/140** campos del baseline triados (`rendered|accepted|bookkeeping|parser-artifact|duplicado`, causa obligatoria), `web/ENGINE_VIEW_TRIAGE.md` y `engineViewCoverage.test.ts` extendido (fila por campo, huérfanas, ref/causa; rojo→verde sobre `PlayerView.speed`); A3 (maxHandSize, drops, canGainLife, range, K'rrik…) aceptado con causa. **WP-C6/D5**: `FloatingChat` ya no tapa el pie de Mazos (panel z-4 y pie z-6 **solo con panel abierto**; el FAB cerrado conserva z-60 — la primera versión bajaba el FAB y el orquestador cazó la regresión en el test existente), caso nuevo 1000×800 (falla sin el fix); `printing-preview` endurecido con leave+enter forzado (el preview solo se refresca en `mouseenter`), `auto-pod` con `waitForFunction`; estrés 4 workers (100 runs) verde y `hand-bar` neutralizado (Chrome recalcula hover tras el cambio de layout al arrancar). **WP-C (validaciones en vivo C1–C5, MCP+web)**: C1 pass (submit de `SideboardScreen` por UI en Bo3; game 2 con biblioteca 54), C2 pass (visor de exilio), C3 aceptada con causa (mismo flujo que U5), C4 pass (replay + jugabilidad tras recarga), C5 pass (staging + bracket en vivo); evidencia en `docs/history/qa/p6-live-validations.md` + screenshots y filas U4/U5/U10/U11/U12 de `COMPONENT_PARITY.md`. **2 hallazgos corregidos**: **F1** — el tablero quedaba congelado en la partida 2 de Bo3 porque `eventHandler.ts` descartaba el `GAME_INIT` nuevo (guard temprano de `gameId` + `switchingGame` solo eximían `START_GAME`; el `START_GAME` previo ya había actualizado el id): `GAME_INIT` pasa a ser switching incondicional, 3 tests rojo→verde en `store.test.ts` (uno replica el orden real del proxy) y Bo3/Bo5 fake verdes; **F2** — el default `tournamentType:'Elimination'` del MCP provocaba NPE en el server (nombre no registrado): default a `Constructed Elimination` + descripción, test en `mcp/test/tournament.test.ts` y `mcp/README.md`; `npm --prefix mcp test` 41 passed. **Verificación final** (stack reiniciado): suite `scripts/test.mjs` **9/9** — unit 1870, coverage, typecheck, build, java, self-test, human-test, e2e 262 passed/4 skipped, i18n; `mage_validate_generated` al día. Sin commits. |
| 2026-09-18 | PLAN-6 creado — triage de huecos fuera de plan4/plan5: estados de motor sin representar, UI pendiente, deudas del plan2 y drift documental | Rastreo de exclusiones e2e, baselines de guardas, TODOs, `COMPONENT_PARITY` e `INTERACTION_COVERAGE` (más verificación directa en el fork, solo lectura). **4 familias**: (A) **estados de motor sin representación** — `speed` de Aetherdrift ("Start your engines!"): el motor lo lleva (`PlayerImpl.java:159/4866-4890`; `Speed.java` con designación + trigger), `designationNames` lleva la **presencia** pero el **nivel no** está en `PlayerView`/contrato/hints/web ni en el desktop, y el baseline `engine-view-gap.baseline.json:114` lo lista **sin causa**; rooms de Duskmourn (`PermanentImpl.java:2336-2364`: `unlockDoor` solo hace `informPlayers`; triage erróneo en `INTERACTION_COVERAGE.md:135` como "mazmorra interna"); y estados de jugador en el cajón "resto de `PlayerView`" (maxHandSize, drops extra, can't gain life, rango de ataque, K'rrik). (B) **UI pendiente con frame/oráculo ya existente** — fail-to-find (`INTERACTION_COVERAGE.md:242` "Pendiente del web"; frame `fail-to-find.json` con `flag:false`), indicador day/night (heurística muerta en `:167`) y línea de "casos fuera de alcance" obsoleta tras el badge D2. (C) **deudas del plan2 sin plan** (`COMPONENT_PARITY.md`): submit de sideboard por UI en vivo (A.3; ahora factible con `mage_submit_deck`), visor de exilio (A.5), `UserActionModal` humano↔humano (A.6), reconexión web mid-game (A.7), staging de torneo (A.4) y fix de `FloatingChat` (z-60) tapando botones en Mazos (B.9). (D) **drift documental y flakes** — AGENTS dice que los e2e de `invite-link` siguen excluidos "pendiente de triage" cuando `known-broken.ts` está vacío desde 2026-09-12, `ROADMAP.md` con la matriz desactualizada, `web/AGENTS.md` con 151 entradas de galería (el builder lee el manifest) y flakes de hover (`printing-preview`/`auto-pod`, verdes aislados). Incluye guarda propuesta (`ENGINE_VIEW_TRIAGE.md` + causas por entrada en `engineViewCoverage`, patrón `hintRegistry`), 8 work packages, criterios de aprobado y 6 decisiones pendientes (D1 speed, D2 rooms, D3 estados de jugador, D4 validaciones, D5 guarda, D6 B.9). Docs-only. |
| 2026-09-18 | Triaje y fix del proxy — failover JMX sin `org.jboss.mx` + spam de `publishLobby` | **Síntoma**: `.run/proxy.err.log` de **648 MB** y `Lobby publish failed: NoClassDefFoundError org/jboss/mx/util/ObjectNameFactory` (luego `Could not initialize class ... InternalTransporterServices`) cada 2 s durante las caídas del server. **Causa raíz** (código de jboss-remoting 2.5.4.SP5 leído de las sources): `TransporterClient.invoke` llama a `findAlternativeTarget()` **solo tras un `CannotConnectException`** (failover por clustering/JMX); ese path inicializa `InternalTransporterServices`, cuyo `<clinit>` crea dos `ObjectName` con `ObjectNameFactory` de jboss-mx. El jar sombreado del proxy incluye `org.jboss.remoting` pero **no** `org.jboss.mx`, y el artefacto (`org.jboss.mx:jbossmx:4.0.2.GA`) ya **no se publica en ningún repo** (404 en JBoss public, Red Hat GA, Mulesoft y ausente en Central; el fork tampoco lo declara). Efecto: la primera caída del server envenena el `<clinit>` para siempre en ese JVM (`NoClassDefFoundError` en cada failover) y cada tick del lobby escribe ~70 KB de stack; al reconectar, las invocaciones normales no tocan el failover y se recupera. **Fix**: (1) shim `Mage.Proxy/src/main/java/org/jboss/mx/util/ObjectNameFactory.java` — el proxy no usa clustering (conecta por locator explícito): el `NetworkRegistry` queda nulo ⇒ `findAlternativeTarget` devuelve false y se propaga el `CannotConnectException` real; (2) throttle en `publishLobby` (primer fallo con stack + 1 línea/min, reset al publicar) para que una caída no inunde el log; (3) test `ObjectNameFactoryTest`. **Verificación** (caída controlada con proxy + sesión WS vivas, SIGTERM al server): 23 s de caída → 4 líneas/11 KB (1 stack por sesión MCP) vs ~70 KB/2 s por sesión antes; **0** menciones de `org.jboss.mx`; CPU del proxy 0,0%; al volver el server **0 fallos nuevos** (recuperación automática); `verify-spectator-end` **17/17** (WATCHGAME/GAME_INIT/updates/END_GAME_INFO) y `self-test` **15/15** — el fallo inicial de WATCHGAME era **flaky del test IA-vs-IA** (mazos sin win-con que pueden terminar antes del watch), no del proxy; capa `java` verde; jar reconstruido (`build.mjs proxy`) y proxy reiniciado. |
| 2026-09-18 | PLAN-5 implementado con orquestación multiagente (A ∥ B) + decisiones D2/D3 | Ejecutado el runbook `docs/history/plan5-orquestacion.md`: WP0 baseline (unit+typecheck verdes; árbol con el frame `slicer` preexistente sin commitear), recon E1/E2/E3 (`explore`), Gate 1 (embudo `prompt()` + payload del ASK resuelto por código: `HumanPlayer.chooseUse` rellena `secondMessage` con la fuente y `GameImpl.addMessageToOptions` lo publica), implementación A ∥ B (`general`, locks disjuntos), Gate 2 (verificación del orquestador: alcance, suite, guardas rojo/verde), Wave 3 (recon C + decisiones del usuario) y WP-D2. **Familia A**: `web/src/utils/textRefs.ts` (`sanitizePromptText`: `{this}`→`sourceName`, ICON_GOOD/BAD/REQUIRE/RESTRICT→✓/✗/⚠/⊘, `<br/><hintstart/>` fuera, tags/espacios) aplicado en `parse.ts:344` (cubre los 24 cases vía el embudo), `promptText.test.ts` con el string real de `slicer.json:205` + pipeline `parseFeedback` + entrada `prompt:ask-slicer` en la galería (red→green; e2e galería+`@recorded` 115 passed/1 skipped). **Familia B**: `scripts/hint-schema.mjs` (determinista, 2 corridas idénticas; 14 rules · 60 `*Hint` · 42 `addHint` · 6 icons · 19 info; solo `Mage/**`+`Mage.Common/**`; `Mage.Sets` fuera de v1 declarado en `meta.scope`), `web/fixtures/hint-schema.json`, `web/src/board/hintRegistry.ts` (100 filas: 36 rendered/64 known-unrendered con causa) y guarda `web/src/state/hintCoverage.test.ts` (roja al quitar una entrada del baseline y al quitar una fila del registro; verde restaurada). **D2**: badges `CaseSolved`/`Harnessed`/`Evidence`/`Prepared`/`Protector` en `designations.ts`/`CardSlot.tsx` (+CSS) con i18n ×9 y refs del registro a `rendered`. **D3**: `scripts/integration-report.mjs` corre el subconjunto real de 5 specs (`deckvalidation`, `multi-user`, `priority-stop-real`, `skips`, `full-flow`) contra `127.0.0.1:17171`; verificado en vivo (5 passed/2 skipped, 54,2s). **D4** CI visual opt-in documentado; **D5** P5 cerrado por decisión; **D6** `--seed=` pospuesto (repro por `--humanDeck/--simDeck`). Verificación final: unit 1848, coverage/typecheck/build/java/human-test/i18n verdes, `validate_generated` al día, sin commits. Pendiente ajeno al plan: `self-test` WATCHGAME roto por el proxy (`publishLobby failed: NoClassDefFoundError org/jboss/mx/util/ObjectNameFactory`, desde ~15:18; no lo toca el diff; abrir triaje propio). |
| 2026-09-18 | PLAN-5 orquestación multiagente diseñada (`docs/history/plan5-orquestacion.md`) | Runbook de ejecución del plan5 con el agente principal como **orquestador y verificador**: reglas no negociables (sin commits/generados/fork, un escritor por fichero, red→green, verificación del orquestador sobre árbol congelado, e2e de agentes solo fake), contrato de retorno de subagentes, 5 oleadas (WP0 baseline → E1–E3 recon ∥ → Gate 1 → A ∥ B implementación → Gate 2 → decisiones C/D2–D5 → suite final), 7 work packages con briefs listos para lanzar, matriz de propiedad de ficheros (locks A/B disjuntos), protocolo de verificación S1–S3 con comandos exactos, 5 puertas de decisión y tabla de riesgos. Docs-only. |
| 2026-09-18 | PLAN-5 creado — presentación del protocolo y validación restante | Tras el hallazgo del `{this}` al grabar Slicer, se documentan las **2 familias que el plan4 no cubría** (sus guardas miran campos, no textos) y lo que queda de validación: **A) placeholders/residuos en textos de prompt** (el motor tiene `CardUtil.replaceSourceName` pero su uso es opt-in por effect — 3.887 ficheros de `Mage.Sets` usan `{this}`; `parse.ts` tiene 24 `case 'GAME_*'` y solo 5 pasan por `messageWithSource`, `GAME_ASK` extrae `sourceName` pero no sustituye; fix propuesto: helper `sanitizePromptText` aplicado a todos los prompts + corpus de tests con `slicer`/`goad`/`class-level`) y **B) hints textuales** (`rules`/`info` parseados con regex: ~12 restricciones de `PermanentImpl.getRules`, `addInfo` keys, ~60 clases `*Hint`, 6 `HINT_ICON_*`; el web reconoce monstrous/renowned/suspected/paired/classlevel/ring-bearer/dungeon/ring/day-night/commander-damage; huecos conocidos CaseSolved/Prepared/Harnessed/…; guarda propuesta `hintCoverage` + `scripts/hint-schema.mjs` con baseline, patrón `engineViewCoverage`). Incluye frentes no-software (V1 specs reales, V2 CI visual Linux, V3 P5, V4 P6 seed+hook P2, V5–V7 humanos/instalación), criterios de aprobado, orden y 5 decisiones pendientes. Docs-only (sin suite requerida). |
| 2026-09-18 | Frame real `slicer` (Slicer, Hired Muscle) — ceder un permanente al rival en SU upkeep + goad, sin campos nuevos | Motivado por la pregunta "¿está cubierta Slicer?" (no había driver/frame): `scripts/drivers/slicer.mjs` cheatea Slicer al campo propio tras la tierra (la DFC entra por cheat sin decisión "as it enters"; un primer intento murió por la carrera intermedia conocida del `cheatSetup` y el reintento funcionó) y pasa el turno; en el upkeep del SIM llega el `GAME_ASK` del trigger (`BeginningOfUpkeepTriggeredAbility(TargetController.OPPONENT)` + `chooseUse` del controlador) y el hook `onAsk` responde SÍ. **Hallazgos**: (1) el texto del ASK lleva el placeholder `{this}` SIN resolver — un `onAsk` que matchee por el nombre de la carta nunca dispara (el primer intento stalló 60 s por esto); (2) al aceptar, Slicer sale en el battlefield del SIM (`controllerName` del controlador real), `tapped:false` (untap) y goad + "This creature can't be sacrificed." viajan en `rules` + `cardIcons.OTHER_HAS_RESTRICTIONS` ("Goaded by … (must attack)", mismo camino que `goad.json`; `goadingPlayers` sigue sin serializarse); (3) el caso no necesita ningún campo nuevo del contrato (la dirección inversa a Threaten ya estaba cubierta por `threaten`/`switcheroo`). `manifest` 114→115 e invariante `hasSlicerCeded` en `recorded.test.ts`; `fixtures/recorded.test.ts` 116/116 y replay fake `@recorded` del frame en verde; INTERACTION_COVERAGE (fila junto a Switcheroo), `docs/history/qa/p4-frames-log.md` y plan4 §3.10 actualizados. |
| 2026-09-18 | plan4 §5 (parte 100% autónoma) — cierre de prompts 5.1, heurística 5.2 (2ª pasada), latencia 5.4 y sensación de juego 5.7 | **5.1 prompts**: cabecera/reloj por encima del backdrop (`--game-header-z: 2000`); cancelar con disciplina de `required` en `CardGrid`/`TargetBar` y `options.canCancel` en multi-amount; `sourceName` con `messageWithSource()` (sufijo ` (source: X)` + `options.secondMessage`) en ability/pile/X/multi; `UserRequestDialog` sin cierre por backdrop; patrón `Vote for …` en `serverMessageTranslation` (4 claves ×9); `SideboardScreen` envuelto en `Modal` (foco/trampa). **❓ en vivo** (sesión real 2×HUMAN con `cheatSetup` + navegador adjunto a la sesión del rival): el servidor **no** emite chat `"Waiting for X"` — el rival ve pila + controlador + anillo de prioridad + botón de acción (no congelado) y el reloj en cabecera; quedan 2 ⚠️ de motor (tope de X `INT_MAX`, timeout de starting player sin valor en el wire). **5.2 heurística**: evaluador 2 (agente, `docs/history/qa/p5-2-heuristic-findings-eval2.md`) sobre Pod/Commander/Arena + matrices no revisadas → 7 hallazgos, 0 S3–4 (4 S2: fin de partida sin traducir, badge de estado de lobby, estados de torneo, `.replace()` en clasificación; 3 S1: "Tu lado" sobre celda rival en pod-4, tooltip de invocación enferma, Log/Chat en ru/ja/zhs); **los 7 + los 5 del evaluador 1 corregidos** (incl. anclaje+anti-spoof de `[NEXUS_READY]` con `parseReadyMarker`, monograma, chevrón de scroll del cuadro, i18n del voto) y baselines regenerados (`prompt-target`, `screen-lobby-overflow`). **5.4 latencia**: sonda DEV `perfProbe` (`window.__magePerf`) + acuse optimista en `CardSlot` (`is-pending`/`is-chosen-pending`) + `FakeServer echoDelayMs`; `e2e/latency.spec.ts` `@latency` **7/7** con acuse <100 ms y espera explícita con eco de 1,2 s (pasar, tierra, cast, target, maná, combate, draft). **5.7 game feel**: `@media prefers-reduced-motion` en 27 CSS de juego que faltaban + `clearFlights()` en `GAME_INIT` + `e2e/game-feel.spec.ts` (0 animaciones con reduce, vuelo no bloquea el pase, 0 restos tras reload). **Verificación**: unit 1824/1824, typecheck, build, e2e fake **259 passed / 4 skipped / 0 failed (~8,4 min)**, galería visual 2/2 con baselines actualizados. Pendiente de §5: 5.3 (prueba de 5 s), 5.5 (jugadores) y 5.6 (dogfooding) — requieren personas; evaluador 3 idealmente en vivo. |
| 2026-09-18 | E2E fake en paralelo (4 workers) — puerto dinámico por test + `fullyParallel` | El e2e fake era serial (`fullyParallel:false, workers:1`): 253 tests/70 ficheros en **31,6 min**, con `recorded.spec.ts` (112 tests) como camino crítico. **Cambios**: (1) los 3 puntos que arrancaban el FixtureServer en el 8789 fijo (`e2e/fixtures.ts` fixture `fakeServer` —sin tocar `autoShot`—, `chat.spec.ts` y `finished-matches.spec.ts`) pasan a `FakeServer.start(0)` + `setFakePort(server.port)` con restauración del puerto anterior en `finally` anidado (idéntico patrón que `support/fake-backend.ts`, que también se endurece y ya restauraba); (2) `playwright.config.ts`: `fullyParallel: FAKE_MODE` y `workers: FAKE_MODE ? (Number(E2E_WORKERS) || min(4, cpus-1)) : 1` (retries 0); (3) `backendUrl()` de `dual.ts` pasa a usar `proxyPort()` (ya no queda ninguna URL al 8789 muerto; `BACKEND_PORT` queda solo como fallback de `getFakePort`). **Verificación**: smoke dirigido con `--workers=4` (chat+finished+full-flow+recorded creature) **7/7 en 11,3 s**; suite fake completa **249 passed / 4 skipped / 0 failed en ~8,1 min** (3,8× más rápida; baseline 246/4), **sin colisiones de puerto ni fallos de estado compartido**. La 2ª corrida de verificación destapó **1 flake preexistente por carga** (no del scheduling): el clic geométrico a `.strip-btn.swap` de `decks-gallery.spec.ts:11` caía en `element is not stable` → `.strip-name` interceptaba → se perdía el hover (229 reintentos); endurecido con `hover()` + `expect(visible)` + `dispatchEvent('click')` (mismo patrón que el DnD del propio spec, sin `force`), con estrés `--repeat-each=3 --workers=4` **27/27** y 3ª corrida completa **249/4 en 8,1 min**. real `priority-stop-real.spec.ts` **1/1 con 1 worker en 18,2 s** (serial intacto); unit 1780/1780 + typecheck. **Lección**: el modo fake ya aislaba la partida por test (escenario por servidor), el único acoplamiento global era el puerto fijo 8789 en las fixtures; publicarlo por `setFakePort` lo elimina sin tocar specs ni escenarios. |
| 2026-09-18 | Suite e2e verde — arreglados los 3 `decks-gallery` preexistentes (1 bug DEV de solape + 2 drifts del spec) | La suite completa del commit anterior quedaba con 3 fallos estables de `web/e2e/decks-gallery.spec.ts`, confirmados también en árbol limpio (HEAD `611ed1c5868`): no eran del cambio de priority-stop. Diagnóstico con 2 agentes sobre traces/error-context + `git log`: (1) `precon: Editar clona el mazo...` — el clic de `.decks-edit-btn` lo interceptaba el pill DEV `.debug-toggle-btn` (`.debug-drawer-container`, `position:absolute; bottom:14px; right:20px; z-index:50`) y agotaba los 120 s; fix de producto en `web/src/lobby/LobbyScreen.css`: `z-index: 50 → 4`, el footer de producto (`z-index:6`) gana el hit-test sin mover píxeles (0 baselines visuales actualizadas; único compromiso: en DEV el toggle "Eventos de red" queda tras el footer en la pestaña Mazos, en producción el drawer no existe). (2) `import .dck text...` y (3) `explores online & meta decks catalog` — drift del spec tras el diálogo unificado de importación (`7dca1a7fd85`): usaba `.decks-import-dialog`/`.browser-url-import-view`, clases ya inexistentes en `web/src`; alineados a `.decks-import-cta` → `.deck-import-modal`/`.import-name-input`/`.deck-import-textarea`/`.import-submit-btn`, conservando el intent de import por URL con `.import-badge.success` (Moxfield). **Verificación**: decks-gallery 9/9, unit 1780/1780, typecheck, `E2E_VISUAL=1` 2/2 sin tocar baselines y `scripts/gallery-visual.mjs` 6/6. |
| 2026-09-18 | plan4 §3.5 — `priority-stop-real.spec.ts` verde: el "1 de 3" era un bug del test (clic sobre la carta tapada del abanico), no del motor | Investigación multiagente (2 `explore` en paralelo: ruta cliente `PhaseBar`/`phaseStops`/`maybeAutoPass` y ruta motor `UserSkipPrioritySteps`/`HumanPlayer.checkPassStep`/cleanup; + 1 `general` como revisor adversarial del trace). **Evidencia forense en `.run/proxy.err.log`**: la partida quedaba atascada en el `GAME_TARGET` de descarte obligatorio del CLEANUP (mano 8 → 7) — 611/612 re-prompts seguidos sin un solo `GAME_SELECT`/`GAME_UPDATE` después (el descarte nunca se resolvía); `HumanPlayer.choose` ignora el booleano `false` de un target `required` y re-pregunta. **Reproducción local** (`E2E_BACKEND=real E2E_SERVER_HOST=127.0.0.1`): el test agota los 240 s dentro de la rama del descarte. **Trace de Playwright**: `params.selector = ".hand-card.targetable, .card-grid-cell >> nth=0"` resolvía a la carta MÁS A LA IZQUIERDA del abanico, cuyo punto de clic lo cubre la siguiente (solape real ~65%: `margin-left = gap − cardW·0.5`; lo confirma el snapshot `--card-w:156 --hand-gap:-24`); 443 líneas `intercepts pointer events` y **cero `sendPlayerUUID`/`sendPlayerBoolean` tras el `GAME_TARGET`** — con `actionTimeout=0`, `click()` reintenta hasta el timeout del test y el `.catch` nunca llega; el bucle no responde y no hay más upkeeps. **Fix (solo spec)**: `.last()` (la carta derecha no tiene hermanos posteriores encima) + `click({timeout:5000})` acotado + `expect.poll` al cierre del prompt en vez de `waitForTimeout(150)`. **Verificación**: 3/3 corridas reales consecutivas verdes (~18 s cada una) con las 3 paradas asertadas; proxy limpio (2 `GAME_TARGET` resueltos vs 611); unit 1780/1780 + typecheck. Sin cambios de producto (el gap real de `maybeAutoPass` no consultando `phaseStops` ya se cerró el 2026-09-17, `actions.ts:315`). **Hallazgo colateral corregido**: `npm run test:e2e:real` sin `E2E_SERVER_HOST` apunta a `beta.xmage.today` y el campo Contraseña está deshabilitado en beta (`passwordDisabledOnBeta`) → `start-game.ts:82` agotaba el timeout en el login (el nightly `integration-report.mjs` lanza el e2e real sin ese env); ahora `login()` solo rellena la contraseña si el campo está habilitado, así el modo real arranca contra beta o contra el stack local (`E2E_SERVER_HOST=127.0.0.1`); verificado en ambos caminos (beta: lobby en 3,4 s). **Suite completa**: `scripts/test.mjs` con el cap de `e2e` subido de 30 a 60 min (`3_600_000`: la capa fake tardaba 31,6 min y moría por timeout, no por fallos) → 8 capas pass, `e2e` 246 passed/4 skipped con **solo 3 fallos preexistentes de `decks-gallery`** (confirmados también en árbol limpio; corregidos a continuación). |
| 2026-09-17 | Cierre de §3 del plan4: Suizo headless, espectadores con END_GAME_INFO (parche en el fork), 2HG/casos fuera de alcance y 114 frames | **Suizo (R)**: nuevo `scripts/verify-swiss.mjs` — torneo **Constructed Swiss** real de 4 `COMPUTER_MAD` con `numberRounds:2` (bots juegan solos, sin prompts ni `joinGame`): `tournamentState` avanza `Starting → Dueling → Finished`, 2 rondas × 2 emparejamientos en `rounds[].games` (`players`/`result`/`state`; el wire no tiene `pairings`), standings coherentes (campeón 6 pts `Finished (Winner)`, 3/3/0, `R1`+`R2` en `results`, agregado 12 = 4 matches decisivos × 3) — **TODO PASS 20/20**. **Espectar (R)**: el TODO "inform watchers about game end and who won" de `GameController.endGameInfo()` se cerró con un parche **aditivo** en el fork (misma versión 1.4.61, beta intacta): `GameSessionWatcher.endGameInfo(Table)` dispara `END_GAME_INFO` con `GameEndView` y `playerId=null` (GameSessionWatcher.java:125-129) y `GameController` lo llama por cada `getGameSessionWatchers()` (GameController.java:899-901); sin cambios en proxy/contrato (el proxy ya enrutaba `END_GAME_INFO` y el web ya lo pinta con `handleEndGameInfo`). Build completo del fork + `build.mjs proxy` + `restart all`; verificado con `scripts/verify-spectator-end.mjs` (HUMAN+SIM en curso → espectador entra tarde por `watchTable`→`WATCHGAME`→`watchGame`→`GAME_INIT`+`GAME_UPDATEs` → concede el humano): jugador y espectador reciben `END_GAME_INFO`, el del espectador con `players` (ambos), `matchInfo "sim-000001 won the match!"` y `clientPlayer:null` — **TODO PASS 17/17**. **Fuera de alcance documentado**: 2HG no existe como gameType en el config del servidor (oráculo `server-state-schema.json`: 17 gameTypes, ninguno 2HG; `config.xml` sin registro; no es gap de cliente) y "casos" fuera por acuerdo (con corrección: el motor SÍ implementa Case — `CaseAbility` + 13 cartas; el "gap del motor" del plan era erróneo). **P4 104→114 frames** (8 mecánicas nuevas + re-grabaciones). **Suite**: unit web **1780/1780** (191 files) + typecheck + build + java + human-test + i18n PASS; `self-test` FAIL en la primera pasada post-restart (flake conocido: WATCHGAME no llega y el proxy loguea "lost connection to server") y **PASS al reintentar** con el servidor caliente (18.1 s); `e2e` fake directo **246 passed / 3 failed / 4 skipped** (los 3 `decks-gallery` son el WIP preexistente de `web/src/decks/*`; 2 de ellos por timeout de 120 s y 1 por `.browser-url-import-view` ausente; los 4 skipped son los known-broken de invite-link) — ojo: el e2e completo tarda ~31 min y el límite de 1800 s de `scripts/test.mjs` lo corta, hay que subir ese límite. plan4 §3.11 cerrada y §3.9/§3.5 al día. |
| 2026-09-17 | Space atrapado por la carta enfocada — Space vuelve a ser atajo global en la región de juego | Reporte del usuario: al seleccionar cartas en la mesa, `Space` re-activaba el objeto con foco en lugar de pasar prioridad. Causa raíz: `clickableProps` (audit a11y C.14) había convertido cada carta clicable en `role=button tabIndex=0` con activación Enter+Space, y el clic de ratón enfoca el div; el guard `GameScreen.isSpaceShortcutTargetIgnored` ignora `Space` sobre `[role=button]` (anti doble disparo) → el atajo moría en el elemento enfocado. **Fix por región (elegido con el usuario tras descartar el parche de `preventDefault` en `mousedown`)**: `.game-body` se marca `data-space-passes-priority` (`SPACE_PASS_REGION_SELECTOR` en `ui/clickable.ts`) y dentro de la región Space es SIEMPRE el atajo global (pasar/confirmar) mientras las cartas se activan con **Enter**; `clickableProps` consulta `closest(selector)` en tiempo de evento (sin prop drilling ni efectos globales de ratón) y el guard deja pasar los `[role=button]` de la región (los controles nativos — `ActionButton`, `PassMenu`, inputs — no cambian; los diálogos quedan fuera de la región y conservan Space para seleccionar). **Tests**: unit `clickable.test` (Space no activa dentro / Enter sí), `GameScreen.space.test` (roles dentro/fuera + nativos + descendientes), `CardSlot.test` (región); e2e fake `control.spec.ts`: clic en la criatura atacante → `Space` confirma atacantes (`sendPlayerBoolean`) y no re-activa la carta — verificado rojo→verde quitando el marcador. **Overlays**: `data-space-shortcut-off` en `CrossZoneOverlay`/`PileOverlay`/`HandViewer` + `isSpaceShortcutBlocked` (consulta global, además del foco) → con un visor abierto Space no pasa prioridad aunque el foco caiga al `<body>` al clicar el fondo; e2e `cross-zone.spec` (Space con el visor abierto no envía `sendPlayerBoolean`, verificado rojo→verde quitando el marcador). Unit **1769/1769** (191 files) + typecheck ✅; e2e fake `control` 1/1, `cross-zone` 2/2, `pile-visual`/`interactions`/`hand-bar` 6/6, cola de 12 specs 37/37 y decks-gallery/invite-link/flights/full-flow 10/13 (los 3 fallos de `decks-gallery` se reproducen con los cambios stasheados → WIP previo del árbol). **Lección**: un atajo de juego no debe depender del foco; si un control de la superficie consume la misma tecla, el conflicto reaparece con cualquier elemento focusable futuro — la regla por región es determinista y mantiene Enter para la a11y. |
| 2026-09-17 | P3 galería: matriz §4 completa, Pod/Arena y matriz visual navegador×resolución (2 vías en paralelo) | **Vía A (galería)**: 124→**151 entradas** (+27) — set completo de pantallas/diálogos con estados de §4 (setup wizard, wizard de mesa con preset real, staging de jugador, ajustes/apariencia/acerca de/ayuda, fin de partida y de match, torneo ×4 con cargando/error/vacío/espera, panel de torneo, draft cuñado, lobby con error, login conectando/error, tablero Pod 2×2/Arena/Pod Commander 3 con GameView clonado de frames y `playerId` únicos, mano de 15, nombres largos, idioma largo ru, CJK ja, zoom 125 %, banner de reconexión); no representables documentados con causa (wizard inválido — los setters auto-corrigen por diseño; `pickError`/`expired` del draft — backend/tiempo vivo; lobby "cargando" — no hay flag; construct error — sin UI; editor con IndexedDB real; FFA 5+ fuera de alcance). Bugs propios de la galería corregidos: orden de efectos al sembrar localStorage (siembra en el click, antes del montaje) y bleeding de estado (`error`/`chatMessages` sin reset + remonte por `Fragment key`). **Vía B (matriz visual)**: runner `scripts/gallery-visual.mjs` (chromium+webkit × 1366×768/1920×1080/2560×1440 + 1600×900; `--update`/`--dry-run`/`--browser`/`--viewport`, detecta navegadores ausentes) + scripts npm `test:e2e:visual[:update]`; `VISUAL_ENTRIES` 21→48; **336 baselines macOS** (315 nuevos, ~239 MB) en `web/e2e/gallery.spec.ts-snapshots`. **Dos causas de no-determinismo cazadas y cerradas**: (1) **carrera real de medición** en `CombatArrowsOverlay` — solo recalcula en resize del tablero/ventana, así que el primer cálculo podía caer antes de que asiente `useZoneScale` y unas sesiones recibían luego un tick del ResizeObserver y otras no (hasta 9 px de diferencia; probe de píxeles: 0 diff forzando recompute tras asentar) → fix en el harness (`showEntry` despacha `resize` tras el asentado) + geometría entera (centros y punto de control); (2) **animaciones infinitas** no congelables en WebKit pese a `animations:'disabled'` → `@media (prefers-reduced-motion: reduce)` en 19 CSS de producto (patrón ya existente; cubre el estado reduced-motion de §4/§5.7) + `test.use({ reducedMotion: 'reduce' })` en la galería. Jitter residual de rasterizado: tras el recompute, el trazo discontinuo seguía variando por sesión (452–1008 px y hasta **2 820 px** en `board:arena-4`@2560, estable dentro de cada sesión) → bajo movimiento reducido los trazos de flechas y targeting pasan a **sólidos** (`stroke-dasharray: none`), eliminando la fase de guion ligada a la longitud sub-píxel del path; `maxDiffPixels: 1200` se mantiene como margen del AA del marcador y las regresiones reales (>2 500) siguen fallando. WebKit 1366: update + 3 corridas verdes. Vía A se colgó al final de su bucle de verificación (procesos ya muertos, spec con un test DEBUG temporal) — recuperado a mano: debug eliminado, fix confirmado con 3 corridas seguidas de webkit 1366 y regeneración/verificación finales por B. Pendiente para cerrar P3: CI con baselines Linux (`playwright install --with-deps webkit`) y `solo teclado`. Unit 1761/1761 + typecheck + smoke verde sin pageerror (159 entradas con el manifest actual) ✅. |
| 2026-09-17 | Mindslaver / control total del turno ajeno (G12-1) — cerrado en cliente, sin Java | Diagnóstico en vivo (MCP + navegador, partida real con Mindslaver contra el SIM): la web ya recibía todo — prompts en el turno del controlado marcados "(as sim-000040)", su mano en `GameView.opponentHands` y sus objetos jugables en `canPlayObjects` (`GameSessionPlayer.prepareGameView`) — pero la UI no dejaba actuar: `canPass` exigía `me.hasPriority`/`me.isActive` (ambos falsos con el turno ajeno), el Switch Hands vivía en estado local de `BoardZone` que no alimenta la `handBar` del shell (la mano visible en 1v1) y su botón quedaba tapado por el abanico, y la zona del rival no recibía playables/combate. **Fix (solo `web/`)**: (1) helper puro `state/control.ts` `controlInfo`/`isControllingPriority` (indicador = `opponentHands` no vacío, mapeando nombre/playerId; prioridad por `priorityPlayerName` con fallback a `hasPriority`); (2) `GameScreen.canPass` + rama de `ActionButton` "Pasar prioridad" con sublabel `game.controlling_turn` ("Controlas el turno de {name}", i18n ×9) — "Esperando oponente" queda solo sin control; (3) `switchedHandKey` al store (`slices/game` + `setSwitchedHandKey`, poda en `eventHandler` cuando la clave desaparece) y `switchedHandCard`/`useSwitchedHand` alimentando la `handBar` de `GameBoard`/`ArenaBoard`/`TwoHeadedBoard` (normaliza `id`+`faceDown:false`); botón montado por portal en `.board-shell` (los `z-index` internos de la zona quedan capados por su contexto de apilamiento y el abanico se comía el click; verificado en vivo); (4) `OpponentZone` recibe `playableIds` + props de combate y `GameBoard`/`ArenaBoard`/`TwoHeadedBoard` usan `handleCardClick` (mantiene targeting y añade playable/combate); (5) guarda temprana en `maybeAutoPass` si la prioridad es del controlado. **Tests**: unit `control.test.ts` (5), `ActionButton.control.test.tsx` (3), `GameBoard.control.test.tsx` (3), `BoardZone.handSwitch.test.tsx` (store) y `store.test.ts` (poda + auto-pass); e2e fake `control.spec.ts` (`@control`: botón habilitado con el nombre, jugable del controlado clicado envía su UUID y se gira, Switch Hands muestra su mano y su click envía el UUID, pasar envía `sendPlayerBoolean`). Suite unit 1753/1753, typecheck y build ✅; e2e fake completo 237 passed / 3 failed (los 3 `decks-gallery` del WIP preexistente de `web/src/decks/*`) / 4 skipped (known-broken invite-links); `control.spec.ts` verde en solitario. |
| 2026-09-17 | Mulligan gratis de multijugador/Brawl (CR 103.5c) — el "Gap de motor" del plan4 era un falso diagnóstico | El usuario preguntó si bastaba un hint al crear mesa Commander. Verificado contra el fork y las reglas: la exención es **CR 103.5c** ("en multijugador y en cualquier Brawl, el primer mulligan de cada jugador no cuenta"), **por jugador**, NO ligada al que empieza (el plan citaba 103.4a/903.8 — 103.4a es la vida de Two-Headed Giant; el frame anterior probaba en realidad que el 1v1 Commander no lleva mulligan gratis, correcto). El motor ya la implementa (`MatchOptions.freeMulligans` → `LondonMulligan.mulligan()/mulliganDownTo()` con `usedFreeMulligans` por `playerId`; el ASK del servidor dice "Mulligan **for free**, draw another 7 cards?" y el informe "mulligans for free." va al chat), y el proxy ya reenviaba el campo (`MatchOptionsParser`). Faltaba el default: **web** — `recommendedFreeMulligans(gameType, numPlayers)` (`CreateTable/constants.ts`, CR: 1 para FFA/Brawl y para Commander/Oathbreaker no-duel con ≥3 jugadores; 0 en cualquier "Duel") + preset en `useCreateTableForm` con memo/ref (respeta el override manual mientras el formato no cambie) + hint de la pestaña Tiempos solo cuando aplica; i18n ×9 del hint ("multijugador y Brawl"). **mcp** — `mage_create_table` acepta `freeMulligans` (0-5, opcional) + fix de tipo preexistente `CompactGame.phase/step: string | null` (rompía `tsc` desde el contrato nullable de GAME_OVER). **recorder** — `rec-lib` acepta `driver.playerTypes/simDecks/freeMulligans` y ahora evalúa `captureWhen` tras `handleEvent` sobre cualquier evento con gameView (el turno 1 del jugador activo solo llegaba en GAME_SELECT/TARGET); driver reescrito (Commander Free For All de 3 con `freeMulligans:1`, 2 mulligans + keep ⇒ **mano 6, no 5**; los 2 SIM conservan 7) y frame re-grabado; assert `firstMulliganFreeSecondCostsCard` en `recorded.test.ts` + caso de replay en `recorded.spec.ts` (hand-bar 6 + Krenko en la zona de mando). De paso, dos arreglos de deuda ajena: el manifest ganó la entrada de `game-clock-timeout` (WIP del árbol; su caso de replay se añadió junto a `hasConcedeMulligan`) y `CompactGame.phase/step` pasó a nullable. Bug real cazado al verificar en navegador: `Commander Two Player Duel` con `numPlayers` heredado >2 daba 1 (ahora cualquier nombre con "Duel"/"Two Player" fuerza 0, verificado en vivo: FFA→chip 1 + hint, Duel→chip 0 sin hint y contador a 2p). docs/history/plan4.md §3.1 pasa de Gap a R con la corrección documentada. Unit web 1738/1738, typecheck web/mcp ✅, mcp 40/40, replay del frame ✅. |
| 2026-09-16 | P6 — `[NPH] Life for Death.dck` aislado: 2 bugs del fuzzer + 1 bug real del producto (`SimManager`) corregidos | Con `--humanDeck=`/`--simDeck=` (repro dirigida, nuevo) y un dump de prompts sin responder (`.run/fuzz-unanswered.jsonl`), se grepeó `.run/proxy.err.log` por el `gameId` del stall: el último evento antes del silencio era siempre `GAME_CHOOSE_CHOICE`, y el juego no colgaba de verdad — un `GAME_OVER` llegaba ~24000 líneas después por el idle-timeout del propio servidor. **2 bugs del fuzzer** (`scripts/fuzz.mjs`, no del producto): `optionList()` asumía `{id,value}` en los items del array, pero `choice.choices` en "modo texto" (Cavern of Souls/Pithing Needle) es un array de STRINGS planos — `c?.id ?? c?.value` sobre un string da `''`, y el check `if (opts[0]?.value)` (falsy en string vacío) nunca enviaba nada; y el extractor `keyChoices ?? choices` usaba `??`, pero `keyChoices` llega como `{}` (objeto vacío, NO null) en modo texto — `??` nunca cae al fallback. Confirmado con el payload real capturado (`choices:["White","Blue","Black","Red","Green","Artifacts"]`, `keyChoices:{}`). Con ambos arreglos: 8/8 partidas con ese mazo fijado, cero stalls. **1 bug real del producto**: `Mage.Proxy/SimManager.nextSimUsername()` generaba el nombre del bot con un contador por-instancia (siempre arranca en 0) + `currentTimeMillis()%1000` como única fuente de unicidad entre sesiones — con varias mesas SIM creándose casi a la vez (confirmado en el log: el mismo `sim-000001-951` falla al unirse a una mesa y milisegundos después se reconecta y se une a OTRA — colisión entre 2 sesiones concurrentes) el servidor mata la conexión más antigua como si fuera una reconexión, dejando esa mesa sin el asiento SIM (`startMatch` falla). Fix: contador atómico global (`AtomicLong` estático compartido por todas las instancias de `SimManager` del proceso). Verificado: 16 partidas a `--concurrency=8` (el escenario que antes disparaba la colisión) → 0 fallos; `mvn test` en `Mage.Proxy` 100% verde. |
| 2026-09-16 | Cierre post-campaña de frames (3 agentes: fix SimPlayer/FoF + P2 v3 + Conceder en Mulligan) | **Fix de `SimPlayer`** para el reparto de Fact or Fiction: con la sobrecarga `Cards` de `fireSelectTargetEvent` (`targets=null`, los ids en `options.possibleTargets` y el cierre en "Done" de `UI.right.btn.text`) el bot hacía `cancel()` y pile1 quedaba vacía (0/5); ahora elige carta por `sendPlayerUUID` (menor UUID, determinista) y cierra con `sendPlayerBoolean(false)`; helpers `cardTargetDone`/`cardTargetPick`/`optionUuids` y constructor package-private para tests; **4 tests Java nuevos** (suite 83/83), jar reconstruido y proxy reiniciado; `fof.json` **re-grabado** (pile1=[Grizzly Bears] al cementerio, pile2 [Mountain/Island/Forest/Runeclaw] a la mano) con `hasFactOfFiction` intacto y note del driver/manifest actualizada. **P2 fidelidad v3**: adjuntos (grupos `data-attachment-host` + `.attachment-subcard[data-card-id]` en orden/conjunto, `attachments-mismatch`) y tamaños por jugador (`hand/library/graveyard/exile-mismatch`; la mano propia prioriza el `hand-bar` y se omite el `.hand-zone` con Switch Hands; solo zonas pintadas), data-attrs en `BoardZone`/`HandZone`/`HandBar`/`ResourceBar`, subcards omitidos de badges; unit 1692/1692, typecheck, build y `fidelity.spec.ts` ×7 frames (aura/flashback/escape incl.) 0 discrepancias. **Conceder en el `MulliganDialog`**: botón danger (`data-testid="mulligan-concede"`) en ambas ramas que replica el camino del menú (`confirmDialog` → `sendPlayerAction CONCEDE`), i18n en 9 idiomas, cierre por `clearFeedback`; 5 tests unit nuevos + e2e de mulligan 3/3. **Regresión cazada y corregida**: el escenario fake del Ring-bearer usaba `isRingBearer` (campo inexistente) y el fix de `MechanicsTray` lo dejó en "Sin objetivo" → scenario actualizado a `cardIcons` RINGBEARER (mechanics 3/3); los 3 fallos de `decks-gallery` son WIP previo del árbol y `draft.spec` fue flaky bajo carga (5/5 en solitario). |
| 2026-09-16 | P6 — `--concurrency` + lote de 300 partidas: 2 candidatos reales, protocolo robusto bajo carga | El cuello de botella del fuzzer no es red sino el "pensar" de la IA real por partida, así que se paralelizó en vez de acelerar cada partida — el proxy ya soporta varias sesiones reales concurrentes (mismo patrón que P4 con grabadores en paralelo), techo arquitectónico `maxGameThreads=20` (`local-server/config.xml`). Lote de 300 partidas a `--concurrency=6` (~50 min): `{"timeout":144,"turn-cap-conceded":51,"startMatch-failed":1,"game-over":103,"stalled":1}`; de las 147 "anomalías" 144 son solo el marcador informativo de presupuesto corto (no bugs) y **0 callbacks desconocidos, 0 cierres de WS inesperados, 0 excepciones** en 300 partidas con mazos reales distintos — el protocolo/proxy/servidor aguanta carga real sin sorpresas. 2 candidatos genuinos: `startMatch-failed` único (`SIM sim-000001-428 failed to join` con `Tribal Zoo - Billy Hahn.dck`, sin investigar) y **un stall que se repitió** — el mazo HUMAN `[NPH] Life for Death.dck` [2011 Starter, cargado de maná pirexiano] apareció en el único stall de este lote y en el de la tanda anterior de 8, cada vez con un SIM/rival distinto: la coincidencia del mazo y no del rival apunta a un candidato real, no a ruido de máquina compartida. Siguiente paso: `--seed=` (no implementado aún) para aislarlo y logs de proxy/servidor correlacionados con el `gameId`. |
| 2026-09-16 | P6 v1 — fuzzer de self-play (`scripts/fuzz.mjs`), primer hallazgo real y corregido | En vez de "self-play MCP" literal (un LLM llamando tools de `mcp/` turno a turno sería demasiado lento/caro para muchas partidas largas), dos sesiones WS independientes contra el proxy real: HUMAN con política genérica ingenua (tierra/hechizo al azar con prioridad, si no pasa) + SIM (`SimPlayer.java`, ya probado). Mazos reales al azar del pool de 1172 `.dck` de `Mage.Client/release/sample-decks` del fork (parser propio `N [SET:NUM] Nombre`). Vigila por partida: bloqueo (sin eventos durante `--stallMs`), callback desconocido (fuera del enum real, misma fuente que `callbackCoverage.test.ts`), error fatal o cierre inesperado del WS; no cubre discrepancia P2 (necesitaría un navegador real pintando DOM, pendiente). Verificado en vivo: la primera corrida (8 partidas) encontró un bug real de la propia política del fuzzer — bucle de re-prompt de `GAME_SELECT` disparaba el anti-flood del Gateway (`code 1008`, límite 100 msg/s) y cerraba la conexión; el primer intento de fix (descartar respuestas en un gap fijo) rompió el mulligan [solo se pregunta una vez] y colgó las 8 partidas siguientes en turno 1; fix correcto: cola de salida a ritmo seguro (40 msg/s) sin descartar nada. Con el fix, 8/8 sin cierres espurios: 3 limpias, 4 timeout de presupuesto de prueba (no bug) y 1 stall real sin confirmar reproducibilidad (sin seed determinista de mazos aún para aislarlo). Pendiente: `--seed=` para reproducir hallazgos, hook P2 vía Playwright, lote grande (100+) sin contención de CPU compartida. |
| 2026-09-16 | P5 auditoría de fidelidad JsonUtil (Mage.Proxy) — bug real de serialización corregido | Investigando cómo abordar docs/history/plan4.md §P5 (harness diferencial con el cliente de escritorio Swing) se encontró que `ProxyClient.java` no tiene switch por tipo de callback (`onCallback` genérico por reflexión) y que el riesgo que P5 perseguía —"Nexus se salta un prompt"— ya lo cubre `web/src/state/callbackCoverage.test.ts` contra el enum real del fork. El hueco no cubierto era la fidelidad de *contenido* de `JsonUtil.toJson()`. Auditoría más barata en su lugar (sin tocar `../xmage-fork`): `mage.view.ExileView`/`MutateView` extienden `CardsView`→`LinkedHashMap`, y `writeValue()` tomaba la rama `instanceof Map` sin llegar nunca a la reflexión de campos propios — `name`/`id` se descartaban en silencio. Confirmado en código real: `web/src/board/boardShared.ts`/`crossZone.ts` leían `exile.cards`/`exile.name` sobre `GameView.exiles` (clave inexistente en el JSON real, búsqueda de cartas en zonas de exilio nombradas siempre en blanco) y `system/fidelity.ts` tenía el mismo punto ciego; `BoardZone.tsx`/`deckTracker.ts` leían `mutateView` como mapa plano y funcionaban solo por coincidencia. Fix: `JsonUtil.java` anida las entradas del mapa bajo `cards` cuando la clase declara campos propios (mismo patrón que las POJO `RevealedView`/`LookedAtView`, que nunca tuvieron el bug); `contract.schema.json` corregido + `gen-types`/`gen-zod`; los 2 consumidores actualizados a `.cards`; migración estructural de los 101 frames de P4 (`name`/`id` irrecuperables en frames viejos, quedan opcionales; `cards` se recupera en todos). Bonus: `NaN`/`Infinity` en double/float ya no producían JSON inválido para el navegador, ahora `null`. `Mage.Proxy` tests 100% verde (3 tests nuevos en `JsonUtilTest`), `tsc --noEmit` limpio, `vitest run` 1692/1692 (188 archivos) incluida la validación zod estricta de las 101 fixtures migradas. Pendiente si se quiere el P5 completo: cliente Java de referencia jugador (SessionImpl directo) para comparar comportamiento por defecto Swing-vs-Nexus — no construido, este hallazgo hizo prioritario el fix. Re-grabar con nombre real solo `foretell`/`suspend`/`plot` (zonas de exilio nombradas) queda como follow-up opcional. |
| 2026-09-16 | P4 14ª tanda (3 grabadores, 3 frames) | **manifest 100→103**. `emblem` (Gideon of the Trials, habilidad 3 del `GAME_CHOOSE_ABILITY` ["3. 0: You get an emblem with …"]: **los emblemas de planeswalker NO viajan por `myHelperEmblems`** — ese campo solo lleva helper emblems del motor [Radiation/storm/day-night] — sino por **`players[].commandList`** como `EmblemView` `{id,name:"Emblem Gideon",expansionSetCode,rules,…}` **sin `mageObjectType`**; el web ya lo pinta en `.emblem-slot` por el prefijo "emblem "; el UUID de la habilidad no activa nada: hay que clicar el permanente), `commander-zone` (**hallazgo de harness integrado**: `rec-lib` gana el passthrough `driver.tableGameType` y el driver pierde su parche de `WebSocket.prototype.send`; la mesa debe ser variant **"Commander Two Player Duel"** — con "Two Player Duel" el motor no instancia `GameCommanderImpl` y no hay zona de mando ni impuesto; deckType legal es **"Variant Magic - Commander"**, `'Constructed - Commander'` no existe; el SIM también necesita comandante legal o su `joinTable` falla; el comandante vive en `players[].commandList` con `mageObjectType:"COMMANDER"` y **el impuesto viaja en su `rules`** ["<b>Commander</b> 2 times played from the command zone." — el `castCount` que lee `parseCommandList` no existe en el wire]; 1er cast "Pay {2}{R}{R}", 2º "Pay {2}{R}{R}{2}" [+{2} literal]; el retorno al morir es un **`GAME_ASK`** "Move … to the command zone or leave it in current zone (GRAVEYARD)?" con botones `UI.left/right.btn.text`, `true`=comando; vida 40) y `karn-restart` (Karn cheateado entra con 6 contadores; +4 [TARGET "Select a player"] → 10 → 14 y −14: **el reinicio llega SOLO como `GAME_UPDATE`** — mismo `gameId`, **sin `GAME_INIT` nuevo ni `GAME_OVER`** [`KarnLiberatedEffect` → `clearOnGameRestart` + `Player.init/useDeck` + `game.start(null)`] y reaparece el `GAME_ASK` de mulligan; frame: turno 1 con el dueño de Karn activo, manos 7/7, bibliotecas 53/93, mesa/cementerio/pila vacíos y Karn barajado; **ojo**: las exiliadas con Karn no se recolocan de forma fiable entre runs [run con `exiles` vacío vs 1 Forest del SIM] → la UI no debe fiarse). §3.3 commander desde zona de mando→R, §3.10 reiniciar partida→R y emblemas→R (fila "Monarch, initiative/dungeon, the ring, emblemas" **completa**). unit de fixtures 104/104 + typecheck + replay de los 3 frames ✅. |
| 2026-09-16 | P4 13ª tanda (3 grabadores + 1 fix, 6 frames) | **manifest 94→100**. `dungeon` (Find the Path: **la mazmorra SÍ es estructurada** — `GameImpl.addDungeon → state.addCommandObject` se proyecta en `players[].commandList` como **DungeonView** `{id,name,imageFileName,rules,…}`; **no hay `currentRoom` ni grafo**: la sala actual es `rules[0]` = `"<i>(Currently in Goblin Lair)</i>"` y las salidas llevan prefijos literales `ICON_DUNGEON_ROOM_CURRENT/_NEXT`; `selectDungeon` llega como `GAME_CHOOSE_CHOICE` string mode ["Choose a dungeon to venture into"] y la sala como `GAME_ASK` ["Choose which room to go to in" → true=Goblin Lair]; el web hacía tracking propio por prompt+chat y ya puede leer `rules[0]`), `the-ring` (Call of the Ring: **ni nivel ni portador tienen campo propio** — el nivel es `EmblemView "The Ring".rules.length` en `commandList` [clamp 1..4] y el portador es el permanente con `cardIcons[] {cardIconType:"RINGBEARER",hint:"Ring-bearer"}` [`PermanentView` no expone `isRingBearer/ringBearer`]; con una sola criatura `PlayerImpl.chooseRingBearer` no pregunta; **bug corregido** en `MechanicsTray.findRingBearer` [buscaba campos inexistentes] + tests con el propio frame), `concede-mulligan` (**el server SÍ honra CONCEDE en el mulligan**: `GameImpl.setConcedingPlayer` usa `state.getChoosingPlayerId()` [fijado por `Mulligan.executeMulliganPhase`] y `signalPlayerConcede→checkConcede→leave→end` dejan `players[].hasLeft:true` en ~3 ms; el problema es de UI: el `MulliganDialog` no ofrece Conceder y su backdrop tapa el menú ⋯; **hallazgo de contrato**: en ese fin de partida el GameView trae `phase`/`step`/`activePlayerId` a `null` [también `canPlayObjects`] — `contract.schema.json` ampliado a `["string","null"]` + `gen-types`/`gen-zod` regenerados/validados y 5 puntos del web tolerantes a null [`sceneBridge`, `GameScreen`, `gameUtils`, slice `game`]; el smoke de replay admite frames de fin sin cartas), `initiative` (White Plume Adventurer: **`players[].initiative:boolean`** [PlayerView.java:60/155] con `designationNames` vacío; de paso crea `Undercity` en `commandList` y su venture pide `GAME_TARGET "Select a basic land card"`), `radiation` (Mariposa Military Base: el contador se llama **`rad`**, no "radiation" [`CounterType.RAD`]; su trigger es **al comienzo de la fase principal precombate de cada jugador**, no un upkeep, y no hay tirada de dado [`RadiationEmblem`]; en el frame muele Grizzly+Forest → vida 19 y rad 2→1; el ETB "may" es un `GAME_ASK`) y `starting-player` (opt-out nuevo `skipStartingPlayerChoice:false` en `rec-lib`: el sorteo lo gana uno y **solo el ganador recibe** `GAME_TARGET "Select a starting player"` con `targets=[SIM,yo]`; si lo gana el SIM su `ComputerPlayer.makeChoice` se elige a sí mismo sin prompt; `startResponseIdleTimeout` hasta responder). §3.1 elegir quién empieza→R y conceder durante mulligan→R, §3.10 initiative/dungeon/the ring→R [emblemas siguen S]. unit de fixtures 101/101 + typecheck + replay de los 6 frames ✅. |
| 2026-09-16 | P4 12ª tanda (3 grabadores, 6 frames) | **manifest 88→94**. `plot` (Rictus Robber {3}{B} con Plot {2}{B}: la activación es una **SpecialAction directa sin chooser** — el hardcast no era pagable y el plot sí; el pago son `GAME_PLAY_MANA` normales `Pay {2}{B}` → `{2}` → `{1}` con Swamps giradas y la carta queda en la zona "Plots of `<jugador>`"; **ojo**: en el exilio llega con `faceDown:false` y `zone:null` [la ocultación al rival es implícita]; el lanzamiento gratis al turno siguiente pide `GAME_CHOOSE_ABILITY` con la opción única "Cast … using Plot" [el picker NO se suprime con una sola opción] y resuelve por `{0}` sin girar tierras), `floating-mana` (Forest activado por clic → `manaPool {green:1}`; al pasar prioridad con pool > 0, `HumanPlayer.passWithManaPoolCheck` emite el `GAME_ASK` `<font color='#D2691E'>You still have mana in your mana pool and it will be lost. Pass anyway?</font>` que requiere `confirmEmptyManaPool=true` [default del proxy y reenviado por el web en `GAME_INIT`]; el frame es el estado inmediatamente posterior [el ASK no es capturable por `captureWhen`]), `attack-battle` (Invasion of Gobakhan {1}{W} Battle—Siege: **la batalla debe cheatearse al campo PROPIO** — `Combat.setDefenders` exige `ProtectedByOpponentPredicate`, y con la batalla propia el protector es el SIM; con la del SIM el humano nunca puede atacarla; `defense:"3"`→`"1"` con `counters [{name:"defense"}]`, atacante en `combat[].attackers` y `combat[].defenderId`/`defenderName` apuntando a la batalla; mismo `GAME_TARGET` "Select a player, planeswalker, or battle to attack" que el PW), `always-attack` (Rubblebelt Recluse 6/5 `AttacksEachCombatStaticAbility` del SIM: ataca en `DECLARE_ATTACKERS` [el SIM manda "All attack"], `tapped:true` en `combat[].attackers`; la restricción llega por la misma vía que goad — `HintUtils.HINT_ICON_REQUIRE` → `cardIcons[] OTHER_HAS_RESTRICTIONS` con `hint:"Must attack (Rubblebelt Recluse [b22])"` y `text` vacío — y persiste fuera del combate), `day-night` (Reckless Stormseeker daybound cheateado en T1 sin lanzar hechizos: en el destapado del SIM `UntapStep.handleDayNight` hace NOCHE y transforma el DFC en Storm-Charged Slasher; **hallazgo**: el flag day/night **no existe como campo de `GameView`** [`GameState.hasDayNight` no se serializa] y `designationNames` tampoco lo lleva [no hay `DesignationType`]: la evidencia estructurada es el hint en `rules` del DFC ["It's currently night, active player has cast 0 spells this turn. It will not become day next turn."] y `setDaytime` informa "It has become day/night" solo por el chat de partida; `myHelperEmblems` llegó vacío; la heurística de `MechanicsTray`/`PlayerInfoBar` que busca 'day'/'night' en `designationNames` nunca dispara en 1.4.61) y `cloak` (Vannifar, Evolved Enigma: el trigger de inicio de combate **modal llega como `GAME_CHOOSE_ABILITY`** — no `GAME_CHOOSE_CHOICE` — con "1. Cloak a card from your hand."/"2. Put a +1/+1 counter…"/"Cancel" y se responde `sendPlayerUUID`; la carta de la mano es un `GAME_TARGET "Select a card"` con `targetZone:"HAND"`; el resultado es un 2/2 boca abajo con **`cloaked:true`** y nombre "Cloak: Grizzly Bears", ward {2} solo en `rules`). §3.2 maná flotante→R, §3.3 plot→R, §3.6 atacar batalla→R y "ataca cada combate"→R, §3.8 boca abajo→R (morph/manifest/disguise/cloak completos), §3.9 DFC/day-night→R. unit de fixtures 94/94 (6 asserts nuevos) + replay de los 6 frames ✅. |
| 2026-09-16 | P4 11ª tanda (3 grabadores, 6 frames) | **manifest 82→88**. `stun-oil` (Incubation Sac con `counters` `{name:"oil",count:3}` + Grizzly del SIM girado con `{name:"stun",count:1}` tras Rime Chill; **hallazgos**: en 1.4.61 `Rime Chill` es un vivid `{6}{U}` "hasta dos criaturas" — no "tap target" — y vivid solo cuenta 1 color porque las básicas solo llevan `frameColor` → prompt real "Pay {5}{U}"; el TARGET "hasta dos" se cierra con un solo UUID), `colorless-c` (Spatial Contortion `{1}{C}` con mazo todo Wastes: el pago trocea `GAME_PLAY_MANA "Pay {1}{C}"` → `"Pay {1}"`, a veces con re-emisión no determinista del `{1}{C}`; el `{C}` lo paga una Wastes; objetivo AUTO-elegido [Craw Wurm +3/-3 → 9/1, P/T como strings]), `coin-dice` (Tavern Swindler `{T}, Pay 3 life`: flip con `GAME_ASK "Heads or tails?"` → vida 17/23; Recruitment Drive d20 → Knight Tokens ×2; **hallazgo de contrato**: dado/moneda NO son eventos estructurados — no hay campos en `GameView`; el resultado viaja SOLO como texto al chat de partida [los `GAME_INFORM` no se usan para esto] y el web lo pinta por `CHATMESSAGE` → `addLog`; el frame solo asertará el EFECTO), `fail-to-find` (**el "fail to find" SÍ existe** — `TargetCardInLibrary` hace `setRequired(!filter.hasPredicates())` [regla 701.15b]: una búsqueda de básica nace `required=false` y su `GAME_TARGET` llega `flag:false` con `message:"Select a basic land card"` → `sendPlayerBoolean(false)` rompe el bucle y la biblioteca queda intacta [53]; `Demonic Tutor` es `FILTER_CARD` sin predicados → `required=true` y por eso declinar re-preguntaba; el Swing manda el mismo `false`; **para el web**: `flag` es el `required` y hoy no se usa para ofrecer el "fail to find"), `disguise` (Unyielding Gatekeeper `{1}{W}` + Disguise `{1}{W}` [única con disguise no-híbrido]: clic → `GAME_CHOOSE_ABILITY` "Cast … using Disguise"; pago `{3}` → 2/2 con `faceDown:true` y **`disguised:true`**, nombre "Disguise: …", `manaValue:0`; **ward {2} solo en `rules`**, no en `cardIcons` [medido]; el giro boca arriba por `TurnFaceUpAbility` verificado en exploratorio) y `win-effect` (Approach of the Second Sun ×2: 1ª +7 vidas [27] y 7ª desde arriba, 2ª gana — `GAME_OVER "… is the winner"`; **fix de `rec-lib` 2026-09-16**: el estado de victoria solo llega en `GAME_UPDATE` PLANO [GameView aplanado en `data`, sin clave `gameView`] y en `GAME_OVER`, así que ahora se captura también ahí — sirve para cualquier `won()`/`lostForced()`). §3.2 `{C}`→R, §3.7 dados/moneda→R, §3.8 tutor "fallar búsqueda"→R y disguise→R parcial [cloak, Vannifar viable con la API actual], §3.9 stun/oil→R, §3.10 ganar/perder→R. unit de fixtures 88/88 (6 asserts nuevos) + typecheck + replay de los 6 frames ✅. |
| 2026-09-16 | P4 10ª tanda (3 grabadores, 6 frames) | **manifest 76→82**. `hexproof` (Slippery Bogle rival: con hexproof el permanente **ni es objetivo legal** — el `GAME_TARGET "Select any target"` del Bolt trae solo las dos caras y el Bogle queda `damage:0`; contraste con ward, donde sí se lanza), `choice-color` (**en 1.4.61 no existen `GAME_CHOOSE_COLOR`/`GAME_CHOOSE_NUMBER`** — verificado en `ClientCallbackMethod`; el web los maneja como legado/fake: el color llega como `GAME_CHOOSE_CHOICE` string mode con `ChoiceColor` → `sendPlayerString "Red"`, y el número como `GAME_GET_AMOUNT` [min 0, max INT_MAX] → `sendPlayerInteger 3`; resultados en `rules` "Chosen color:/Number:"), `goad` (Disrupt Decorum: el estado llega por `rules` con prefijo `ICON_REQUIRE` + `cardIcons[] OTHER_HAS_RESTRICTIONS` con el texto en `hint`; no hay campo `goadingPlayers`), `attack-pw` (atacar a un Tibalt del SIM: `GAME_TARGET "Select a player, planeswalker, or battle to attack"` con `targets=[playerId, UUID del PW]` → UUID + confirmar con booleano [NO `special`/"All attack" con 2+ defensores: abre un segundo target]; lealtad 5→3 en `permanent.loyalty` y `combat[].defenderId`), `switcheroo` (con exactamente 2 criaturas legales se AUTO-elige sin `GAME_TARGET`; intercambio de control permanente visible por zona/`controllerName`) y `monarch` (Palace Sentinels → `players[].monarch:true`; **ojo**: `designationNames` NO refleja Monarch [designación global], usar el booleano). §3.4 hexproof→R (fila ward/hexproof cerrada), §3.6 goad→R parcial [falta "ataca cada combate"] y atacar PW→R parcial [falta batalla], §3.7 color/número→R, §3.10 switcheroo→R y monarch→R parcial. Unit 83/83 + replay por frame ✅. |
| 2026-09-16 | P4 8ª+9ª tanda (4 grabadores en paralelo, 12 frames) | **manifest 64→76**. 8ª: `ponder` (el orden repite el patrón de Brainstorm: TARGETs secuenciales con "…(last one chosen will be topmost)"; en 1.4.61 NO existe `GAME_CHOOSE_CARDS_ORDER` [verificado en `ClientCallbackMethod`]; ASK "Shuffle your library?" → NO; orden probado con doble robo), `fof` (rec-lib gana el hook `GAME_CHOOSE_PILE` [booleano, true=pile1]; **hallazgo**: el SIM **no reparte** — su GAME_TARGET de separación llega con `data.targets=null` [los ids van en `options.possibleTargets`, sobrecarga `Cards` de `fireSelectTargetEvent`] y `SimPlayer.onTarget` hace `cancel()` → pile1 vacía y las 5 a pile2; mejora pendiente en SimPlayer), `manifest-mechanic` (outFile renombrado para no pisar el índice `manifest.json`: Soul Summons manifiesta un Elite Vanguard; girar boca arriba es CLIC directo → `GAME_PLAY_MANA "Pay {W} <face down creature>"`, habilidad en `canPlayObjects.other`; `TurnFaceUpAbility` NO está en acciones especiales) y `mass-tokens` (Krenko + 50 Raging Goblin: los nombres DUPLICADOS en `cheatSetup.battlefield` funcionan; X=51 fichas `isToken`, 811 KB — el frame más pesado del registro; replay OK). 9ª: `class-level` (Wizard Class a nivel 3: el nivel NO está en `counters`/`classLevel` sino como hint en `rules` "Class level: 3"; clic directo sin picker), `poison` (Glistener Elf infect: `PlayerView.counters` `{name:"poison",count:1}` con la vida aún 20 — infect da veneno, no daño), `twincast` (objetivo de la copia auto-resuelto; al crearla llega `GAME_ASK "Change this 1 of 1 target: <sim>?"` [`StackObjectImpl.chooseNewTarget`]; resuelven copia y original, SIM 14), `ward-no-pay` (**corrección**: Sythis no tiene Ward en el fork y el viejo `ward.json` es un Bolt normal matándolo; el trigger pregunta `"Pay {1}?"` [chooseUse, sin la palabra "ward"]; false ⇒ contrarrestado, Thornfist `damage:0`), `improvise` (botón special → `GAME_CHOOSE_ABILITY` `ImproviseSpecialAction` → `GAME_TARGET "Select artifact to tap as Improvise's pay (selected N of 3, min 1)"`; tras improvise el motor fija `ActivationManaAbilityStep.AFTER` y bloquea más maná → las Islas se pagan antes), `discover` (Carnosaur: ASK "Cast spell without paying its mana cost (Giant Growth [9ab])?" → sí; objetivo AUTO-elegido; exiliadas al fondo), `trample-deathtouch` (**retry resuelto**: Lure vence a la IA que se negaba a bloquear deathtouch; `GAME_GET_MULTI_AMOUNT` con un único ítem `max:4, defaultValue:1` — el default ya baja el letal a 1; SIM 17 y lifelink a 24; ajuste: el 2/2 del plan moría, se usó un 2/3) y `cant-block` (Tormented Soul no elegible como bloqueadora por regla: ataque sin bloqueo, icono de restricción, 20→18). Fix de e2e: `hasTrampleDeathtouch` entra en la excepción de `.card-attachment-group`. Unit 77/77 + replay por frame ✅. §3.2 improvise→R, §3.3 discover→R, §3.4 copiar hechizo→R y ward-no-pay→R parcial [hexproof sigue], §3.6 trample+deathtouch→R, "no puede bloquear"→R [goad sigue], §3.7 pilas→R parcial [Sim no reparte], §3.8 Ponder→R y manifest+giro→R parcial [disguise/cloak], §3.9 clases→R parcial y fichas masivas→R, §3.10 veneno→R parcial. |
| 2026-09-16 | P4 7ª tanda en paralelo: 6 drivers (emerge, reduce-cost, double-strike, phasing, brainstorm-order, energy) | **manifest 58→64**. `emerge` (Wretched Gryff por Emerge {5}{U}: **sin chooser** — `EmergeAbility` es BASE_ALTERNATE y al ser la única habilidad activable el clic lanza directo; el sacrificio del Grizzly se auto-elige con un único legal y reduce {5}{U}→{3}{U} por su MV), `reduce-cost` (Lightning Strike {1}{R} pagado con {R} por Goblin Electromancer: el propio `GAME_PLAY_MANA` pide ya el coste reducido; el objetivo del Strike se responde con el playerId del rival presente en `data.targets`), `double-strike` (Fencing Ace a la cara: en `FIRST_COMBAT_DAMAGE` el rival ya está a 19 y el atacante `damage:0`; el segundo golpe llega en el paso regular), `phasing` (Reality Ripple: el permanente **no cambia de zona** — sigue en `battlefield` con `phasedIn:false` y es `BoardZone` quien lo oculta), `brainstorm-order` (el orden **no** usa `GAME_CHOOSE_CARDS_ORDER`: son DOS `GAME_TARGET "Select a card"` secuenciales con los ids de la mano en `data.targets`; el orden elegido se prueba encadenando el robo de Opt) y `energy` (Attune with Aether → `PlayerView.counters` `{name:"energy",count:2}`; la búsqueda llega con `options.possibleTargets` + `cardsView1` de la biblioteca). Asserts `hasEmerge`/`hasReduceCost`/`hasDoubleStrike`/`hasPhasing`/`hasBrainstorm`/`hasEnergy`; unit de fixtures 65/65 + replay por frame ✅. §3.2 costes alternativos→R (evoke/overload/dash/emerge) y reducción→R; §3.6 first/double strike→R; §3.8 mirar N y ordenar→R parcial; §3.9 phasing→R; §3.10 energía→R parcial. |
| 2026-09-16 | P4 6ª tanda en paralelo: 6 drivers (escape, foretell, ninjutsu, must-block, pithing-needle, cavern) | **manifest 52→58** con 3 grabadores en paralelo: `escape` (Phoenix of Ash desde el cementerio; el exilio del coste se **auto-elige** con exactamente el mínimo legal 3→3, `+1/+1` `EscapesWithAbility(1)` y cementerio vacío), `foretell` (Saw It Coming: {2} → exiliada boca abajo con nombre real para el dueño y `faceDown:true`; al turno siguiente se lanza desde el exilio por {1}{U} — clicarla dispara `GAME_ASK "Look at …"` → false; `canPlayObjects.other` expone "Foretell {1}{U}"; el mismo turno está prohibido por `foretoldTurn`), `ninjutsu` (el Ninja entra girado y atacando en `DECLARE_BLOCKERS` devolviendo el atacante a la mano; la habilidad va en `canPlayObjects.other` y con un único atacante legal el motor auto-elige), `must-block` (Lure lanzado **de verdad** — el cheatSetup no puede adjuntar auras — y el bloqueador del SIM forzado por `MustBeBlockedByAllAttachedEffect` pese a ser mala decisión; el bloqueo se monta en el mismo turno del ataque porque el SIM gira al atacar), `pithing-needle` (el "as it enters" llega como `GAME_CHOOSE_CHOICE` con la lista COMPLETA en `choice.choices` — ~25k nombres, `keyChoices` vacío, `searchEnabled:true` — y se responde con el nombre EXACTO; `rules` fija "Chosen name: Lightning Bolt") y `cavern` (`GAME_CHOOSE_CHOICE` con `ChoiceCreatureType`: 396 `keyChoices` con relevance-sort "Elf (me)" y respuesta por CLAVE; `rules` "Chosen type: Elf"). **Hallazgo de harnés/motor**: `cheatSetup` directo al CAMPO de una carta con decisión "as it enters" revienta el WorkerThread del proxy (`Wrong code usage: game related code must run in GAME thread`) — se cheatea a la mano y se lanza. Fix del smoke: `must-block` (adjunto real) entra en la excepción de `.card-attachment-group`. Asserts `hasEscape`/`hasForetell`/`hasNinjutsu`/`hasMustBlock`/`hasPithingNeedle`/`hasCavern`; unit de fixtures 59/59 + replay por frame ✅. §3.3 escape→R y foretell→R parcial, §3.6 ninjutsu→R parcial y "debe bloquear"→R parcial, §3.7 nombrar carta→R y elegir tipo→R parcial. |
| 2026-09-16 | P4 5ª tanda en paralelo: 6 drivers (dash, suspend, vigilance, first-strike, surveil, jump-start) | Los drivers salen de `record.mjs` a módulos `scripts/drivers/*.mjs` (factory + `meta` con assert y nota) con **auto-carga en el registro** (`record.mjs`) y `scripts/record-sync.mjs` (sincroniza `manifest.json` desde el meta y avisa de asserts pendientes); 3 grabadores en paralelo graban 6 frames reales (**manifest 46→52**): `dash` (Lightning Berserker por dash {R}; **hallazgo**: el chooser es `GAME_CHOOSE_CHOICE` con keyChoices "Cast with Dash alternative cost…"/"Cast with no alternative cost…", no `GAME_CHOOSE_ABILITY`; entra con prisa: `rules` Haste + `summoningSickness:false`), `suspend` (Rift Bolt exiliado con `time:1`; la `SpecialAction` de suspend **no** aparece en `GameView.special` — se clica la carta en mano; `PlayerView.exile` agrega la zona nombrada "Suspended cards"), `vigilance` (Vanguard of Brimaz ataca y el grupo lo registra `tapped:false`), `first-strike` (el Grizzly Bears bloqueador muere en `FIRST_COMBAT_DAMAGE` con el atacante `damage:0`; **hallazgos**: los permanentes cheateados entran SIN mareo y el SIM ataca con todo, así que el bloqueo se monta en el mismo turno), `surveil` (Consider: TARGET "PUT into your GRAVEYARD (Surveil)", gemelo del scry con filtro distinto) y `jump-start` (Radical Idea; **hallazgo**: el descarte como coste llega como `GAME_TARGET "Select a card"` con los ids legales en `data.targets`, sin `possibleTargets`). Bug de cliente corregido: `gameSoundDispatcher` miraba `FIRST_STRIKE_DAMAGE` (step inexistente) → `FIRST_COMBAT_DAMAGE`. Asserts `hasDash`/`hasSuspend`/`hasVigilance`/`hasFirstStrike`/`hasSurveil`/`hasJumpStart`; unit de fixtures 52/52 + replay por frame ✅. §3.2 dash→R parcial, §3.3 suspend→R y jump-start→R parcial, §3.6 vigilance→R parcial y first-strike→R parcial, §3.8 surveil→R. |
| 2026-09-16 | U4-2 (login): contraseña deshabilitada en beta | El servidor público no comprueba contraseñas (`authenticationActivated=false`), así que el campo **Contraseña (opcional)** del LoginScreen y del SetupWizard se deshabilita cuando el destino es `beta.xmage.today` (`passwordDisabledOnBeta`: se limpia y `doConnect`/`buildConn` envían ''), con aviso `password_disabled_beta` ×9 locales. Reactivo al cambiar de preset (Local ↔ Oficial Beta). Verificado en navegador real con screenshot (beta → `[disabled]` + nota; Local → habilitado). Tests `SetupWizard.test` 9/9. |
| 2026-09-16 | U4-2 (crear mesa): contraseña deshabilitada en beta | El fix U4-2 solo cubría `JoinTableDialog`; el campo Contraseña del wizard **Crear Mesa** (pestaña Restricciones, `CreateTable/SecurityTab.tsx`) seguía activo contra `beta.xmage.today`. Ahora el hook `useCreateTableForm` calcula `passwordDisabledOnBeta` (conn.serverHost), limpia el valor y usa `passwordValue=''` en todos los payloads (createTable/createTournamentTable/joinTable/joinTournamentTable), el input y el toggle se deshabilitan y aparece el aviso (`create_password_disabled_beta` ×9 locales). Verificado en navegador real: con `conn.serverHost='beta.xmage.today'` el campo queda `[disabled]` + nota ámbar y al volver a localhost se rehabilita (reactivo). Unit 1607/1607 + typecheck ✅. |
| 2026-09-16 | P4 drivers: híbrido, any-color, planeswalker, saga y morph | 5 frames reales más (manifest 35→40, unit 41/41, replay e2e 39/39): `hybrid` (Kitchen Finks {1}{G/W}{G/W} con 3 Bosques — el híbrido se paga con verde, sin pregunta de color), `anycolor` (Birds of Paradise paga el {U} de Opt; **hallazgo de motor**: con un solo color pagable no hay `GAME_CHOOSE_CHOICE` de color — el servidor auto-resuelve), `planeswalker` (Teferi, Hero of Dominaria {3}{W}{U}: activar +1 robar, lealtad 4→5; **hallazgo del harnés**: enviar el UUID de la habilidad no activa nada, `HumanPlayer` resuelve el UUID con `game.getObject()` y una habilidad no es un objeto de juego → hay que clicar el permanente y responder `GAME_CHOOSE_ABILITY`; el helper `playAbility` de `rec-lib` pasa a hacer click), `saga` (History of Benalia: capítulo I → lore:1 + ficha Knight 2/2) y `morph` (Den Protector boca abajo por {3}: la habilidad va a `canPlayObjects.other` por `SpellAbilityType.BASE_ALTERNATE` y el picker la etiqueta "Cast … using Morph: Megamorph {1}{G}"). Asserts `hasHybrid`/`hasAnyColor`/`hasPlaneswalker`/`hasSaga`/`hasFaceDown`; §3.2 híbrido→R, §3.2 any-color→R parcial, §3.8 morph→R parcial, §3.9 planeswalker→R, §3.9 sagas→R parcial. |
| 2026-09-15 | P3 galería de estados v1 | Ruta dev `#/gallery` (`App.tsx` por hash + `dev/GalleryScreen` en import dinámico tras `import.meta.env.DEV`, así el build de producción no embebe los frames): **44 estados** = 33 frames reales de `fixtures/recorded` (leídos del `manifest.json` con `import.meta.glob`), 10 prompts montados sobre `gang-block.json` (target obligatorio/opcional con objetivo criatura real, mana con tierras jugables, combate atacantes/bloqueadores, ask, mulligan, grid de biblioteca, orden de triggers, votación) y login; el selector hace `setState` del estado del store y lo restaura al salir. Fix de apilado: los overlays de la app llegan a `z-index: 999999` (backdrop de diálogo 501 en partida) y el sidebar se quedaba debajo → `z-index: 1000000`. `e2e/gallery.spec.ts`: recorrido de las 44 entradas con 0 `pageerror` (sin stack, 14,5 s) + regresión visual opt-in `E2E_VISUAL=1` (12 estados, red externa bloqueada → placeholders deterministas, `animations: disabled`, baselines por plataforma generados con `--update-snapshots`); unit del builder (ids únicos, grupos, objetivo/pago reales del frame base). Verificado en navegador real (snapshot+screenshot: mutate, mana, target, login). Pendiente: estados de lobby/editor/draft/torneo y matriz §4, 3 resoluciones y WebKit, baselines Linux para CI. Unit **1600/1600** + typecheck ✅. |
| 2026-09-15 | P2 fidelidad de render v2 | Cierra el "pendiente v2" del comprobador: `CardSlot` expone `data-tapped`/`data-pt`/`data-damage`/`data-counters`, `PlayerInfoBar` `data-life`, `FeedbackDialog` un marcador `data-prompt-method` y `collectPainted` la zona `[data-player-id]` de cada carta; `checkFidelity(game, painted, pendingPrompt)` compara P/T (badge presente y fiel), girado (exime al atacante que la UI gira por convención), contadores (firma exacta en ambos sentidos), daño, vida por jugador, lado del controlador y prompt pendiente sin diálogo pintado. En la pila de mutate la última ocurrencia del DOM es la canónica (las partes se pintan antes que el permanente fusionado). 10 casos unit nuevos + regresión `e2e/fidelity.spec.ts` parametrizada a 5 frames reales (mutate/combat/xcosts/aura/trigger-order) con 0 discrepancias; unit **1596/1596** + typecheck + build ✅. Pendiente v3: adjuntos y tamaños de mano/biblioteca/cementerio/exilio. |
| 2026-09-15 | MCP: exposición de `cheatSetup` (P1/P4) | Cierra el último pendiente anotado de P4: nueva tool `mage_cheat_setup` (`session?`/`playerId?`/`hand`/`battlefield`/`library`/`graveyard`/`exile`, `library` apila encima) → acción `cheatSetup` del proxy con default del jugador controlado y error claro sin zonas; describe el gate testMode (beta `ok:false`) y la regla anti-congelación (turno propio tras ≥1 acción). Test hermético con FakeServer (`mcp/test/cheatSetup.test.ts`: zonas+orden preservados, playerId rival, error sin zonas, propagación de `ok:false`) + lista de tools de `server.test.ts`; suite `mcp` **40 pass/1 skip** + typecheck. **Verificado en vivo** contra el stack local (partida HUMAN+SIM, 60 Islas): tierra T1 + `ok:true` → 2×Counterspell en mano y 3 Islas en campo, la partida siguió (auto-pass) y la mesa se limpió (lobby 0). Nota: el proceso MCP vivo de una sesión abierta no tiene la tool hasta reiniciar opencode (Node sin hot-reload). |
| 2026-09-15 | P4 drivers convoke + flashback (en paralelo) | Recorders en paralelo OK (tras blindar USER ≤14c: el servidor rechaza nombres largos). `convoke` (Chord X=1: taps como PLAY_MANA — mareo no estorba; X=0 sin objetivo legal) y `flashback` (Looting→exilio; descarte doble con memoria anti-bucle) + asserts y replay (14/14). §3.2 convoke→R, §3.3 flashback→R. Unit 1566/1566 + typecheck OK. |
| 2026-09-15 | P4 drivers response + triggers | `response` (Growth vs Sting propio, pila LIFO al mismo Mystic; el SIM retiene Bolt con criaturas fuera), `trigger-order` (2 Wardens+Mystic→22 SIN diálogo: auto-orden) y `may-trigger` (Solemn may→SÍ + búsqueda vía TARGET+possibleTargets) + asserts y replay (12/12). Harnés: `onTarget(ctx,q,data)` y fallback onAsk→default (el mulligan no arrancaba). §3.5 triple→R. Unit 1564/1564 + typecheck OK. |
| 2026-09-15 | P4 driver xcosts (Ballista X=2) | Driver `xcosts` (Ballista X=2 vía `onTargetAmount`, 2/2 con 2 +1/+1, coste {X}{X}) + assert `hasXCostCounters` en unit y replay (9/9). Hallazgo: el max del prompt es INT_MAX (sin acotar). §3.2 X/XX→R con nota. Unit 1561/1561 + typecheck ✅. |
| 2026-09-15 | P4 driver modal (Boros Charm) | Driver `modal` (Charm en modo daño vs rival, pago {R}{W} por color) grabado en vivo + assert `hasModalOnStack` en unit y replay (8/8). Hallazgo: los modos del modal llegan como GAME_CHOOSE_ABILITY, no CHOICE → elección por texto (`onChooseAbility`). §3.7 modal→R. Unit 1560/1560 + typecheck ✅. |
| 2026-09-15 | P4 drivers aura + tokens | Drivers `aura` (Rancor→Mystic 3/1, pago solo-Bosques por bucle del pago por defecto con Mystic mareado) y `tokens` (Fodder→2 Goblin isToken) grabados en vivo + asserts `hasAttachedAura`/`hasTokens` en unit y replay e2e (7/7). §3.4 objetivo-en-pila→R. Unit + typecheck pendientes de cierre. |
| 2026-09-15 | P4 primer driver con cheatSetup (counterspell) | `ctx.cheatSetup` + hook `onPlayMana` en `rec-lib.mjs`; driver `counterspell` graba `counterspell.json` (CS vs Bolt con objetivos cruzados, UU pagado) + assert `hasCounterOnStack` en unit y replay e2e (5/5). Hallazgo bisecado: cheat en la 1ª prioridad de T1 congela el loop → regla "tras ≥1 acción" (docs). Unit 1557/1557 + typecheck ✅. |
| 2026-09-15 | P1 núcleo (cheatSetup) | Parche aditivo fork `nexus` (7 ficheros, gate `testMode`, resultado síncrono) + acción WS `cheatSetup` en proxy. Misma versión, beta intacta (fail-soft). Verificado en vivo vs stack local: setup OK + partida sigue; error limpio con carta desconocida. Fork sin commitear; pendiente P4 (drivers por mecánica). Java proxy ✅. |
