# PLAN-4 — verificar que la interfaz es correcta, usable y fiel al juego (2026-09-14)

> ESTADO AL CREAR: rama `master`, último commit `7cafe4ab079`. Resync del draft
> **sin commitear** (11 ficheros) + 3 PNG sueltos en la raíz. Documento **solo de
> planificación**: no se ha tocado código.
>
> **Modelo de distribución** (decisión del usuario): cada jugador se monta el stack
> por su cuenta o descarga el launcher de escritorio (Tauri: servidor + proxy + JRE).
> No hay un servicio público alojado. Por eso este plan se centra en **qué falta por
> probar de la interfaz, la UX y las mecánicas, y cómo probarlo**. La seguridad y la
> escala pasan a un anexo (§10).

---

## 0. Diagnóstico: qué significa hoy "✅ testeado"

| Capa | Qué demuestra de verdad | Qué **no** demuestra |
|---|---|---|
| ~1.516 tests unitarios | La lógica pura y los componentes hacen lo que su autor esperaba | Que el servidor real mande esa forma de datos |
| ~150 tests e2e en ~70 specs | La UI reacciona bien a escenarios **escritos a mano** (`fixtures/scenarios/*.ts`, motor `humanGame.ts`) | Que XMage real produzca esa secuencia de prompts y datos |
| e2e en modo real | Solo ~4 specs corren contra XMage (`full-flow`, `setup-wizard`, `multi-user`, `mutate.real`); **58 ficheros son solo simulados** (`fake-mode.ts`) | Casi ninguna mecánica en real |
| Frames grabados | 5 estados reales (`mutate`, `creature`, `combat`, `sealed-pool`, `tournament-end`) | El resto del catálogo de mecánicas |
| Guardas de contrato | Todo callback y todo campo que el servidor puede emitir está **modelado** | Que se **pinte** bien o que el flujo sea usable |
| QA en vivo (MCP + navegador) | Sesiones sueltas muy valiosas (gang-block, mutate, draft 2×HUMAN, Bo3…) | Repetible: no queda como test |
| Capturas (`web/e2e/shots/`, 360 PNG) | Alguien las miró | Regresión visual: **0 usos de `toHaveScreenshot`** |
| Auditoría UX | C.13 revisó lobby y editor en estático; C.14 teclado | La partida, draft y torneo no tienen auditoría UX; no hay pruebas con jugadores |

### Hallazgos concretos

1. **La cobertura de mecánicas es sobre todo "contra nuestra simulación"**. Si un escenario simulado está mal escrito, el test pasa y la partida real falla. Justo así nació el bug del draft de hoy: la UI ignoraba la respuesta real de `sendCardPick`.
2. **Sin regresión visual**: un cambio de CSS puede romper el tablero sin que falle ningún test. Varias capturas `-FAILED.png` se revisaron a mano.
3. **Una sola resolución** (1600×900, `web/playwright.config.ts`) y un solo navegador (Chromium). En escritorio, Tauri usa WebView2 en Windows y WKWebView en macOS, así que **Safari/WebKit es un motor real de producción**, no opcional.
4. **El mapa de paridad no es un oráculo fiable**: `COMPONENT_PARITY.md` marca G12-3 (menú contextual nunca renderizado) y G12-4 (`phasedIn` sin leer) como gaps, pero el código ya los tiene (`GameScreen.tsx:376` renderiza `PlayerContextMenu`; `BoardZone.tsx:230` filtra `phasedIn`). Hay que re-verificar el mapa antes de usarlo.
5. **Gaps funcionales conocidos** que afectan a la evaluación: permisos de mano y Mindslaver (G12-1 ❌); visores looked-at/companion/sideboard (G12-2 ⚠️); `MAX_BOARD_PLAYERS = 4` (`boardShared.ts:6`) mientras el servidor admite FFA de 3 a 10; `END_GAME_INFO` no llega a espectadores; replay aplazado; U8 generador de mazos ➖ (fuera de alcance 2026-09-14).

---

## 1. Tres oráculos para decidir qué es "correcto"

| Oráculo | Responde a | Cómo se usa |
|---|---|---|
| **Servidor XMage** (verdad de reglas y estado) | ¿Lo que se pinta coincide con el `GameView`? | Comprobador automático de fidelidad de render (§2 P2) en toda partida real |
| **Cliente de escritorio** (verdad de flujo) | ¿Aparecen los mismos prompts, con las mismas opciones y en el mismo orden? | Pruebas diferenciales: misma partida guionada en Swing y en Nexus (§2 P5) |
| **Jugadores** (verdad de UX) | ¿Se entiende qué pasa y qué hacer? | Heurística, prueba de 5 segundos, tareas moderadas, dogfooding (§5) |

---

## 2. Infraestructura de pruebas que falta (habilitadores)

| # | Pieza | Qué es | Por qué es necesaria | Tamaño |
|---|---|---|---|---|
| P1 | **Escenarios reales deterministas** | Colocar cartas concretas en zonas concretas de una partida real. Ya existe `skipInitShuffling` (mano top-first); falta exponer en el proxy el comando de trampas del modo test de XMage (verificar en el fork `MageServer.cheatMultiple` / `SystemUtil.addCardsForTesting`), solo con `testMode=true` | Sin esto, llegar a "tengo Counterspell y el rival lanza Bolt" en real depende de la suerte. Es la pieza que convierte el catálogo de mecánicas en tests reales | M |
| | ✅ hecho 2026-09-15 (núcleo): no existía comando remoto (solo `game.cheat` en-JVM) → parche aditivo en el fork `nexus` (7 ficheros: `cheatSetup` en `MageServer`/`Testable`/`SessionImpl` + `MageServerImpl` con gate `testMode` y resultado síncrono `executeWithResult` + `GameManager`/`GameController` con construcción vía `CardRepository`; misma versión, beta intacta) + acción WS `cheatSetup` en el proxy (fail-soft `ok:false` fuera de testMode o contra beta). Verificado en vivo: 2×Counterspell a mano + 2×Island al campo, partida sigue; carta desconocida → `ok:false` limpio. ✅ 2026-09-15: drivers de `record.mjs` (P4) y **exposición MCP** (`mage_cheat_setup`: test hermético `mcp/test/cheatSetup.test.ts` + verificado en vivo 2×Counterspell/2×Island con la partida siguiendo y mesa limpiada). | | | |
| P2 | **Comprobador de fidelidad de render** | Modo dev que, tras cada `GAME_UPDATE`, compara DOM/`window.__mageScene` con el `GameView`: cada permanente visible (salvo faseados), P/T, contadores, girado, adjuntos, controlador (lado correcto), vida, tamaño de mano/biblioteca/cementerio/exilio, pila en orden, fase y jugador activo, prompts pendientes visibles. Emite un informe de discrepancias | Detecta automáticamente "el servidor dice X y la UI pinta Y" en **cualquier** partida real, incluidas las del fuzzing y el self-play | M |
| | ✅ hecho 2026-09-14 v1 (`system/fidelity.ts`, hook en `eventHandler` tras fijar el `GameView`, flag `localStorage mage-web-fidelity=1`, informe al log `fidelidad` sin repetir firmas + `window.__mageFidelityRuns`): cubre permanentes presentes (salvo faseados), ids pintados desconocidos, orden de pila, turno en `game-status`, una sola pastilla de fase activa. Regresión: `e2e/fidelity.spec.ts` (mutate.json, 0 discrepancias). Unit 1556/1556 + typecheck ✅. ✅ v2 2026-09-15: P/T, contadores, girado y daño (data-attrs en `CardSlot`), vidas (`data-life` en `PlayerInfoBar`), lado del controlador (zona `[data-player-id]`) y prompt visible (marcador `data-prompt-method` en `FeedbackDialog`); `checkFidelity(game, painted, pendingPrompt)` con la última ocurrencia del DOM como canónica (la pila de mutate pinta las partes antes que el permanente fusionado). Regresión `e2e/fidelity.spec.ts` ×5 frames reales (mutate/combat/xcosts/aura/trigger-order, 0 discrepancias) + 10 casos unit nuevos. Unit 1596/1596 + typecheck + build ✅. ✅ v3 2026-09-16: **adjuntos** (grupos `data-attachment-host` con `.attachment-subcard[data-card-id]` en orden/conjunto; detecta también grupos pintados sin adjuntos server — `attachments-mismatch`) y **tamaños de zona** por jugador (`hand-mismatch`/`library-mismatch`/`graveyard-mismatch`/`exile-mismatch`; la mano propia prioriza el `hand-bar` y se omite el `.hand-zone` con Switch Hands; solo se comparan zonas realmente pintadas); data-attrs nuevos en `BoardZone`/`HandZone`/`HandBar`/`ResourceBar`; los subcards adjuntos se omiten de los chequeos de badges (no los pintan por diseño). Regresión `e2e/fidelity.spec.ts` ×7 frames reales (incluye `aura` con Rancor y `flashback`/`escape` con cementerio/exilio) con 0 discrepancias; unit 1692/1692 + typecheck + build ✅ | | | |
| P3 | **Galería de estados + regresión visual** | Ruta dev (`#/gallery`) que monta cada pantalla y diálogo en sus estados clave con fixtures (vacío, cargando, error, desbordado, idioma largo, CJK). Playwright `toHaveScreenshot` con animaciones desactivadas, en 1366×768, 1920×1080 y 2560×1440, en Chromium y WebKit | Cualquier regresión visual falla en CI; también es la superficie para la evaluación heurística | M |
| | ✅ slice 1 2026-09-15: ruta dev `#/gallery` (`App.tsx` la detecta por hash y carga `dev/GalleryScreen` en diferido; el build de producción no incluye los frames) con **44 estados**: 33 frames reales del registro (tablero completo, reutiliza `manifest.json` + `import.meta.glob`), 10 prompts montados sobre el frame `gang-block` (target obligatorio/opcional, mana, combate ×2, ask, mulligan, grid de búsqueda, orden de triggers, votación) y login. El selector no debe quedar bajo los modales del estado (overlays de la app llegan a z=999999) → sidebar `z-index: 1000000`; al salir restaura el snapshot del store. Spec `e2e/gallery.spec.ts`: recorre las 44 entradas y exige 0 `pageerror` (SIEMPRE, sin stack) + regresión visual opt-in (`E2E_VISUAL=1`, red externa bloqueada para placeholders deterministas) de 12 estados con baselines por plataforma (`--update-snapshots`); unit del builder (ids únicos, grupos, objetivos/pago reales). Pendiente: estados de lobby/editor/draft/torneo y de la matriz §4 (vacío/cargando/error/desbordado/idioma/CJK), baselines en 1366×768/1920×1080/2560×1440 y WebKit, correrlas en CI (requiere baselines Linux). Unit 1600/1600 + typecheck ✅ | | | |
| P4 | **Regla "ningún escenario simulado sin frame real"** | Ampliar `record.mjs` para un driver por mecánica del §3 (usando P1) y derivar los escenarios simulados de esos frames | Cierra el hueco entre "pasa en simulado" y "funciona en real" | M–L |
| | ✅ en marcha 2026-09-15: `ctx.cheatSetup(zones)` + hook `driver.onPlayMana` en `rec-lib.mjs`; driver `counterspell` (mazo todo-Islas vs SIM Montañas+Bolt; setup T1, counter al Bolt, captura pila con ambos + objetivos cruzados) → `counterspell.json` + assert `hasCounterOnStack` (unit + replay e2e). **Regla P1 bisecada**: el cheat en la 1ª prioridad de T1 congela el loop (corre fuera del hilo de juego aún arrancando) → cheatear tras ≥1 acción normal (documentado en README + rec-lib; enrutar al hilo de juego daría deadlock porque espera el prompt). Suman `aura` (Rancor sobre Mystic 3/1, pago solo-Bosques: el Mystic mareado rompía el pago por defecto en bucle) y `tokens` (Dragon Fodder → 2 Goblin isToken WAR sin número) con asserts y replay (7/7), más `modal` (Boros Charm modo daño vs rival; modos por `GAME_CHOOSE_ABILITY`, pago por color; replay 8/8) y `xcosts` (Ballista X=2 con `onTargetAmount`, 2/2 +2 contadores; max INT_MAX sin acotar; replay 9/9), más `response` (Growth vs Sting propio al Mystic — el SIM retiene el Bolt con criaturas fuera), `trigger-order` (2 Wardens sin diálogo: auto-orden) y `may-trigger` (Solemn may→SÍ + búsqueda por TARGET; harnés: `onTarget(ctx,q,data)` + fallback onAsk→default). Replay 12/12. §3.4 pila→R, §3.7 modal→R, §3.2 X/XX→R, §3.5 triple→R. Suman `convoke` (taps como PLAY_MANA; X=0 sin objetivo) y `flashback` (descarte con memoria), grabados en paralelo (USER≤14c blindado), replay 14/14: §3.2 convoke→R, §3.3 flashback→R. ✅ 2026-09-16: cierran `vote` y `warp`, los dos últimos drivers del registro sin frame (33→35 en `manifest.json`; `monstrosity`/`combat-probe` quedan aparte, son solo sondas de investigación, no fixtures). `vote` (Council's Judgment) estaba roto: cheatear solo la mano propia no basta — el SIM no tenía ningún permanente sin tierras tan pronto (2 Mystics en 60 cartas), el voto se quedaba sin candidato y el hechizo fizzleaba mientras la partida se iba 10+ turnos a un descarte de limpieza en bucle (el `onTarget` de descarte devolvía un id fuera de `options.possibleTargets`, así que el servidor repetía la misma pregunta). Fix: cheat encadenado (no en paralelo, P1) que además pone un Mystic votable en el campo del rival; el voto llegó como `GAME_TARGET` (no `GAME_CHOOSE_CHOICE`). Hallazgo de motor sin resolver: el rival cheateado terminó con 2 Mystics exiliados en vez de 1 (posible bug de voto que deja votar por permanentes propios cuando no hay ajenos). `warp` (Time Warp) nunca se había corrido: sin `onTarget` para "Select a player" se congelaba hasta el timeout; con un `onTarget` sin discriminar por pregunta, el descarte de limpieza posterior heredaba la respuesta equivocada (mismo patrón que vote); y el propio `captureWhen` miraba `exile` con un comentario ("se exilia al resolver") que es falso para el texto real de Time Warp (va al cementerio, sin robo). Con las tres correcciones captura en el primer turno. Ambos con asserts (`hasVote`, `hasExtraTurn`), unit 36/36 y replay e2e 34/34 (`npx playwright test recorded.spec.ts`). ✅ 2026-09-15: cerrado también el pendiente de P1 — exposición MCP (`mage_cheat_setup`). ✅ 2026-09-16 (2ª tanda): cinco drivers más con cheatSetup — `hybrid` (Kitchen Finks {1}{G/W}{G/W} con 3 Bosques: el híbrido se paga con verde, sin pregunta de color), `anycolor` (Birds of Paradise paga el {U} de Opt; **hallazgo de motor**: con un solo color pagable NO hay `GAME_CHOOSE_CHOICE` de color, el servidor lo auto-resuelve), `planeswalker` (Teferi, Hero of Dominaria {3}{W}{U}: +1 robar, lealtad 4→5; **hallazgo del harnés**: enviar el UUID de la habilidad no activa nada — `HumanPlayer` resuelve el UUID con `game.getObject()` y una habilidad no es un objeto de juego; hay que clicar el permanente y responder `GAME_CHOOSE_ABILITY`; el helper `playAbility` pasa a hacer click), `saga` (History of Benalia {1}{W}{W}: capítulo I → lore:1 + ficha Knight 2/2 con vigilancia) y `morph` (Den Protector boca abajo por {3}: la habilidad va a la bolsa `other` por `SpellAbilityType.BASE_ALTERNATE` y el picker la etiqueta "Cast … using Morph: Megamorph {1}{G}"). Con asserts (`hasHybrid`, `hasAnyColor`, `hasPlaneswalker`, `hasSaga`, `hasFaceDown`), manifest 35→40, unit 41/41 y replay e2e 39/39. §3.2 híbrido→R, §3.2 any-color→R parcial, §3.8 morph→R parcial, §3.9 planeswalker→R, §3.9 sagas→R parcial. ✅ 2026-09-16 (3ª tanda): cinco drivers más — `ward` (Lightning Bolt a Sythis, Harvest's Hand [Ward {1}]: `GAME_ASK` "pay... Ward" sin default de motor), `illegal-target` (Bolt propio a Elvish Mystic + Viscera Seer lo sacrifica en la misma ventana: el Bolt se retira de la pila sin efecto), `upto-zero` (Frost Breath declinando 0 de 2 objetivos posibles), `redirect` (Bolt a la cara + Redirect sobre el propio Bolt: con un solo hechizo legal, Redirect auto-resuelve su objetivo y llega un `GAME_ASK` "Change this 1 of 1 target?" en vez de un segundo `GAME_TARGET`) y `big-stack` (10x Opt sin pasar prioridad, pila de 10). manifest 40→45, unit 46/46. §3.4 hasta-N→R, objetivo-en-pila-cambio→R parcial, ward→R parcial, objetivo-ilegal→R parcial; §3.6 pila-grande→R. ✅ 2026-09-16 (4ª tanda): nuevo hook `driver.onMultiAmount` + soporte de `GAME_GET_MULTI_AMOUNT` en `rec-lib.mjs` (asignación de daño de combate con trample: un ítem por bloqueador, se responde con `sendPlayerString` de los valores en orden separados por espacio, igual que `confirmMultiAmount` en el cliente web) para intentar `trample-deathtouch` (§3.6). **Sin frame** tras ~10 partidas reales: (1) error propio — Vorapede en realidad es Vigilance/Trample/Undying, sin deathtouch, y "Quilled Slagwyrm" no existe en el catálogo (cheatSetup `ok:false FAILED`); (2) **hallazgo de motor**: dos `cheatSetup` que meten una criatura cada uno (jugadores distintos) casi a la vez disparan un `java.util.ConcurrentModificationException` real en el servidor — un respiro de 800 ms entre cheats lo evita; (3) **hallazgo de IA** (el más relevante): el SIM evalúa deathtouch correctamente al decidir bloqueos — un Vorapede+Basilisk Collar real (5/4 deathtouch+trample, equipado en vivo, equip {2}) contra un Terra Stomper (8/8) rival, el MISMO emparejamiento que sí bloqueaba sin deathtouch real, el SIM se negó a bloquear las 4 veces seguidas y perdió la partida por daño directo antes que perder su criatura contra deathtouch; ni siquiera un Terra Stomper equipado contra otro Terra Stomper rival (cambio mutuo parejo) lo bloqueó. Encantar con Lure (fuerza el bloqueo por regla) debería sortear esto pero las últimas corridas chocaron con la carrera intermitente ya documentada del cheat ("ok:true y silencio", ver P1) antes de completar el equipo. El hook `onMultiAmount` queda listo en `rec-lib.mjs` para cuando se retome. Sí sale de este intento, ya con el mismo `onMultiAmount`: `menace` (Ripscale Predator 6/5 Menace ataca sin bloqueadores pese a un único Grizzly Bears rival disponible — restricción de regla, no decisión de la IA). manifest 45→46, unit 47/47. §3.6 menace→R parcial. ✅ 2026-09-16 (5ª tanda, 3 grabadores en paralelo): los drivers salen de `record.mjs` a módulos `scripts/drivers/*.mjs` con `meta` (assert+nota) auto-cargados en el registro + `scripts/record-sync.mjs`; 6 frames nuevos — `dash` (Lightning Berserker por dash {R}: el chooser es `GAME_CHOOSE_CHOICE` con keyChoices "Cast with Dash alternative cost…"/"Cast with no alternative cost…" — no `GAME_CHOOSE_ABILITY`; entra con prisa: `rules` Haste + `summoningSickness:false`), `suspend` (Rift Bolt exiliado con `time:1`; la `SpecialAction` **no** aparece en `GameView.special` — se clica la carta en mano; `PlayerView.exile` agrega la zona "Suspended cards"), `vigilance` (Vanguard of Brimaz ataca `tapped:false` en el grupo), `first-strike` (Precinct Captain: el bloqueador muere en `FIRST_COMBAT_DAMAGE` con el atacante `damage:0`; hallazgos: los permanentes cheateados entran sin mareo y el SIM ataca con todo — el bloqueo hay que montarlo en el mismo turno del cheat), `surveil` (Consider: TARGET "PUT into your GRAVEYARD (Surveil)") y `jump-start` (Radical Idea: el descarte como coste llega como `GAME_TARGET "Select a card"` con los ids en `data.targets`, sin `possibleTargets`; el descarte de limpieza sí dice "discard"). Bug de cliente corregido: `gameSoundDispatcher` miraba `FIRST_STRIKE_DAMAGE` (inexistente). manifest 46→52, asserts `hasDash`/`hasSuspend`/`hasVigilance`/`hasFirstStrike`/`hasSurveil`/`hasJumpStart`, unit 52/52 + replay por frame. §3.2 dash→R parcial, §3.3 suspend→R y jump-start→R parcial, §3.6 vigilance→R parcial y first-strike→R parcial, §3.8 surveil→R. ✅ 2026-09-16 (6ª tanda, 3 grabadores en paralelo): 6 frames más (manifest 52→58) — `escape` (Phoenix of Ash desde el cementerio; el exilio del coste se auto-elige con exactamente el mínimo legal 3→3; `+1/+1` y cementerio vacío), `foretell` (Saw It Coming exiliada boca abajo pagando {2} y lanzada al turno siguiente por {1}{U}; clicarla dispara `GAME_ASK "Look at …"` → false; `canPlayObjects.other` expone "Foretell {1}{U}"; el mismo turno está prohibido), `ninjutsu` (el Ninja entra girado y atacando en `DECLARE_BLOCKERS` devolviendo el atacante a la mano; habilidad en `canPlayObjects.other`; auto-elige el atacante), `must-block` (Lure lanzado de verdad — el cheatSetup no puede adjuntar auras — y bloqueo forzado por `MustBeBlockedByAllAttachedEffect`; el bloqueo se monta en el mismo turno del ataque porque el SIM ataca con todo), `pithing-needle` (`GAME_CHOOSE_CHOICE` string mode con la lista completa en `choice.choices` ~25k y `searchEnabled`, respuesta por nombre exacto; `rules` "Chosen name: …") y `cavern` (`ChoiceCreatureType`: 396 `keyChoices` con relevance-sort, respuesta por clave; `rules` "Chosen type: Elf"). **Hallazgo de harnés/motor**: `cheatSetup` directo al campo con decisión "as it enters" revienta el WorkerThread del proxy (`Wrong code usage: game related code must run in GAME thread`) → cheatear a la mano y lanzar. Fix del smoke: `must-block` (adjunto real) entra en la excepción de `.card-attachment-group`. §3.3 escape→R, foretell→R parcial; §3.6 ninjutsu→R parcial, "debe bloquear"→R parcial; §3.7 nombrar carta→R, elegir tipo→R parcial. ✅ 2026-09-16 (7ª tanda, 3 grabadores en paralelo): 6 frames más (manifest 58→64) — `emerge` (Wretched Gryff por Emerge {5}{U}: sin chooser — BASE_ALTERNATE y única habilidad activable; el sacrificio se auto-elige y reduce {5}{U}→{3}{U} por el MV del Grizzly), `reduce-cost` (el `GAME_PLAY_MANA` ya pide el coste reducido; el objetivo del Strike va con el playerId del rival en `data.targets`), `double-strike` (en `FIRST_COMBAT_DAMAGE` el rival está a 19 y el atacante `damage:0`), `phasing` (el permanente sigue en `battlefield` con `phasedIn:false`; `BoardZone` lo oculta), `brainstorm-order` (NO usa `GAME_CHOOSE_CARDS_ORDER`: DOS `GAME_TARGET "Select a card"` secuenciales con ids en `data.targets`; orden probado con el robo de Opt) y `energy` (`PlayerView.counters` con `{name:"energy",count:2}`). §3.2 costes alternativos→R y reducción→R; §3.6 first/double strike→R; §3.8 mirar N y ordenar→R parcial; §3.9 phasing→R; §3.10 energía→R parcial. ✅ 2026-09-16 (8ª tanda, 3 grabadores): 4 frames (manifest 64→68) — `ponder` (orden = TARGETs secuenciales con "(last one chosen will be topmost)"; doble robo lo prueba; NO existe `GAME_CHOOSE_CARDS_ORDER` en 1.4.61), `fof` (hook nuevo `GAME_CHOOSE_PILE` en rec-lib; **el SIM no reparte**: cancela y las 5 van a pile2 — mejora pendiente en SimPlayer), `manifest-mechanic` (manifest + girar boca arriba por clic directo; nivel del view: faceDown:false tras `GAME_PLAY_MANA "Pay {W}"`) y `mass-tokens` (Krenko + 50 Raging Goblin: duplicados del cheat funcionan; 51 fichas `isToken`; 811 KB). ✅ 2026-09-16 (9ª tanda, 4 grabadores): 8 frames (manifest 68→76) — `class-level` (nivel 3, hint en `rules`), `poison` (infect → `counters` poison:1 con vida 20), `twincast` (copia con `GAME_ASK "Change this 1 of 1 target"`; SIM 14), `ward-no-pay` (**corrige** el viejo `ward.json`: Sythis no tiene Ward; el trigger real es `"Pay {1}?"` y false contrarresta), `improvise` (special action + `GAME_TARGET` de artefactos; `AFTER` bloquea más maná), `discover` (ASK de lanzar gratis; objetivo auto), `trample-deathtouch` (Lure fuerza el bloqueo; multi-amount default 1; 2/3 para sobrevivir) y `cant-block` (Tormented Soul inbloqueable legal; icono de restricción). §3.2 improvise→R, §3.3 discover→R, §3.4 copiar→R y ward-no-pay→R parcial, §3.6 trample+deathtouch→R y "no puede bloquear"→R, §3.7 pilas→R parcial, §3.8 Ponder→R y manifest/giro→R parcial, §3.9 clases→R parcial y fichas masivas→R, §3.10 veneno→R parcial. ✅ 2026-09-16 (10ª tanda, 3 grabadores): 6 frames (manifest 76→82) — `hexproof` (el permanente NI es objetivo legal: el TARGET solo trae las caras), `choice-color` (**NO existen `GAME_CHOOSE_COLOR`/`_NUMBER` en 1.4.61**: color = `GAME_CHOOSE_CHOICE` string con `ChoiceColor`, número = `GAME_GET_AMOUNT`), `goad` (solo `rules` ICON_REQUIRE + `cardIcons.OTHER_HAS_RESTRICTIONS`), `attack-pw` (TARGET "Select a player, planeswalker, or battle to attack" → UUID del PW + booleano; lealtad en `permanent.loyalty`), `switcheroo` (autoelige con 2 legales; intercambio permanente) y `monarch` (`players[].monarch`; `designationNames` no aplica). §3.4 ward/hexproof→R, §3.6 goad→R parcial y atacar PW→R parcial, §3.7 color/número→R, §3.10 switcheroo→R y monarch→R parcial. ✅ 2026-09-16 (11ª tanda, 3 grabadores): 6 frames (manifest 82→88) — `stun-oil` (contadores `stun`/`oil` en `permanent.counters`; **ojo**: Rime Chill en 1.4.61 es vivid `{6}{U}` "hasta dos" y vivid cuenta 1 color → "Pay {5}{U}"), `colorless-c` (Spatial Contortion `{1}{C}`: pago troceado `Pay {1}{C}` → `Pay {1}` con Wastes, objetivo auto), `coin-dice` (flip `GAME_ASK "Heads or tails?"` + d20; **hallazgo**: dado/moneda solo viajan como texto al CHAT — no hay campos estructurados en el view), `fail-to-find` (**la vía existe**: `TargetCardInLibrary.setRequired(!filter.hasPredicates())` → básicas nacen `required=false`, el TARGET llega `flag:false` y `sendPlayerBoolean(false)` falla la búsqueda; Demonic Tutor es `required=true` y por eso re-preguntaba), `disguise` (`disguised:true`, ward {2} solo en `rules`) y `win-effect` (**fix de `rec-lib`**: captura también `GAME_UPDATE` plano [GameView aplanado en `data`] y `GAME_OVER`, porque el fin de partida no viaja en `AND_INFORM`). §3.2 `{C}`→R, §3.7 dados/moneda→R, §3.8 fail-to-find→R y disguise→R parcial [cloak], §3.9 stun/oil→R, §3.10 ganar/perder→R. ✅ 2026-09-16 (12ª tanda, 3 grabadores): 6 frames (manifest 88→94) — `plot` (Rictus Robber con Plot: SpecialAction directa sin chooser, pago manual, carta en la zona "Plots of `<jugador>`" con `faceDown:false`; el lanzamiento gratis pide `GAME_CHOOSE_ABILITY` y resuelve por `{0}`), `floating-mana` (el ASK de pool con texto exacto y `confirmEmptyManaPool`; el frame es el estado posterior con `manaPool {green:1}`), `attack-battle` (**la batalla debe ser tuya**: `Combat.setDefenders` exige `ProtectedByOpponentPredicate`; `defense` 3→1 y `combat[].defenderId`), `always-attack` (Rubblebelt Recluse del SIM ataca; restricción en `cardIcons` `hint:"Must attack (…)"` como goad), `day-night` (**el flag no está en `GameView`**: hint en `rules` del DFC transformado + chat "It has become day/night"; `designationNames` no lo lleva) y `cloak` (trigger modal de Vannifar como `GAME_CHOOSE_ABILITY` + `GAME_TARGET` de mano → 2/2 `cloaked:true` con ward {2} en `rules`). §3.2 maná flotante→R, §3.3 plot→R, §3.6 atacar batalla→R y "ataca cada combate"→R, §3.8 boca abajo→R (morph/manifest/disguise/cloak completos), §3.9 DFC/day-night→R. ✅ 2026-09-16 (13ª tanda, 3 grabadores + 1 fix): 6 frames (manifest 94→100) — `dungeon` (**sí es estructurado**: `players[].commandList` DungeonView con `rules[0]` = "<i>(Currently in X)</i>"; prompts: `GAME_CHOOSE_CHOICE` string mode para elegir mazmorra y `GAME_ASK` para la sala), `the-ring` (nivel = `rules.length` del emblem en `commandList`; portador = `cardIcons[] RINGBEARER`; **bug del cliente corregido**: `MechanicsTray.findRingBearer` usaba campos inexistentes, ahora con test unit sobre el propio frame), `concede-mulligan` (**el server SÍ honra CONCEDE en el mulligan**; el problema es que la UI no lo ofrece; **hallazgo de contrato**: el fin de partida en mulligan trae `phase`/`step`/`activePlayerId` a `null` → `contract.schema.json` ampliado a nullable + gen-types/gen-zod regenerados + 5 puntos del web tolerantes a null), `initiative` (White Plume Adventurer: `players[].initiative:boolean`; `designationNames` vacío), `radiation` (contador **`rad`** [no "radiation"]; trigger en la fase principal precombate, sin dado) y `starting-player` (opt-out `skipStartingPlayerChoice:false` nuevo en `rec-lib`; `GAME_TARGET "Select a starting player"` solo al ganador del sorteo). §3.1 elegir quién empieza→R y conceder durante mulligan→R, §3.10 initiative/dungeon/the ring→R (fila "Monarch… "→R parcial por emblemas). ✅ 2026-09-16 (14ª tanda, 3 grabadores): 3 frames (manifest 100→103) — `emblem` (Gideon of the Trials: el emblema va en `players[].commandList` como EmblemView, **NO** en `myHelperEmblems` [eso es solo helper emblems del motor]; `.emblem-slot` ya lo pinta por el prefijo "emblem "), `commander-zone` (**el recorder gana el passthrough `driver.tableGameType`** y el driver pierde su parche de WebSocket; mesa "Commander Two Player Duel" con `commanders` en el deck: el comandante vive en `commandList` con `mageObjectType:"COMMANDER"` y el impuesto viaja en `rules` ["2 times played from the command zone", el `castCount` no existe en el wire]; 2º cast "Pay {2}{R}{R}{2}"; el retorno al morir es `GAME_ASK` con botones UI.left/right) y `karn-restart` (Karn −14: **el reinicio llega solo como `GAME_UPDATE`** — mismo gameId, sin GAME_INIT ni GAME_OVER — con turno 1, manos 7/7, bibliotecas 53/93, mesa vacía y Karn barajado; las exiliadas no se recolocan de forma fiable entre runs). §3.3 commander desde zona de mando→R, §3.10 reiniciar partida→R y emblemas→R (fila "Monarch, initiative/dungeon, ring, emblemas" **completa**). ✅ 2026-09-16 (cierre post-14ª tanda, 3 agentes): **fix de `SimPlayer` para FoF** (con la sobrecarga `Cards` de `fireSelectTargetEvent` los ids van en `options.possibleTargets` y el cierre en "Done" de `UI.right.btn.text`: el bot ahora elige carta por `sendPlayerUUID` [menor UUID] y cierra con `sendPlayerBoolean(false)`; antes hacía `cancel()` y pile1 quedaba vacía) → `fof.json` **re-grabado** (pile1=[Grizzly Bears], pile2 a la mano) y 4 tests Java nuevos (suite 83/83; jar reconstruido y proxy reiniciado); **P2 v3** de fidelidad (adjuntos + tamaños de zona, arriba); y **botón Conceder en el `MulliganDialog`** (el server ya lo honraba; i18n ×9, 5 tests unit + e2e de mulligan) con el escenario fake del Ring-bearer actualizado a `cardIcons` [la UI ya no mira campos inexistentes]. | | | |
| P5 | **Harness diferencial con escritorio** | Guion de partida (mazos + acciones) ejecutado dos veces: con el cliente Swing (o un espectador Swing) y con Nexus/MCP. Se compara la secuencia de callbacks, sus opciones y el estado final | Detecta prompts que Nexus resuelve distinto o se salta | M |
| | ⚠️ replanteado 2026-09-16: antes de construir el cliente Java de referencia se investigó `Mage.Proxy/ProxyClient.java` — no tiene switch por tipo de callback (un solo `onCallback` genérico que serializa por reflexión), y el riesgo real que P5 perseguía ("Nexus se salta un prompt") ya lo cubre `web/src/state/callbackCoverage.test.ts` (compara el enum `ClientCallbackMethod.java` del fork contra los `case` de `eventHandler.ts`/`feedback/*.ts` y contra `INTERACTION_COVERAGE.md`). El hueco real no cubierto era la **fidelidad de contenido** de `JsonUtil.toJson()`. **Auditoría hecha en su lugar** (mismo objetivo, coste mucho menor, sin tocar el fork): tests nuevos en `Mage.Proxy/JsonUtilTest.java` encontraron un bug real — `mage.view.ExileView`/`MutateView` (Mage.Common) extienden `CardsView`→`LinkedHashMap`, y `writeValue()` tomaba la rama `instanceof Map` antes de llegar nunca a la reflexión de campos, así que sus propios campos (`name`, `id`) se descartaban en silencio. Consecuencia real confirmada en el código: `web/src/board/boardShared.ts` y `crossZone.ts` leían `exile.cards`/`exile.name` sobre `GameView.exiles` — clave que nunca existía —, así que la búsqueda de cartas en cualquier zona de exilio nombrada (foretold/suspended/plotted) nunca encontraba nada, y `system/fidelity.ts` tenía el mismo punto ciego con cartas exiliadas; `BoardZone.tsx`/`deckTracker.ts` leían `mutateView` como mapa plano y funcionaban solo por coincidencia. Fix: `JsonUtil.java` ahora anida las entradas del mapa bajo `cards` cuando la clase declara campos propios (igual que sus primas POJO `RevealedView`/`LookedAtView`, que nunca tuvieron el bug); `contract.schema.json` corregido (`ExileView`/`MutateView` → `{name?, id?, cards}`) + `gen-types`/`gen-zod`; `BoardZone.tsx`/`deckTracker.ts` actualizados a `.mutateView.cards`; migración estructural de los 101 frames de P4 (`name`/`id` quedan vacíos en frames viejos — ese dato ya se había perdido al grabarlos — pero `cards` se recupera en todos). Bonus: `NaN`/`Infinity` en double/float ya no salían como JSON inválido, ahora `null`. Verificado: `mvn test` en `Mage.Proxy` 100% verde, `tsc --noEmit` limpio, `vitest run` en `web/` 1692/1692 (188 archivos), incluida la validación zod estricta de las 101 fixtures migradas. Cero cambios en `../xmage-fork`. **Pendiente si se quiere el P5 original completo**: el cliente Java de referencia jugador (SessionImpl directo, sin proxy) para comparar comportamiento por defecto Swing-vs-Nexus en el mismo prompt — no se construyó, este hallazgo hizo prioritario el fix antes de invertir en eso. Re-grabar con nombre real solo `foretell`/`suspend`/`plot` (las zonas de exilio nombradas) queda como follow-up opcional con el harness de `record.mjs` ya existente. |
| P6 | **Fuzzing y partidas largas con detector de bloqueos** | Self-play MCP con mazos aleatorios reales; alerta si hay prioridad sin prompt visible durante más de X s, un evento sin manejar, una excepción o una discrepancia de P2 | Encuentra los casos que nadie pensó en escribir | M |
| | ✅ v1 2026-09-16: `scripts/fuzz.mjs` — no es "self-play MCP" literal (un agente LLM llamando tools de `mcp/` una partida entera sería demasiado lento/caro para muchas partidas largas); en su lugar, dos sesiones WS independientes contra el proxy real, igual de "self-play" en espíritu: asiento HUMAN con política genérica ingenua (juega tierra/hechizo al azar con prioridad, si no pasa; nunca ataca/bloquea a propósito) + asiento SIM (`SimPlayer.java`, ya probado en P4). Mazos **reales** al azar del pool de 1172 `.dck` de `Mage.Client/release/sample-decks` del fork (parser propio del formato `N [SET:NUM] Nombre`, nada de mazos de tierras sintéticos). Por partida vigila: bloqueo (sin eventos de juego durante `--stallMs`, default 45s), callback desconocido (fuera del enum real `ClientCallbackMethod.java`, misma fuente que `callbackCoverage.test.ts`), error fatal del proxy o cierre inesperado del WebSocket; **no cubre discrepancia P2** (eso necesita un navegador real pintando DOM — sin construir aún, ver nota más abajo). Informe JSON en `.run/` (gitignored) con resultado + anomalías por partida. Verificado en vivo contra el servidor de test local: primera corrida de 8 partidas con throttle ingenuo (200ms de gap fijo, descartando respuestas) encontró un hallazgo real — 2 conexiones cerradas por el propio Gateway (`code 1008 "message rate limit exceeded"`, límite real `maxMessagesPerSecond=100`) por un bucle de re-prompt de `GAME_SELECT`; el primer intento de fix (descartar respuestas dentro del gap) rompió el mulligan [solo se pregunta una vez] y las 8 partidas siguientes se colgaron todas en turno 1; fix correcto: cola de salida drenada a ritmo seguro (40 msg/s) en vez de descartar. Con el fix, 8/8 partidas sin cierres espurios: 3 game-over limpios, 4 timeout (presupuesto de prueba de 150s corto para partidas completas, no bug) y **1 stall real sin confirmar reproducibilidad** (`[NPH] Life for Death` vs `Jund Control TDtB ST Dec 2012`, turno 9, sin eventos 30s+ — candidato a investigar, no se aisló por falta de seed determinista de mazos en el CLI). Pendiente si se quiere P6 completo: seed determinista de mazos (`--seed=`) para reproducir hallazgos, hook de discrepancia P2 vía Playwright espectando la partida con `mage-web-fidelity=1`. ✅ **`--concurrency` + lote de 300 (2026-09-16)**: el cuello de botella no es red sino "pensar" de la IA real por partida, así que en vez de acelerar cada partida se paralelizó — el proxy ya soporta varias sesiones reales concurrentes (mismo patrón que los grabadores de P4 en paralelo), techo arquitectónico `maxGameThreads=20` de `local-server/config.xml`. Con `--concurrency=6` verificado limpio (sin cierres espurios) antes de escalar. **Lote de 300 partidas a concurrency=6** (`--maxTurns=40 --maxGameMs=180000 --stallMs=45000`, ~50 min): `{"timeout":144,"turn-cap-conceded":51,"startMatch-failed":1,"game-over":103,"stalled":1}` — de las 147 "anomalías" reportadas, 144 son solo el marcador informativo `gameTimeout` (presupuesto de prueba corto frente a una política pasiva + IA real, no bugs) y **0 callbacks desconocidos, 0 cierres de WS inesperados, 0 excepciones del script** en 300 partidas con mazos reales distintos — señal fuerte de que el protocolo/proxy/servidor aguanta carga real sin sorpresas. Quedan 2 candidatos genuinos: (1) `startMatch-failed` único — `SIM sim-000001-428 failed to join` con el mazo `Tribal Zoo - Billy Hahn.dck` (2014 Modern), sin investigar la causa; (2) **el stall SÍ se repitió** — el mazo HUMAN `[NPH] Life for Death.dck` [2011 Starter, cargado de maná pirexiano: Gut Shot, Porcelain Legionnaire, Moltensteel Dragon, Blinding/Immolating Souleater] apareció en el stall de este lote de 300 (turno 11, sin eventos 45s+) **y en el de la tanda anterior de 8** (turno 9, sin eventos 30s+), cada vez emparejado con un SIM distinto — la coincidencia del mazo HUMAN y no del rival apunta a un candidato real ligado a ese mazo/mecánica, no a ruido de máquina compartida. Siguiente paso natural: aislar `[NPH] Life for Death.dck` con `--seed=` (aún no implementado) y logs del proxy/servidor correlacionados con el `gameId` para diagnosticar la causa real. ✅ **`[NPH] Life for Death.dck` aislado y ambos hallazgos cerrados (2026-09-16)**: en vez de `--seed=` se añadió `--humanDeck=`/`--simDeck=` (subcadena del nombre del .dck) para repro dirigida, y un dump de diagnóstico (`.run/fuzz-unanswered.jsonl`) de cualquier prompt que exige respuesta y no la recibió. Grepeando `.run/proxy.err.log` por el `gameId` del stall: el último evento antes del hueco de silencio era siempre `GAME_CHOOSE_CHOICE`, y el juego no colgaba de verdad — un `GAME_OVER` aparecía ~24000 líneas de log después (el idle-timeout del propio servidor forzaba el fin). **Dos bugs del propio fuzzer, no del producto**: (1) `optionList()` asumía que los items de un array eran `{id,value}`; cuando `choice.choices` llega en "modo texto" como array de STRINGS planos (Cavern of Souls/Pithing Needle, ya documentado en §3.7), `c?.id ?? c?.value` sobre un string da `''` → con el check `if (opts[0]?.value)` (string vacío es falsy) nunca se enviaba nada — cambiado a comprobar `opts.length`, no la veracidad del valor; (2) el extractor `m.data?.choice?.keyChoices ?? m.data?.choice?.choices` usaba `??`, pero `keyChoices` llega como `{}` (objeto vacío, NO `null`/`undefined`) en modo texto — `??` nunca cae al fallback de `{}` a `choices`, así que `optionList({})` daba `[]`. Confirmado por payload real capturado (`choice.choices:["White","Blue","Black","Red","Green","Artifacts"]`, `keyChoices:{}`, mensaje "Choose protection from"). Con ambos arreglos: **8/8 partidas con `[NPH] Life for Death.dck` fijado, cero stalls**. El `startMatch-failed` (SIM sin unirse) SÍ era un bug real y del producto: `Mage.Proxy/SimManager.nextSimUsername()` generaba `"sim-" + counter-por-instancia + "-" + (currentTimeMillis()%1000)` — el contador siempre arranca en 0 por sesión y el único factor de unicidad entre sesiones concurrentes era un espacio de 1000 valores; con varias mesas SIM creándose casi a la vez (confirmado en el log: `sim-000001-951` se conecta, falla al unirse a una mesa, y milisegundos después el MISMO nombre se reconecta y se une a OTRA mesa — colisión entre dos sesiones concurrentes) el servidor trata la segunda conexión como reconexión del mismo usuario y mata la primera, dejando esa mesa sin el asiento SIM. Fix: contador atómico **global** (`AtomicLong` estático, compartido por todas las instancias de `SimManager` del proceso) en vez de por-instancia+reloj. Verificado: lote de 16 partidas a `--concurrency=8` (el escenario que antes disparaba la colisión) → 0 `startMatch-failed`. `mvn test` en `Mage.Proxy` 100% verde tras el fix. |
| P7 | **Kit de sesiones manuales** | Plantillas: charters de testing exploratorio por área, diario de partida, informe de incidencia (pasos, severidad, captura, `ws-frames`) y botón "Exportar diagnóstico" en la app (logs de cliente, proxy y servidor + últimos frames) | Hace repetibles y comparables las sesiones humanas | S |
| | ✅ hecho 2026-09-14: `docs/qa/` (charter, diario, informe) + Acerca de → Exportar diagnóstico (`system/diagnostics.ts`: versión, conexión sin contraseña, fase/mesa, resumen de partida, ajustes, log, últimos 60 frames vía `net/frameBuffer.ts` con hook en `Gateway`; payload completo solo si < 64 KB, digest de GAME_UPDATE). Logs proxy/servidor se adjuntan a mano (`tail.mjs`) según la plantilla. Unit 1549/1549 + typecheck ✅ | | | |

---

## 3. Matriz de mecánicas a verificar en real

Estados: **R** = verificado en real (sesión o spec) · **S** = solo simulado · **—** = sin test (no aparece en el catálogo) · **Gap** = falta implementación.

Método por fila: (1) escenario real con P1 → (2) jugarlo desde la UI → (3) P2 activo sin discrepancias → (4) checklist de prompt del §5.1 → (5) grabar frame (P4) → (6) test de regresión.

### 3.1 Inicio de partida
| Mecánica | Cartas / montaje | Qué comprobar en la UI | Estado |
|---|---|---|---|
| London mulligan hasta 5 + fondo | Mazo cualquiera, mulligan ×2 | Mano en abanico, contador N, elegir N cartas al fondo, no se puede confirmar con menos | R (sesión 09-12) |
| Mulligan gratuito de Commander / multijugador | Commander 4 jugadores | Que el primero no reste carta y el texto lo diga | — |
| Elegir quién empieza | Ganar la tirada | Diálogo con jugadores, timeout | R (frame `starting-player.json`: con `skipStartingPlayerChoice:false` [opt-out nuevo en `rec-lib`] el servidor sortea ("won the toss") y **solo el ganador recibe** `GAME_TARGET "Select a starting player"` con `targets=[playerId SIM, playerId propio]` y `flag:true`; se responde `sendPlayerUUID`; si el sorteo lo gana el SIM su `ComputerPlayer.makeChoice` se elige a sí mismo sin prompt; el elegido tiene `startResponseIdleTimeout` para responder; tras la elección solo hay un inform de log [el frame captura turno 1 con nosotros activos]; driver P4 09-16) |
| Companion revelado | Lurrus en banquillo | Visible en zona propia y del rival; pagar {3} para ponerlo en mano | Gap parcial (G12-2) |
| Conceder durante mulligan | — | La UI no ofrece algo que el server ignora (lección plan3) | R (frame `concede-mulligan.json`: **el server SÍ honra el CONCEDE durante el mulligan** — `GameImpl.setConcedingPlayer` usa `state.getChoosingPlayerId()` [fijado por `Mulligan.executeMulliganPhase`], `HumanPlayer.signalPlayerConcede(true)` despierta `waitForResponse` y `checkConcede→leave→end` dejan un GAME_UPDATE inmediato con `players[].hasLeft:true` [turno 1, mano 0, sin tierras]; el CONCEDE se procesa antes que la respuesta keep/mulligan que manda el driver; **la carencia es de UI**: el `MulliganDialog` no ofrece Conceder y su backdrop tapa el menú ⋯ de la cabecera [la lección plan3 no aplica: no es que el server lo ignore]; **hallazgo de contrato**: en ese fin de partida el GameView llega con `phase`/`step`/`activePlayerId` a `null` → modelado en `contract.schema.json` [+ gen-types/gen-zod] y tolerado por el web; driver P4 09-16) |

### 3.2 Maná y costes
| Mecánica | Cartas | Qué comprobar | Estado |
|---|---|---|---|
| Pago estándar tocando fuentes | Tierras básicas | Fuentes jugables resaltadas, pool visible, auto-pago | R |
| Híbrido y pirexiano | Kitchen Finks, Dismember | Elegir vida o maná; vida baja reflejada | R (híbrido: frame `hybrid.json` — Finks 3/2 pagado con 3 Bosques, sin pregunta de color; pirexiano: frame `dismember.json` — 4 vidas; drivers P4 09-16) |
| X y XX | Blaze, Hangarback Walker, Villainous Wealth | Stepper con máximo real; XX cuenta doble | R (frame `xcosts.json`: Ballista X=2, XX doble ✓; el max del prompt es INT_MAX — el servidor no acota, driver P4 09-15) |
| Maná de cualquier color / elección | Birds of Paradise, Treasure | Elección de color y pool | R parcial (frame `anycolor.json`: Birds paga el {U} de Opt; el motor **auto-resuelve** el color cuando solo uno es pagable — no emite `GAME_CHOOSE_CHOICE` de color; driver P4 09-16) |
| Nieve, incoloro específico `{C}` | Blizzard Brawl, Eldrazi | Solo fuentes válidas resaltadas | R (nieve: frame `snow.json` — Blizzard Brawl con Snow-Covered Forest, pelea Oso vs Mystic SIM; incoloro específico `{C}`: frame `colorless-c.json` — Spatial Contortion {1}{C} con mazo todo Wastes: el pago trocea `GAME_PLAY_MANA "Pay {1}{C}"` → `"Pay {1}"` (en una run el motor re-emitió el `{1}{C}` antes del `{1}`; no determinista, ambos frames válidos) y el `{C}` lo paga una Wastes [ogw 183]; el objetivo se AUTO-elige [Craw Wurm propio 6/4 +3/-3 → 9/1, P/T como strings]; drivers P4 09-16) |
| Convoke / Improvise / Delve | Chord of Calling, Treasure Cruise | Seleccionar criaturas, artefactos o cartas del cementerio como pago | R (convoke: frame `convoke.json` — Chord X=1 con 3 Mystics girados; delve: frame `delve.json` — Treasure Cruise {7}{U} con {U} + 7 exilios vía botón especial; improvise: frame `improvise.json` — Reverse Engineer {3}{U}{U} con solo 3 Islas: el botón especial abre `GAME_CHOOSE_ABILITY` con la `ImproviseSpecialAction` y los giros llegan como `GAME_TARGET "Select artifact to tap as Improvise's pay (selected N of 3, min 1)"` (3 Ornithopter girados); las Islas se pagan ANTES porque tras usar improvise el motor fija `ActivationManaAbilityStep.AFTER` y bloquea más maná; driver P4 09-16) |
| Costes alternativos: evoke, overload, dash, emerge | Solitude, Cyclonic Rift | Elegir modo de lanzamiento, coste mostrado | R (evoke: frame `evoke.json` — Solitude por evoke; overload: frame `overload.json` — Cyclonic Rift pagado por {6}{U}; dash: frame `dash.json` — Lightning Berserker por dash {R}, el chooser llega como `GAME_CHOOSE_CHOICE` (no `GAME_CHOOSE_ABILITY`) con las keyChoices "Cast with Dash alternative cost…"/"Cast with no alternative cost…" y entra con prisa visible (`rules` incluye Haste, `summoningSickness:false`); emerge: frame `emerge.json` — Wretched Gryff por Emerge {5}{U} con el Grizzly sacrificado como coste y la reducción por su MV ({5}{U} → {3}{U}), `EmergeAbility` es BASE_ALTERNATE y el clic lanza directo por emerge sin picker cuando el hardcast es impagable) |
| Kicker / multikicker / strive | Goblin Bushwhacker | Pregunta y coste final | R parcial (frame `kicker.json`: Bushwhacker con kicker {R} pagado — ASK "Pay Kicker {R}?" → SÍ; multikicker/strive siguen —; driver P4 09-16) |
| Reducción de coste | Goblin Electromancer | Coste mostrado ya reducido | R (frame `reduce-cost.json`: Lightning Strike nominal {1}{R} pagado con {R} por el Electromancer — el propio `GAME_PLAY_MANA` pide ya el coste reducido; el objetivo del Strike se responde con el playerId del rival presente en `data.targets`; driver P4 09-16) |
| "Solo para gastar en…" / maná flotante al cambiar de fase | Eldrazi Temple | Aviso de vaciar pool (`confirmEmptyManaPool`) | R (frame `floating-mana.json`: Forest activado [clic = `sendPlayerUUID`, sin chooser] → `manaPool {green:1}`; al pasar prioridad `HumanPlayer.passWithManaPoolCheck` emite el `GAME_ASK` `<font color='#D2691E'>You still have mana in your mana pool and it will be lost. Pass anyway?</font>` [requiere `confirmEmptyManaPool=true`: default del proxy y reenviado por el web en `GAME_INIT` con `updatePreferences`]; el frame es el estado inmediatamente posterior con el maná aún en el pool porque `captureWhen` no ve el ASK; driver P4 09-16) |

### 3.3 Lanzar desde otras zonas
| Mecánica | Cartas | Qué comprobar | Estado |
|---|---|---|---|
| Flashback / escape / jump-start | Faithless Looting, Uro | Carta jugable resaltada en el cementerio (`CrossZoneOverlay`), coste alternativo | R (flashback: frame `flashback.json`; jump-start: frame `jump-start.json` — Radical Idea, descarte del coste como `GAME_TARGET "Select a card"` con ids en `data.targets` y exilio al resolver; escape: frame `escape.json` — Phoenix of Ash por escape {2}{R}{R} desde el cementerio, exilio de 3 Montañas con auto-elección al haber exactamente el mínimo legal y `+1/+1` `EscapesWithAbility(1)`, cementerio vacío; drivers P4 09-16) |
| Adventure desde exilio | Bonecrusher Giant | Lanzar la criatura desde exilio tras la aventura | R (frame `adventure.json`: Stomp a la cara del rival → Giant exiliado → criatura lanzada desde el exilio; driver P4 09-16) |
| Foretell / plot / exilio boca abajo | Saw It Coming | Carta boca abajo propia visible para mí, oculta al rival | R (foretell: frame `foretell.json` — Saw It Coming exiliada boca abajo pagando {2} y lanzada desde el exilio por {1}{U} al turno siguiente; clicar la carta boca abajo dispara `GAME_ASK` "Look at …" → false; `canPlayObjects.other` expone "Foretell {1}{U}"; el mismo turno está prohibido (`foretoldTurn`); plot: frame `plot.json` — Rictus Robber {3}{B} con Plot {2}{B}: la SpecialAction se activa directa [sin chooser], el pago es manual y la carta queda en la zona "Plots of `<jugador>`" con **`faceDown:false` y `zone:null`** [la ocultación al rival es implícita]; el lanzamiento gratis pide `GAME_CHOOSE_ABILITY` [opción única "Cast … using Plot", el picker NO se suprime] y resuelve por `{0}` sin girar tierras; drivers P4 09-16) |
| Cascade / discover | Bloodbraid Elf | Pregunta "¿lanzar?" con la carta revelada | R (cascade: frame `cascade.json` — Bloodbraid Elf encuentra el Bolt de la cima y se lanza; discover: frame `discover.json` — Trumpeting Carnosaur (7/6) hace discover 5: exilia la Montaña y para en Giant Growth (MV 1); el ASK exacto es "Cast spell without paying its mana cost (Giant Growth [9ab])?" → sí ⇒ se lanza gratis desde el exilio con el objetivo AUTO-ELEGIDO (único legal, sin GAME_TARGET) y el 10/9 resultante (+3/+3) prueba la resolución; las exiliadas restantes van al fondo; driver P4 09-16) |
| Suspend / madness | Rift Bolt, Fiery Temper | Contadores de tiempo; ventana de madness | R (madness: frame `madness.json` — Faithless Looting descarta Fiery Temper y se lanza por madness; suspend: frame `suspend.json` — Rift Bolt suspendido, exiliado con contador `time:1` [zona "Suspended cards"; `PlayerView.exile` agrega todas las zonas de exilio]; la SpecialAction de suspend no se registra en `GameView.special` — se clica la carta en mano; drivers P4 09-16) |
| Commander desde zona de mando + impuesto | Cualquier comandante | Coste con impuesto; volver a la zona de mando (pregunta al morir) | R (frame `commander-zone.json`: mesa variant **"Commander Two Player Duel"** [`rec-lib` gana el passthrough `driver.tableGameType`] + deck con `commanders`; el comandante vive en `players[].commandList` con `mageObjectType:"COMMANDER"` y **el impuesto en su `rules`**: "<b>Commander</b> 2 times played from the command zone." [el `castCount` no existe en el wire]; 1er cast pide "Pay {2}{R}{R}", 2º "Pay {2}{R}{R}{2}" [+{2} literal]; al morir el retorno es un **`GAME_ASK`** "Move … to the command zone or leave it in current zone (GRAVEYARD)?" [botones `UI.left/right.btn.text`; true=comando]; driver P4 09-16) |

### 3.4 Objetivos
| Mecánica | Cartas | Qué comprobar | Estado |
|---|---|---|---|
| Un objetivo (criatura o jugador) | Lightning Bolt | Válidos con brillo, flecha, avatar del jugador clicable | R |
| Dividir daño entre objetivos | Arc Trail, Fireball | Reparto con total correcto | R (frame `split.json`: Arc Trail 2+1 entre Mystic SIM y cara del rival — el segundo TARGET no repite el primero; driver P4 09-16, ya grabado en el lote previo pero sin reflejar aquí) |
| "Hasta N" / cero objetivos | Aether Gust | Poder terminar con menos | R (frame `upto-zero.json`: Frost Breath declinando el objetivo — 0 de un máximo de 2 — con un Mystic rival legal disponible vía cheatSetup; el Mystic sigue sin tocar. Driver P4 09-16) |
| Objetivo en la pila | Counterspell | Hechizos de la pila seleccionables | R (frame `counterspell.json`, driver P4 09-15) |
| Cambiar objetivos / copiar hechizo | Redirect, Twincast | Elegir nuevos objetivos para la copia | R (cambiar objetivo: frame `redirect.json` — Bolt a la cara del rival + Redirect sobre el Bolt en la pila; con un solo hechizo legal Redirect auto-resuelve su objetivo [patrón any-color-mana] y en vez de GAME_TARGET llega GAME_ASK "Change this 1 of 1 target?" sin default de motor; se acepta y el nuevo objetivo [nosotros mismos] se elige por GAME_TARGET — vida 20→17 propia, rival intacto. Copiar hechizo: frame `twincast.json` — Twincast {U}{U} sobre nuestro propio Bolt: el objetivo de la copia se AUTO-RESUELVE y al crear la copia llega otro `GAME_ASK "Change this 1 of 1 target: <sim>?"` (`StackObjectImpl.chooseNewTarget`; false mantiene el objetivo original); resuelven copia y original: SIM 20→14, Twincast+Bolt en el cementerio. Driver P4 09-16) |
| Ward / hexproof visible | Adeline, Sigarda | Icono; pagar ward o hechizo contrarrestado | R (ward sin pagar: frame `ward-no-pay.json` — Lightning Bolt al Thornfist Striker (3/3, Ward {1}, `WardAbility` auténtica) cheateado al campo rival; el trigger pregunta `"Pay {1}?"` (`chooseUse` de `CounterUnlessPaysEffect`, SIN la palabra "ward") y al responder false el hechizo queda contrarrestado por reglas: Thornfist vivo con `damage:0` [distingue "contrarrestado" de "sobrevive a 3 daños"], Bolt en el cementerio propio y pila vacía. **CORRECCIÓN 2026-09-16**: Sythis, Harvest's Hand NO tiene Ward en el fork — el antiguo frame `ward.json` es en realidad un Bolt normal matándolo (premisa del driver legacy errónea). Hexproof: frame `hexproof.json` — con Slippery Bogle en el campo rival el `GAME_TARGET` del Bolt solo ofrece las dos caras (el Bogle NI es objetivo legal, a diferencia de ward donde sí se puede lanzar); Bolt a la cara, SIM 17, Bogle intacto con `rules:["Hexproof"]`; driver P4 09-16) |
| Objetivo ilegal al resolver | Bolt a criatura que se sacrifica | Log claro de "contrarrestado por reglas" | R parcial (frame `illegal-target.json`: Bolt propio a nuestro Mystic + Viscera Seer lo sacrifica en la misma ventana de prioridad antes de pasar — el Bolt se retira de la pila sin efecto; Mystic + Bolt en el propio cementerio, pila vacía. Driver P4 09-16. Pendiente: confirmar que el log/UI muestra el mensaje explícito de "sin objetivo legal") |

### 3.5 Pila y prioridad
| Mecánica | Cartas | Qué comprobar | Estado |
|---|---|---|---|
| Responder en la pila | Bolt en respuesta a Giant Growth | Pila en orden, mi prioridad obvia, resolución una a una | R (frame `response.json`: Growth vs Sting propio al Mystic, driver P4 09-15; el SIM retiene el Bolt con criaturas fuera) |
| Split second | Sudden Shock | La UI no ofrece jugar lo que no se puede | R (frame `shock.json`: Sudden Shock en la pila; driver P4 09-16, ya grabado en el lote previo pero sin reflejar aquí. Pendiente: comprobar que la UI deshabilita otras acciones mientras split second está en la pila) |
| Orden de triggers simultáneos | Dos triggers de ETB | Diálogo de orden | R (frames `trigger-order.json`: 2 Wardens+Mystic → vidas 22 **sin diálogo** — el motor auto-ordena; driver P4 09-15) |
| Triggers "may" | Solemn Simulacrum | Pregunta Sí/No con la carta origen visible | R (frame `may-trigger.json`: may→SÍ + búsqueda de Bosque vía TARGET con `possibleTargets`; driver P4 09-15) |
| F2/F4/F9, stops por fase, auto-pass, hold | — | Nunca se pasa una ventana que el jugador marcó | S |
| Pila de 10+ objetos | Storm | Scroll y legibilidad | R (frame `big-stack.json`: 10x Opt lanzados sin pasar prioridad entre medias — Opt no pide objetivo al lanzarse; driver P4 09-16) |
| Timeout de reloj de partida | Timer bajo | Aviso visible, pérdida por tiempo | S |

### 3.6 Combate
| Mecánica | Cartas | Qué comprobar | Estado |
|---|---|---|---|
| Atacar y bloquear básico | Criaturas vanilla | Girado, flechas, daño y vida | R |
| Gang block + asignación de daño | 1 atacante vs 2 bloqueadores | Reparto multi-cantidad | R (sesión 09-12) |
| Trample + deathtouch | Rhox Faithmender | Asignación letal mínima | R (frame `trample-deathtouch.json`: Elvish Warrior 2/3 + Rancor = 4/3 con deathtouch por Basilisk Collar equipado y Lure forzando el bloqueo por regla — la IA se negaba a bloquear deathtouch; el reparto llega como `GAME_GET_MULTI_AMOUNT` con un único ítem `max:4, defaultValue:1` (el default ya rebaja el letal a 1 por deathtouch) → Grizzly bloqueador muerto, SIM 20→17 (1 letal + 3 de trample) y lifelink 20→24; hallazgo de ajuste: un atacante 2/2 con Rancor moría al bloqueo [deathtouch no "pega antes"], se usó un 2/3; driver P4 09-16) |
| First / double strike | Boros Swiftblade | Dos pasos de daño visibles | R (first strike: frame `first-strike.json` — Precinct Captain ataca, el Grizzly Bears bloqueador muere en `FIRST_COMBAT_DAMAGE` y el atacante queda `damage:0` con `ABILITY_FIRST_STRIKE` en `cardIcons`; double strike: frame `double-strike.json` — Fencing Ace ataca a la cara: en `FIRST_COMBAT_DAMAGE` el rival ya está a 19 y el atacante `damage:0` (`ABILITY_DOUBLE_STRIKE`), el segundo golpe queda para el paso regular; hallazgo: `gameSoundDispatcher` comprobaba `FIRST_STRIKE_DAMAGE` [step inexistente], corregido a `FIRST_COMBAT_DAMAGE`; drivers P4 09-16) |
| Menace, "no puede bloquear", "debe bloquear" | Goblin War Drums, Lure | Bloqueos ilegales rechazados con motivo | R (menace: frame `menace.json` — Ripscale Predator [6/5 Menace] ataca sin bloqueadores pese a un único Grizzly Bears rival disponible vía cheatSetup; el servidor no ofrece ningún bloqueo de 1 sola criatura, es una restricción de regla, no una decisión de la IA; "debe bloquear": frame `must-block.json` — Lure lanzado de verdad (adjunto real; el cheatSetup no puede adjuntar auras) sobre el atacante y el Grizzly del SIM bloqueando forzado por `MustBeBlockedByAllAttachedEffect` pese a ser mala decisión; "no puede bloquear": frame `cant-block.json` — Tormented Soul destapada y única criatura propia: el Grizzly del SIM ataca y por regla la Soul no es bloqueadora elegible (`rules` "can't block and can't be blocked", icono de restricción, ataque sin bloqueo, vida 20→18; invariante `hasCantBlock`); goad/"ataca cada combate" siguen S; drivers P4 09-16) |
| Atacar a planeswalker o batalla | Multijugador con PW | Elegir defensor por atacante | R (planeswalker: frame `attack-pw.json` — Grizzly propio ataca al Tibalt, Rakish Instigator del SIM cheateado; al declarar atacante con 2+ defensores llega `GAME_TARGET "Select a player, planeswalker, or battle to attack"` con `targets=[playerId SIM, UUID del PW]` y se responde el UUID del PW + confirmar con booleano (NO usar `special`/"All attack": abre un segundo target para asignar defensor a todos); daño a lealtad 5→3, expuesta como `permanent.loyalty` (y en `counters`) con `combat[].defenderId`; batalla: frame `attack-battle.json` — Invasion of Gobakhan {1}{W} Battle—Siege cheateada **al campo PROPIO**: `Combat.setDefenders` usa `ProtectedByOpponentPredicate`, así que una batalla solo es defensor legal si su protector ∈ oponentes del atacante [con la batalla del SIM el humano nunca puede atacarla]; defiende el SIM, `defense:"3"`→`"1"` con `counters [{name:"defense"}]` y el atacante en `combat[].attackers`; driver P4 09-16) |
| Ninjutsu / trucos durante el combate | Ninja of the Deep Hours | Ventana tras declarar bloqueos | R parcial (ninjutsu: frame `ninjutsu.json` — el Ninja entra girado y atacando en `DECLARE_BLOCKERS` devolviendo el Runeclaw Bear a la mano; la habilidad va en `canPlayObjects.other` y con un único atacante legal el motor auto-elige a quién devolver; otros trucos de combate siguen —; driver P4 09-16) |
| Vigilance, goad, "ataca cada combate" | — | Badges y restricciones aplicadas | R (vigilance: frame `vigilance.json` — Vanguard of Brimaz ataca y el grupo de combate lo registra `tapped:false`, contraste con el vanilla de `combat.json`; goad: frame `goad.json` — Disrupt Decorum {2}{R}{R} goadea al Grizzly del SIM: el estado llega por DOS vías derivadas de `PermanentImpl.getRules` — `rules` con prefijo `ICON_REQUIRE` ("Goaded by … (must attack)") y `cardIcons[]` `OTHER_HAS_RESTRICTIONS` con el texto en `hint` (no hay campo `goadingPlayers` en el view); "ataca cada combate": frame `always-attack.json` — Rubblebelt Recluse (6/5 con `AttacksEachCombatStaticAbility`) cheateada al campo del SIM: en su turno ataca (el SIM manda "All attack"), queda `tapped:true` en `combat[].attackers` y la restricción llega igual que goad (`hint:"Must attack (Rubblebelt Recluse [b22])"`, `text` vacío) y persiste fuera del combate; drivers P4 09-16) |
| Combate multijugador (varios defensores) | Pod de 4 | Flechas al jugador correcto | S |

### 3.7 Elecciones y aleatoriedad
| Mecánica | Cartas | Qué comprobar | Estado |
|---|---|---|---|
| Nombrar carta | Pithing Needle | Búsqueda por texto sobre 25.000 nombres | R (frame `pithing-needle.json`: el "as it enters" llega como `GAME_CHOOSE_CHOICE` con la lista COMPLETA en `choice.choices` (~25k nombres; `keyChoices` vacío → string mode) y `searchEnabled:true`, se responde con el nombre EXACTO (`sendPlayerString`); el view fija `rules` "Chosen name: Lightning Bolt"; driver P4 09-16) |
| Elegir color, tipo de criatura, número | Cavern of Souls | Lista larga filtrable | R (tipo de criatura: frame `cavern.json` — `GAME_CHOOSE_CHOICE` con `ChoiceCreatureType`, 396 `keyChoices` con relevance-sort ["Elf (me)"…] y `searchEnabled:true`, se responde con la CLAVE ("Elf"); el view fija `rules` "Chosen type: Elf". Color y número: frame `choice-color.json` — **en 1.4.61 NO existen `GAME_CHOOSE_COLOR`/`GAME_CHOOSE_NUMBER`** (verificado en `ClientCallbackMethod`; el manejo del web es legado/fake): el color llega como `GAME_CHOOSE_CHOICE` string mode con `ChoiceColor` (`choice.choices=[White…Green]`, `keyChoices` vacío) → `sendPlayerString "Red"` y el número como `GAME_GET_AMOUNT` (`min:0, max:INT_MAX`) → `sendPlayerInteger 3`; resultados en `rules` ("Chosen color: Red" / "Chosen Number: 3"); drivers P4 09-16) |
| Modal "elige uno/dos" | Boros Charm, Kolaghan's Command | Modos deshabilitados sin objetivos | R (frame `modal.json`: modo daño vs rival, driver P4 09-15) |
| Pilas (FoF) | Fact or Fiction | Dos pilas claras para quien elige y quien separa | R (frame `fof.json`: la elección de pila llega como `GAME_CHOOSE_PILE` con las 5 reveladas en `cardsView2` y `cardsView1` vacía, se responde booleano [true=pile1]; el SIM separa con la sobrecarga `Cards` de `fireSelectTargetEvent` [`targets=null`, ids en `options.possibleTargets` y cierre con "Done" en `UI.right.btn.text`] — **fix de `SimPlayer` 2026-09-16**: con targets null elige carta por `sendPlayerUUID` [menor UUID] y cierra con `sendPlayerBoolean(false)`; antes hacía `cancel()` y dejaba pile1 vacía [0/5]; re-grabado: pile1=[Grizzly Bears] al cementerio y las 4 de pile2 a la mano; 4 tests nuevos en `SimPlayerTest`; driver P4 09-16) |
| Votación | Council's Judgment | Paso 1/2, resultados | R (frame `vote.json`: voto de un único jugador — el otro no tiene candidato — llega como `GAME_TARGET`, no como `GAME_CHOOSE_CHOICE`; Mystic del rival exiliado; driver P4 09-16, requiere cheatear también un permanente votable en el rival o el hechizo fizzlea) |
| Dados, moneda, clash | Krark's Thumb | Resultado visible en el log y en pantalla | R (frame `coin-dice.json`: Tavern Swindler activado — `{T}, Pay 3 life`: el flip pregunta `GAME_ASK "Heads or tails?"` y el efecto deja la vida en 17 [perdió] o 23 [ganó]; + Recruitment Drive {2}{W} que tira d20 y crea fichas [Knight Token ×2]; **hallazgo de contrato**: dado/moneda NO son eventos estructurados — `GameView` no tiene ningún campo roll/dice/coin; el resultado viaja SOLO como texto al chat de partida [los `GAME_INFORM` no se usan para esto] y el web lo pinta con `CHATMESSAGE` → `addLog`; el frame solo puede asertar el EFECTO [vida/fichas]; driver P4 09-16) |

### 3.8 Biblioteca e información oculta
| Mecánica | Cartas | Qué comprobar | Estado |
|---|---|---|---|
| Scry / surveil | Opt, Consider | Arriba/abajo/cementerio por carta | R (scry: frame `scry.json` — Opt, TARGET "PUT on the BOTTOM (Scry)" con UUID=abajo/declinar=arriba; surveil: frame `surveil.json` — Consider, misma forma con filtro distinto, TARGET "PUT into your GRAVEYARD (Surveil)"; drivers P4 09-16) |
| Mirar N y ordenar | Brainstorm, Ponder | Orden resultante correcto | R (Brainstorm: frame `brainstorm-order.json` — el orden NO llega como `GAME_CHOOSE_CARDS_ORDER` sino como DOS `GAME_TARGET "Select a card"` secuenciales con los ids de la mano en `data.targets` (uno por carta, cada elección va arriba al instante); el orden elegido se prueba encadenando el robo de Opt — la última devuelta queda arriba. Ponder: frame `ponder.json` — mismo patrón con el mensaje "Select a card ORDER to put on the TOP of your library (last one chosen will be topmost)", las cartas miradas en `data.cardsView1` (`zone:"LIBRARY"`) y los jugables en `options.possibleTargets` (3 y luego 2); la NO elegida se mueve la última a la cima (`PlayerImpl.putCardsOnTopOfLibrary`); `GAME_ASK "Shuffle your library?"` → NO; orden probado con doble robo [Ponder roba Grizzly, el robo natural siguiente trae Forest]. Drivers P4 09-16) |
| Tutor con "fallar la búsqueda" | Demonic Tutor, Evolving Wilds | Grid HD, filtro, poder no encontrar | R (tutor: frame `tutor.json` — Demonic Tutor trae carta a la mano; fail to find: frame `fail-to-find.json` — Evolving Wilds activada y búsqueda DECLINADA: **la vía existe**: `TargetCardInLibrary` hace `setRequired(!filter.hasPredicates())` [regla 701.15b] así que una búsqueda de básica (`FILTER_CARD_BASIC_LAND`) nace `required=false` y el `GAME_TARGET` llega con `flag:false` y `message:"Select a basic land card"` → `sendPlayerBoolean(false)` rompe el bucle y `SearchLibraryPutInPlayEffect` no mueve nada (biblioteca intacta: 53 cartas, 1 sola tierra en el campo); **Demonic Tutor es `FILTER_CARD` sin predicados** → `required=true` y por eso declinar re-preguntaba; el Swing manda exactamente el mismo `false` [botón derecho del FeedbackPanel]; **para el web**: `flag` es el `required` y hoy no se usa para ofrecer "fallar la búsqueda"; driver P4 09-16) |
| Descarte de mano revelada | Thoughtseize | Grid con la mano rival | R (frame `seize.json`: TARGET "Select a player" + carta de la mano rival al cementerio; driver P4 09-16) |
| Boca abajo: morph, manifest, disguise, cloak | Hidden Dragonslayer | Yo veo la carta; el rival ve un 2/2 anónimo; girar boca arriba | R (morph: frame `morph.json` — Den Protector boca abajo 2/2 con `faceDown`; manifest + girar boca arriba: frame `manifest-mechanic.json` — Soul Summons manifiesta un Elite Vanguard boca abajo y el giro es un CLIC directo (sin picker) que llega como `GAME_PLAY_MANA "Pay {W} <face down creature>"`, con la habilidad en `canPlayObjects.other` ["{W}: Turn this face-down permanent face up."]; `TurnFaceUpAbility`/MANIFESTED NO se registra en acciones especiales, `sendPlayerString('special')` no lo activa; disguise: frame `disguise.json` — Unyielding Gatekeeper `{1}{W}` con Disguise `{1}{W}` [la única de las 4 con disguise no-híbrido]: clic → `GAME_CHOOSE_ABILITY` "Cast … using Disguise: Disguise {1}{W} …" [en `canPlayObjects.objects[<id>].other`], pago `{3}` → 2/2 con `faceDown:true` + **`disguised:true`** [nombre "Disguise: Unyielding Gatekeeper", `manaValue:0`, subTypes vacíos] y **ward {2} SOLO en `rules`** [no en `cardIcons`, medido]; el giro boca arriba por `TurnFaceUpAbility` verificado en run exploratorio [mismo camino que manifest]; cloak: frame `cloak.json` — Vannifar, Evolved Enigma `{2}{G}{U}`: su trigger de inicio de combate **modal llega como `GAME_CHOOSE_ABILITY`** [no `GAME_CHOOSE_CHOICE`] con opciones "1. Cloak a card from your hand." / "2. Put a +1/+1 counter…" / "Cancel", se responde `sendPlayerUUID`; la carta de la mano es un `GAME_TARGET "Select a card"` con `targetZone:"HAND"` y `secondMessage` con el nombre de la fuente; el resultado es un 2/2 boca abajo con **`cloaked:true`** [`ManifestEffect.doManifestCards(cloakNotManifest=true)` → `FaceDownType.CLOAKED`], nombre "Cloak: Grizzly Bears", `manaValue:0` y ward {2} solo en `rules`; drivers P4 09-16) |
| Mirar la mano o la biblioteca del rival | Gitaxian Probe, Telepathy | Visor temporal | Gap parcial (G12-2) |

### 3.9 Permanentes y estados
| Mecánica | Cartas | Qué comprobar | Estado |
|---|---|---|---|
| Auras y equipos (mover, equipar) | Bonesplitter | Adjunto visible y movido | R |
| Contadores varios, anulación +1/−1 | Walking Ballista, Hapatra | Badges, anulación | R (Ballista) |
| Stun, shield, oil, etc. | — | Badge genérico legible | R (frame `stun-oil.json`: Incubation Sac propio con `counters` `{name:"oil",count:3}` Y el Grizzly del SIM girado con `{name:"stun",count:1}` [Rime Chill resuelto, cementerio]; los contadores de permanente llegan como CounterView en `permanent.counters`; **hallazgos**: en 1.4.61 `Rime Chill` es un `{6}{U}` vivid "hasta dos criaturas" [no "tap target"], y vivid solo cuenta 1 color porque las básicas llevan `frameColor` y `color` vacío → prompt real "Pay {5}{U}"; el TARGET "hasta dos" se cierra con un solo UUID; driver P4 09-16) |
| Fichas masivas (50+) | Krenko, Tendershoot | Agrupación, rendimiento, sin solaparse | R (frame `mass-tokens.json`: Krenko, Mob Boss + 50 Raging Goblin cheateados [los nombres duplicados en `cheatSetup.battlefield` SÍ funcionan] → habilidad activada vía `GAME_CHOOSE_ABILITY` crea X=51 fichas Goblin 1/1 `isToken` [Krenko cuenta]; 103 permanentes y 811 KB, el frame más pesado del registro; replay verificado; driver P4 09-16) |
| Clones / "se convierte en copia" | Phantasmal Image | Nombre y arte de la copia | R (frame `clones.json`: Phantasmal Image entra como copia del Mystic del SIM — `copy:true`; el lanzamiento se difiere hasta que el SIM tenga criatura; driver P4 09-16) |
| Transformar / DFC / day-night | Delver of Secrets | Cara correcta, animación | R (DFC: frame `dfc.json` — Moonmist transforma Delver of Secrets en Insectile Aberration; day-night: frame `day-night.json` — Reckless Stormseeker daybound cheateado al campo en T1 sin lanzar hechizos: en el destapado del SIM `UntapStep.handleDayNight` hace NOCHE y el DFC se transforma en Storm-Charged Slasher [`transformed:true`, `secondCardFace` con la cara daybound]; **hallazgo**: el flag day/night NO existe como campo de `GameView` [`GameState.hasDayNight` no se serializa] y `designationNames` tampoco lo lleva [no hay `DesignationType`]: la evidencia estructurada es el **hint en `rules` del DFC** ["It's currently night, active player has cast 0 spells this turn. It will not become day next turn."] y `setDaytime` informa "It has become day/night" SOLO por el chat de partida; `myHelperEmblems` llegó vacío en el frame; driver P4 09-16) |
| Phasing | Teferi's Protection | Permanentes desaparecen y vuelven | R (frame `phasing.json`: Reality Ripple hace fase al Runeclaw Bear — sigue en `me.battlefield` con `phasedIn:false` [PlayerView no filtra faseados; `BoardZone.tsx` los oculta] y el hechizo va al cementerio; driver P4 09-16) |
| Sagas, clases, casos | — | Capítulo / nivel / resuelto | R parcial (sagas: frame `saga.json` — History of Benalia lore:1 + ficha Caballero; clases: frame `class-level.json` — Wizard Class subida a nivel 3 con dos clics directos (sin `GAME_CHOOSE_ABILITY` cuando la única habilidad es "Gain a level") pagando {U}+{2}{U}+{4}{U} con 9 Islas; el nivel NO vive en `counters` ni en `classLevel` — llega como hint en `rules` ("Class level: 3", tras `"<br/><hintstart/>"`); el nivel 2 dispara robo de 2; casos: gap del motor; driver P4 09-16) |
| Mutate | Gemrazer | Pila y badge Under/Over | R (sesión 09-14) |
| Planeswalkers: activar | Liliana | Diálogo de lealtad, un uso por turno | R (frame `planeswalker.json` — Teferi, Hero of Dominaria +1 robar, lealtad 4→5 vía picker `GAME_CHOOSE_ABILITY`; driver P4 09-16) |

### 3.10 Control y jugadores
| Mecánica | Cartas | Qué comprobar | Estado |
|---|---|---|---|
| Ganar control hasta fin de turno | Act of Treason | La criatura cambia de lado y vuelve | R parcial (frame `treason.json`: Act of Treason roba el Mystic del SIM a nuestro campo; el retorno al final del turno no está en el frame; driver P4 09-16) |
| Intercambio de control | Switcheroo | Ambos lados actualizados | R (frame `switcheroo.json`: Switcheroo {4}{U} con exactamente 2 criaturas legales — mi Grizzly y el Elvish Mystic del SIM — se AUTO-elige (sin `GAME_TARGET`: `TargetImpl.tryToAutoChoose` con possibleTargets == min); el intercambio es permanente: el Mystic pasa a mi campo y el Grizzly al del SIM (`controllerName`/zona lo confirman, hechizo en mi cementerio); driver P4 09-16) |
| Controlar el turno de otro jugador | Mindslaver | Ver mano ajena y actuar por él | **Gap** (G12-1) |
| Monarch, initiative/dungeon, the ring, emblemas | — | Indicadores | R parcial (monarch: frame `monarch.json` — Palace Sentinels {3}{W} lanzado de verdad ⇒ `players[].monarch:true` en el view [PlayerView.java:154] y GameView NO tiene campo de nivel superior; **ojo**: `players[].designationNames` NO refleja Monarch [designación global del GameState] — usar el booleano; initiative: frame `initiative.json` — White Plume Adventurer ⇒ **`players[].initiative:boolean`** [PlayerView.java:60/155] con `designationNames` vacío; dungeon: frame `dungeon.json` — Find the Path ⇒ **`players[].commandList` con DungeonView** `{name:"Lost Mine of Phandelver", rules}` donde `rules[0]` = "<i>(Currently in Goblin Lair)</i>" y las salidas llevan prefijos `ICON_DUNGEON_ROOM_CURRENT`/`_NEXT` [no hay `currentRoom` ni grafo; el web hacía tracking propio y ya no lo necesita]; the ring: frame `the-ring.json` — Call of the Ring ⇒ emblem "The Ring" en `commandList` cuyo **nivel = `rules.length`** [1 línea por habilidad, clamp 1..4] y portador = `cardIcons[] RINGBEARER` [sin campo `ringBearer`; **bug corregido**: `MechanicsTray.findRingBearer` buscaba campos inexistentes]; emblemas: frame `emblem.json` — Gideon of the Trials: el emblema llega por **`players[].commandList`** como `EmblemView` ["Emblem Gideon", sin `mageObjectType`] — `myHelperEmblems` es solo para helper emblems del motor [Radiation/storm/day-night] y el web lo pinta en `.emblem-slot` por el prefijo "emblem "; drivers P4 09-16) |
| Veneno, energía, radiación | — | Contadores de jugador | R (energía: frame `energy.json` — Attune with Aether da 2 contadores `energy` al jugador; el view los expone en `PlayerView.counters` como `CounterView {name,count}` [PlayerInfoBar los pinta]; veneno: frame `poison.json` — Glistener Elf (infect) conecta al rival: `counters` con `{name:"poison",count:1}` y su VIDA sigue 20 [infect da veneno, no daño]; radiación: frame `radiation.json` — Mariposa Military Base [ETB "may" como `GAME_ASK`] da 2 rad: el contador se llama **`rad`**, no "radiation" (`CounterType.RAD`), y su trigger es **"al comienzo de la fase principal precombate de cada jugador"**, no un upkeep (`RadiationEmblem`: muele N cartas y por cada no-tierra pierde 1 vida y quita 1 rad; sin tirada de dado); en el frame muele Grizzly+Forest → vida 19 y rad 1; drivers P4 09-16) |
| Turnos extra / saltar turno | Time Warp | Indicador de turno correcto | R (frame `warp.json`: TARGET "Select a player" → uno mismo → carta en el cementerio (sin exilio ni robo, pese al comentario original del driver); driver P4 09-16) |
| Ganar/perder por efecto, empate | Approach of the Second Sun | Pantalla de fin correcta | R (frame `win-effect.json`: Approach of the Second Sun {6}{W} ×2 vía cheatSetup — la 1ª sube 7 vidas [20→27] y va 7ª desde arriba; la 2ª gana la partida [el servidor declara "Player <u> is the winner" en `GAME_OVER`]; **hallazgo/fix del harnés**: el estado de victoria solo llega en un `GAME_UPDATE` PLANO [GameView aplanado en `data`, sin clave `gameView`] y en `GAME_OVER`, y `rec-lib` solo evaluaba `m.data.gameView` → ahora acepta el `data` aplanado [si trae `players`] y `GAME_OVER`; sirve para cualquier "ganas/pierdes la partida" [`won()`/`lostForced()` → `game.end()` sin inform posterior]; driver P4 09-16) |
| Reiniciar la partida | Karn Liberated | La UI se re-inicializa | R (frame `karn-restart.json`: Karn cheateado con 6 contadores, +4 [TARGET "Select a player"] → 10 → 14 y −14; **el reinicio llega SOLO como `GAME_UPDATE`** — mismo `gameId`, sin `GAME_INIT` ni `GAME_OVER` [KarnLiberatedEffect → `clearOnGameRestart` + `Player.init/useDeck` + `game.start(null)`] y reaparece el `GAME_ASK` de mulligan; frame: turno 1 con el dueño de Karn activo, manos 7/7, bibliotecas 53/93, mesa/cementerio/pila vacíos y Karn barajado; **ojo**: las exiliadas por Karn no se recolocan de forma fiable entre runs; driver P4 09-16) |

### 3.11 Multijugador y formatos
| Mecánica | Montaje | Qué comprobar | Estado |
|---|---|---|---|
| Commander 4 jugadores | 4 cuentas o 1+3 IA | Daño de comandante, anillo de turnos, impuesto | S |
| FFA 5–10 jugadores | 1 + 5 IA | Layout standard con switcher (cae solo a standard con 5+); verificar la experiencia hojeando rivales | R pendiente (decisión §9: solo standard) |
| Two-Headed Giant | 2 vs 2 | Vida compartida, turno de equipo | S |
| Rango de influencia / atacar izquierda | FFA con rango 1 | Solo objetivos en rango | — |
| Jugador que abandona a mitad | Pod de 4 | Sus permanentes desaparecen; la partida sigue | — |
| Bo3 / Bo5 con banquillo | 2 humanos | Validación, timer, siguiente partida | R (B.10) |
| Draft 8 → construir → torneo | 2 humanos + bots | Picks, espera, construct, cuadro | R (sesión 09-14) |
| Sealed | 2 humanos | Pool de 90, tierras sugeridas | R |
| Suizo | 4+ jugadores | Emparejamientos, standings | S |
| Espectar partida y torneo | — | Sin controles de jugador, fin de partida visible (hoy no llega `END_GAME_INFO`) | R parcial |

**Aprobado de §3**: todas las filas en **R** con test o frame grabado, salvo las declaradas fuera de alcance; 0 discrepancias P2 en 200 partidas de fuzzing (P6); diferencias con escritorio (P5) documentadas y aceptadas una a una.

---

## 4. Inventario de interfaz: pantallas × estados

Cada pantalla se evalúa en **todos** estos estados (a través de la galería P3 cuando sea posible):

`vacío` · `cargando` · `error` · `desbordado` (nombres muy largos, 60+ mesas, 100+ fichas, mano de 15) · `idioma largo` (de/ru) · `CJK` (ja) · `1366×768` · `2560×1440` · `zoom 125 %` · `solo teclado` · `reduced-motion` · `desconexión`

| Pantalla | Qué mirar además | Evidencia actual |
|---|---|---|
| Setup wizard y login | Primera ejecución sin configuración, errores de conexión explicados | `setup-wizard.spec`, `join-errors.spec` |
| Lobby (mesas, filtros, chat flotante, historial, clasificación) | Actualización en vivo sin saltos, mesas que desaparecen mientras las miras | varios specs S |
| Crear mesa (wizard) | Combinaciones inválidas imposibles, resumen fiel | `wizard.spec`, `verify-wizard-matrix.mjs` |
| Sala de espera / staging | Roster, listo/no listo, cambiar mazo | `staging.spec` S, `multi-user` R |
| Editor de mazos, galería, import/export, mano de prueba | Mazos de 100 cartas, errores del validador, arrastrar | `decks-gallery.spec`, `deckvalidation.spec` |
| Partida 1v1 | Legibilidad a distancia, mano llena, pila grande, prompts encima del tablero | extenso S |
| Pod / Commander / Arena | 3 y 4 jugadores, alternar rival, anillo | `pod-bands`, `auto-pod`, `arena-board` S |
| Draft | Espera larga, recarga, 15 cartas en pantalla pequeña | `draft.spec` S + R |
| Construct y banquillo | Timer, contador de cartas, tierras básicas | S |
| Torneo / cuadro | Suizo con 8+, cuadro terminado | `tournament.spec` S |
| Fin de partida / fin de match | Resultado claro, siguiente acción obvia | `defeat.spec` S |
| Ajustes, apariencia, sonido, idioma, acerca de, ayuda | Cambios aplicados en vivo y persistidos | `settings.spec`, `zoom.spec`, `cjk.spec` |

**Checklist común a los 27 diálogos y modales**: ESC cierra (salvo prompts obligatorios), botón cerrar consistente, foco atrapado dentro y devuelto al cerrar, confirmación en acciones destructivas (conceder, abandonar mesa, borrar mazo), scroll interno si desborda, título y acción principal claros, no tapa información que el jugador necesita para decidir (p. ej. el tablero al elegir objetivo).

**Aprobado de §4**: toda la galería con captura de referencia aprobada; 0 cortes o solapes en la matriz de estados; checklist de diálogos 100 %.

---

## 5. Evaluación de UI y UX

### 5.1 Checklist de prompt (cada tipo de pregunta al jugador)
Para los ~20 tipos de prompt (target, choose ability, choice, pile, ask, select, play mana, X, amount, multi-amount, mulligan, trigger order, library order, card grid, voting, planeswalker, starting player, user request, sideboard, draft pick):

| Criterio | Pregunta |
|---|---|
| Instrucción | ¿Se lee qué hay que hacer en una frase, en el idioma del jugador? |
| Origen | ¿Se ve qué carta o efecto lo pide? |
| Opciones válidas | ¿Están resaltadas, y las inválidas no se pueden clicar? |
| Contexto | ¿Se sigue viendo el tablero necesario para decidir? |
| Salida | ¿Hay cancelar/atrás cuando el motor lo permite, y no aparece cuando no? |
| Teclado | ¿Se resuelve con teclado? |
| Tiempo | ¿Se ve el reloj si corre? |
| Acuse | ¿Hay respuesta visual en menos de 100 ms tras elegir (antes de que conteste el servidor)? |
| Vista del rival | ¿El otro jugador ve "esperando a X" en lugar de una pantalla congelada? |

Resultado: una tabla prompt × criterio con ✅/⚠️/❌, revisada en real.

### 5.2 Evaluación heurística
- 2–3 evaluadores **independientes** recorren cada pantalla de §4 con las 10 heurísticas de Nielsen + heurísticas de juego: estado del juego siempre visible (turno, fase, prioridad, vidas), respuesta inmediata, legibilidad del tablero, carga cognitiva, consistencia con convenciones de Arena/MTGO, errores prevenibles.
- Severidad 0–4 por hallazgo; consolidar; los de severidad 3–4 bloquean.

### 5.3 Prueba de 5 segundos del tablero
Mostrar 10 capturas de estados de partida reales (1v1, pod, combate, pila llena, prompt abierto) durante 5 s y preguntar: ¿de quién es el turno? ¿en qué fase? ¿cuánta vida tiene cada uno? ¿qué hay en la pila? ¿tengo que hacer algo? **Aprobado: ≥ 80 % de respuestas correctas.**

### 5.4 Latencia percibida
Medir, por acción (jugar tierra, lanzar, elegir objetivo, pasar, declarar atacantes, pick de draft), el tiempo desde el clic hasta el primer cambio visual y hasta la confirmación del servidor. **Aprobado: acuse visual < 100 ms; si el servidor tarda > 1 s, estado de espera explícito** (el fallo del draft era exactamente esto).

### 5.5 Pruebas con jugadores
- **Dos rondas de 5–8 jugadores** cada una: mitad veteranos de XMage escritorio, mitad jugadores de Arena/MTGO que nunca usaron XMage.
- Tareas sin ayuda, cronometradas, con pensar en voz alta:
  1. Instalar el launcher y llegar al lobby
  2. Importar un mazo de Arena y validarlo
  3. Jugar contra la IA hasta el turno 5
  4. Pagar un coste X y un coste híbrido
  5. Responder a un hechizo del rival con un instantáneo
  6. Bloquear con dos criaturas y repartir daño
  7. Buscar en la biblioteca con un tutor
  8. Hacer banquillo en un Bo3
  9. Completar 3 picks de draft
- **Veteranos de XMage**: repetir 3–6 en el cliente de escritorio para comparar tiempo y errores.
- Métricas: éxito por tarea, SEQ por tarea, SUS al final, incidentes "no sé qué hacer ahora".
- **Aprobado**: éxito ≥ 80 % por tarea; SUS ≥ 70; veteranos igual o más rápidos que en escritorio en 3 de 4 tareas comparadas.

### 5.6 Dogfooding estructurado y bug bash
- **Diario de partidas**: 10 partidas reales por formato (Standard, Modern, Pauper, Commander 4, draft, sealed) con plantilla P7: duración, incidencias, momentos de confusión.
- **Bug bash** de 90 min por área con charter (ej.: "explorar el pago de maná con costes raros para descubrir pagos imposibles o confusos").
- **Aprobado**: 0 incidencias bloqueantes abiertas y ≤ 3 graves por formato.

### 5.7 Sensación de juego
Las animaciones no bloquean la entrada y se pueden saltar; `reduced-motion` quita el movimiento no esencial; volúmenes y sonidos coherentes; ninguna animación se queda colgada tras recargar o reconectar.

---

## 6. Instalación, launcher y autoalojado

Como cada jugador se lo monta, **la primera hora de uso es parte del producto**.

| Escenario | Sistemas | Qué medir | Aprobado |
|---|---|---|---|
| Primera ejecución del launcher en máquina limpia | Win 10/11 x64, macOS arm64 y x64, Ubuntu | Tiempo hasta el lobby (incluido el escaneo de la BD de cartas, ~15 min en frío en CI), mensajes durante la espera | < 10 min con progreso visible y honesto |
| Avisos del sistema | Win SmartScreen, macOS Gatekeeper | Si el binario sin firmar se puede abrir y cómo se explica | Instrucciones claras o binario firmado |
| Puertos ocupados (17171, 8787, 8788) | Todos | Error comprensible o puerto alternativo | Nunca un fallo silencioso |
| Sin internet | Todos | Jugar contra la IA offline; imágenes de Scryfall no disponibles | Juego posible con placeholders |
| Actualización N → N+1 y actualización fallida | Todos | Updater firmado (minisign pendiente), vuelta a N | Sin reinstalar a mano |
| Recursos | Portátil de 8 GB | RAM y CPU del JVM (servidor + proxy) + WebView durante una partida Commander | Uso documentado; sin swapping |
| Jugar con un amigo | 2 launchers | Contra servidor público o uno hace de host en LAN | Pasos documentados y probados por un tercero |
| Autoalojado sin launcher | Guía `install.mjs` | Un desarrollador ajeno sigue la guía desde cero | Éxito sin ayuda en < 30 min (sin contar compilación) |
| Diagnóstico | Todos | Botón "Exportar diagnóstico" (P7) | El informe basta para reproducir el bug |
| Desinstalar | Todos | Datos y procesos restantes | Sin procesos Java huérfanos |

Bloqueantes actuales: JRE solo empaquetado para mac-arm64; updater sin clave de firma en GitHub.

---

## 7. Criterios de aprobado (resumen)

| Área | Umbral |
|---|---|
| Mecánicas (§3) | 100 % de filas en R o fuera de alcance declarado; 0 discrepancias P2 en 200 partidas |
| Flujo vs escritorio (P5) | Todas las diferencias documentadas y aceptadas |
| Visual (§4) | Galería completa con referencia; 0 cortes en la matriz de estados |
| Prompts (§5.1) | 0 ❌ en la tabla prompt × criterio |
| Heurística (§5.2) | 0 hallazgos de severidad 3–4 abiertos |
| Legibilidad (§5.3) | ≥ 80 % correctas |
| Latencia (§5.4) | Acuse < 100 ms en todas las acciones |
| Jugadores (§5.5) | Éxito ≥ 80 %, SUS ≥ 70 |
| Dogfooding (§5.6) | 0 bloqueantes, ≤ 3 graves por formato |
| Instalación (§6) | Todas las filas aprobadas en los 3 sistemas |

---

## 8. Orden de trabajo

1. **Base** — commitear o cerrar el draft; re-verificar `COMPONENT_PARITY.md` e `INTERACTION_COVERAGE.md` contra el código (filas desactualizadas como G12-3/G12-4); añadir WebKit y las 3 resoluciones a Playwright.
   - ✅ 2026-09-14: paridad re-verificada (filas G12-1–G12-4 apuntan al cierre U12; Mindslaver total fuera de alcance); `INTERACTION_COVERAGE` y `content.json` sin refs obsoletas. Playwright: `E2E_BROWSER=webkit` (`test:e2e:webkit`, WebKit instalado) + `E2E_VIEWPORT=AxB` (loop diario sigue en Chromium 1600×900).
   - ✅ resuelto 2026-09-15 — Hallazgo §4: `zoom.spec.ts` "tablero con 150%" fallaba a `E2E_VIEWPORT=1366x768` con `Element is outside of the viewport` en el submit de "Crear Mesa" (no era un loop de animación: con `zoom:1.5` en `<html>`, `.dlg-lg { max-height: 90vh }` deja al panel del diálogo genuinamente más alto que la pantalla física, y `.dlg-backdrop` — `position:fixed;inset:0` sin `overflow` — no tenía ninguna forma de desplazarse para alcanzar la parte recortada). Fix: `place-items: safe center` + `overflow-y: auto` en `.dlg-backdrop` (`DialogShell.css`) — centra cuando cabe, se ancla arriba y permite scroll cuando no; más `scrollbar-gutter: stable` en `.dlg-panel`/`.create-table-body`/`.setup-body` y un `scrollIntoViewIfNeeded()+click({force:true})` en el submit de `createTable()` (mismo patrón ya usado en `startMatch()`, con el mismo comentario). Verificado: `zoom.spec.ts` 3/3 en Chromium (1366×768 y viewport por defecto) y WebKit; `wizard.spec.ts`+`setup-wizard.spec.ts` 9/9 sin regresión; typecheck y unit (`CreateTableDialog.test.tsx`+`SetupWizard.test.tsx`, 30/30) verdes.
2. **Habilitadores** — P1 (trampas de modo test en el proxy) → P2 (fidelidad de render) → P3 (galería + regresión visual) → P7 (kit manual).
3. **Mecánicas en real** — recorrer §3 por bloques (3.1–3.5 primero: son todas las partidas), grabando frames (P4) y convirtiendo cada sesión en test.
4. **Diferencial y fuzzing** — P5 y P6 sobre lo ya cubierto.
5. **UX interna** — §5.1 checklist de prompts, §5.2 heurística, §5.4 latencia; corregir severidad 3–4.
6. **Jugadores ronda 1** — §5.3 y §5.5; corregir.
7. **Instalación** — §6 en máquinas limpias.
8. **Jugadores ronda 2 + dogfooding** — §5.5 con participantes nuevos y §5.6.

---

## 9. Decisiones pendientes

1. **FFA de 5–10 jugadores**: ✅ decidido 2026-09-14 — **solo posible en standard**. El switcher se queda solo en standard (en pod va el anillo); con 5+ jugadores el layout cae a standard automáticamente (`effectiveBoardLayout` en `board/boardLayout.ts`: `GameBoard` + switcher no recortan, pod/arena sí). Queda como gap menor la pestaña de daño de comandante (recorta a 4).
2. **Mindslaver y permisos de mano (G12-1)**: ✅ decidido 2026-09-14 — fuera de alcance.
3. **Replay, visores G12-2**: ✅ decidido 2026-09-14 — **visores dentro** (looked-at/companion/top-library/sideboard), **replay fuera**. (U8 generador: fuera de alcance 2026-09-14; `GAME_REDRAW_GUI`: fuera de alcance 2026-09-14.)
4. **Formatos objetivo** de la evaluación (§5.6): cuáles cuentan para el aprobado.
5. **Firma de binarios** (Windows/macOS): pagar certificados o documentar el bypass.
6. **Lenguas** que se evalúan con nativos (§4, estado idioma largo).

---

## 10. Anexo: qué queda del enfoque "servicio público"

Con distribución local, estos puntos bajan de prioridad pero no desaparecen:

- **`ws://` fijo** (`web/src/state/gateway.ts:145`): solo afecta a quien exponga el proxy por HTTPS; basta con elegir `wss://` según `location.protocol`.
- **Contraseña en `localStorage`** (`web/src/state/persistence.ts:85`): riesgo local; ofrecer "recordar" opcional.
- **Observabilidad**: se sustituye por el botón "Exportar diagnóstico" (P7).
- **Carga**: solo relevante para quien aloje para amigos; una prueba con 10 usuarios simultáneos es suficiente.
- **Legal**: aviso de no afiliación con Wizards (ya en `App.tsx:138`), atribución de Scryfall y licencias de terceros en launcher y "Acerca de".
