{
  "$schema": "https://schema.tauri.app/config/2/capability",
  "identifier": "default",
  "description": "Splash progress events and window control",
  "windows": ["splash"],
  "permissions": [
    "core:default",
    "opener:default",
    "updater:default"
  ]
}
