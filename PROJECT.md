# Project: XMage Nexus (Web) — Master Working Document

> Last updated: 2026-09-22 (**v0.2.2 preparada** — fix del proxy: `Gateway.originAllowed` rechazaba el virtual host de WebView2 en Windows (`http://tauri.localhost`), impidiendo al launcher de Windows conectar; **v0.2.1 publicada** — launcher de Windows arreglado, splash con progreso real y release con componentes desacoplados (reutiliza los de v0.2.0; el `success()` implícito de Actions obligó a un `if` explícito en `bundle` cuando `modules` queda saltado); **Tablero adaptable + arte único**: bandas que encogen/reparten cartas en campos llenos, tokens resueltos con la tabla del motor y un solo hook de arte para tablero/pila/preview; **Limpieza de archivos y documentación**: `PROJECT.md` partido, Work Log anterior a 2026-09-20 en `docs/history/worklog/`; **Pulido UI**: flechas del stack sobre el drawer, `select` unificado, `DropdownMenu` y pie de Mazos con menús; **Panel derecho eliminado**: franja de control sobre el divisor del tablero; ver Work Log). Las entradas anteriores de esta cabecera están íntegras en `docs/history/worklog/cabecera-last-updated.md`.

---

## 1. Objective

Replicate the full XMage experience (multiplayer and vs AI) with a
**modern, Arena-grade** web client: hardware-accelerated DOM/CSS animations, visual targeting,
and instantly playable **without installing anything** (one link and play).

We do not re-implement game rules: the XMage Java server remains the authoritative rules engine,
card database (+25,000 cards), and social/multiplayer backend. We build a modern client on top.

## 2. Current Status

| Phase | Name | Status |
|---|---|---|
| 0 | XMage Proxy (bridge) | ✅ Completed and verified (2026-08-08) |
| 1 | Web Client: login + lobby + rendered board | ✅ Completed and verified (2026-08-08) |
| 2 | Full Interaction: feedback, targeting, playing | ✅ **Completed and verified (2026-08-15)** — X costs, multi-target, modal choices, and counters validated via WS (human-test 83 checks) and browser E2E (spells 4/4) |
| 2.5 | 1v1 Competitive Parity (clocks, DFC/Sagas, card selection, phase stops) | ✅ **Completed (2026-08-25)** — Match Chess Clock (+buffer, F4/F9), DFC/MDFC back-face, Saga lore, HD card-selection grid, phase stops, descarte interactivo desde reveal de mano (Thoughtseize) |
| 3 | Visual Polish, Audio & Deck Builder | ✅ **Completed (2026-09-03)** — Audio 15 sfx + 3 buses, VFX donut pie + bars, Deck Builder 3.3 completo: Scryfall full syntax + help, vertical CMC sort, Plain/DCK/Arena export+clipboard, file drag-drop, Goldfish T1-3 (land drops + jugables), 9-lang |
| 3.1 | Desktop launcher (Tauri) | ✅ **Publicado** — v0.1.0 (2026-09-10), v0.2.0 (2026-09-19), v0.2.1 (2026-09-22) y v0.2.2 (2026-09-22): bundles firmados Win/macOS/Linux, servidor+proxy+JRE en el primer arranque, updater (`latest.json`) y, desde v0.2.1, componentes desacoplados del tag de la app (v0.2.2 SÍ recompila el módulo proxy: fix de `Gateway.originAllowed`). Validación en máquinas limpias pendiente (plan5 V7) |
| 5 | Advanced Formats & Tournaments (Commander 4-max, Draft 8, Swiss) | ✅ **Completed (2026-08-26)** — PodBoard 2x2 max 4 (TurnOrderRing + CommanderDamageMatrix sin tocar BoardZone.css), DraftScreen + ConstructScreen (40), TournamentBracket/Panel + Replay viewer, contrato Draft/TournamentView + proxy 12 actions |
| 6 | Wizard Crear Mesa — cierre de gaps vs desktop `NewTableDialog` | ✅ **Cerrado 2026-09-06** — re-auditoría §F (la mayoría ya estaba hecha sin documentar); delta: skill por plaza 1-10 (`seatSkills`→`SimManager`→`SimPlayer`) + skill propia, UI vetados/rondas, range/attack por flags del servidor, validación por paso, hint vivo por plaza. F6 emblemas fuera de alcance |
| MCP | Servidor MCP (`mcp/`): DevOps + jugador headless + UI Playwright | ✅ **A + C1 + C2 + UI Playwright + C3 (2026-09-11)** — arnés FixtureServer en CI sin Java, maná/interacciones complejas (special/X/orden/trigger) con auto-pass anti-flood, reconexión/resync con replay en el proxy + `data.attached`, y multi-sesión (`mage_use_session`/`mage_sessions`); `mage_cheat_setup` expone la siembra del modo test (P1) con verificación en vivo (2026-09-15) |

## 3. General Architecture

```
┌──────────────┐   WS JSON    ┌────────────────┐   XMage Protocol     ┌───────────────────┐
│  Browser     │ ───────────▶ │ Java Proxy     │ ───────────────────▶ │ XMage Server      │
│  React 19    │ ◀─────────── │ (Mage.Proxy)   │ ◀─────────────────── │ (Mage.Server)     │
│  CSS3 / SVG  │              │ Real Session   │   jboss-serialization│ 1.4.61-V1         │
└──────────────┘              └────────────────┘                      └───────────────────┘
```

- **XMage Server**: Rules engine and game state (Java, `Mage.Server`). Existing project, untouched.
- **Proxy (`Mage.Proxy/`)**: Custom Java module. Opens a real XMage session (`SessionImpl`),
  receives server callbacks, re-exposes them over WebSocket as JSON, and forwards client actions
  to the server. Necessary because browsers cannot speak jboss-serialization.
- **Web Client (`web/`)**: React 19 app. Communicates solely with the proxy,
  never directly with Mage.Common → modifying the client does not break the proxy or vice versa.

## 4. Technical Decisions (and Rationale)

| Decision | Choice | Rationale |
|---|---|---|
| Client Stack | React 19 + Vite + TypeScript + Web Audio | Hardware-accelerated CSS transforms + SVG overlays for targeting/combat; pure React DOM UI. Eliminates PixiJS/canvas overhead for crisp text, zero GPU memory leaks, and native responsiveness |
| State Architecture | **Snapshot-diff → animation**: each `GAME_UPDATE` delivers the full `GameView`; client calculates transitions (card A moved from hand→battlefield, B tapped...) and animates them | Pure testable logic; effects are data-driven rather than hardcoded per action |
| Effects | Declarative transitions (`gameTransitionEngine.ts`, `feedbackFx.ts`): animations, screen shakes, floating damage numbers, sound dispatch | High-performance CSS/DOM animations without canvas overhead |
| Distribution | Pure browser (Phases 1-2) → Tauri launcher (Phase 3) | Web link = zero installation (core advantage vs desktop Swing); Tauri = ~15 MB desktop app running the embedded proxy automatically |
| Maintainability | `types.ts` = single source of protocol truth; pure logic decoupled from rendering; minimal pinned dependencies | Real project risk is XMage protocol versions (proxy), not client-side churn |
| Performance | Shared sprite sheets, unique card textures, particle pooling, DPI-aware rendering | Game is not performance-bound; memory usage and GC pauses are kept strictly bounded |

## 5. Phase 0 — XMage Proxy (✅ Completed & Verified 2026-08-08)

### Delivered Components (`Mage.Proxy/`)

| File | Purpose |
|---|---|
| `src/main/java/.../ProxyClient.java` | Implements `MageClient`; real XMage session via `SessionImpl`; forwards server callbacks as JSON; executes web client commands |
| `src/main/java/.../Gateway.java` | WebSocket server (Java-WebSocket) at `ws://localhost:8787` |
| `src/main/java/.../JsonUtil.java` | Reflection-based JSON serializer with cycle detection (critical for huge `GameView` objects) |
| `src/main/java/.../Main.java` + `Config.java` | Startup; flags `--host --port --username --password --wsPort --httpPort` |
| `src/main/java/.../DeckJson.java` | JSON deck parsing → `DeckCardLists` |
| `src/main/resources/web/index.html` | Test page served at `http://localhost:8788/index.html` |
| `README.md` | WS protocol documentation (commands, events, examples) |
| `pom.xml` (root) | Registered `Mage.Proxy` module; `Mage.Proxy/pom.xml` with dependencies for Mage.Common |

### Real Verification (Local Server in Test Mode, `local-server/`)

Full end-to-end flow verified:
- Login/session OK · lobby broadcast (tables/users/messages) every ~2s OK
- Room chat (joinChat + messages) OK
- Create table with AI + human, join with JSON deck, `startMatch` OK
- JSON events received: `START_GAME`, `GAME_INIT` (full GameView), `GAME_UPDATE`, `GAME_ASK` (mulligan) OK

### Lessons Learned (Crucial for Phases 1-3)

1. **Proxy AND Server** must run with `--add-opens=java.base/java.io=ALL-UNNAMED` (jboss-serialization fails on JDK 17 without these flags).
2. `beta.xmage.de` is obsolete/down (2026-08-08); use `beta.xmage.today:17171` or local server.
3. `deckType` must match a **real server configuration name** (e.g. `Constructed - Modern`).
4. AI seats are filled **before** the human seat (matching the official client behavior).
5. `createTable` requires the deck's `quitRatio` to be ≥ the user's `quitRatio` (default to 100).
6. Client version must strictly match server version; local server requires `config.xml` with `${project.version}` replaced and plugins placed in `plugins/`.
7. **Connect must NEVER restart an already active session for the same user+host** (browser reloads send repeated connects). In test mode, duplicate user connections from the same host are kicked (`Session.java` anon-dedup) → restarting triggered reconnect loops (~1.5s) that cleared WS registries and killed broadcasts. Fix: idempotent connect + `synchronized`.
8. **Game events continue arriving at the proxy for ~1 min after the watcher closes** (GAME_UPDATE/GAME_OVER from ongoing AI matches). "Broadcast to 0 connections" for game events is normal; for `lobby` events it is an issue (active clients should always be present).
9. **Unmounting the board requires disconnecting the ResizeObserver**: Pixi 8 sets `renderer` to null on destroy; a leaked observer triggers `resize()` against null after unmount.
10. Log checks must be **time-windowed** (offset to test start): proxy logs are append-only across restarts and historic traces cause false positives.
11. **Real XMage Boolean Semantics**: `sendPlayerBoolean(true)` = **take mulligan**, `false` = **keep** (the official client's "Mulligan" button sends true). For priority (`GAME_SELECT`), any boolean yields priority.
12. **Real London Mulligan**: After `GAME_ASK "Mulligan down to N?"`, taking a mulligan prompts `GAME_TARGET "Select a card (N more) to put on the bottom of your library"` (card UUIDs live in `targets`, **not** in `cardsView1`) and re-prompts with N−1.
13. **"Select a starting player" is random**: `GameImpl.pickChoosingPlayer()` randomly selects who chooses; if the human wins, a blocking `GAME_TARGET` arrives requiring a player UUID response.
14. **"Any target" prompts include both players**: When validating damage in tests, select the opponent's UUID (`gameView.players[].controlled`), not the first target in `targets` (which could be oneself).
15. **macOS `/usr/bin/java` stub breaks daemon scripts**: `scripts/lib.mjs` resolves the real binary (`javaBin()`, Homebrew `openjdk@17` in `/opt/homebrew/opt` first) and `daemon()` uses the absolute path.
16. **`SESSION CALLBACK EXCEPTION - Unable to create socket` becomes persistent if dead proxy sessions linger** (killed via kill -9): old client sockets remain on the server, causing new logins to fail with endless retries. Fix: restart server and start proxy cleanly.
17. **Real Mana Payment (`GAME_PLAY_MANA`) sends no color hints**: `data.options` only provides `{"queryType":"PLAY_MANA"}`. Payment occurs by tapping mana sources on the board (UUID from `canPlayObjects` in `players[].battlefield` — taps and adds mana to pool) and **then** paying from the pool with `sendPlayerManaType`.
18. **Browser E2E tests leave matches running on the server** (test mode, `maxGameThreads`=10): long test runs saturate server threads. Restart server between heavy test batches.
19. **Port map (post-2026-08-21)**: proxy WS `8787`, proxy HTTP test page `8788` (owned by the Java process whenever the stack is up), fake FixtureServer `8789`. The 2026-08-20 choice of 8788 for fake only worked with the proxy stopped.
20. **Real-mode e2e vs scripted fixtures**: the XMage server never auto-starts IA-vs-IA tables (`startMatch` must be sent explicitly) and advances turns by priority timers. Fixture-scripted specs (fixed hands/windows, forced Sim wins) are therefore fake-only until the HumanHelper learns to HOLD priority in main instead of auto-passing.
21. **Demo AI deck must be stable** (Islands + Mountains + 4 Bolts): 16 Bolts make AI vs AI matches end in 2-3 turns, preventing spectators from seeing board interactions. Decks separated in `web/src/lobby/decks.ts` (`STABLE_DECK` demo / `DEFAULT_DECK` for human matches).

---

## 6. Phase 1 — Web Client: Login + Lobby + Rendered Board (✅ Completed & Verified 2026-08-08)

### Objective

Web application connecting to the proxy, authenticating, displaying the lobby (tables, users, chat), allowing table creation/spectating, and **rendering the board with HD card art (Scryfall)**. Flagship demo: **spectating live AI vs AI matches** running autonomously.

### Codebase Structure (`web/`)

```
web/
├── index.html
├── package.json / vite.config.ts / tsconfig.json
└── src/
    ├── main.tsx / App.tsx          — Entry point, phase routing (login/lobby/game)
    ├── net/
    │   ├── types.ts                — PROTOCOL TYPES (single source of truth, mirrors proxy JSON)
    │   ├── Gateway.ts              — WS connection, reconnect backoff, event routing, result promises
    │   └── commands.ts             — Proxy action helpers
    ├── state/
    │   ├── store.ts                — Connection and lobby state
    │   └── gameStore.ts            — Current GameView snapshot + match log
    ├── lobby/
    │   ├── LoginScreen.tsx         — host/ws/user/pass → connect
    │   ├── LobbyScreen.tsx         — Tables, users, room chat, create table
    │   └── CreateTableDialog.tsx   — Game type, format, AI count
    ├── cards/
    │   └── cardImages.ts           — Scryfall (set + number) with memory LRU + 75ms throttle, Accept + 429 Retry-After; IndexedDB 24h cache planned Phase 3
    ├── board/
    │   ├── BoardShell.tsx          — Game board viewport container & scale management
    │   ├── GameBoard.tsx           — Main 1v1 and pod board orchestration
    │   ├── BoardZone.tsx           — Symmetric zone layout: lands, creatures, non-creatures
    │   └── CardSlot.tsx            — Card slot component with tap/counters/damage badges
    └── ui/
        ├── TopBar.tsx              — Turn, phase/step, player life/mana
        └── GameLog.tsx             — Match log from GAME_UPDATE_AND_INFORM / CHATMESSAGE
```

### Quality & Continuous Integration

| Layer | Tool | Command | Status |
|---|---|---|---|
| Unit (Pure Logic) | vitest | `npm --prefix web run test` | ✅ Clean (104 files / 731 tests, 2026-09-05) |
| Web Core Coverage | vitest/v8 | `npm --prefix web run test:coverage` | ✅ 81.4% stmts / 73.8% funcs (2026-09-05) |
| Typecheck | tsc | `npm --prefix web run typecheck` | ✅ Clean |
| Production Build | vite | `npm --prefix web run build` | ✅ Clean |
| Java Proxy | Maven/JUnit 5 | `mvn -pl Mage.Proxy -am test` | ✅ 142 tests PASS (2026-09-05) |
| Headless E2E (Real Proxy) | `scripts/self-test.mjs` | `node scripts/self-test.mjs` | ✅ 15 checks PASS (2026-09-05) |
| Human vs AI E2E | Node/WebSocket | `node scripts/human-test.mjs` | ✅ PASS (2026-09-05) |
| Browser E2E | Playwright | `npm --prefix web run test:e2e` | ✅ full suite PASS (2026-09-05) |
| Interaction Coverage | vitest callbackCoverage.test.ts | `npm --prefix web run test` | ✅ 43 callbacks contabilizados · anti-drift doc↔código |
| Contract Drift Guard | CI | `gen-zod:validate` + `gen-types:validate` | ✅ up-to-date (contract → schema.generated.ts + types.generated.ts) |

- **One Command for Everything**: `node scripts/test.mjs [unit|coverage|typecheck|build|java|self-test|human-test|e2e]`.
- **Zero-Setup User Script**: `node scripts/install.mjs` (Maven build + plugins setup + npm install) → `node scripts/ctl.mjs start` → `node scripts/test.mjs`.

---

## 7. Phase 2 — Full Interaction (✅ Completed & Verified 2026-08-15)

- Comprehensive support for server interaction callbacks (`GAME_ASK`, `GAME_TARGET`, `GAME_PLAY_MANA`, `GAME_CHOOSE_*`, `GAME_SELECT`, quantities).
- Visual targeting: animated dotted lines, glowing highlight outlines on valid targets.
- Click-to-play card interactions, mode selections, X-cost resolution, and +1/+1 counters.
- Dispatches `sendPlayerAction`, `sendPlayerString`, `sendPlayerUUID`, `sendPlayerInteger` matching server asks.
- **Validated E2E**: `scripts/human-test.mjs` executes full match: starting player choice, London mulligan, land drops, turn passing, casting *Lightning Bolt*, *Blaze* (X=2), *Arc Trail* (multi-target), *Boros Charm* (modal choice), and *Walking Ballista* (X=4 counters). 83 checks PASS.

---

## 8. Phase 3 — Visual Polish, Audio & Distribution (In Progress)

- Declarative visual effect engine (`gameTransitionEngine.ts`, `feedbackFx.ts`, `soundManager.ts`): card trajectory arcs, CSS glow/outline effects, screen shake, floating $-X$ combat damage numbers, Web Audio sound pack.
- Structured event feed (`ActionFeed.tsx`) parsing rules engine logs into visual action cards.
- Tauri desktop packaging: lightweight native launcher (~15 MB) bundling embedded proxy.
- PWA support: installable browser app with offline card caching.

---

## 9. Work Log (Changelog)

> Aquí solo el registro reciente (desde 2026-09-20); lo anterior está íntegro en `docs/history/worklog/` (ver su `README.md`).
> Fila nueva **arriba**; al pasar de ~25 filas, mover las más antiguas al archivo de su periodo.

| 2026-09-22 | Release v0.2.2 preparada: publica el fix de `origin not allowed` (fila inferior) | **Bump** 0.2.2 en `launcher/src-tauri/tauri.conf.json`, `launcher/Cargo.toml`, `launcher/src-tauri/Cargo.toml` y `web/package.json` (+ `web/package-lock.json`, `launcher/Cargo.lock` vía `cargo check` en ambos crates del workspace). **Importante**: a diferencia de v0.2.1, este release usa `components_tag` VACÍO — el fix vive en `Mage.Proxy`, que es uno de los componentes desacoplados (`modules.yml`), así que hay que recompilar el motor y republicar el tarball del proxy, no reutilizar v0.2.1/v0.2.0. **Verificado**: `mvn -f Mage.Proxy/pom.xml test` (con el test nuevo de origin), `cargo check`/`cargo test` (6/6) en `launcher/` y `launcher/src-tauri/`, unit+typecheck de `web/` en verde. `site/content.json`, `ROADMAP.md` §Phase 4/Distribution y `.opencode/skill/mage-deploy/SKILL.md` actualizados. |
| 2026-09-22 | Fix: el launcher de Windows no conectaba al proxy (`origin not allowed`) | **Síntoma**: log del usuario con `[proxy] ws rejected origin=http://tauri.localhost` / `ws close ... code=1008 reason='origin not allowed'` en bucle — el launcher de Windows nunca lograba abrir el WebSocket. **Causa raíz**: `Gateway.originAllowed()` (`Mage.Proxy/.../Gateway.java`) solo acepta host `localhost`/`127.0.0.1`/`::1`; en macOS/Linux el WebView de Tauri sirve la app con el esquema custom `tauri://localhost` (host ya `"localhost"`, por eso ahí nunca falló), pero WebView2 (Windows) no soporta esquemas custom y Tauri sirve la app vía un *virtual host* `http://tauri.localhost` — mismo proceso local, pero `getHost()` devuelve `"tauri.localhost"`, que no matcheaba. **Fix**: `originAllowed` acepta también `tauri.localhost` e `ipc.localhost` (mismo patrón de virtual host de WebView2) sin tocar el resto de la política (orígenes ajenos siguen rechazados). **Verificado**: `GatewaySecurityTest` nuevo caso (`allowsTheWindowsTauriLauncherVirtualHost`) + `mvn -f Mage.Proxy/pom.xml test` en verde; jar reconstruido (`build.mjs proxy`) y proxy reiniciado; probe WS crudo en vivo contra el proxy local confirma `http(s)://tauri.localhost` conecta sin rechazo mientras `https://attacker.example` sigue devolviendo `code=1008 reason='origin not allowed'` en el log. |
| 2026-09-22 | Evaluador de London Mulligan (`docs/enhancements.md` 1.4) | **Qué**: `game/mulliganEvaluator.ts` calcula, a partir de la mano inicial, tierras/hechizos, los colores de maná producibles (parsea `{T}: Add {X}.` en `CardView.rules`, con fallback por nombre para tierras básicas) y la probabilidad hipergeométrica de robar la 3ª tierra antes del T3 (2 robos), reutilizando `computeDeckTracker` para el tamaño/composición de biblioteca restante (sin heurística duplicada). `MulliganHandEvaluator` en `game/MulliganDialog.tsx` lo muestra como fila de chips solo en la decisión inicial Keep/Mulligan (no en el paso de bottoming de London, que ya implica haberte quedado). Sin probabilidad si no hay mazo cargado (espectador) en vez de mostrar un 0 % engañoso. **Verificado**: `mulliganEvaluator.test.ts` nuevo (hipergeométrica + evaluación de mano), unit 1997/1997, typecheck, build, `styleTokens.test.ts` (CSS con `--sp-*`), `i18n.coverage.test.ts` (3 claves nuevas en los 9 idiomas) y `e2e/mulligan.spec.ts` 3/3 con captura verificada visualmente. **ROADMAP.md** §4.2 y **docs/enhancements.md** actualizados. |
| 2026-09-22 | v0.2.1 publicada (release desacoplado) y lección de Actions: el `success()` implícito salta por el grafo transitivo de `needs` | **Run 1** (`components_tag=v0.2.0`): `modules` se saltó como se esperaba y `manifest` descargó los `nexus-*` de v0.2.0 en 3 s y generó `release: 0.2.0`, pero **`bundle` salió saltado** (y `publish` detrás) aunque solo necesita `manifest`, que sí corrió: el chequeo implícito `success()` de Actions recorre los `needs` **transitivos**, y `modules` skipped lo invalidaba. **Fix**: `bundle` declara `if: !cancelled() && needs.manifest.result == 'success'` (usar una función de estado sustituye el chequeo por defecto). **Run 2** (18 min, sin recompilar el motor): check-version + manifest + 3 bundles firmados + publish → draft publicado con `--latest`. **Verificado**: `latest.json` 0.2.1 con las 3 plataformas y su firma, `components-manifest.json` con `release: 0.2.0` apuntando a v0.2.0 (los usuarios de v0.2.0 no re-descargan nada), instalador Windows (`XMage.Nexus_0.2.1_x64-setup.exe`, 9,3 MB) descarga 200. `site/content.json`, `ROADMAP.md` y esta tabla actualizados. **Nota**: el auto-update comprueba cada 24 h (`updater-last-check` en el data dir); para forzarlo, borrar ese fichero. |
| 2026-09-22 | Release v0.2.1 preparada: componentes (jre+server+proxy) desacoplados de la versión de la app | **Antes**: `gen-manifest.mjs` derivaba `release` y las URLs del tag de la app, así que **cualquier** release —aunque solo tocara el launcher— publicaba tarballs de componentes nuevos y obligaba a todos los usuarios a re-descargar ~200 MB, además de recompilar el motor (~1 h) con `modules.yml`. **Cambio** (`.github/workflows/release.yml`): nuevo input `components_tag`; vacío o igual al tag → comportamiento actual (`modules` compila y los tarballs van al release nuevo); un tag anterior → el job `modules` se salta (`if: components_tag == '' || components_tag == tag`), los `nexus-*` se descargan de ese release con `gh release download`, y el manifest se genera con `--tag <components_tag>` (release, base-url y nombres de asset de ahí); `publish` solo sube los tarballs si `modules` corrió y `manifest` declara `permissions: contents: read` para el download. La primera release desacoplada (v0.2.1) reutiliza los componentes de **v0.2.0**: quien tenga v0.2.0 ya tiene `components/0.2.0/.installed` y no descarga nada. **Bump 0.2.1** en `launcher/src-tauri/tauri.conf.json`, `launcher/Cargo.toml`, `launcher/src-tauri/Cargo.toml` y `web/package.json` (+ los dos locks). **Verificado**: YAML parsea, `cargo check`/`cargo test` OK, `gen-manifest --tag v0.2.0` con tarballs dummy produce `release: 0.2.0` y URLs de `v0.2.0`, `npm install --package-lock-only` actualizó el lock, unit+typecheck+i18n en verde. Publicada el mismo día (ver fila superior). |
| 2026-09-22 | Launcher: el arranque en Windows se quedaba en "Preparando la base de cartas" (encoding) y el splash gana progreso real | **Síntoma**: en Windows el splash esperaba los 600 s completos y moría con "proxy card database did not become ready (see logs)" aunque `proxy.log` demostraba que el proxy terminaba el scan en ~10-20 s (`card db READY in 19927ms`). **Causa raíz**: el launcher buscaba el marcador con `std::fs::read_to_string`, que falla con **todo** el fichero si no es UTF-8 válido; JUL en Windows escribe el nivel en el code page ANSI (`INFORMACIÓN` → byte `0xD3`) y el `if let Ok` descartaba la lectura en silencio. En macOS JUL es UTF-8, por eso el bug solo aparecía en el instalador de Windows. **Cambios** (`launcher/src-tauri/src/main.rs`): `wait_log` lee bytes y compara con `from_utf8_lossy`; rotación de `server.log`/`proxy.log` a `.previous.log` antes de cada arranque (el log se abre en append: sin rotar, el READY de una ejecución anterior haría abrir la ventana antes de que el proxy caliente); comprobación de puerto libre antes de arrancar el servidor y `wait_port_or_exit` (detecta la caída del hijo en vez de agotar los 240 s); `CREATE_NO_WINDOW` en Windows (se acaban las dos consolas negras vacías — la salida sigue en los `.log`); los tres fallos de arranque (server/proxy/card-db) y el panic interno pasan a códigos `ERR_*` con la **carpeta de logs** en el mensaje, traducidos a los 9 idiomas (`err_server_start`, `err_proxy_start`, `err_warming`, `err_internal`). **Splash** (`web/public/splash.html`): la barra es ya el indicador único de progreso por fases (2 → 6 → 74 % repartido entre componentes con los bytes reales → 78 → 86 → 90) con shimmer en las fases sin progreso medible, cronómetro en el warming (avanza lento hasta el 99 %), barra roja en el error y sin spinner; el porcentaje sale del texto de descarga (decía 42 % mientras la barra marcaba 14 %) y quedan solo los bytes. **Verificado**: `cargo test` 6/6 (caso no-UTF-8 y rotación incluidos), unit+typecheck+i18n de web en verde, `gen-splash-i18n --validate` OK y capturas con bridge Tauri simulado (descarga con bytes, warming a `0:15`, ready al 100 %, error en español con la ruta de logs y rojo al 90 %). |
| 2026-09-22 | Drawer de la Pila: Resolver y alternador de vista en la cabecera, entre el título y la X | **Antes**: dentro del drawer, `StackZone` pintaba su propia fila de acciones (Resolver + alternador compacta/expandida) debajo de la cabecera del drawer (`STACK (n)` … `X`), gastando una fila y dejando el botón lejos de donde se mira. **Cambio** (`game/drawerHeadSlot.ts`, `GameDrawer.tsx/.css`, `StackZone.tsx`): la cabecera del drawer expone un hueco (`game-drawer-head-actions`, oculto si está vacío) por contexto y `StackZone` teletransporta ahí sus acciones con un portal; sin drawer (uso suelto, tests) se sigue pintando en línea. Resolver mantiene su condición (`canResolve`): solo aparece cuando puedes resolver. Test en `StackZone.test.tsx` y captura del drawer con `frame:big-stack`. |
| 2026-09-22 | Tablero adaptable a campos llenos y arte único (tablero = pila = previsualización = vuelo) | **Antes**: (1) el ancho de carta de una zona (`useZoneScale`) solo dependía del alto, así que con muchas criaturas la banda desbordaba y había que hacer scroll horizontal (peor en Pod/Arena de 4); (2) el arte de los tokens se adivinaba por nombre en Scryfall y se **descartaban `imageFileName`/`imageNumber`** que manda el motor (el motor elige la variante del token por set y a veces al azar entre sets, p. ej. `XLN/Treasure/1..4`), y cada componente (`CardSlot`, `StackThumbnail`, `FloatingCardPreview`, `CardPreview`, `FlyingCardOverlay`) resolvía la imagen con su propia lista de dependencias de efecto y conservaba la imagen anterior mientras cargaba la nueva; la pila además pedía la variante `small` (otra petición distinta a la `normal` del tablero). **Cambio**: (1) `useBandFit` + `bandFit.ts` (puro, con tests): cada banda de criaturas/permanentes mide sus hijos y elige la carta más grande que cabe sin scroll, repartiéndola en 2-3 líneas (`data-band-lines`, `flex-wrap`) cuando eso da cartas mayores que una sola línea; suelo 46 px (38 en pod) y por debajo queda el scroll de siempre; las filas siguen siendo estáticas. (2) `scripts/gen-token-images.mjs` extrae la tabla `SET/Nombre[/imageNumber] → set/coleccionista` del `ScryfallImageSupportTokens` del escritorio de XMage a `web/src/data/tokenImages.generated.json` (2639 entradas, 65 KB); `cardKey` la aplica primero para tokens (`tokenTableKey`: set + `imageFileName` + `imageNumber`, con `#back` para caras traseras) y **ancla** la primera variante vista por jugador+set+nombre+P/T para que las 15 Treasures se vean iguales y cada set respete la impresión elegida en el mazo. (3) `cards/useCardImageUrl` es ya la única vía de arte: depende de la clave resuelta y nunca devuelve la imagen de otra carta; la pila usa la misma URL `normal` que el tablero. **Boca abajo y objetos propios de XMage**: los permanentes boca abajo (Morph/Manifest/Disguise/Cloak) llegaban con `cardKey` nulo porque `BoardZone` nunca pasaba `faceDown` a `CardSlot`, así que se veía el rectángulo con el texto `Morph: Nombre`. El generador incluye ahora la tabla `XMAGE/<Nombre>/<n>` de `TokenRepository` (Morph, Manifest, Disguise, Cloak, Foretell, Copy, Day/Night, Monarch, Ring, Radiation, Speed…, todas cartas de Scryfall) y `cardKey` la aplica a los boca abajo; `CardSlot` pinta esa imagen (o el dorso de la funda si no hay) con su insignia y P/T, y el vuelo la reutiliza. Para el controlador, `hiddenFaceDownName` recupera el nombre real (`Morph: Den Protector`) y `FloatingCardPreview` muestra la carta real al pasar el ratón (por nombre: el motor no revela su impresión); el rival sigue sin preview. Las copias de permanentes ya resolvían por el set/número de la original y siguen igual. **Galería**: `board:pod-4-crowded` y `board:arena-4-crowded` (16 criaturas + 14 tierras por jugador). **Límite conocido**: en Arena de 4 las columnas de rival son tan bajas que solo cabe una línea y con 16 criaturas se llega al suelo (38 px) → scroll; repartir alto entre filas de forma dinámica se descartó a propósito (las filas estáticas evitan reescalar todo al jugar la primera carta). Si cambia la tabla de tokens del escritorio: `node scripts/gen-token-images.mjs`. |
| 2026-09-22 | Cajón de la franja: no salta de `down` a `up` mientras el layout se asienta | **Antes**: al cargar una partida, la fila superior del rival (`.oz-top-row`, que contiene el abanico de su mano) mide provisionalmente más de lo que acabará midiendo (208 → 96 px durante ~230 ms por las transiciones `margin` de 250 ms y los `cardAppear` de 220 ms de las cartas). `GameStrip` medía en ese instante, veía poco hueco arriba (216 < 240) y abría el cajón hacia `down`, sobre la zona propia, para saltarlo a `up` ~50 ms después. `recorded.spec` › `planeswalker` medía en esa ventana y fallaba siempre en aislado (pasaba en la suite completa por carga). **Cambio** (`GameStrip.tsx`): pasar de `up` a `down` solo se aplica si la geometría lleva `SETTLE_MS` (200 ms) sin cambiar; cada `ResizeObserver`/rAF reinicia la espera y volver a `up` es inmediato. El clic o el foco del usuario en la franja miden y aplican al momento (ahí el layout ya está quieto), así que un `down` legítimo no parpadea. **Verificado**: 6 tests nuevos en `GameStrip.test.tsx` (geometría simulada + `ResizeObserver` controlado), unit 1959/1959, typecheck, `planeswalker` 3/3 en aislado y e2e fake `stack-priority|spells|targeting|hand-bar|arena-board|pod-bands|defeat|fidelity` 23/23 y `recorded|gallery|mutate|mutate-recorded|mechanics` 129/130 (1 skip previo). **Causa de fondo sin tocar**: el abanico de la mano del rival anima su altura al montar (transiciones de `margin` en `.hand-card-slot`); el arreglo es en el consumidor, no ahí. |
| 2026-09-21 | Anexos (Auras/Equipo): el anfitrión va delante y cada anexo asoma en una pestaña con su nombre | **Antes**: cada anexo se pintaba a tamaño completo, desplazado 16 px y **por encima** del anfitrión: tapaba su nombre, arte y texto, y los badges del anfitrión (`TR`, `CI`, `+2`, `5/3`) quedaban encima del Aura, así que parecían suyos (`Bonesplitter 5/3` en vez de `Grizzly Bears 5/3`). **Cambio** (`BoardZone.tsx`, `CardSlot.css`): el anfitrión tiene `z-index: 2` y los anexos van detrás; cada uno asoma a la derecha en una pestaña de `--attach-step` (24 px) con su nombre en vertical (`::after` con `attr(data-card-name)`, sin tocar `CardSlot`), con el primero encima del siguiente para que ninguna pestaña tape a otra (`zIndex: n - i`). Hover: el anexo se desliza 6 px y la pestaña se dora (sin subir de capa, así no tapa a su vecino); los anexos pasan a ser opacos (antes `opacity: .95` dejaba ver el texto del de debajo). El bloque duplicado del pile de mutación y del grupo pasa a un único `renderAttachments`. **Verificado**: test nuevo en `OpponentZone.test.tsx`, unit 1953/1953, typecheck, capturas antes/después y hover, e2e fake `fidelity|mechanics|mutate|mutate-recorded|recorded` 124/125. **Pendiente (no es de este cambio)**: `recorded.spec` › `planeswalker` fallaba de forma determinista en aislado; resuelto en la fila siguiente (`GameStrip`). |
| 2026-09-21 | Pod y arena: los asientos de jugadores eliminados se pliegan y el espacio pasa a los que siguen | **Antes**: un rival derrotado o que abandonaba seguía ocupando su celda entera (con un overlay encima), así que en una partida de 4 los 3 vivos se repartían el tablero como si aún hubiera 4. **Cambio**: `DefeatedSeat.tsx/.css` (nuevo) y `useSeatStates`/`isPlayerOut` en `boardShared.ts`. Un rival con `life <= 0` o `hasLeft` se pliega a una **tira**: columna de 72 px con calavera/puerta y el nombre en vertical, y los vivos absorben el ancho (`flex` con transición de 0,3 s, gobernada por `--fx-speed`, `fx-off` y `reduced-motion`). En pod, si **los dos** de una fila caen, la fila baja a 44 px con tiras horizontales ("Bob ha sido derrotado") y la otra fila gana la altura; si cae el de abajo a la derecha, la barra de mano cede ese hueco (`pod-board--br-out`). Se conserva la posición de cada asiento (no se reordenan los que quedan) y **un clic en la tira la reabre** para consultar cementerio y campo (cabecera `is-open`, overlay de derrota oculto); otro clic la pliega. Mi propio asiento nunca se pliega. Espectadores igual. **Verificado**: 12 tests nuevos (`defeatedSeats.test.tsx`), unit 1952/1952, typecheck, capturas de pod (0, 1 y 2 caídos, y abajo-derecha) y arena (1 y 2 caídos), e2e fake `arena-board|pod-bands|hand-bar|defeat|commander-turn-mark|combat-multiblock` 12/12 y `recorded`+`gallery` 125/125. **No tocado**: el layout `standard` (5+ jugadores, `OpponentSwitcherBar`) y el resto de copias de `life <= 0 || hasLeft` (`BoardZone`, `PlayerInfoBar`, `TurnOrderRing`, `OpponentSwitcherBar`, `CombatArrowsOverlay`), que podrían pasar a usar `isPlayerOut`. |
| 2026-09-21 | Stack: efectos de entrada, pulso del origen y ajustes de efectos respetados | **Antes**: el nodo verde del tope pulsaba siempre (solo respetaba `prefers-reduced-motion`, ni `fx-off` ni la velocidad), las filas aparecían de golpe y un trigger no marcaba su permanente origen. **Cambio**: (1) `pulse-node` escala con `--fx-speed` y se apaga con `html.fx-off`; (2) las filas nuevas entran con fundido + desplazamiento de 6 px (0,2 s) y un destello del borde (0,5 s) (`stackEntryIn`/`stackEntryFlash` en `StackZone.css`); (3) al entrar una habilidad en la pila su permanente origen recibe `stack-source-pulse` (0,6 s, `pulseStackSource` en `gameTransitionEngine.ts`, gobernado por `fxDuration`). Las filas usan como `key` el id del **fondo** del grupo `×N`, así que al resolverse el tope la fila se conserva (sin remontar ni repetir la entrada) y `RecordedStackEntry` registra el rect del id saliente para el vuelo de resolución. Todas las duraciones son literales en el CSS y `SOURCE_PULSE_MS` en el motor. **Verificado**: 3 tests nuevos (pulso del origen, sin pulso con efectos off, fila estable del grupo), sonda en Chromium real de `animationName/Duration` con efectos on, velocidad 2x, `fx-off` y `reduced-motion`; unit 1940/1940, typecheck y e2e fake `@spells|@targeting|@combat` 11/11. **No hecho**: distinguir contrarrestado/resuelto (el motor no lo expone en el estado; habría que leer el log). El ritmo de las animaciones solo se juzga jugando. |
| 2026-09-21 | Stack: las habilidades idénticas consecutivas se agrupan en una fila `×N` | **Antes**: tres disparos iguales (p. ej. tres Soul Warden) ocupaban tres filas. **Cambio** (`StackZone.tsx/.css`, `groupAdjacent.ts`): se funden en una sola fila con insignia `×3` y posición `#1–3` (la siguiente fila sigue en `#4`; la cabecera cuenta el total real). Solo se agrupan **habilidades** **consecutivas** con mismo controlador, tipo, carta origen, reglas y objetivos; los hechizos (incl. copias de tormenta) nunca, y tampoco las entradas que son objetivo elegible (`targetIds`), para que el clic de targeting siga siendo por entrada. La fila conserva el `data-card-id` de la del tope, así que las flechas de objetivo y el vuelo de resolución siguen saliendo de ella (los miembros ocultos comparten objetivos). **Verificado**: 6 tests nuevos (`groupAdjacent.test.ts`, `StackZone.test.tsx`), unit 1937/1937, typecheck, `stack-priority.spec` (fake) y captura de una página temporal con 3 triggers + activada + hechizo. Ojo: la guarda `styleTokens.test.ts` obliga a usar `--sp-*` en CSS nuevo. |
| 2026-09-21 | Stack: las habilidades muestran sus reglas también en vista compacta | **Antes**: en compacto una habilidad en la pila solo enseñaba el nombre de la carta origen (las reglas solo salían en vista expandida), y el icono de disparada/activada se decidía con `typeLabel.includes('Disparada')`, que se rompía en cualquier idioma distinto del español. **Cambio** (`StackZone.tsx/.css`): las habilidades enseñan su texto de reglas siempre (3 líneas máx. en compacto, texto completo en `title`; los hechizos siguen mostrándolo solo en expandido) y el icono sale de `abilityType` (`stackAbilityIcon`). **Verificado**: el orden de la pila era correcto (`SpellStack` itera del tope hacia abajo, `CardsView` es un `LinkedHashMap` y las claves UUID no se reordenan en JS), así que el `#1` es el tope; solo se quitó el alias `ordered = entries`. 2 tests nuevos en `StackZone.test.tsx`; unit 1930/1930, typecheck y `stack-priority.spec` (fake) en verde, captura revisada. |
| 2026-09-21 | Franja de fase con nombre y drawer que no tapa el HUD del rival | **Antes**: la fase actual solo se distinguía por el color de una insignia de dos letras (UP, DR, MI…) y el drawer de la Pila/Log/Chat crecía hasta el borde superior del tablero, tapando mazo, cementerio y exilio del rival en la esquina superior derecha. **Cambio**: `PhaseName` (en `PhaseBar.tsx`, `aria-live="polite"`) pinta el nombre completo de la fase junto a "Turno N" y se oculta por debajo de 1180 px; `GameStrip` calcula el alto máximo del drawer restando la parte inferior de `.oz-top-row` (observa esas filas y vuelve a medir en el siguiente fotograma) y `PREFERRED_UP` baja de 300 a 240 para que en alturas medianas siga abriendo hacia arriba en vez de invadir el campo propio. **Verificado** con capturas de `big-stack` a 1600×900 y 1180×800 (el drawer queda bajo el HUD rival), `vitest` 1928/1928, `typecheck` y 16 e2e fake (pila, fases, disparos, combate, mulligan, skips, arena, pod). **Pendiente**: mano legible en reposo (paso D) y la disposición de la partida por debajo de 820 px. |
| 2026-09-21 | Tablero: vida grande, pilas vacías compactas y bandas sin caja | **Antes**: la vida era el dato más pequeño del HUD (`--fs-base`/`--fs-sm`), cementerio, exilio y "rayo" vacíos ocupaban cada uno una carta entera (68×96) con un 0, y cada banda de criaturas/permanentes era una caja con borde y, si estaba vacía, discontinua. **Cambio**: `.life-counter` a `--fs-2xl` (`--fs-lg` en el pod compacto); `ResourceBar` añade `is-empty` a cementerio/exilio/rayo sin carta y `ResourceBar.css` los reduce a 44×62 con opacidad 0.5 y sin contador (opacidad 1 al pasar el ratón o enfocar); `BoardZone.css` quita borde y fondo de las bandas, la banda vacía deja un halo tenue y la frontera criaturas/permanentes es una línea fina dorada (`.bz-permanents-row::before`). **Verificado** con capturas de fotogramas grabados (`big-stack`, `combat`, `commander-4`) antes y después, `vitest` 1928/1928, `typecheck` y 22 e2e fake de tablero (arena, pod, mano, pilas, cross-zone, maná). **Corrección de la auditoría anterior**: los cuatro cuadros "0 0 0 0" de la esquina inferior eran las pilas (mazo, cementerio, exilio, rayo), no el maná flotante, que ya se ocultaba a cero. **Pendiente**: franja de fase y turno, carril lateral de la Pila y mano más legible (pasos C y D). |
| 2026-09-21 | Auditoría visual multi-viewport: crédito de Scryfall fuera de la partida y login alineado | **Auditoría** con FixtureServer y capturas a 1600, 1366, 1024, 820 y 390 px de login, lobby, Crear Mesa, partida 1v1 y pod de 4. **Arreglado**: (1) el crédito de Scryfall (`.app-attribution`, `position: fixed`) tapaba la vida del jugador y la mano en partida y un botón de la barra inferior del lobby móvil → nuevo `system/Attribution.tsx`; el pie ya no se pinta con `phase === 'game'`, el texto se añade a la pestaña Acerca de del `AboutModal` y en el lobby móvil (`body:has(.lobby-mobile-bottom-nav)`) se eleva sobre la barra. (2) Login: `.user-name-and-flag-grid` con `align-items: end`, así el input de usuario y el selector de país quedan a la misma altura aunque una etiqueta ocupe dos líneas. **Pendiente (no arreglado)**: la partida no tiene disposición para ≤ 820 px (pila y botón de acción se solapan con las zonas, ver ROADMAP §4.2 "Touch gestures / iPad"); insignias de fase de ~30×24 px por debajo del objetivo táctil; chips de Crear Mesa cortados en el borde del scroll sin pista visual. **Corrección de la auditoría**: el panel de la Pila y RESOLVER solo tapan cartas rivales por debajo de ~820 px de ancho; a 1024 y más caen en bandas vacías. |
| 2026-09-21 | Pulido UI (auditoría visual del resto de la app): 10 arreglos | **Auditoría** con la app abierta (lobby real contra beta + capturas de e2e fake). **Arreglado**: (1) Leaderboard: la tabla se cortaba por la derecha (STATUS recortado, scroll horizontal; necesitaba 1053 px de 988) → menos relleno de celda y panel de 1180 px; estilos en línea de la cabecera y colores de medalla pasados a CSS/tokens (`--medal-gold/silver/bronze`). (2) `PingBadge`: sin ms el servidor devolvía el texto crudo `(online: 0:00; seen: 4 sec ago)` en la cabecera → ahora `—` y el detalle en el tooltip (+test). (3) Cabecera del lobby: la navegación se movía según el ancho del ping y de «N players online» → rejilla `1fr auto 1fr` por encima de 900 px. (4) La burbuja de chat tapaba «Edit» (galería de Mazos) y «Save» (editor) → `--chat-fab-clearance` en los pies; el pie del editor agrupa las 4 exportaciones en un `DropdownMenu` (antes 8 botones con texto cortado; la aserción `.dek` de `decks-gallery.spec` abre el menú). (5) Ajustes cambiaba de alto entre secciones y el menú se movía bajo el cursor → alto fijo `min(460px, 56vh)` (Gameplay cabe sin scroll). (6) Botón de pasar prioridad: el texto traducido ya incluía «(Espacio)» y además está el `kbd` → se quita el paréntesis en `ActionButton`, el dock del botón ocupa el 100 % y el atajo ya no se recorta. **Verificado**: `tsc` limpio; vitest 207/207 (1924); e2e fake de mazos, ajustes, combate, prioridad y mano 22/22. **Segunda tanda (misma auditoría)**: (7) Diálogo de mesa: al reabrir con «Tournament / Constructed» guardado, `tournamentType` (que no se persiste) quedaba en «Booster Draft Elimination», y el resumen y el envío usaban ese tipo → nueva `tournamentTypeForCategory` (`tableKind.ts`, 4 tests) aplicada por un efecto en `useCreateTableForm`. (8) `DraftScreen`: `setBoosterLoaded` sin gateway lanzaba un rechazo sin manejar (`gateway no inicializado`) → `.catch`; el spec del draft pasó de varios rechazos a 0. (9) History: en duelos se oculta la línea «Result:» (repite el marcador; se mantiene en partidas de 3+ jugadores, donde el scoreboard solo enseña 2), el panel se centra a 1120 px y el pie vacío no se pinta. (10) Draft: diálogo con los tokens `--dlg-*`, título en la fuente de los demás diálogos y subtítulo como pastilla. **Verificado**: `tsc` limpio; vitest 207/207 (1928); e2e fake de draft, historial, torneo, wizard, invitaciones, mazos y ajustes 33/33. **Sin revisar**: login, sideboard/construct, anchos móviles y la partida con arte real de cartas |
| 2026-09-21 | Limpieza de archivos sin uso y de documentación cerrada | **Código**: borrados 5 componentes que nadie importaba (`SleevePickerModal.tsx`, `TargetingOverlay`, `GameLog`, `PlayerStatusCard`, `LobbyAside`; el CSS del sleeve se conserva porque lo usa `AppearanceSettingsModal`) y 4 sondas de Playwright sueltas en `web/` (`board-probe`, `test-board`, `test-debug`, `test-quick`); `styleBaseline.json` regenerado (solo quita las dos entradas huérfanas). **Docs**: `plan4`–`plan7`, `plan5-orquestacion` y los logs de evidencia P4/P5/P6 de `docs/qa/` movidos a `docs/history/` (con `git mv`) y todas las referencias reescritas (skills, scripts, comentarios, `PROJECT.md`, `COMPONENT_PARITY.md`); borrada `docs/history/tcg-arena-ui-replica-spec.md` (2472 líneas, no normativa). **`PROJECT.md` partido (426 KB → 65 KB)**: el 90 % era el Work Log (382 filas) y otro 6 % la línea 3 «Last updated», que había acumulado 26 KB de changelog; las 361 filas anteriores a 2026-09-20 pasaron íntegras a `docs/history/worklog/` (4 archivos por periodo + `README.md` índice), la cabecera larga a `cabecera-last-updated.md`, y el recurso MCP `mage://status/project` toma ahora las 5 filas más recientes en vez de las últimas 40 líneas (que eran lo más antiguo). Comprobado que el conjunto de filas es idéntico al original. Verificado: `tsc -b --noEmit` limpio, `vitest` 207/207 archivos, 1923/1923 tests, y `mcp` typecheck + 41 tests. **Lección**: al proponer borrar, mirar el contenido y no el número de líneas: `p5-detail-log.md`/`p6-detail-log.md` tienen 7 líneas pero cada una es un párrafo con hallazgos reales, así que se movieron en vez de borrarse. Hay un flake previo, no causado por esto: `store.test.ts` › «GAME_OVER autosaves the game log» falló una vez en la corrida completa (`getLatest` devolvió el log de otro test) y pasó 5/5 aislado y 2/2 completo. **Roadmaps condensados**: `ROADMAP.md` tiene ahora una única lista viva de pendientes (§4: validaciones humanas V5–V7, ideas de producto sin construir verificadas contra el código, deuda de pulido de UI, riesgo de flakes) y las fases 3–5 y las filas de lobby fusionadas en sus tablas §2/§3; el plan por fases cerrado (§4–§5 antiguos) está íntegro en `docs/history/roadmap-phases.md`, `lobby_roadmap.md` (100 % cerrado) pasó a `docs/history/`, `enhancements.md` se queda como catálogo con el estado de cada idea, y `plan7.md` apunta a `ROADMAP.md` §4. Corregido de paso un dato desfasado del ROADMAP («105 unit tests» → 1.900+). `site/content.json`/`status.json`: solo se ajusta un texto que citaba «lobby_roadmap §F» (las fases no cambian, todas `done`). |
| 2026-09-21 | Pulido UI: flechas del stack sobre el drawer, `select` unificado, pie de Mazos con menús, fin de partida, Ajustes, torneo y espaciado tokenizado del lobby | **Antes**: las flechas de objetivo del stack se pintaban por debajo del drawer del stack (`z-index` 28 vs 40) y desaparecían en su borde; los 36 `<select>` mezclaban la flecha nativa con 14 recetas de CSS propias; el pie de Mazos era una fila de 9 botones (backup, 4 formatos de exportación, copiar, borrar, favorito) que se partía en dos líneas. **Cambio**: `CombatArrowsOverlay` sube a `z-index:45` solo con el drawer del stack abierto (`.game:has(.game-drawer[data-tab='stack'])`, si no cruzaría la franja de fases) y ancla la flecha en la miniatura `.stack-thumb`; `select:not([multiple])` en la capa `base` de `styles.css` (flecha con tokens, `option` del tema) y los overrides pasan de `padding` a `padding-block`/`padding-inline-start` para conservar el hueco de la flecha; la columna del país del login pasa de 140 a 176 px ("United State" recortado); nueva primitiva `ui/DropdownMenu` (disparador `Button`, `aria-expanded`, Escape/clic fuera, `direction`/`align`) con 3 tests; el pie de `DecksGallery` pasa a resumen del mazo seleccionado + `Más ▾` (backup/restaurar) + Favorito + Copiar + `Exportar ▾` (.dck/Arena/Plain/.dek) + Eliminar + Editar; nueva clave `decks.favorite` ×9 (antes el botón decía "★ Todos"); fallback de `DeckBox` sin arte con degradado por identidad de color (`--mana-*`). **Verificación**: unit 1923/1923, typecheck, e2e fake de mazos 16/16 (`decks-gallery`, `deckvalidation`, `printing-preview`, `scryfall-budget`, `chat-overlap`) y matriz visual 6/6 con 30 baselines regenerados (login ×3, wizard, mazos). **Lección**: las baselines de `1600x900` NO están en la matriz por defecto (1366/1920/2560) y llevan sin regenerarse desde 2026-09-16: se ven violeta/naranja y no representan la UI actual — no auditar contra ellas. **Borradas las 48** (2026-09-21) y `test:e2e:visual[:update]` pasa a chromium 1920×1080 (`E2E_VIEWPORT`); 1600×900 queda solo como viewport del loop e2e diario. **Paso 4**: `GameEndDialog` pasa de 5 estilos de texto apilados a resumen (info `fg-2`, línea del match `fg-1`), fila de estadísticas (marcador y duración como píldoras) y una sola fila de acciones (`Descargar log` subtle + acción primaria); el aviso `match_continues` deja de pegarse a las estadísticas y el badge del ganador admite nombres largos (`--r-mid`, `overflow-wrap:anywhere`); `SettingsModal` de `lg` (1100 px) a `md` con `width:min(760px,94vw)` sobre `.dlg-panel.settings-modal` y 12 px sobre las pestañas; `TournamentBracketModal` deja de pasar `onClose` a `TournamentBracket` (dos × en el mismo diálogo; el panel en partida ya no lo pasaba). Verificación: unit 1923/1923, typecheck, e2e fake `best-of-3/5/n`, `defeat`, `finished-matches`, `tournament`, `settings` en verde y baselines regenerados (decks, login ×3, settings, wizard, torneo ×3; 54 PNG). **Hallazgo (deuda de la red visual)**: desde `17383549704` (2026-09-20, "flechas enmascaradas") 17 de las 48 entradas de la galería (`board-arena-4`, `board-pod-4`, `frame-block`, `frame-combat`, `game-hand-15`, `game-long-names`, `global-lang-game-ja`, los 7 `prompt-*`, `screen-gameend-game/match` y `screen-tournament-panel`) tienen el baseline 100 % magenta (relleno de `mask`): `.combat-arrows-overlay` mide el `game-body` entero, así que enmascararlo tapa todo el tablero y esas entradas no detectan ninguna regresión. **Resuelto el mismo día**: `gallery.spec.ts` oculta el overlay con `stylePath` (`e2e/gallery-visual.css`, `visibility:hidden`) en vez de enmascararlo; 288 baselines regenerados (0 con magenta) y matriz 6/6 verificada dos veces seguidas tras la regeneración (la geometría de flechas la siguen cubriendo los e2e de combate). Lección: tras regenerar baselines, medir el porcentaje de píxeles de relleno (`#ff00ff`) o abrir uno de cada tipo; "6/6 en verde" no prueba que la captura contenga la UI. **Corrección de la auditoría**: Construcción y Draft NO están "sin miniatura ni coste" — `ConstructScreen` ya usa `ArenaCardStrip` (arte, coste, tipo y agrupación por categoría en el mazo principal) y `DraftScreen` pinta la imagen de cada carta; la galería los muestra pelados porque sus fixtures (`BLB Card 0`) no resuelven metadatos de Scryfall y la red está bloqueada (verificado con Scryfall simulado). Para auditar estas pantallas, simular `api.scryfall.com`. Único hueco real observado: el pool de Construcción no se ordena ni agrupa (orden de llegada). **Espaciado (lobby)**: escala `--sp-*` ampliada con medios pasos (`--sp-0h` 2, `--sp-1h` 6, `--sp-2h` 10, `--sp-4h` 20 px) y las 22 hojas de `lobby/` pasan de 630 valores px sueltos en `padding`/`margin`/`gap` a tokens: primero solo los valores exactos (524, verificado sin ningún píxel de diferencia en chromium+webkit 1920×1080) y después los sueltos ajustados al vocabulario (3→4, 5→6, 7→8, 9→10, 14→16, 18→20, 22→24, 28/30→32; ~100 usos). Quedan 12 literales de layout (chat flotante 380, holgura 56, inset de icono 34, `safe-area`, `clamp`). Nueva métrica `spacing` en el ratchet `styleTokens.test.ts` (px > 1 literales en declaraciones de espaciado; el resto de la app parte de su baseline: 1321 en el resto de hojas). Verificación: unit 1923/1923, typecheck, e2e fake 264 passed / 4 skipped / 0 failed (8,5 min) y matriz visual 6/6 tras regenerar. **`decks/` migrado con el mismo método** (2026-09-21): 264 exactos + 56 ajustados (14→16, 3→4, 5→6, 18→20, 22→24, 28/30→32, 7→8, 9→10), de 322 a 2 literales; sin baselines para el editor de mazos, así que se comparó con un spec temporal (galería, diálogo de importar y editor con Scryfall simulado) antes/después del ajuste: 2–3 % de píxeles distintos (desplazamientos de 2–4 px, sin roturas). Verificación: unit 1923/1923, typecheck, e2e de mazos 16/16 y matriz visual 6/6 tras regenerar. **`ui/`, `settings/`, `appearance/` y `setup/` migrados** (2026-09-21): 133 exactos (0 píxeles de diferencia en chromium+webkit 1920×1080) + 25 ajustados; de 158 literales a 1 (más 1 negativo). Al tocar las primitivas y `DialogShell`, el cambio llega a todos los diálogos. Verificación: unit 1923/1923, typecheck, e2e fake completa 264 passed / 4 skipped / 0 failed (8,6 min) y matriz visual 6/6 tras regenerar; total de literales de espaciado en la app: 1321 → 843. Pendiente: `game/` (606), `board/` (165), `system/`, `i18n/`, `dev/` y `styles.css` con el mismo método (exactos primero, ajuste después) y los `style={{}}` inline. Limitación: las etiquetas de backup siguen siendo `export_backup_count`/`import_backup_json` ("Exportar Mazo (N)"), ambiguas frente a `Exportar ▾`; cambiarlas exige traducir ×9. |
| 2026-09-21 | Panel derecho eliminado: franja de control en el divisor del tablero | **Antes**: cabecera de 40 px + columna fija de 250 px (`.game-right-panel`, 6 pestañas y botón Pasar). **Cambio**: no hay cabecera ni columna; el tablero ocupa toda la pantalla (~+45 px de alto y +250 px de ancho). `BoardDivider` (compartido por `GameBoard`/`ArenaBoard`/`TwoHeadedBoard`) publica su elemento por `DividerSlotContext` y `GameScreen` pinta ahí por portal la nueva `game/GameStrip` (divisor de 18→36 px, sin diamante ni etiquetas de pod): izquierda turno+`PhaseBar`, centro `TurnOrderRing`/`OpponentSwitcherBar` (pills `nowrap`, scroll), derecha iconos + `GameMenu`. Sin hueco (primer render) cae a un `<header class="game-top">` con la misma franja. Iconos (`DrawerToggles`, `data-testid="drawer-tab-*"`): pila (contador verde pulsante), log, chat (no leídos), tracker, comandante (solo Commander) y mecánicas (★). Un único desplegable (`GameDrawer`) se abre bajo demanda con scroll; **mide el espacio libre** (`GameStrip`, `--dd-max` + `data-dropdown-side`, observa el tablero y sus zonas hijas) y se abre hacia arriba —tapa la zona del rival, no tus planeswalkers— o hacia abajo si arriba no cabe; `GameMenu` usa el mismo espacio. La pila se abre sola al pasar de 0 a N (sin pisar otra pestaña abierta) y se cierra sola al vaciarse; el preview de `StackZone` sale a la izquierda. `GameDock` queda en barra de prompt (portal `DockPrompt`) + botón Pasar sobre los recursos (`--dock-bottom` medido). Entradas de la pila compactas (`#n`+nombre+maná en una fila, pill del controlador en la de tipo, miniatura 34×48). **Dos bugs de fondo cazados**: (1) `backdrop-filter` en un ancestro de `FloatingCardPreview` (`position: fixed`) lo saca de pantalla — regla: nada con `filter`/`backdrop-filter`/`transform` persistente sobre un preview; (2) `scrollIntoView` de `GameChat`/`ActionFeed` desplazaba también a los ancestros con `overflow: hidden` (el tablero entero subía 123 px al abrir el chat) → ahora `scrollTo` del contenedor de mensajes. **Tests**: unit `GameStrip` (hueco del divisor, zonas, lado del desplegable), `GameDrawer` (incl. pila), `GameDock`, `StackZone` (fila compacta); helper e2e `openDrawerTab` en 8 specs; `recorded.spec` — `counterspell` exige preview dentro del viewport (verificado en rojo con el blur) y `planeswalker` que el desplegable de la pila no solape el marquee; `hand-bar.spec` pasa a hover sobre `.board-shell-divider`. Unit 1918/1918, typecheck y build limpios, e2e fake 261 passed + 3 fallos (2 flakes de carga —`draft`, `reduce-cost`— y 1 selector del diamante) verdes al reejecutar. **Baselines visuales** regeneradas con `scripts/gallery-visual.mjs --update` (6/6 combinaciones; verificación sin `--update` estable en chromium y webkit 1366×768, 120 PNG cambiados). El hueco vacío bajo el tablero en las entradas de galería a 1920/2560 ya existía. |
| 2026-09-20 | Rediseño UI fases 18–21: los `<button>` sueltos pasan de 259 a 91 | **Antes**: 259 `<button>` a mano con ~90 recetas de CSS propias (fondo, borde, radio, hover) para lo mismo: acciones de texto, iconos cuadrados, chips seleccionables, ítems de menú y grupos de modo. **Cambio**: `Button` gana `soft` (acento tintado), `soft-danger` y `link` y absorbe ~75 acciones (decks, lobby, draft, mulligan, torneo, setup, ajustes, diálogos con `<button>` pelado); nuevo `ui/IconButton` (`label` obligatorio → `aria-label`+`title`, `size` xs–xl, `round`, `variant` subtle/ghost/primary; 19 botones: pasos de zoom, steppers, cierre de curva, enviar chat, ojo de contraseña, ajustes/info, etc.); `ChipButton` absorbe 7 filtros más (tipo, CMC, avanzado, más palabras clave, sugeridor de básicas, tracker, chat); nuevo `ui/MenuItem` (`danger`, `selected`; 14 ítems del menú de partida, menú de pasar, contextual y selectores de idioma); 9 grupos de modo pasan a `Tabs segmented` (importar mazo, feed, comandante, pila, torneo limitado/construido, sets del draft, ámbito de triggers, velocidad de efectos, presets del login) con `aria-label` automático en pestañas solo-icono; los 13 ✕/× de borrar ya estaban en `CloseButton`. **Efectos colaterales**: `rollback-game-btn` dejó de ser un botón tintado en `RollbackDialog.css` (era código muerto tapado por `.game-menu-panel`); `aria-pressed`/`aria-selected` sustituyen a las clases `active/on/is-active` como estado. **Tests**: los ganchos por clase (`.import-submit-btn`, `.builder-done`, `.watch-btn`, `.pile-choose-btn`…) pasan a `data-testid` o a rol (`tab`); `rawButtons.test.ts` falla si aparece un `<button>` nuevo fuera de `ui/` por encima de 91; `primitives.test.tsx` 15 casos. **Balance**: CSS −1.686, TS/TSX −280 más 65 de primitivas; ≈ −1.900 netas en 131 ficheros. **Verificado**: typecheck y `npm run build` limpios, vitest 204 ficheros / 1.901 tests, e2e fake 262 passed / 4 skipped / 2 failed en la pasada completa (`skips` y `tournament` T5 con selectores obsoletos, corregidos y en verde), comparación antes/después de las 56 entradas a ojo (dos regresiones halladas y corregidas: pestañas Draft/Constructed sin ancho completo y cabecera de la pila partida en dos líneas), baselines regenerados y matriz visual 6/6 tres veces seguidas. **Lo que queda a mano (91)**: tarjetas seleccionables (mazos de draft, mangas, cartas de modo de mesa), navegación del lobby (`top-nav`, `sidebar`, `mobile-nav`), pilas y chips del `ResourceBar`, orbes de maná, `ActionButton` grande y diálogos de opciones con maquetación propia. |
| 2026-09-20 | Rediseño UI fases 12–17: estados vacíos, casillas/interruptores, chips, inputs, hooks e i18n | **Antes**: ~40 estados vacíos con CSS propio (y un `.empty-icon` global definido dos veces), 20 casillas `<label><input>` con 6 recetas, un interruptor duplicado (`.fx-toggle` = `.ui-toggle`), 13 botones ✕ de borrar/limpiar a mano, ~35 chips/badges/píldoras de estado con la misma receta y ~30 chips seleccionables, 38 reglas que repetían el estilo del `<input>` global, 7 listeners de Escape idénticos, 12 lecturas de `localStorage` repetidas en el formulario de mesa, `i18n/types.ts` de 1.566 líneas duplicando `en.ts`, 107 claves i18n × 9 idiomas sin uso y ~500 líneas de CSS sin selector vivo. **Cambio**: `ui/EmptyState` (icono/título/descripción/acción; `size`, `boxed`, `fill`, `italic`; 27 sitios) · `ui/Checkbox` (`card`, `description`, `icon`; 20 sitios) y `Switch` exportado desde `Toggle` (SoundFxControls) · `CloseButton` variante `danger` (13 botones ✕; la guarda de la X de cerrar ahora también rechaza `<button>✕</button>`) · `Chip` con `size` xs/sm/md y `pill` (35 sitios; las variantes `.lands/.spells/.turn/.rated/.tournament/.meta-format…` pasan a `tone`) y `ui/ChipButton` (`active`, `activeTone`, `aria-pressed`; 33 chips seleccionables de filtros/formatos/presets/avatares) · base `input, select, textarea` movida a `@layer base` con `:disabled` y `box-sizing`, y 38 reglas locales reducidas a lo diferencial (foco unificado en el anillo de marca) · `ui/useEscape` (7 listeners) · `useCreateTableForm` lee `localStorage` una vez (`readSaved`) · `TranslationSchema = typeof en` (menos `keywords`) · 107 claves × 9 idiomas y 66 reglas CSS sin uso eliminadas (el escáner respeta clases dinámicas por prefijo/minúsculas: `rank-badge.<tier>`) · 14 reglas adyacentes con cuerpo idéntico fusionadas. **Guardas**: `cssIntegrity.test.ts` falla con reglas vacías o comas colgando (un `@media { .a, }` rompió el build durante el trabajo), `primitives.test.tsx` (10 casos), ratchet regenerado a colores 0 / radios 4 / fuentes 31 (antes 0/6/44). **Balance**: CSS −2.003 líneas, i18n −2.520, TS/TSX −227 más 247 de primitivas y tests nuevos; ≈ −4.500 netas en 136 ficheros. **Verificado**: typecheck y `npm run build` limpios, vitest 203 ficheros / 1.895 tests, e2e fake 262 passed / 4 skipped / 2 failed en la pasada completa (dos specs con selectores de clases retiradas: `auto-answers` y `decks-gallery`), corregidos y re-ejecutados en verde, capturas antes/después de las 56 entradas revisadas a ojo (diferencias intencionadas: chips de filtro en azul de marca en vez de púrpura, estados vacíos centrados sin caja, casillas más compactas), baselines visuales regenerados (102 PNG) y matriz 6/6 tres veces seguidas. **Lección**: el codemod que quitó `.error-box button` dejó un botón «Retry» del draft sin estilo (lo delató la comparación visual) — quitar reglas de contexto exige buscar TODOS los hijos del contenedor, no solo el sitio que motivó el cambio; y una regla agrupada dentro de `@media` puede quedar con la coma colgando tras un borrado automático. |
| 2026-09-20 | Rediseño UI fase 11: componente `CloseButton` | **Antes**: el botón de cerrar (X) estaba redefinido en 22 hojas (~433 líneas de CSS en 21 clases: `dlg-close`, `settings-close`, `pile-overlay-close`, `leaderboard-close-btn`…), con tamaños de 22 a 32 px, dos apariencias (cuadrado con borde / glifo plano) y 12 botones sin `aria-label`. **Cambio**: `ui/CloseButton.tsx` (`variant` solid/plain, `size` sm/md/lg, `label` opcional; `aria-label` y `title` por defecto = `t('common','close')`) con `.ui-close*` en `primitives.css` (`@layer components`); 25 botones migrados por codemod en 25 ficheros (incluido el de `DialogShell`, que usan los 33 diálogos) conservando `className` posicional y `data-testid`; se eliminan 36 reglas de apariencia/hover y solo queda el posicionamiento (p. ej. `card-preview-close` absoluto). Se descartan del alcance los botones de "borrar filtro/búsqueda" y "eliminar" (otra semántica). **Tests**: `modalCloseCentering.test.ts` pasa de 16 casos por clase a comprobar `.ui-close` una vez, y añade una guarda que falla si un componente vuelve a hacer una X de cerrar a mano. **Verificado**: typecheck y `npm run build` limpios, vitest 202/202 (1884; −16 casos parametrizados por la guarda unificada), e2e fake 264 passed / 4 skipped / 0 failed, capturas antes/después ≤0,2 % de píxeles (cruces mejor centradas). **Regresión visual**: tras regenerar, `board:arena-4`@2560 fallaba de forma intermitente (1.279–3.504 px frente a `maxDiffPixels: 1200`, concentrados en las flechas de combate; el jitter documentado era 452–1.008) sin cambios en esa capa; se enmascara `.combat-arrows-overlay` en `gallery.spec.ts` (su geometría la cubren los e2e de combate), se regenera y la matriz da 6/6 tres veces seguidas (regenerar + 2 verificaciones). **Balance**: 51 ficheros +114/−538 en lo versionado (CSS −349, TS/TSX −65) más ~35 líneas de componente: neto ~−380. **Pendiente**: chips/pills/badges/tags (2.673 líneas), campos (534), toggles (399), estados vacíos (360), botones de borrar filtro (`*-clear`, `stat-clear-btn`, ~10 sitios). |
| 2026-09-20 | Rediseño UI fase 10: componente `Tabs` (pestañas accesibles) | **Antes**: 10 grupos de pestañas hechos a mano, cada uno con su CSS de base, hover y activo; solo `FloatingChat` tenía `role="tablist"`. **Cambio**: `ui/Tabs.tsx` (+`tabPanelProps`) con `role="tablist"/"tab"`, `aria-selected`, `aria-controls`/`id` opcionales, tabindex móvil (solo la pestaña activa recibe foco) y flechas/Inicio/Fin con selección al mover; variantes `pill`, `segmented`, `underline` y tamaños `sm/md` en `primitives.css` (`@layer components`); `data-testid` y `className` por pestaña para conservar los hooks de e2e/tests. Migrados: glosario/ayuda (`HelpWikiModal`), ranking (`LeaderboardModal`), chat flotante, selector Mis mazos/Meta (`DecksGallery`), "Acerca de", caras de carta (`CardPreview`), mecánicas (`MechanicsTray`) y panel derecho de la partida (`GameScreen`, 6 pestañas con insignias). El CSS de esas pestañas queda solo con layout y tamaño compacto. `AvatarPickerModal` no se migra: son chips de filtro, no pestañas (irá con la familia de chips). **Tests**: `Tabs.test.tsx` (semántica, tabindex móvil, teclado con envoltura), `HelpWikiModal.test` y 4 e2e (`fidelity`, `full-flow`, `chat`, `decks-gallery`) localizan ahora `getByRole('tab', …)` en vez de `button`. **Verificado**: typecheck limpio, `npm run build` ok, vitest 202/202 (1898), e2e fake 264 (`fidelity`/`full-flow`/`chat`/`decks-gallery` corregidos y verdes; `draft.spec` recargar es el flake conocido), baselines regenerados y verificados. **Balance**: en lo versionado +146/−391 (CSS −160, TS/TSX −85) más el componente (~85 líneas) y su test; neto real ~−160 líneas. **Corrección de estimación**: la cifra de 971 líneas de pestañas de la fase 9 estaba inflada (el patrón `\btab` también captaba `table…`); las reglas reales de estos grupos eran ~294 líneas. **Lección**: al convertir botones en `role="tab"`, los `getByRole('button', {name})` de e2e y tests dejan de encontrarlos. **Pendiente** (re-medido con patrones estrictos por nombre de clase, no por subcadena): chips/pills/badges/tags 2.673 líneas (293 reglas, 57 hojas), campos 534 (54 reglas, 20 hojas), botón de cerrar 461 (46 reglas, 22 hojas: candidato a `CloseButton`, el de mejor retorno/riesgo), toggles/checks 399 (42 reglas, 19 hojas), estados vacíos 360 (40 reglas, 28 hojas). Son techos: incluyen layout propio y variantes semánticas (rangos, estados), de modo que el ahorro realista es ~40 %. |
| 2026-09-20 | Rediseño UI fase 9: `Button` como sistema único de botón (piloto en diálogos) | **Diagnóstico** (medido): el 41 % del CSS (10.584 de 25.968 líneas) eran reglas de patrones genéricos (botones 4.071 líneas/472 reglas en 64 hojas, chips 2.909, campos 1.332, pestañas 971, vacíos 567, toggles 536); `Button`/`Chip`/`Toggle` existían pero los usaban 1/2/2 ficheros, con 435 `<button>` a mano (247 con clase `*-btn` propia) y 4 sistemas paralelos (`button`, `button.primary`, `.ui-btn`, `.dlg-btn*`). **Cambio**: `Button` con variantes `primary/secondary/subtle/success/ghost/danger` y tamaños `sm/md/lg`, sin `<span>` envoltorio; `primitives.css` en `@layer components` y el `button` base de `styles.css` en `@layer base` (orden declarado en ambos ficheros: `primitives.css` se evalúa antes y fijaba el orden al revés) para que toda regla de contexto gane sin depender de la especificidad ni del orden de carga. 50 sitios migrados con codemod (`className="primary"`, `cancel-btn` y `*-cancel-btn`; `ConfirmModal`; barras de acción; `TriggerOrderDialog`, `DeckBrowser`, `SampleHandModal`, `SpectatorStagingScreen`, `Login`…); se eliminan `button.primary` global, `.dlg-btn*`, `.confirm-ok-danger`, `.trigger-btn`, ~24 reglas de cancelar/primario por pantalla y 13 variables `--dlg-*` sin uso; `.action-prompt-actions button` y `.mulligan-actions button` quedan solo con layout. Se resuelve además el choque de `.import-submit-btn`, definido a la vez en `DeckImportModal.css` y `JoinTableDialog.css`. **Lecciones**: (1) una regla de contexto que fija `background/border/color` para todos los botones de un contenedor pisa las variantes: hay que reducirla a layout y migrar también los botones hermanos; (2) las clases legacy que usan tests/e2e (`.primary`, `.trigger-btn`) pasan a `.ui-btn--primary` o a selectores por rol/contenedor. **Verificado**: typecheck limpio, vitest 200/200 (1893), e2e fake 264 (`draft.spec` recargar y `decks-gallery` hover son los flakes conocidos, pasan aislados; `trigger-order.spec` actualizado a `.ui-btn--primary`/`.trigger-actions button`), capturas antes/después de login, prompts de combate/maná/triggers, fin de partida y sala de espera, baselines visuales regenerados. **Balance**: 54 ficheros, +283/−589 líneas (CSS −289 netos, TSX 0). **Pendiente**: el resto de familias (chips/badges 2.909 líneas, campos 1.332, pestañas 971 con solo 1 `role="tablist"`, estados vacíos 567, toggles 536) y ~385 `<button>` a mano; extrapolación: 1.000–1.500 líneas más. |
| 2026-09-20 | Rediseño UI fase 8: cola de colores a 0, paleta categórica de maná/rareza y corrección de regresiones de las fases 2–3 | **Cola** (468→0 literales fuera de `tokens.css`, guarda de colores fijada en 0): pasada 3 según propiedad y color de texto del bloque (rellenos verdes/rojos/ámbar/azules → `--ok/err/warn/brand` o su `-strong` si el texto es claro; navy/verde/granate/ámbar oscuros → `--*-surface`; grises medios → `--ink-4`/`--line-3`/`--fg-3`), pasada 4 de `rgba` genéricos (`rgb(var(--ink-2-rgb)|--purple-rgb / a)`, superficies) y limpieza final (`--shade-0`, `--veil-7/8`, texto oscuro sobre ámbar → `--ink-0`). Tokens nuevos: `--ok/err/warn-strong`, `--ok/err/warn/brand-surface` (`color-mix` 22 % sobre `--ink-2`), `--mana-w/u/b/r/g/c`, `--rarity-*`. **Regresiones propias corregidas** (auditoría regla a regla contra `4f837b79969`): las pasadas 2–3 habían aplanado colores categóricos (píldoras de maná de `DeckBox` con el mismo `var()` en las 3 paradas y maná blanco ámbar; barras de color de `ArenaCardStrip`/`DeckTrackerPanel`, orbes de `ArenaFilterBar`, rareza de `CardPrintingsModal`/filtro fusionadas, `ResourceBar .mana-c` en azul) y clasificado como azul ~100 textos casi blancos por tener poco croma (`#f1f5f9`, `#edf2f7`…) y fondos casi negros como superficies coloreadas → devueltos a `--fg-1/2/3` y `--ink-*`. **Maná unificado**: una receta `--mana`/`--pip` local de la que derivan degradado, borde y brillo con `color-mix` (`FormattedText.css`, `DeckBox.css`, barras, orbes), −~100 líneas de degradados a mano; los `border-color` de `.mana-badge` eran código muerto (sin `border`). Fix de especificidad: `.mana-u{color}` de `ResourceBar` pisaba el texto de `.mana-badge` (mismo color que el fondo) → color por variante con 2 clases, comprobado renderizando los badges. 32 degradados con todas las paradas iguales → color plano. **Tests**: `a11yContrast.test.ts` calcula la razón WCAG del color resuelto (≥4,5 frente a `--ink-4`) en vez de comparar hex literales. **Verificado**: typecheck limpio, vitest 200/200 (1893), e2e fake 263 passed / 4 skipped / 1 failed (`draft.spec › recargar a mitad de draft…`, el mismo flake de fases previas; pasa aislado), baselines regenerados y verificados 6/6. **Mobile (390 px)**: revisado, 3 problemas ya existentes antes del rediseño (solape en sala de espera, asistente de crear mesa cortado, tarjetas del lobby justas), sin arreglar. |
| 2026-09-20 | Rediseño UI fase 7: colores literales a tokens (overlays, grises, semánticos) | **Medida corregida**: `styleTokens.test.ts` contaba como literal cualquier `rgb(var(--x-rgb) / a)` (~590 falsos positivos); ahora excluye `rgb(var(`, así que la cifra real de partida era **2.702** (no 3.290). **Tokens nuevos** en `tokens.css`: `--fg-hi` (#fff), `--veil-1..6` (blanco translúcido 0,04/0,07/0,11/0,16/0,22/0,30), `--shade-1..5` (negro translúcido 0,35/0,5/0,65/0,8/0,92), `--purple`/`--purple-fg`/`--purple-rgb` (categórico de mítica/comandante, sin cambio visual). **Pasada 1** (1.564 sustituciones, 97 hojas): `color: #fff`→`--fg-hi`, alfas de blanco/negro al escalón más cercano (≤0,03 de alfa), `#ffd070`/`rgba(255,208,112,a)`→`--gold-300`/`--gold-rgb`, mints/verdes/rojos/azules sueltos→`--ok`/`--err`/`--brand*`, grises lavanda/slate (`#8c96c0`, `#c4cae8`, `#e8eaf6`…)→`--fg-1/2/3`, navys→`--ink-*`; se salta `StackZone`/`CardSlot`/`ActionFeedCard` en grises por los tests de contraste. **Pasada 2** (670, 89 hojas, según propiedad): `color`→token semántico/fg por tono y luz (solo si L≥0,45), `background`/`border` navy oscuro→`--ink-0..4`, tintes con alfa (`rgba(r,g,b,a)`)→`rgb(var(--ok|err|warn|brand|purple|ink-rgb) / a)`; conserva a propósito rellenos sólidos con texto blanco y tintes oscuros (granate, verdes profundos). **Verificado**: typecheck limpio, vitest 200/200 (1893), e2e fake 264 passed / 4 skipped / 0 failed, baselines visuales regenerados (`gallery-visual.mjs --update`, 6/6). Comparación antes/después con umbral fino (>4 niveles): la pasada 1 mueve ≤13 % de píxeles en prompts y ≤3 % en pantallas, la pasada 2 hasta ~10 % por cambio de tono (violeta-navy→ink), sin cambios de estructura. Baseline de la guarda: colores 2.702→468 (81 ficheros). **Pendiente**: la cola de 468 literales (colores de maná/rareza, rellenos sólidos, sombras de brillo coloreadas); ver `styleBaseline.json`. |
| 2026-09-20 | Rediseño UI fase 6: diálogos hechos a mano y `--dlg-*` | **Auditoría**: de los 5 candidatos, solo `TournamentPanel` era un diálogo modal hecho a mano (`role="dialog"` propio, sin trampa de foco, sin Escape, z-index fijo 38); `DraftScreen`/`ConstructScreen`/`SideboardScreen` son pantallas completas que ya usan `Modal` (foco, Escape, z-index) y `DialogShell` no encaja con ellas; el `role="dialog"` de `ArenaFilterBar` es un popover de ayuda, no un modal. **Cambio**: `TournamentPanel` usa `Modal` (`testId="tournament-panel"` ahora en la sección; Escape lo minimiza; foco atrapado y restaurado; z-index del asignador) con test nuevo en `TournamentPanel.test.tsx`; los valores de `--dlg-*` en `styles.css` apuntan a tokens (`--ink-*`, `--line-*`, `--gold-*`, `--brand*`, `--fg-*`, `--shadow-3`, `--r-xl/--r-mid`); los nombres se mantienen como variables semánticas de componente. Efecto visible: el nombre de la fuente en diálogos pasa de cian `#38bdf8` a `--brand-fg` y el texto de mensaje sube de `#94a3b8` a `--fg-2`. **Verificado**: typecheck limpio, vitest 200/200 (1893), e2e fake 264 passed / 4 skipped / 0 failed, capturas antes/después ≤0,4 % de píxeles, baselines visuales regenerados (235 PNG modificados en total desde la fase 5). Baseline de la guarda: colores 3308→3290. **Pendiente**: ~3.290 colores literales (blancos/negros con alfa, requieren tokens de overlay). |
| 2026-09-20 | Rediseño UI fase 5: escala tipográfica y de radios | **Cambio**: `tokens.css` gana `--fs-4xs/3xs/base/heading` (escala completa 9→26 px) y `--r-2xs/xs/base/mid/xl`; codemod que ajusta cada `font-size` a su token más cercano dentro de ±0,5 px (897 declaraciones en 97 hojas) y cada `border-radius` de 2–16 px, 99px y 999px al suyo (573 en 93 hojas; 7→8, 5→6, 9→8, 11→10, 3→4). Quedan literales solo los tamaños de título (1,05–2,6 rem), `8px`/`0.5rem` de microtexto y `20px`/`1px` de radio. `a11yContrast.test.ts` resuelve `var(--fs-*)` desde `tokens.css` para seguir exigiendo ≥0,687 rem. **Verificado**: typecheck limpio, vitest 200/200 (1892), e2e fake 263 passed / 4 skipped / 1 failed (`draft.spec › recargar a mitad de draft…`: aserción de orden de `data-card-id`, solo falla en repeticiones 2–3 del mismo worker y pasa sola 3/3; ya fallaba en la fase 4 y `draft.spec.ts`/`DraftScreen` tienen cambios sin commitear ajenos a este trabajo); 56 capturas de galería antes/después con diferencia ≤4 % de píxeles en fuentes (AA del texto) y ≤0,09 % en radios, sin cambios de tamaño de imagen. Baseline de la guarda: fuentes 941→44, radios 579→6, colores 3308. **Baselines visuales regenerados** con `node scripts/gallery-visual.mjs --update` (336 PNG macOS, chromium+webkit × 1366×768/1920×1080/2560×1440; 231 cambian, 105 idénticos) y verificados sin `--update`: 6/6 combinaciones en verde. **Pendiente**: ~3.300 colores literales (blancos/negros con alfa); diálogos a mano de Construct/Draft/Sideboard/TournamentPanel/ArenaFilterBar a `DialogShell` y `--dlg-*` a tokens. |
| 2026-09-20 | Rediseño UI fase 4: HUD de partida (`game/` + `board/`) al sistema de tokens | **Alcance**: solo el "chrome" del HUD, mapeado mecánicamente en 44 hojas: dorados (`#ffd070`, `rgba(255,208,112,a)`…)→`--gold-*`, verdes→`--ok`, azules/celestes→`--brand*`, rojos→`--err`, grises→`--fg-1/2/3`, navys→`--ink-*` y `rgb(var(--ink-rgb|--ink-2-rgb) / a)` (nuevos en `tokens.css`); el tinte violeta del fondo del tablero (`BoardShell`, `BoardZone`, `PodBoard`: `rgba(148,130,255,a)`, `rgba(96,72,148,a)`, `#151930`…) pasa a marca/ink. **Se conserva a propósito**: púrpura categórico (mítica/comandante/manifest/planeswalker: `#a855f7`, `#c084fc`), colores de maná, sólidos con texto blanco y, en `StackZone`/`CardSlot`/`ActionFeedCard`, los grises exactos que exige `a11yContrast.test.ts` (`#cbd5e1`, `#9ca3af`) — en esas tres solo se mapean colores de acento. **Verificado**: vitest 200/200 (1892), e2e fake completo 262 passed / 4 skipped / 2 failed (`draft.spec › recargar a mitad de draft…` y `latency.spec › pick de draft…`), ambos flakes por carga en el run paralelo: pasan 12/12 aislados con `--workers=1`; capturas de tablero 2×2, Arena 4P, Commander, mano de 15, pago de maná y declarar atacantes. Baseline de la guarda: colores 3690→3308 (103 ficheros). **Pendiente**: baselines visuales (`test:e2e:visual:update`, 336 PNG); resto de literales (~3.300 colores, 579 radios, 941 tamaños de fuente), sobre todo blancos/negros con alfa y los tamaños de fuente, que requieren escala tipográfica y no mapeo mecánico. |
| 2026-09-20 | Rediseño UI fase 3: familia de mazos (Mazos, DeckBuilder, Construct, Draft, Sideboard) al sistema de tokens | **Auditoría de modales**: 33 de los diálogos ya usaban `DialogShell`; solo quedan a mano `ConstructScreen`/`DraftScreen`/`SideboardScreen` (pantallas completas sobre `Modal`), `TournamentPanel` y el popover de `ArenaFilterBar`, así que no se reestructura nada — el `DialogShell` ya da la coherencia. **Causa de la disonancia**: esas pantallas usaban una paleta propia estilo Chakra/Tailwind v1 (`#ecc94b`, `#a0aec0`, `#3182ce`, `#e2e8f0`…) y negros verdosos (`#080c10`, `#11161d`…). **Cambio**: mapeo mecánico en 21 hojas a tokens (dorados→`--gold-*`, grises→`--fg-1/2/3`, azules→`--brand*`, rojos/verdes/naranja→`--err/--ok/--warn`, `rgba(...)` de esos colores→`rgb(var(--*-rgb) / a)`) y de los negros neutros por luminancia a `--ink-0..3` (15 hojas, 35 sustituciones); `--ink-rgb` añadido a `tokens.css`. **Bug encontrado**: `ConstructScreen`/`SideboardScreen` usaban `var(--bg, …)` y `--bg` valía `#000000` global, así que el fallback nunca aplicaba y las filas salían negro puro; ahora usan `--ink-1` y `--bg` apunta a `--ink-0`. Se dejan los rojos/verdes sólidos con texto blanco (badges Commander/Freeform) para no perder contraste. **Verificado**: vitest 200/200 (1892), e2e fake 26/26 (galería completa, decks-gallery, draft, printing-preview, tournament, best-of-5, deckvalidation) y capturas de Mazos, Construct y Draft. Baseline de la guarda: colores 4029→3690. |
| 2026-09-20 | Rediseño UI fase 2: guarda de ratchet de estilos y sala de espera con `Chip`/`Button` | **Guarda** `web/src/ui/styleTokens.test.ts` (+ `styleBaseline.json`): cuenta por hoja de estilos los colores literales (`#hex`/`rgb(a)`), `border-radius` y `font-size` en px/rem fuera de `ui/tokens.css` y falla si alguna hoja supera su baseline (una hoja nueva parte de 0); solo puede bajar. Baseline actual: 104 ficheros, 4029 colores, 579 radios, 941 tamaños. Se regenera con `UPDATE_STYLE_BASELINE=1 npx vitest run src/ui/styleTokens.test.ts` tras migrar a tokens (verificado con una hoja sonda: falla con `colors 1 > 0`). **Sala de espera** (`SpectatorStagingScreen`): los 7 tags de colores distintos pasan a `Chip` (neutro salvo valorada=latón y privada=aviso) conservando clases legacy, y `InviteLinkButton` usa `Button` `sm`. **Verificado**: typecheck limpio, vitest 200/200 archivos y 1892 tests, e2e staging/invite/multi-user 13/13. Nota: `store.test.ts › GAME_OVER autosaves…` falló una vez en la suite paralela y pasó 3/3 aislado y en la re-ejecución completa (flake por carga, sin relación con estilos). |
| 2026-09-20 | Rediseño UI fase 1 (piloto Login + Lobby): tokens de diseño, primitivas `Button`/`Chip` y acento de marca azur+latón | **Diagnóstico**: ~26k líneas de CSS con ~2.100 hex y ~2.100 `rgba()` sueltos, 15+ tamaños de fuente, 12 radios, dos paletas en paralelo (acento violeta `--accent` vs azul `--dlg-primary`) y sin componente Button. **Cambio**: `web/src/ui/tokens.css` (superficies `--ink-*`, líneas `--line-*`, texto `--fg-*`, marca `--brand`/`--brand-rgb`, latón `--gold-*`, semánticos `--ok/--warn/--err`, escala `--fs-*`, `--sp-*`, `--r-*`, sombras, foco) enlazado desde `styles.css` con los alias antiguos (`--panel`, `--border`, `--text`…) apuntando a los tokens; recolor mecánico de la familia violeta/índigo (`#7c5cff`, `#a78bfa`, `rgba(124,92,255,a)`, `#6366f1`…) a `var(--brand)`/`rgb(var(--brand-rgb) / a)` en 60 ficheros y del naranja tipo Arena (`#ffa834→#ff7300`) a degradado de marca (identidad propia, sin el naranja de botón principal de Arena); base `button`/`input`/`select`/`:focus-visible` restilizada (hover por `filter`, sin `background` para no pisar botones con tema propio; sin foco en `[tabindex]` para no tapar el glow de cartas). **Primitivas**: `ui/Button.tsx` (primary/secondary/ghost/danger × sm/md/lg, icono, block) y `ui/Chip.tsx` (neutral/brand/gold/ok/warn/err, `solid`) con `ui/primitives.css`. **Login**: card con hairline latón, presets como control segmentado, CTA primario de marca. **Lobby**: `TableCard` migrado a `Chip` conservando las clases legacy (tests/e2e), color solo donde informa (torneo=latón, estado=ok/marca/warn/neutro, valorada=latón, anfitrión=latón); cabecera con pestañas planas y `New Table` primario; título de mesa en `--font-display`. **Verificado**: typecheck limpio, vitest 1890/1890, e2e fake 41/41 (galería completa 160+ entradas sin `pageerror`, wizard, staging, settings, zoom…) y capturas antes/después de Login, Lobby, Mazos, Ajustes, tablero y prompt de objetivo. **Pendiente**: regenerar baselines visuales (`npm run test:e2e:visual:update`, opt-in, 336 PNG por plataforma); guarda de ratchet que falle si crecen los colores/tamaños literales fuera de `tokens.css`; migrar el resto de pantallas (Mazos, DeckBuilder, modales a `DialogShell`, HUD de partida) y sustituir `--dlg-*` por tokens; el púrpura `#a855f7` del tablero (mítica/comandante) se deja como color categórico. |
| 2026-09-20 | Presupuesto de peticiones a Scryfall: cliente único con cola, 429 y caché persistente | **Auditoría**: solo `cardImages.ts` (tablero) limitaba el ritmo; el editor de mazos lanzaba una ráfaga sin control — abrir un mazo de 100 cartas disparaba `useDeckMetadata` con una petición a `api.scryfall.com` por impresión a la vez (medido en e2e: **64 peticiones en una ventana de 1 s** con 60 cartas; el límite de Scryfall es ~10/s y bloquea la IP si se abusa). Otros 9 puntos hacían `fetch` directo (`DeckInspectorModal`, `DeckBox`, `DecksGallery`, `CardPrintingsModal`, `DraftScreen`, `SideboardScreen`, `ConstructScreen`, `cardLocalization`, `scryfallSearch` con su throttle propio), `DeckBrowser` usaba `api.scryfall.com/…?format=image` como `<img src>` (cada tarjeta = una petición de API) y la caché `xmage-card-images-v1` se leía pero nadie la escribía. **Fix**: `web/src/cards/scryfallClient.ts` — cola global (100 ms entre peticiones, 4 en vuelo, `urgent` para las de usuario como búsqueda/impresiones, pausa común de todos los pedidos al recibir 429 con `Retry-After`, hasta 3 intentos, timeout medido desde que sale la petición y no desde que se encola) + `scryfallJson` con caché en memoria (incluye 404), deduplicación en vuelo y caché IndexedDB de 7 días con el JSON adelgazado (~1 KB/carta); `scryfallCards.fetchCardJson` (localizada→inglés→nombre) y `useCardArtUrl` (arte por URL de CDN). Los 9 puntos y `cardImages.ts` migrados; el set se normaliza a minúsculas para compartir caché. **Verificación**: `scryfallClient.test.ts` (14 tests: ritmo, tope de concurrencia, 429/Retry-After, agotar intentos, urgencia, abort, dedupe, 404, fallos transitorios sin cachear, adelgazado, localizada→inglés), e2e `scryfall-budget.spec.ts` (60 cartas: **≤10 peticiones/s y ≤4 simultáneas**; rojo con el hook antiguo: 64/s), unit 1890/1890, typecheck, build, `@decks`+`@draft` 18/18. Regla en `web/AGENTS.md`. |
| 2026-09-20 | Galería de mazos: tick verde en mazos Commander con pareja de comandantes ilegal | **Diagnóstico**: `DeckBox` llamaba `validateDeckForFormat(deck)` sin `metaMap`, así que todas las comprobaciones que dependen del oráculo (elegibilidad, pareja Partner/Trasfondo/Doctor, identidad de color, legalidad) se saltaban y solo quedaban tamaño/copias ⇒ ✓ aunque el builder marcara error. **Fix**: `DeckBox` carga con `useDeckMetadata` solo los metadatos de los comandantes designados (1–2 fetches Scryfall por mazo Commander, sin tocar el resto de la lista) y valida con ellos; el badge de aviso muestra `⚠ N/mín` si falta tamaño y `⚠ <formato>` si el problema es otro (tooltip con la lista). Limitación conocida: la identidad de color y las prohibiciones del resto de cartas solo se comprueban dentro del builder (requeriría metadatos de todas las cartas de cada mazo en la galería). **Verificación**: `DeckBox.test.tsx` +2 (rojo sin el fix, verde con él), unit 1876/1876, typecheck, `decks-gallery.spec` 9/9 (fake). |
| 2026-09-20 | Diálogo de problemas del mazo sin salida: "Andúril → Anduril" bloqueaba unirse/crear partida | **Diagnóstico**: `DeckIssuesDialog` (pre-unión) solo ofrecía "Quitar N y jugar" cuando había cartas **rechazadas** (`missing`); con solo cartas que el servidor **carga como otra** (`mismatches`) el único botón era Cancelar (más las sugerencias, que no existen si el nombre lleva tilde) ⇒ el jugador no podía aceptar el aviso ni continuar. Además `DeckValidation.sameName` comparaba con `equalsIgnoreCase`, así que `Andúril`/`Anduril` (misma carta, mismo set/número) se marcaba como mismatch solo por el diacrítico. **Fix**: (1) botón `Aceptar y jugar` (`deck-issues-accept-and-play`, `issues_accept_and_play` ×9) visible cuando `missing=0 && mismatches>0`, resuelve con el mazo tal cual (el servidor lo acepta igual); no aparece si hay rechazadas; (2) `sameName` pliega diacríticos y mayúsculas (`Normalizer` NFD + `\p{M}`), de modo que esos casos ya no generan aviso. **Verificación**: `DeckIssuesDialog.test.tsx` (2 tests: aceptar devuelve el mazo, no se ofrece con rechazadas), `DeckValidationTest.accentDifferenceInNameIsNotAMismatch` (11/11), unit 1874/1874 + typecheck, jar del proxy reconstruido y proxy reiniciado. |
| 2026-09-20 | Mito derribado: el "handshake roto de `beta.xmage.today`" era un username de más de 14 caracteres | **Diagnóstico**: `AGENTS.md`, `ROADMAP.md`, `readme.md`, `docs/testing.md` y `docs/deploy-playit.md` afirmaban desde Fase 0 que el login anónimo a beta fallaba de forma intermitente por un bug de handshake *del servidor remoto*, "no arreglable por el proxy", y degradaban beta a best-effort. Es **falso**. `Can't receive server state before other data` (fork `SessionImpl.java:621`) es un **síntoma de cualquier login fallido**, no una causa: `connectStart()` pide `getServerState()` **después** del login, así que al fallar éste `serverState` queda `null` y el `SHOW_USERMESSAGE` con el motivo real dispara ese `logger.error`. En `.run/proxy.out.log` las 205 ocurrencias correlacionan 1:1 con `Logging: FAIL` y se reparten **114 en `localhost`/`127.0.0.1` vs 72 en beta** — nunca fue específico de beta. **Causa raíz**: el servidor rechaza todo nombre de más de `maxUserNameLength` (**14**, `local-server/config/config.xml:48`, validado en el fork `Mage.Server/.../Session.java:153`) con `User name may not be longer than 14 characters`. La "intermitencia" era el largo variable de los nombres generados (`warmup-825244967` = 16 fallaba; `warmup-510227` = 13 pasaba). **Medido en vivo contra beta** (probe WS por el proxy): barrido de longitud 13 ✅ / 14 ✅ / 15 ❌ / 16 ❌ / 20 ❌ con el mensaje exacto del servidor, y **17/17 logins correctos** (~1,6 s) con nombres válidos ⇒ beta es estable. **Arreglo**: (1) los 5 documentos corregidos con la causa real; (2) **red anti-drift** — `FixtureServer` (`web/fixtures/fake.ts`) ahora valida el username con las reglas del servidor (`SERVER_MIN_USER_NAME_LENGTH` 3 / `SERVER_MAX_USER_NAME_LENGTH` 14 + `validateServerUserName`) y devuelve el mismo texto, cerrando el hueco que dejaba pasar nombres imposibles en fake; (3) 13 usernames generados en 7 specs e2e acortados a ≤14 (`chatov_`, `chatovn_`, `chat-`, `chatlay-`, `deck_`/`deck2_`/`deck3_`/`deck_u6_`/`deck_u7_`/`deck_rsp_`, `hist-`, `print_`, `draftrl-`) — eran drift latente que solo sobrevivía porque `LoginScreen` recorta a `maxLength={14}` y Playwright `fill()` respeta el atributo. `LoginScreen.tsx:244` y `e2e/support/start-game.ts:272` ya respetaban el límite. **Sin cambios en Java ni en el fork.** Verificado: typecheck limpio, unit **1873/1873**, e2e fake **256 passed / 4 skipped** (los 7 fallos de la corrida en paralelo — fidelity, fizzle-feed, flights, full-flow, gallery — verdes en aislado **21/21**: flakes de carga ya documentados). |
| 2026-09-20 | U4-2 revertido para mesas: la contraseña de mesa vuelve a estar operativa en `beta.xmage.today` | **Diagnóstico**: el disable de U4-2 (2026-09-14/16) se justificó con `authenticationActivated=false`, que es cierto para la contraseña de **cuenta** (el login la ignora) pero **no** para la de **mesa**: `TableController.joinTable`/`joinTournament` la comprueban incondicionalmente cuando `options.password` no está vacío (`Wrong password.`; `TableView.passworded = !options.password.isEmpty()`), también en beta. **Verificación en vivo** (proxy + 2 cuentas anónimas contra `beta.xmage.today`): mesa `QA pwd probe` con password `secret` creada por una cuenta; la segunda recibe `Wrong password.` con credencial incorrecta y entra con la correcta (lobby WS: `passworded:true`; mesa eliminada al terminar). El bloqueo era 100% de cliente y dejaba a la web sin poder crear/unirse a mesas privadas (el botón de unión quedaba muerto porque el campo deshabilitado nunca tenía valor). **Fix**: `JoinTableDialog`, `useCreateTableForm` (hook) y `CreateTable/SecurityTab` ya no calculan ni aplican `passwordDisabledOnBeta` para mesas (la password viaja en create/join de mesa y torneo); LoginScreen/SetupWizard mantienen el disable (ahí la de cuenta sí se ignora). Eliminadas `create_password_disabled_beta`/`join_password_disabled_beta` (types + 9 locales) y el CSS muerto; `JoinTableDialog.test`/`CreateTableDialog.test` reescritos para exigir campo activo en beta y password en el payload. | unit 1872/1872 (197 files) + typecheck + build ✅; UI real contra beta (Playwright MCP, capturas `create-table-password-beta.png`/`join-dialog-password-beta.png`: campo activo en Crear Mesa y Unirse, mesa `QA pwd beta` creada y visible como `Privada`, diálogo con password pre-rellena y botón habilitado, limpieza completa); protocolo verificado con 2 sesiones MCP (wrong → `Wrong password.`; correcta → join) |





---

## 10. Execution Notes

- Rule: At the completion of each step, update Section 6 (checklist) and Section 9 (changelog) with verified reality.
- Verification must always be performed against the live environment (local server + proxy), never in theory.
- Document any discoveries in Section 5 (lessons learned).
- Keep this file small: the `Last updated:` header carries only the date and the latest 2–3 items; when §9 exceeds ~25 rows, move the oldest ones verbatim to `docs/history/worklog/` (see its `README.md`).
