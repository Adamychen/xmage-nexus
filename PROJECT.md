# Project: XMage Nexus (Web) — Master Working Document

> Last updated: 2026-09-08 (U15 sistema cerrada: About + news remoto doble-feed GitHub + auto-log IndexedDB + duración fin partida + set picker tierras; suite efectiva: unit 984, typecheck, build, e2e about 1/1 + settings/decks-gallery sin regresión, i18n).

---

## 1. Objective

Replicate the full XMage experience (multiplayer and vs AI) with a
**modern, Arena-grade** web client: hardware-accelerated DOM/CSS animations, visual targeting,
and instantly playable **without installing anything** (one link and play).

We do not re-implement game rules: the XMage Java server remains the authoritative rules engine,
card database (+25,000 cards), and social/multiplayer backend. We build a modern client on top.

## 2. Current Status

| Phase | Name | Status |
|---|---|---|
| 0 | XMage Proxy (bridge) | ✅ Completed and verified (2026-08-08) |
| 1 | Web Client: login + lobby + rendered board | ✅ Completed and verified (2026-08-08) |
| 2 | Full Interaction: feedback, targeting, playing | ✅ **Completed and verified (2026-08-15)** — X costs, multi-target, modal choices, and counters validated via WS (human-test 83 checks) and browser E2E (spells 4/4) |
| 2.5 | 1v1 Competitive Parity (clocks, DFC/Sagas, card selection, phase stops) | ✅ **Completed (2026-08-25)** — Match Chess Clock (+buffer, F4/F9), DFC/MDFC back-face, Saga lore, HD card-selection grid, phase stops, descarte interactivo desde reveal de mano (Thoughtseize) |
| 3 | Visual Polish, Audio & Deck Builder | ✅ **Completed (2026-09-03)** — Audio 15 sfx + 3 buses, VFX donut pie + bars, Deck Builder 3.3 completo: Scryfall full syntax + help, vertical CMC sort, Plain/DCK/Arena export+clipboard, file drag-drop, Goldfish T1-3 (land drops + jugables), 9-lang |
| 3.1 | Desktop launcher (Tauri) | ⬜ Pendiente (Phase 4) |
| 5 | Advanced Formats & Tournaments (Commander 4-max, Draft 8, Swiss) | ✅ **Completed (2026-08-26)** — PodBoard 2x2 max 4 (TurnOrderRing + CommanderDamageMatrix sin tocar BoardZone.css), DraftScreen + ConstructScreen (40), TournamentBracket/Panel + Replay viewer, contrato Draft/TournamentView + proxy 12 actions |
| 6 | Wizard Crear Mesa — cierre de gaps vs desktop `NewTableDialog` | ✅ **Cerrado 2026-09-06** — re-auditoría §F (la mayoría ya estaba hecha sin documentar); delta: skill por plaza 1-10 (`seatSkills`→`SimManager`→`SimPlayer`) + skill propia, UI vetados/rondas, range/attack por flags del servidor, validación por paso, hint vivo por plaza. F6 emblemas fuera de alcance |

## 3. General Architecture

```
┌──────────────┐   WS JSON    ┌────────────────┐   XMage Protocol     ┌───────────────────┐
│  Browser     │ ───────────▶ │ Java Proxy     │ ───────────────────▶ │ XMage Server      │
│  React 19    │ ◀─────────── │ (Mage.Proxy)   │ ◀─────────────────── │ (Mage.Server)     │
│  CSS3 / SVG  │              │ Real Session   │   jboss-serialization│ 1.4.61-V1         │
└──────────────┘              └────────────────┘                      └───────────────────┘
```

- **XMage Server**: Rules engine and game state (Java, `Mage.Server`). Existing project, untouched.
- **Proxy (`Mage.Proxy/`)**: Custom Java module. Opens a real XMage session (`SessionImpl`),
  receives server callbacks, re-exposes them over WebSocket as JSON, and forwards client actions
  to the server. Necessary because browsers cannot speak jboss-serialization.
- **Web Client (`web/`)**: React 19 app. Communicates solely with the proxy,
  never directly with Mage.Common → modifying the client does not break the proxy or vice versa.

## 4. Technical Decisions (and Rationale)

| Decision | Choice | Rationale |
|---|---|---|
| Client Stack | React 19 + Vite + TypeScript + Web Audio | Hardware-accelerated CSS transforms + SVG overlays for targeting/combat; pure React DOM UI. Eliminates PixiJS/canvas overhead for crisp text, zero GPU memory leaks, and native responsiveness |
| State Architecture | **Snapshot-diff → animation**: each `GAME_UPDATE` delivers the full `GameView`; client calculates transitions (card A moved from hand→battlefield, B tapped...) and animates them | Pure testable logic; effects are data-driven rather than hardcoded per action |
| Effects | Declarative transitions (`gameTransitionEngine.ts`, `feedbackFx.ts`): animations, screen shakes, floating damage numbers, sound dispatch | High-performance CSS/DOM animations without canvas overhead |
| Distribution | Pure browser (Phases 1-2) → Tauri launcher (Phase 3) | Web link = zero installation (core advantage vs desktop Swing); Tauri = ~15 MB desktop app running the embedded proxy automatically |
| Maintainability | `types.ts` = single source of protocol truth; pure logic decoupled from rendering; minimal pinned dependencies | Real project risk is XMage protocol versions (proxy), not client-side churn |
| Performance | Shared sprite sheets, unique card textures, particle pooling, DPI-aware rendering | Game is not performance-bound; memory usage and GC pauses are kept strictly bounded |

## 5. Phase 0 — XMage Proxy (✅ Completed & Verified 2026-08-08)

### Delivered Components (`Mage.Proxy/`)

| File | Purpose |
|---|---|
| `src/main/java/.../ProxyClient.java` | Implements `MageClient`; real XMage session via `SessionImpl`; forwards server callbacks as JSON; executes web client commands |
| `src/main/java/.../Gateway.java` | WebSocket server (Java-WebSocket) at `ws://localhost:8787` |
| `src/main/java/.../JsonUtil.java` | Reflection-based JSON serializer with cycle detection (critical for huge `GameView` objects) |
| `src/main/java/.../Main.java` + `Config.java` | Startup; flags `--host --port --username --password --wsPort --httpPort` |
| `src/main/java/.../DeckJson.java` | JSON deck parsing → `DeckCardLists` |
| `src/main/resources/web/index.html` | Test page served at `http://localhost:8788/index.html` |
| `README.md` | WS protocol documentation (commands, events, examples) |
| `pom.xml` (root) | Registered `Mage.Proxy` module; `Mage.Proxy/pom.xml` with dependencies for Mage.Common |

### Real Verification (Local Server in Test Mode, `local-server/`)

Full end-to-end flow verified:
- Login/session OK · lobby broadcast (tables/users/messages) every ~2s OK
- Room chat (joinChat + messages) OK
- Create table with AI + human, join with JSON deck, `startMatch` OK
- JSON events received: `START_GAME`, `GAME_INIT` (full GameView), `GAME_UPDATE`, `GAME_ASK` (mulligan) OK

### Lessons Learned (Crucial for Phases 1-3)

1. **Proxy AND Server** must run with `--add-opens=java.base/java.io=ALL-UNNAMED` (jboss-serialization fails on JDK 17 without these flags).
2. `beta.xmage.de` is obsolete/down (2026-08-08); use `beta.xmage.today:17171` or local server.
3. `deckType` must match a **real server configuration name** (e.g. `Constructed - Modern`).
4. AI seats are filled **before** the human seat (matching the official client behavior).
5. `createTable` requires the deck's `quitRatio` to be ≥ the user's `quitRatio` (default to 100).
6. Client version must strictly match server version; local server requires `config.xml` with `${project.version}` replaced and plugins placed in `plugins/`.
7. **Connect must NEVER restart an already active session for the same user+host** (browser reloads send repeated connects). In test mode, duplicate user connections from the same host are kicked (`Session.java` anon-dedup) → restarting triggered reconnect loops (~1.5s) that cleared WS registries and killed broadcasts. Fix: idempotent connect + `synchronized`.
8. **Game events continue arriving at the proxy for ~1 min after the watcher closes** (GAME_UPDATE/GAME_OVER from ongoing AI matches). "Broadcast to 0 connections" for game events is normal; for `lobby` events it is an issue (active clients should always be present).
9. **Unmounting the board requires disconnecting the ResizeObserver**: Pixi 8 sets `renderer` to null on destroy; a leaked observer triggers `resize()` against null after unmount.
10. Log checks must be **time-windowed** (offset to test start): proxy logs are append-only across restarts and historic traces cause false positives.
11. **Real XMage Boolean Semantics**: `sendPlayerBoolean(true)` = **take mulligan**, `false` = **keep** (the official client's "Mulligan" button sends true). For priority (`GAME_SELECT`), any boolean yields priority.
12. **Real London Mulligan**: After `GAME_ASK "Mulligan down to N?"`, taking a mulligan prompts `GAME_TARGET "Select a card (N more) to put on the bottom of your library"` (card UUIDs live in `targets`, **not** in `cardsView1`) and re-prompts with N−1.
13. **"Select a starting player" is random**: `GameImpl.pickChoosingPlayer()` randomly selects who chooses; if the human wins, a blocking `GAME_TARGET` arrives requiring a player UUID response.
14. **"Any target" prompts include both players**: When validating damage in tests, select the opponent's UUID (`gameView.players[].controlled`), not the first target in `targets` (which could be oneself).
15. **macOS `/usr/bin/java` stub breaks daemon scripts**: `scripts/lib.mjs` resolves the real binary (`javaBin()`, Homebrew `openjdk@17` in `/opt/homebrew/opt` first) and `daemon()` uses the absolute path.
16. **`SESSION CALLBACK EXCEPTION - Unable to create socket` becomes persistent if dead proxy sessions linger** (killed via kill -9): old client sockets remain on the server, causing new logins to fail with endless retries. Fix: restart server and start proxy cleanly.
17. **Real Mana Payment (`GAME_PLAY_MANA`) sends no color hints**: `data.options` only provides `{"queryType":"PLAY_MANA"}`. Payment occurs by tapping mana sources on the board (UUID from `canPlayObjects` in `players[].battlefield` — taps and adds mana to pool) and **then** paying from the pool with `sendPlayerManaType`.
18. **Browser E2E tests leave matches running on the server** (test mode, `maxGameThreads`=10): long test runs saturate server threads. Restart server between heavy test batches.
19. **Port map (post-2026-08-21)**: proxy WS `8787`, proxy HTTP test page `8788` (owned by the Java process whenever the stack is up), fake FixtureServer `8789`. The 2026-08-20 choice of 8788 for fake only worked with the proxy stopped.
20. **Real-mode e2e vs scripted fixtures**: the XMage server never auto-starts IA-vs-IA tables (`startMatch` must be sent explicitly) and advances turns by priority timers. Fixture-scripted specs (fixed hands/windows, forced Sim wins) are therefore fake-only until the HumanHelper learns to HOLD priority in main instead of auto-passing.
19. **Demo AI deck must be stable** (Islands + Mountains + 4 Bolts): 16 Bolts make AI vs AI matches end in 2-3 turns, preventing spectators from seeing board interactions. Decks separated in `web/src/lobby/decks.ts` (`STABLE_DECK` demo / `DEFAULT_DECK` for human matches).

---

## 6. Phase 1 — Web Client: Login + Lobby + Rendered Board (✅ Completed & Verified 2026-08-08)

### Objective

Web application connecting to the proxy, authenticating, displaying the lobby (tables, users, chat), allowing table creation/spectating, and **rendering the board with HD card art (Scryfall)**. Flagship demo: **spectating live AI vs AI matches** running autonomously.

### Codebase Structure (`web/`)

```
web/
├── index.html
├── package.json / vite.config.ts / tsconfig.json
└── src/
    ├── main.tsx / App.tsx          — Entry point, phase routing (login/lobby/game)
    ├── net/
    │   ├── types.ts                — PROTOCOL TYPES (single source of truth, mirrors proxy JSON)
    │   ├── Gateway.ts              — WS connection, reconnect backoff, event routing, result promises
    │   └── commands.ts             — Proxy action helpers
    ├── state/
    │   ├── store.ts                — Connection and lobby state
    │   └── gameStore.ts            — Current GameView snapshot + match log
    ├── lobby/
    │   ├── LoginScreen.tsx         — host/ws/user/pass → connect
    │   ├── LobbyScreen.tsx         — Tables, users, room chat, create table
    │   └── CreateTableDialog.tsx   — Game type, format, AI count
    ├── cards/
    │   └── cardImages.ts           — Scryfall (set + number) with memory LRU + 75ms throttle, Accept + 429 Retry-After; IndexedDB 24h cache planned Phase 3
    ├── board/
    │   ├── BoardShell.tsx          — Game board viewport container & scale management
    │   ├── GameBoard.tsx           — Main 1v1 and pod board orchestration
    │   ├── BoardZone.tsx           — Symmetric zone layout: lands, creatures, non-creatures
    │   └── CardSlot.tsx            — Card slot component with tap/counters/damage badges
    └── ui/
        ├── TopBar.tsx              — Turn, phase/step, player life/mana
        └── GameLog.tsx             — Match log from GAME_UPDATE_AND_INFORM / CHATMESSAGE
```

### Quality & Continuous Integration

| Layer | Tool | Command | Status |
|---|---|---|---|
| Unit (Pure Logic) | vitest | `npm --prefix web run test` | ✅ Clean (104 files / 731 tests, 2026-09-05) |
| Web Core Coverage | vitest/v8 | `npm --prefix web run test:coverage` | ✅ 81.4% stmts / 73.8% funcs (2026-09-05) |
| Typecheck | tsc | `npm --prefix web run typecheck` | ✅ Clean |
| Production Build | vite | `npm --prefix web run build` | ✅ Clean |
| Java Proxy | Maven/JUnit 5 | `mvn -pl Mage.Proxy -am test` | ✅ 142 tests PASS (2026-09-05) |
| Headless E2E (Real Proxy) | `scripts/self-test.mjs` | `node scripts/self-test.mjs` | ✅ 15 checks PASS (2026-09-05) |
| Human vs AI E2E | Node/WebSocket | `node scripts/human-test.mjs` | ✅ PASS (2026-09-05) |
| Browser E2E | Playwright | `npm --prefix web run test:e2e` | ✅ full suite PASS (2026-09-05) |
| Interaction Coverage | vitest callbackCoverage.test.ts | `npm --prefix web run test` | ✅ 43 callbacks contabilizados · anti-drift doc↔código |
| Contract Drift Guard | CI | `gen-zod:validate` + `gen-types:validate` | ✅ up-to-date (contract → schema.generated.ts + types.generated.ts) |

- **One Command for Everything**: `node scripts/test.mjs [unit|coverage|typecheck|build|java|self-test|human-test|e2e]`.
- **Zero-Setup User Script**: `node scripts/install.mjs` (Maven build + plugins setup + npm install) → `node scripts/ctl.mjs start` → `node scripts/test.mjs`.

---

## 7. Phase 2 — Full Interaction (✅ Completed & Verified 2026-08-15)

- Comprehensive support for server interaction callbacks (`GAME_ASK`, `GAME_TARGET`, `GAME_PLAY_MANA`, `GAME_CHOOSE_*`, `GAME_SELECT`, quantities).
- Visual targeting: animated dotted lines, glowing highlight outlines on valid targets.
- Click-to-play card interactions, mode selections, X-cost resolution, and +1/+1 counters.
- Dispatches `sendPlayerAction`, `sendPlayerString`, `sendPlayerUUID`, `sendPlayerInteger` matching server asks.
- **Validated E2E**: `scripts/human-test.mjs` executes full match: starting player choice, London mulligan, land drops, turn passing, casting *Lightning Bolt*, *Blaze* (X=2), *Arc Trail* (multi-target), *Boros Charm* (modal choice), and *Walking Ballista* (X=4 counters). 83 checks PASS.

---

## 8. Phase 3 — Visual Polish, Audio & Distribution (In Progress)

- Declarative visual effect engine (`gameTransitionEngine.ts`, `feedbackFx.ts`, `soundManager.ts`): card trajectory arcs, CSS glow/outline effects, screen shake, floating $-X$ combat damage numbers, Web Audio sound pack.
- Structured event feed (`ActionFeed.tsx`) parsing rules engine logs into visual action cards.
- Tauri desktop packaging: lightweight native launcher (~15 MB) bundling embedded proxy.
- PWA support: installable browser app with offline card caching.

---

## 9. Work Log (Changelog)

| 2026-09-08 | Parity/U14+Dialogs | **U14 cerrada + sistema de diseño de diálogos unificado (D0–D3)**: `DialogShell` (`ui/`) con tokens `--dlg-*` en `styles.css` (kicker-pill/título/mensaje/acciones/search/topRight/sectionProps; regla kicker≠título) y 24 modales migrados (partida: Generic/Mulligan/Voting/Trigger/StartingPlayer/LibraryOrder/Planeswalker/PileDialog nuevo/CardGrid/Rollback/UserRequest/LimitedDeck/GameEnd/HelpWiki; lobby: Join/DeckIssues/DownloadImages/Leaderboard/TournamentBracket/RandomPacks/CreateTable/DeckManager-import; mazos: Printings/Import/Inspector/SampleHand; Appearance/Sleeves/Avatar/Settings/About) con aliases legacy para e2e. Fuera de alcance declarado: pantallas full-screen (Draft/Sideboard/Construct/TournamentPanel) y popovers (UserAction/ArenaFilterBar). U14: P14-1 búsqueda+teclado en grid, P14-2 `sortData`, P14-3 `hintData`, P14-4 `choiceMemory` solo-cliente (diferencia declarada: sin reenvío `isSpecial`), P14-5 por construcción (sin dato en red), P14-6 doble-click/Enter, `PileDialog` visual, ShowCards por combinación (sin callback en protocolo). Barras maná/objetivo/combate conservan postura no-bloqueante con el mismo idioma | vitest 1001 ✅; typecheck ✅; e2e grid-search 2/2 + choice-memory 2/2 + pile-visual 1/1 + mulligan/voting/trigger/pw/interactions/staging/wizard/about/deckvalidation/decks-gallery/printing-preview/mana-payment/targeting/combat ✅ |
| 2026-09-08 | Fix/flight-anim | **Animación de movimiento intermitente**: diagnóstico con telemetría (`noteFlightEvent` + `window.__mageFlights` + `e2e/flights.spec.ts` con inyección sintética de `GAME_UPDATE`s y ráfaga real a 50ms). Causas probadas: (1) el descarte mano→cementerio NUNCA volaba (sin rama en el engine + update in-place del top); (2) en ráfaga la 3ª transición se teletransportaba (guard `hasFlightFor` + cancel `dedupe` a los ~57ms); (3) §2C volaba fantasmas erróneos al cementerio para bounces/tokens. Fixes: `startCardFlight` **encadena** (redirige el fantasma en vuelo, reprograma timers; ignora mismo-destino <120ms; reemplaza solo si caducó) en vez de cancelar; ramas nuevas mano→cementerio (descarte/molino), campo→mano (bounce), campo→exilio; §2C acotada a llegadas reales; origen de reanimar desde el cementerio; fallback a la banda en entrada a campo | vitest 1003 ✅; e2e flights 1/1 + spells/reveal/missing-prompts/full-flow/mechanics/combat/mana-payment ✅ |
| 2026-09-08 | Fix/staging-overflow | **Meta del asiento desbordaba la tarjeta del anillo** (132px fijos; `RankBadge nowrap` + historial sin wrap): `.player-seat-meta` con `flex-wrap` + centrado (cubre anillo y duelo), `RankBadge` con `max-width+ellipsis`. Test e2e `staging.spec.ts` (mesa Commander 2 asientos + rating + historial, assert `scrollWidth<=clientWidth`; verificado que falla sin el fix) | vitest 1003 ✅; staging+staging-chat 11/11 ✅ |
| 2026-09-08 | Fix/lobby-i18n | **Textos quemados en español → i18n ×9**: `ui_scale_*`/`cjk_boost_*` (11 claves, SettingsModal + AppearanceSettingsModal incl. descriptores de preset) y `filter_format_limited`/`filter_selected_format` (chip `Limited` + `Formato: X` en TableFilterBar) | typecheck ✅; i18n.coverage 4/4 ✅; vitest 1003 ✅ |
| 2026-09-08 | Feat/feed-i18n | **Log de partida traducido ×9 + parser con idioma UI**: `parseGameEvent` devolvía español hardcodeado (mezcla ターン + `pierde vidas` con UI≠es) y 6 familias de líneas caían en inglés crudo. Refactor: el parser devuelve `{i18nKey, params}` (+ `verbatim` para avisos ya-españoles) y `ActionFeed` formatea con `t()` vía `formatFeedText`/`toFeedItem` (sub-claves `@fase/@destino` reutilizan `step_*`). Nuevos patrones: `X - Waiting for Y`, `attacks <def> with N creatures` (+plural), `Player - Ability triggers … [- targeting T]`, `Attacker:/Attacked player:`, `reveals`, `puts … into/hand/grave/top/exile/battlefield (source:)`, `won the game/match`, `has conceded`. 45 claves `feed_*` ×9 locales; nombres de jugador/carta siempre verbatim | vitest 1012 ✅ (+15 parser); typecheck ✅; build ✅; i18n.coverage 4/4 ✅; e2e full-flow + mechanics ✅ |
| 2026-09-08 | Fix/lobby-chat | **Abrir el chat desplazaba/encogía la vista de mesas**: la regla `:has(aside.floating-chat) > .lobby-main { margin-right:380px }` (≥601px) reservaba el hueco del panel en TODAS las vistas → en ventanas ~870px el contenido quedaba comprimido a la izquierda con un vacío a la derecha. Fix: la reserva solo aplica dentro del DeckBuilder (`has-deck-builder`, donde viven Exportar/Importar) y en pantallas anchas (≥1100px); en mesas el flotante solapa sin mover el layout (es arrastrable/minimizable). Regresión e2e en `chat.spec` (ancho de `.lobby-main` invariante al abrir el FAB; falla con la regla vieja) | chat.spec 4/4 ✅; decks-gallery responsive 1/1 ✅; typecheck ✅ |
| 2026-09-08 | Fix/deck-art | **Arte no se actualizaba al cambiar de edición**: `updateMetaForDeck` saltaba la descarga si existía la clave por nombre (misma carta, nuevo set/nº) → tira/preview/portada quedaban con el arte viejo. Fix: con set+nº se descarga siempre que falte la clave exacta (sin nº se mantiene el atajo por nombre). En partida no había bug (`cardKey` por set/nº del servidor; el arte nuevo llega al re-enviar el mazo). Cazado por unit (`useDeckMetadata.test`, falla con el código viejo) + e2e `printing-preview.spec` (Scryfall ruteado A→B, también falla sin el fix) | vitest 986 ✅; typecheck ✅; e2e decks-gallery + printing-preview 7/7 ✅ |
| 2026-09-08 | Parity/U15 | **U15 Sistema auditada y cerrada (G15-1…G15-7)**: `AboutModal` (versión `package.json`, links; entrada en GameMenu/login/lobby-header) + news remoto doble feed (releases GitHub Nexus+XMage oficial, caché 24h, badge ●, offline-first, coste 0 — requiere publicar releases para contenido propio) + auto-guardado de logs a IndexedDB (tope 20, toggle default ON, descarga HTML fondo negro como desktop) + duración en fin de partida (contrato ya la traía) + selector de set de tierras (DMU/ZEN/BFZ/UNH/UST full-art, nºs verificados vía Scryfall — BFZ no consecutiva) + docs. Diferencias declaradas: atajos fijos, música de carpeta, proxy SOCKS, vida final (no viaja en contrato), playmats (ROADMAP 3.4). Resta solo **U8 generador** como ❌ del mapa | vitest 984 ✅ (19 nuevos); typecheck ✅; build ✅; e2e `about.spec` 1/1 + settings/decks-gallery sin regresión ✅ |
| 2026-09-08 | Parity/U11 | **G11-5 paradas de fase con defaults persistentes**: default todo-marcado (7 fases × tu turno/rival) en `game/phaseStops.ts` (única fuente); `mage-web-phase-stops` + campo en settings (patrón `manaPayment`); bloque en ajustes lobby/Jugabilidad (grid compartido `PhaseStopGrid`, i18n 2 claves ×9); `PhaseStopSelector` montado en menú ⋯ (sesión); seed desde settings en `GAME_INIT` + push de settings al login (fix: el gateway empujaba los de sesión sin sembrar). Cazado con e2e: el push de login llegaba antes del seed | vitest 128/965 ✅; typecheck ✅; e2e `phase-stops.spec` 2/2 (persistencia cross-sesión, seed + solo-sesión) + mana-payment/settings/skips sin regresión ✅ |
| 2026-09-08 | Parity/U11 | **U11 cerrada (G11-4 macros → no-aplica)**: el botón desktop solo envía `TOGGLE_RECORD_MACRO` (`GamePanel.java:2855`) sin consumidor — 0 referencias a `Macro` en el motor `Mage/` y el servidor `Mage.Server/`; caso de uso cubierto por skips (G11-1) + auto-respuestas (G11-3). Fila U11 → ✅ en `COMPONENT_PARITY.md` | docs-only (sin suite requerida) ✅ |
| 2026-09-05 | Validación global | **`node scripts/test.mjs`: 9/9 capas verde (676s)**: unit, coverage (tras fix: `vitest.config.ts` incluía el `feedback.ts` eliminado en R3 → funcs 63.6% < 70%; re-apuntado a `feedback/*.ts` → 81.4% stmts / 69.2% branches / 73.8% funcs / 83.2% lines), typecheck, build, java (142 tests), self-test 15/15, human-test, e2e full Playwright (583s), i18n. Sin cambios de código salvo el glob de coverage | 8 pass + 1 fail inicial (coverage) → 9/9 tras el fix ✅ |
| 2026-09-05 | Parity/U14 | **Matriz de paridad por módulo `web/COMPONENT_PARITY.md` + auditoría U14**: inventario verificado (desktop 292 `.java`/~65 componentes, web 179 `.tsx`/27 diálogos); tabla madre 15 unidades (U8 generador mazos ❌ confirmado por grep, U2 filtros/PM ⚠️, U3 auditada); **U14 auditada**: PickNumber/MultiNumber/CheckBox-básico/AbilityPicker/UserRequest/Undo ✅, PickChoice-básica/subMessage ✅, 6 gaps menores UX (P14-1 búsqueda en grid, P14-2 sort, P14-3 hover-hints, P14-4 remember-choice, P14-5 preselect, P14-6 doble-click); PickPile ⚠️ visual, ShowCards ❓→U12. Regla de mantenimiento en `web/AGENTS.md` | docs-only (sin suite requerida) ✅ |
| 2026-09-05 | Fix/Dungeon | **Mapa de mazmorras ramificado (bug reportado jugando)**: `MechanicsTray` pintaba lista lineal con bifurcaciones fusionadas (Tomb mostraba Oubliette tras Veils; faltaban Muiral's/Deep Mines/Fungi/Catacombs) y el 📍 nunca se movía (`currentRoom` no existe en el wire: `DungeonView` solo trae id/nombre/imagen/reglas). Nuevo `game/dungeons.ts` (4 grafos 1:1 de `Dungeon*.java` + `pathToRoom`/split de etiquetas compartidas/parse del prompt venture) + `DungeonMap.tsx` (capas por profundidad, forks lado a lado, chips "leads to", ✓ visitadas) + tracking en `useFeedbackForm.selectOption` al resolver *"Choose which room to go to in"* (`recordDungeonRoom`→`dungeonProgress`, solo en `ok`) + tracking del OPONENTE vía broadcast del servidor *"X has entered Y (dungeon: Z)"* (`sniffDungeonEntry` en GAME_UPDATE_AND_INFORM/GAME_INFORM_PERSONAL, clave `gameId‖player‖dungeon`) + 5 claves i18n × 9 idiomas. Fila Initiative/Dungeon actualizada en `INTERACTION_COVERAGE.md` | vitest 105 files / 743 tests ✅; typecheck ✅; build ✅ |
| 2026-09-05 | Fix/Dungeon follow-up | **El oponente SÍ es rastreable**: el servidor emite `"X has entered Y (dungeon: Z)"` a todos (`Dungeon.java:104`), así que el tracking ya no es solo propio — `parseDungeonEntry` + `sniffDungeonEntry` + clave de progreso por jugador | vitest 746 tests ✅; typecheck ✅; build ✅ |
| 2026-09-05 | Fix/Board A+B (pod/arena layout) | **Fase A**: `zone-empty` en `BoardZone` (bandas colapsadas `0px`+`display:none`, la status-row ya no se comprime a 0 en tableros vacíos — aplica a los 3 layouts) + `pod-cell--empty{display:none}` en `TwoHeadedBoard` (<4 jugadores sin media pantalla muerta). **Fase B**: status-row compact 28-32px→44px mín, avatar 32→36px, detalles compactos con ellipsis sin desborde. Tests: slots colapsados + `zone-empty`/`no-creatures` + status renderizada | vitest 748 tests ✅; typecheck ✅; build ✅ |
| 2026-09-05 | Refactor R7 | **Java `ProxyClient.java` 1484→739 + 10 clases**: `ProxyProtocol` (ERR_* + resultJson), `JsonArgs` (str/int/bool/uuid/actionData), `ErrorClassifier` (classify/strip), `MatchOptionsParser` (match+tournament options), `SimManager` (sims lifecycle), `CommandContext` + `Info/Table/Tournament/GameCommands` + `CommandDispatch` (router `handle→boolean`, `throws Exception` fluye al catch existente). `ProxyClient` conserva lifecycle/callbacks/lobby/connect/router+`requiresGameId`. Transcripción verificada por diff normalizado (strings + lógica idénticos; 1 typo propio cazado pre-compile). `Gateway` + 3 tests re-apuntados; guía "Adding a New Event/Action" del README del proxy actualizada | `mvn -pl Mage.Proxy -am test` 142 tests ✅; jar rebuilt + proxy restarted; self-test real-stack 15/15 ✅ (1ª pasada: flake WATCHGAME conocido, proxy sí había reenviado ambos eventos; 2ª TODO PASS) |
| 2026-09-05 | Refactor R6 | **Lobby `LobbyScreen.tsx` 1160→~330 + `DeckBuilder.tsx` 1040→~480**: lobby → `lobbyUtils.ts` (withTimeout/formatos/extract+`LobbyTab`+`fallbackActionUser` que elimina 2 literales idénticos), `useTableActions.ts` (join/human/AI/start/watch), `useTournamentBracket.ts`, `LobbyHeader/Sidebar(+MobileNav)/TableCard/Aside/TournamentBracketModal.tsx`; tests re-apuntados a `lobbyUtils`. Decks → `deckCardOps.ts` puro (key/move/inc/dec/remove/merge/insert-or-increment/addSearch/printing/suggested/meta-builders) + `deckCardOps.test.ts` (18), `useDeckMetadata/useDeckMutations/useDeckValidation.ts`, `exportDeckFile.ts` (trio export deducido a 1 helper), `DeckBuilderFooter/DeckServerIssues/DeckHoverPreview.tsx` | vitest 104 files / 731 ✅; typecheck ✅; build ✅; e2e fake decks-gallery(4)+staging(4)+full-flow+chat(3) = 12 ✅ |
| 2026-09-05 | Refactor R5 | **`state.ts` 200→~50 compositor + `state/slices/` (5)**: `session.ts` (phase/conn/ws/error), `lobby.ts` (lobby/chat/log/events + `LogChannel/Entry`), `game.ts` (game/playables/combat/feedback/sideboard/userRequest/viewer/phaseStops), `limited.ts` (draft/tournament/construct/replayViewer), `settings.ts` (`SettingsState` nombrado + loaders). `AppState extends` los 5 slices, `initialState` por spread, runtime (`setState/getState/addLog/listeners`) y todos los tipos re-exportados intactos → `store.ts`, `actions.ts`, `selectors.ts` y ~15 consumidores sin cambios | vitest 103 files / 713 ✅; typecheck ✅; build ✅; e2e fake staging(4)+full-flow+draft(2)+missing-prompts = 8 ✅ |
| 2026-09-05 | Refactor R4 | **`eventHandler.ts` 516→~230 router + `state/events/` (8 módulos)**: `context.ts` (`Snapshot`/`EmbeddedGame`), `chat.ts` (CHATMESSAGE/SHOW_USERMESSAGE/SERVER_MESSAGE), `game.ts` (JOINED/START/GAME_INIT···/WATCH/INFORM/OVER/END_INFO/ERROR/REDRAW), `prompts.ts` (TARGET/ASK/USER_REQUEST), `sideboard.ts`, `draft.ts`, `tournament.ts`, `replay.ts`, `views.ts`. Router conserva todas las etiquetas `case` en idéntico orden (guard `callbackCoverage` sin cambios), mismo preámbulo (guards, embeddedGame, sounds, parse genérico). Fix colateral: anotaciones `(f: string)` en `callbackCoverage.test.ts` (implicit-any desde R3, oculto en el `tail`) | vitest 103 files / 713 ✅; typecheck ✅ (exit 0); build ✅; e2e fake chat(3)+full-flow+defeat+draft(2)+tournament(2)+reveal+interactions+missing-prompts = 12 ✅ |
| 2026-09-05 | Refactor R3 | **`feedback.ts` 507→carpeta `game/feedback/` + `FeedbackDialog.tsx` 577→router ~70**: `types.ts` (interfaces + `FeedbackTextFn`), `detect.ts` (heurísticas puras sin i18n: mulligan/starting/voting/discard/london/planeswalker/booleanValue), `record.ts` (extracción JSON pura; fallbacks de etiqueta inyectados), `text.ts` (único import i18n), `parse.ts` (`parseFeedback` con `t` inyectable, default idéntico). `useFeedbackForm.ts` (estado+acciones+`sendValue`) + `feedbackModes/` (StartingPlayer/TargetBar/ManaBar/CombatBar/GenericDialog con kicker+títulos; genéricos y starting-player migrados a `Modal`). Guard `callbackCoverage` lee ahora el directorio `feedback/`. `INTERACTION_COVERAGE.md` sin cambios (mismos cases, movidos) | vitest 103 files / 713 ✅ (`detect.test.ts` nuevo: 10); typecheck ✅; build ✅; e2e fake missing-prompts+complex-costs+voting+interactions+mulligan(2) ✅ |
| 2026-09-05 | Refactor R1+R2 | **R1**: `CreateTableDialog.tsx` 1400→shell ~110 + `lobby/CreateTable/` (`constants.ts`, `useCreateTableForm.ts`, 5 tabs, `SummaryStrip`); re-exports preservados, guard `serverStateCoverage` re-apuntado a `CreateTable/constants.ts`. **R2**: base `ui/Modal.tsx` (backdrop+dialog+aria, slot `trailing` para previews con stacking del backdrop) + piloto Mulligan (2 ramas) y Voting, `Modal.test.tsx` nuevo. **Fix pre-existente destapado por el e2e**: `start-game.ts` seleccionaba `Mazo para bots SIM` (etiqueta eliminada en `2dde1a731b`, verificado ausente en HEAD) → timeout global 120s en `mulligan.spec`; selector actualizado a `Mazo global para SIM` | vitest 102 files / 703 ✅; typecheck ✅; build ✅; e2e fake staging+voting+mulligan(2)+spells(4) ✅ |
| 2026-09-05 | Docs | **Esqueleto `docs/` (Fase 0 mapa + Fase 1 docs, track incremental/mapa-docs-refactor)**: `README` (onboarding path + puertos + estado de cada doc, declara `tcg-arena-spec` histórico no normativo), `architecture` (3-tier + multi-tenant + snapshot-diff + Fake vs real + SIM), `code-map` (web/src 368 ficheros por dominio, gigantes con plan R1-R7, Proxy 8 ficheros/3331 líneas, scripts 24/4975), `protocol` (precedencia + regeneración + guards), `testing` (9 capas + tags + flakes conocidos), `deployment` (ctl/dev/tail/build + Pages), `user-manual` (login/lobby/mesa/partida/builder/formatos). Fix: `CONTRIBUTING.md` apuntaba a `export-schema.mjs (Future)` inexistente → `view-schema.mjs` real | docs-only, conteos verificados con `wc -l`; sin cambios de código (suite no requerida) |
| 2026-09-04 | Proxy+Web | **ROOT CAUSE del "no se quita"**: el sondeo real mostró que validando PCY/77 el reporte devolvía missing con setCode **CY** (y PRO→RO) — la normalización promo de `DeckJson.java` retiraba la P inicial cuando el base no era un set pero la carta existía por nombre, mutilando sets reales con P (PCY, PRO, PC2…) → bucle infinito: el drag de la colección (Scryfall da PCY/77) y el botón "Usar PCY #77" re-normalizaban a CY y el informe volvía a marcarla; además el reporte ecoaba el código normalizado, así que `applySuggestion` no encontraba la entrada del mazo. **Fix**: (proxy) el strip de P solo si el ORIGINAL no es set real Y el base sí (`Sets.findSet(normSet)==null && Sets.findSet(base)!=null`, fuera el fallback por nombre); (web) `deckNormalize` deja de hacer `derivePromoBase` — el proxy decide en el borde del protocolo. Nota: Rhystic Tutor implementada es PCY #77 (Prophecy), no PRO. Verificado contra stack real vía UI: pegar CY→banner; clic reparar→banner limpia; re-importar→solo la CY marcada (1 de 2 strips); drag PCY→limpio | java `mvn -pl Mage.Proxy test` 27/27 ✅ (nuevos: `deckJsonNormalizationKeepsRealPSetsWithPrefix`, `realPSetPrintingValidatesClean`); jar reconstruido + proxy reiniciado; vitest 101 files / 694 ✅ (`deckNormalize.test.ts` nuevo); typecheck ✅; e2e fake 55/57 (2 skipped real-only) ✅ |
| 2026-09-04 | Web/Decks | **Reparación de impresiones rotas desde el builder**: (1) arrastrar desde la colección una carta cuyo nombre coincide con una entrada marcada ⚠️ por el servidor la CONVIERTE (reemplaza impresión, conserva amount, actualiza coverCard) en vez de duplicarse — `findFlaggedSameName` + ramas main/sideboard en `handleDropCardOnDeck`; (2) el banner rojo del builder gana botón "Usar PCY #77" (`builder-issue-repair`) que aplica `applySuggestion` (movida a `deckIssues.ts`, compartida con el diálogo pre-join) y persiste vía `schedulePersist` → la re-validación en vivo limpia banner/badges; (3) fix de sobre-marcado: el merge de issues ya no registra la clave por NOMBRE (`merged.set(cardName)`) — marcaba ⚠️ copias válidas del mismo nombre (p.ej. la PCY junto a la CY rota); el key exacto `set:num:name` basta. Diagnóstico con spec temporal de debug (dump de estado tras cada paso): el drag convierte 1→1 sin duplicar, el banner limpia y el botón repara | vitest 100 files / 689 ✅ (`deckIssues.test.ts` nuevo); typecheck ✅; e2e fake 54/56 (2 skipped real-only) ✅ — deckvalidation.spec ampliado: drag-reparación sintética (DragEvent + DataTransfer), botón del banner, sin strips duplicados |
| 2026-09-04 | El usuario reporta que el builder no avisaba de Rhystic Tutor (CY #77). Reproducido con Playwright contra el stack real: el check en vivo SÍ corría (badge ⚠️ en la carta, validación disparada 4× al pegar), pero era poco visible y la validación fallida silenciosa no se reintentaba. **Mejoras**: banner rojo de nivel mazo en el builder (`builder-server-issues`) con detalle + sugerencia, reintento de validación al reconectar (`wsAlive` en deps), hint "misma carta, otra impresión" en diálogo y banner (la sugerencia se busca por nombre ⇒ siempre es la misma carta; PCY = Prophecy, CY no existe como set) | vitest 684 ✅; typecheck ✅; e2e fake 54/56 (2 skipped real-only) ✅ (deckvalidation extendido con asserts del banner) |
| 2026-09-04 | Proxy/Decks | **Pre-validación de mazos contra el servidor (feedback: "Card not found - Rhystic Tutor - CY - 77 todo el rato, no se puede jugar")** — causa raíz doble: (1) mazos importados de Moxfield/Archidekt con códigos de set que no existen en XMage ("CY") o cartas no implementadas en la release, y el servidor oficial hace `Deck.load` estricto por (set, número) IGNORANDO el nombre (verificado en el tag `xmage_1.4.61V1`) → rechazo al unirse con el mensaje crudo; (2) nuestro pipeline tragaba el detalle: `submitDeck`/`updateDeck` respondían "Command failed" sin errorCode, `classifyErrorCode` no reconocía "card not found", el auto-submit de sideboard ignoraba el resultado y los SIM seats fallaban en silencio con el asiento vacío. **Solución SIN tocar el fork** (compatibilidad con el servidor oficial): (1) `Mage.Proxy` añade dependencia `mage-sets` (BD de cartas de la MISMA release que corre el objetivo, 92k filas) y construye su BD perezosamente en background al arrancar (`DeckValidation.ensureCardDatabaseAsync` → `CardScanner.scan()`, ~10s, `Mage.Proxy/db/` gitignored, re-scan automático si cambia el build del fork); (2) nueva acción **`validateDeck`** (`DeckValidation.java`, solo APIs upstream `CardRepository`/`CardScanner`): replica la semántica exacta del servidor oficial y devuelve `{ready, missing:[{reason: OUTDATED_PRINTING|UNIMPLEMENTED, suggestions[], amount}], mismatches:[{resolvedName, suggestions}], fixedDeck}` — distingue "el servidor la rechazará" de **silent-swap** (el servidor la acepta pero carga OTRA carta: "Rhystic Tutor - C20 - 77" carga Banisher Priest); advisory: `ready:false` no bloquea nunca; (3) **Web**: diálogo pre-join `DeckIssuesDialog` (broker por promesas montado en App) integrado en JoinTableDialog/CreateTableDialog (humano + mazo SIM)/SpectatorStagingScreen — lista las cartas con motivo, sugiere impresiones implementadas con un clic (re-valida el bucle) y "Quitar N y jugar" (envía `fixedDeck`); badges ⚠️ por carta en el Deck Builder (`deckIssues.ts` + merge en `cardIssues`) **con re-validación EN VIVO tras cada edición** (debounce 1.2s sobre el objeto del mazo: importar/añadir/quitar/cambiar impresión actualiza los badges sin salir del builder); `translateError` reconoce "card not found" (13 claves nuevas × 9 locales + tipos en `i18n/types.ts`); auto-sideboard maneja `ok:false` con el detalle real; (4) **hardening del proxy**: `ERR_CARD_NOT_FOUND`, `submitDeck`/`updateDeck` con `pollDetailedMessage`+clasificación (helper `sendFailure` deduplicado en create/join/tournament), fallos de `connect` ahora traen el detalle del servidor (`SHOW_USERMESSAGE` con `List<String>` se serializaba como JsonArray y solo se parseaba JsonObject — por eso el error de login llegaba vacío), `startSims` limpia los mazos SIM (`stripMissing`) y emite `{type:'error', fatal:false}` si un bot no puede unirse; SimPlayer gana `getLastJoinError()`. Hallazgo clave: **Rhystic Tutor SÍ está implementada (PRO #77 retro)** — el problema real del usuario era el set code "CY"; la sugerencia repara a PCY #77 en un clic. **Nota E2E**: el WIP sin commit del usuario (CreateTableDialog wizard de 5 pasos) rompía el helper `createTable` de los E2E (`.create-submit-btn` solo existe en el último paso; el paso Dev ya no se llama "Ajustes / Dev") → localizadores actualizados en `start-game.ts` + `full-flow.spec` (verificado con `git stash`: los fallos pre-existían; la validación nueva no era la causa); `mechanics.spec` en rojo por el WIP — **fix incluido**: la bandeja de mazmorras comparaba `currentRoom` del servidor (inglés canónico, p.ej. "Forge") contra las etiquetas TRADUCIDAS ("Forja…"), roto por el commit de i18n en TODOS los locales y también en producto (el marcador 📍 nunca aparecía); ahora `getDungeonRooms` devuelve `{keys[], label}` con los nombres canónicos del fork (`DungeonRoom`) y el match es por clave (`{keys:['forge','lost well'], …}`). Pendiente del WIP del usuario: commit del wizard + resto de cambios sin commitear | vitest 99 files / 684 ✅ (incl. guard i18n 9 locales); typecheck ✅; build ✅; java `mvn -pl Mage.Proxy test` 25/25 ✅ (DeckValidationTest: aceptadas/rechazadas/mismatch/dedup/sideboard/stripMissing, BD real en `Mage.Proxy/db`); smoke real contra stack local: `validateDeck` → CY/77 OUTDATED con PCY/77, C20/77 mismatch→Banisher Priest, fixedDeck correcto; e2e fake **54 passed / 0 failed / 2 skipped (real-only)** en 9.6m ✅ |
| 2026-09-02 | F3/Decks | **Sideboard gestionable en el Deck Builder (cierra el hueco main↔banquillo)** — el sideboard solo podía rellenarse importando (`.dck`/texto) y no había forma de mover main→side ni añadir desde la búsqueda: el drop del panel hacía siempre `onDropCard(data,'main')` (la rama `target:'sideboard'` era código muerto) y el botón ⇄ de `ArenaCardStrip` no estaba cableado en el builder (solo en `SideboardScreen`/`ConstructScreen`). Implementación: (1) `DeckListPanel` — la sección sideboard es drop-zone propia (`onDragOver/onDrop` con `stopPropagation`, highlight `.is-side-drag-over`; un drop de carta `source:'main'` sobre ella mueve 1 copia; `source:'sideboard'` se ignora) + prop `onSwap` pasada a los strips de main (label "Mover al banquillo") y side ("Mover al mazo") + sección renderizada también en layout horizontal (columnas CMC, debajo de la grid); (2) `DeckBuilder` — `handleSwap` mueve 1 copia entre zonas (`moveOneBetween`: decrementa/quita en origen, incrementa/añade en destino con merge por clave, autosave debounced) y `handleDropCardOnDeck` gana semántica de MOVIMIENTO para strips de mazo (main→side y side→main) manteniendo el comportamiento de "añadir copia" para orígenes search/externos; (3) i18n: `builder_swap_to_side`/`builder_swap_to_main`/`builder_side_empty` en 9 locales; (4) **la sección sideboard es SIEMPRE visible** (antes solo se renderizaba con cartas: con el banquillo vacío no había zona donde soltar ni indicación de que existía) — con 0 cartas muestra hint dashed "Arrastra cartas aquí para el banquillo" (`.deck-sideboard-empty`) y sigue aceptando drops. Nota e2e: los botones `.strip-btn` viven en `.strip-actions` con `display:none` hasta `:hover` del strip → el spec hace hover del strip antes de click (el click implícito de Playwright no dispara el `:hover` del padre antes del chequeo de visibilidad). **Fix DnD (feedback de usuario: "apunto pero no se añade")**: Chrome CANCELA el drop cuando el `dropEffect` del dragover no está permitido por el `effectAllowed` del origen — las cartas de búsqueda arrastran con `effectAllowed='copy'` (ArenaCardGrid) y los strips con `'move'` (ArenaCardStrip), así que forzar `dropEffect='move'` en la zona sideboard (ni `'copy'` en el contenedor) cancelaba el drop silenciosamente (cursor 🚫); `compatibleDropEffect` elige ahora `'move'`/`'copy'` leyendo `effectAllowed` durante el dragover (legible, a diferencia de `getData`) en zona sideboard, contenedor del panel y panel izquierdo de colección. El e2e ejercita el HTML5 DnD real vía `dispatchEvent('dragstart'/'dragover'/'drop')` con un `DataTransfer` real | vitest 90 files / 599 tests ✅ (8 en `DeckListPanel.test.tsx`, incl. guardia de `dropEffect` compatible para copy/move); typecheck ✅; build ✅; e2e fake 50/50 + decks-gallery 3/3 con DnD real ida y vuelta ✅ |
| 2026-09-02 | F3/Lobby | **Sala de espera de jugador (paridad con el `TableWaitingDialog` de desktop) + eliminación de mesas** — hueco detectado: al crear/unirse a una mesa el web se quedaba en el lobby (desktop abre su ventana de espera por callback `JOINED_TABLE` → `MageFrame.showTableWaitingDialog`) y no había mecanismo para eliminar una mesa propia (`removeTable`/`leaveTable` existían en commands.ts+proxy pero sin UI). Implementación: (1) fase `staging` + `stagingTableId` en el store; `eventHandler.JOINED_TABLE` parsea el payload real (`TableClientMessage`: `currentTableId`/`roomId`/`flag`) y salta a staging solo desde lobby/staging (no interrumpe partida ni espectáculo); `START_GAME` y `returnToLobby` limpian el staging; (2) `SpectatorStagingScreen` gana `mode="player"` (roster/chat reutilizados): Empezar (dueño+`READY_TO_START` → `startMatch`), Salir de la mesa (`leaveTable`) y Eliminar mesa (solo dueño → `removeTable`; el server además elimina la mesa si el dueño sale de una no empezada, `TableController.leaveTable:578`); "Volver al lobby" conserva el asiento (`hideStaging`) y la tarjeta muestra "Ir a la mesa" (asiento propio o `stagingTableId`) para re-entrar — cubre también reconexión; (3) FakeServer: `joinTable` emite `JOINED_TABLE` (paridad con el proxy real); (4) helpers E2E `dismissStaging` + `toPass` en `waitTableReady`/`startMatch` (el salto llega async tras crear); fix de selector preexistente expuesto con server vacío (`.create-submit-btn`); `multi-user.spec` real afirma ahora el salto y vuelve al lobby. i18n: 6 claves nuevas en 9 locales. `INTERACTION_COVERAGE.md`: JOINED_TABLE ✅Unit ✅E2E + Slice D documentado | vitest 586/586 ✅; typecheck ✅; e2e fake 49/49 (3 staging nuevos) ✅; real: multi-user ✅, spells ✅, full-flow ✅ (warm; fallo inicial = flake documentado de WATCHGAME en frío) |
| 2026-09-02 | F3/UI-fix | **Fix oscilación del hover de la mano (feedback: "al subir el ratón a la altura de las tierras se lía y empieza para arriba y abajo")** — doble causa raíz: (1) la caja de layout del `.hand-card-slot` medía la carta COMPLETA (190px) aunque solo se vieran ~95px → tira fantasma de ~83px sobre la mano (sobre las tierras) que disparaba el hover sin carta visible bajo el puntero; (2) bucle de feedback: la carta hovereada volaba −sink−14px (≈109px) + crecimiento del scale 1.3 con origen 50% 150% (≈85px) → su área RENDERIZADA llegaba ~300-390px hacia arriba (zona de tierras) y el hover dependía de áreas en movimiento: hover→sube→puntero sale del área→baja→re-entra→sube… Fix: (a) `HandBar.css` — `height: var(--sink)` en el slot → la caja estática de hover es SOLO la banda visible y coincide con los píxeles visibles en reposo; al abrirse la mano la cobertura crece hacia arriba (revelado anclado abajo) → histéresis geométrica: lo cubierto solo con la mano levantada puede MANTENER el hover pero nunca iniciarlo; (b) carta hovereada contenida: `translateY(−sink×0.15) scale(1.15)` → destaca ~75px sobre el abanico (vs ~200px) sin invadir las tierras; la apertura de la mano (~90%) no cambia. Guardia e2e: invariante "slot dentro de la banda visible" (slot.y ≥ bar.y−2) en `hand-bar.spec.ts`. Nota: la caja de slot reducida no rompe clicks (render de la carta la desborda; e2e clica vía `__mageScene.click`, no por coordenadas) | vitest 575/575 ✅; typecheck ✅; build ✅; e2e fake completo 46/46 (2 skipped real-only) ✅ (9.3m) |
| 2026-09-02 | F3/UI | **Mano "a media carta" con elevación en hover (feedback: "cartas muy pequeñas; que se vean de la mitad para arriba y el hover eleve mano+carta")** — causa raíz del tamaño: topes fijos en `handSizing.ts` (`HAND_BAR_MAX_CARD_W=100` → carta 100×140px, `HAND_BAR_MAX_SPAN=640`) que no crecían con el monitor. Cambios: (1) **tamaño** — `HAND_BAR_MAX_CARD_W 100→136` (carta 136×190px), `HAND_BAR_MAX_SPAN 640→900`; (2) **media carta** — nueva `HAND_BAR_PEEK_RATIO=0.5`: `HandBar` inyecta `--sink` (cardH×0.5) y cada carta lo suma a su transform; el clip lo hace `.board-shell` (`overflow:hidden`), la altura de la `.hand-bar` pasa a ser la banda visible (cardH×0.5+12) y `computeHandBarSizing.barHeight` refleja esa banda; (3) **hover en dos niveles con CSS puro** (sin estado JS): `.hand-bar:has(.hand-card-slot:hover)` sube TODA la mano a ~90% visible (`HAND_BAR_HOVER_SINK_RATIO=0.1` → sink×0.2 para las no-hovereadas, mantiene arco) y la hovereada endereza + `translateY(-sink-14px) scale(1.3)` flotando sobre el abanico (z-index 50 + glow dorado existentes); (4) **preview flotante eliminado para la mano**: `HandBar` pierde `onHover`/`HandBarOrigin`, `BoardShell` deja de pasar `presenter.handleCardHover` al HandBar, `useBoardPresenter` pierde `anchorInHandBar` (el preview 320×448 sigue para battlefield/stack/zonas vía el fallback `relBottom > height-140` de `FloatingCardPreview`). E2E `hand-bar.spec.ts` reescrito a la nueva geometría: slots anclados al borde inferior de la banda + carta transformada hundida tras el borde del tablero con 30-70% visible (tolerancia al arco) + invariante horizontal 55% intacta; `arena-board.spec.ts` no cambia (anclaje al fondo se conserva) | vitest 575/575 ✅; typecheck ✅; build ✅; e2e fake completo 46/46 (2 skipped real-only) ✅ (9.2m) |
| 2026-09-01 | F3/UI | **Modo "Arena" para Commander (feedback: "mi zona se ve enana")** — tercer valor de `boardLayout: 'arena'` (2x2 pod se conserva; el toggle del header ahora cicla standard→pod→arena, claves i18n `arena_view*` en 9 locales). Nuevo `ArenaBoard.tsx`: mitad inferior = mi PlayerZone a ANCHO COMPLETO sin `compactPod` (status row normal, `useZoneScale` da cartas a escala completa), mitad superior = hasta 3 rivales en columnas compactas espejadas (`OpponentZone compactPod+mirrored`, auto-escaladas por zona), divisor central estilo pod, mano overlay anclada al fondo; espectador hasta 4 columnas. GameScreen monta arena solo con `boardLayout==='arena' && rivales≥2` (cae a estándar en 1v1). **Fix incluido**: la tira overlay `.hand-bar` (left:0/right:0) interceptaba clicks del ResourceBar ahora en el borde inferior (cross-zone ray) → `pointer-events:none` en la barra + `auto` en `.hand-card-slot` (solo las cartas son interactivas). E2E `arena-board.spec.ts` (estado inyectado, patrón tournament): 2 columnas, mi zona sin compactar, overlay invariante + screenshot `shots/arena-layout-commander.png`. Nota: el fix de intercepción corrige también el fallo cross-zone introducido con el commit overlay `10d84d4073` | vitest 543/543 ✅; typecheck ✅; build ✅; e2e fake completo 46/46 (2 skipped real-only) ✅; commit previo del trabajo de mano/orbe: `10d84d4073` |
| 2026-09-01 | F3/UI | **Mano overlay (feedback: "desplaza todo hacia arriba")**: la `.hand-bar` reservaba 152px de flex al final del `.game-board` y empujaba el status row (avatar/recursos) hacia arriba. Ahora es `position: absolute; left/right/bottom: 0` — flota sobre el borde inferior sin consumir layout (`.game-board` y `.pod-board` ya eran `position: relative`; mismo CSS para standard y pod). Salvaguarda en `handSizing`: usable = min(ancho−padding, 640, ancho×0.8) → en tableros estrechos la mano centrada nunca tapa avatar (izq) ni recursos (der). `hand-bar.spec.ts`: nueva invariante overlay — player-zone llega al fondo del tablero (±2px) y barra anclada al fondo, sustituye al chequeo "tierras terminan sobre la barra" (obsolete con overlay; las bandas crecen a altura completa y el abanico solapa su borde inferior como en Arena) | vitest 538/538 ✅; typecheck ✅; build ✅; e2e hand-bar 2/2 (standard+pod, nueva invariante) ✅; recorded+combat-human+targeting+mutate-recorded 7/7 ✅; screenshot de targeting verificado (avatar/recursos al borde real, mano flotando centrada) |
| 2026-09-01 | F3/UI | **Ajuste del abanico (feedback de usuario: "muy abierto y flotando")**: modelo Arena real — mano compacta y pequeña anclada ABAJO, el hover es lo que abre y solapa. `handSizing.ts`: `HAND_BAR_MAX_SPAN=640` (usable = min(ancho, span) → en pantallas anchas la mano deja de estirarse), `HAND_BAR_MAX_GAP 24→6`, `HAND_BAR_MAX_CARD_W 116→100`, arco `ROT_PER_CARD 3.4→1.6` / clamp `24°→10°`, rise `0.16/30px→0.03/4px` (plano, pegado abajo), jugables 6→3px. `HandBar.css`: hover `scale 1.14→1.28` + `translateY -46→-34px` (endereza, agranda y solapa por z-index), sombra de contacto más sutil. Tests actualizados a las constantes (HandBar.test dejaba de hardcodear 116px; span-cap nuevo test) | vitest 538/538 ✅; typecheck ✅; build ✅; e2e hand-bar+recorded+combat-human 8/8 ✅; screenshot de targeting verificado visualmente |
| 2026-09-01 | F3/UI | **Interfaz estilo MTG Arena (fase 1-2 + orbe)**: (1) **Mano en abanico** — `computeHandArc` en `handSizing.ts` (rotación ±3.4°/carta clamp ±24° total, elevación parabólica hacia el centro ≤30px, jugables +6px de elevación base); `HandBar` inyecta `--rot`/`--rise` por slot y el transform vive en `.hand-card` (los rects de `.hand-card-slot` que miden e2e/sceneBridge NO cambian); hover endereza+escala 1.14 con glow dorado; respiración de jugables por `filter: brightness` (composited, no box-shadow que repinta cada frame). (2) **Playmat bizonal** — `GameBoard.css`: gradientes radiales mitad rival fría/mitad propia cálida + viñeta perimetral + grano sutil (feTurbulence data-URI SIN `mix-blend-mode: overlay`, que forzaba recomposite de todo el tablero); bandas de permanentes translúcidas redondeadas (`BoardZone.css`) para leer las filas como zonas de mesa; barra de mano transparente con sombra de contacto. (3) **Orbe de prioridad** — `PriorityOrb.tsx` montado en `.board-wrap` (borde derecho-centro): pulsó dorado por `opacity` de un `::after` dedicado (no box-shadow), modo violeta para resolver stack, atenuado en espera; el `ActionButton` del panel se conserva. **Lección (race del helper E2E, causa raíz de un fallo 4/4 de combat-human block)**: el fix del 2026-08-31 anulaba `mainWindow` DENTRO de la rama `hasCombatSelect`, que corre DESPUÉS del return `me.hasPriority !== true`; en el bloqueo del mini-motor el humano NO tiene prioridad (`startHumanBlock` no cambia `priority:'sim'`) → el nulo nunca corría y el fallback de la main previa disparaba a T+1.5s un boolean que el motor interpreta como "confirmar bloqueos", cerrando la ventana antes del click del test (antes pasaba por margen: el screenshot fullPage era rápido; la textura feTurbulence lo ralentizó y destapó el race). Fix: `wshelper.handleSelect` anula `mainWindow` por `gv.phase === 'COMBAT'` ANTES del check de prioridad. Diagnóstico con spec desechable + telemetría (`__mageScene.gameView` vs frames vs DOM: la app estaba sana, la ventana se cerraba server-side) | vitest 537/537 ✅; typecheck ✅; build ✅; e2e fake 45/45 (2 skipped real-only) ✅; combat-human block 4/4 ✅; full-flow real falla igual en master (beta.xmage.today caída, ambiental); hand-bar.spec + recorded.spec ✅ (geometría intacta) |
| 2026-09-01 | F3/Animación | **Fix rotación de atacantes/tap (rota en dev de nuevo)** — las clases `tapped`/`attacking` se aplicaban bien pero el transform computado quedaba en identidad: el cleanup del `useLayoutEffect` de `CardSlot` (deps `[effectiveId, card]`, objeto nuevo por GAME_UPDATE) hacía `clearTimeout` del timer que quita `entering` a los 250ms, y en dev (StrictMode double-mount ejecuta ese cleanup también) la clase quedaba clavada para siempre; la animación `cardAppear` con `fill: both` pisaba `.tapped`/`.attacking` con su keyframe final `scale(1) translateY(0)` (las animaciones ganan a la cascada; por eso el e2e que solo asertaba la clase seguía verde). Fix: timer de entering en ref que **no se limpia nunca** (un setState tras desmonte es no-op inofensivo en React 18+) + `fill: both` → `backwards` en `.card-slot.entering` (defensa en profundidad: la animación nunca puede fijar el transform de estado). Diagnóstico: `window.__mageScene` publica ahora también `gameView` completo (sceneBridge, aditivo) → probe del usuario espectando beta.xmage.today (datos correctos del server: atacante con `tapped:true`, `combat` MAP con `defenderId`, sin vigilancia → bug 100% cliente). Guardas: `combat.spec` (fake) afirma el **transform computado** del atacante (`matrix(0, 1, -1, 0, 0, 0)`, no solo la clase) y 2 tests unitarios de regresión del ciclo de vida de `entering` (rerender con objeto nuevo + StrictMode double mount; verificados rojos sin el fix). Nota aparte: fallo preexistente e independiente en `store.test.ts` SIDEBOARD (vi.doMock post-import; dependía de red/entorno) | vitest 507/508 (1 preexistente); typecheck ✅; e2e fake `--grep @combat` 4/4 ✅ |
| 2026-09-01 | QA/fix-e2e | **decks-gallery 3/3 ✅ (última pieza roja del rescate)** — doble causa raíz, ambas pre-existentes en HEAD: (1) los 3 specs hacían `page.goto('/')` sin `?proxyPort=`: la app usaba el proxy por defecto (nada escuchando en fake) → "⚠️ No se pudo conectar al proxy WebSocket"; el mecanismo que usa el resto de la suite es el de `login()` (`start-game.ts:60`): `?proxyPort=${FAKE_MODE ? getFakePort() : BACKEND_PORT}`. (2) `withFakeServer(null as never, …)` → `makeScenario()` es `null` y `FakeServer.handleConnection` (fake.ts:320) lanza TypeError dentro del handler 'connection' → el upgrade WS muere en cada conexión (verificado con una sonda en vitest: error no capturado en `_http_server`); la entrada "43/43" del 2026-08-31 no pudo incluir estos 3 specs con este código (el `null` y el `goto('/')` ya estaban en HEAD). Fix: goto con `?proxyPort` + escenario lobby-only `web/fixtures/scenarios/decksGallery.ts` (galería/builder/Deck Browser son client-side: `metaDeckCatalog.ts`) + `TABLE.decksGallery` | vitest 506/506 ✅; typecheck ✅; `npx playwright test` (fake) **43 passed / 0 failed / 2 skipped (mutate-real y multi-user son solo-real)** ✅; decks-gallery aislado 3/3 ✅ |
| 2026-08-31 | QA/E2E-rescate | **Suite e2e fake 100% verde (43/43 + 2 skips intencionales, 8.6m)** — segunda ola del rescate, todos pre-existentes en HEAD: (1) `missing-prompts` — el diálogo ya no muestra el método en crudo (título traducido): el spec ancla por el mensaje del escenario y `waitFrameAt` sigue verificando el protocolo; **fix de producto**: el rework del input libre (`mode==='string'`) había ELIMINADO la lista de choices del servidor (GAME_CHOOSE_STRING/ONE/BETWEEN/COLOR) — restauradas como quick-pick buttons que hacen `sendPlayerString` directo. (2) `interactions` — mismo fix (botones de color). (3) `decks-gallery` — selectores al día (placeholder "Nombre de usuario", botón "🃏 Mazos", título "MAZOS", "✏️ Editar Mazo", "Guardar y Salir"; el `#mini-import` ya no existe → modal "📥 Importar Lista" con `.deck-import-textarea`/`.import-submit-btn`; "IMPORT TEXT" → "📝 Pegar lista"). (4) `tournament` — botón bracket por `data-testid="open-bracket"` (label ahora "Ver Cuadro"). (5) `complex-costs` — el entero ahora es stepper (`aria-label="Cantidad"`): el spec usa el helper `resolveInteger`. (6) `chat` — botón de espectar ahora "Espectar" (`/Ver|Espectar/i`). (7) `combat-human`/`combat-multiblock` — **bug del helper E2E**: el fallback de auto-pase (1.5s) armado en la ventana main NO se invalidaba al abrir la ventana de combate → disparaba un `sendPlayerBoolean` extra que el mini-motor interpreta como "fin del paso de combate" y cerraba la ventana antes del click del test (carrera dependiente del timing: pasaba en solitario y fallaba en tandas, y viceversa). Fix: `wshelper.handleSelect` invalida `mainWindow` al ver un select de combate (possibleAttackers/Blockers) — el fallback pendiente muere; con `skipCombat` además no re-auto-pasa. (8) `playwright.config.ts` — **puerto dedicado 5175 + strictPort + reuseExistingServer:false** para el e2e fake (misma lección que el 8789): compartir el 5173 con el dev/stack hacía correr los specs contra un gráfico de módulos stale (UI vieja, labels inexistentes) — detectado por un screenshot con layout antiguo. `draft` 2/2 ✅ (el "skipped" del run anterior era `multi-user`, solo-real) | vitest 506/506 ✅; typecheck ✅; build ✅; **`npx playwright test` (fake) 43 passed / 0 failed / 2 skipped (mutate-real y multi-user son solo-real) ✅** |
| 2026-08-31 | F3/Animación | **Overhaul de animaciones de juego** (4 fixes + jugo): (1) **Flechas al jugador** — `CombatArrowsOverlay.getCenter` resolvía `[data-player-id]` al PRIMERO en orden DOM (`BoardZone` raíz, centro ≈ banda de tierras); ahora prefiere `[data-player-anchor]` (avatar en `PlayerInfoBar`) y si no el elemento de jugador MÁS PROFUNSO; origen de flecha en el borde de la carta, fin recortado 18px antes del avatar, `ResizeObserver` + `scroll` capturado (antes solo `window.resize`), markers más grandes con contorno, dash-flow en ciclo sin salto. (2) **Rotación de atacantes** — el campo `attacking` no existe en el contrato (`PermanentView.java` solo tiene `tapped`) y `OpponentZone` no recibía props de combate: nuevo `combatActorsFrom(game)` deriva atacantes/bloqueadores directamente de `game.combat` (verdad del servidor presente también en vistas de espectador) y se plumbba a GameBoard/PodBoard/TwoHeadedBoard→ambas zonas; `hasVigilance` ahora con límites de palabra + `extractKeywordsFromCard`; eliminado el `(perm as any).attacking` muerto. **Confirmado con frame real** (`record.mjs combat`): el server SÍ envía `tapped:true` del atacante en `battlefield` Y embebido en `combat.attackers`. (3) **Motor de vuelo single-source** — `CardSlot` (mount, rect preciso) y `gameTransitionEngine` (useEffect padre, rects de banda/zona) disparaban DOS vuelos y el dedupe de `flightManager` hacía ganar el del engine (carta volando al centro de la banda): ahora `flyOnce` hace que el engine NO duplique vuelos ya lanzados, destinos siempre al rect del elemento concreto (`[data-card-id]`), el clon aterriza heredando la rotación tapped (keyframes centro-basados) con crossfade 140ms (`markFlightLanded`/`onFlightLanded` + backstop 120ms/240ms, adiós al `setTimeout` paralelo y al pop), `prefers-reduced-motion` desactiva vuelos, registry reseteado al cambiar de partida. (4) **Jugo** — nudge de atacantes hacia el defensor (propiedad `translate` compuesta con `rotate(90deg)`), glow de bloqueadores, shake `took-damage` en criaturas con delta de daño y en `PlayerInfoBar` al bajar vida. E2E fake `combat.spec` aserta clase `tapped` del atacante del Sim. **Rescate de la suite e2e (100% rota en HEAD)**: la i18n de 2026-08-30 usa `navigator.language` → Playwright (en-US) renderizaba la UI en inglés y NINGÚN spec pasaba el login; fix: `locale: 'es-ES'` en `playwright.config.ts` + selectores resilientes en `login()`/`createTable()`/`waitTableReady()` (`Servidor XMage`, `Nombre de usuario`, `Conectar y Jugar`, `Iniciar Partida`) y `decks-gallery.spec` (placeholders/títulos/botones). **Fallos pre-existentes PENDIENTES (no relacionados con animación, triage aparte)**: `complex-costs` (timeout mid-flujo, también falla en HEAD), `interactions` (botones de color no aparecen), `missing-prompts` (espera `GAME_SELECT_PLAYER` literal en el diálogo), `tournament` (botón bracket), `decks-gallery` 2/3 (builder: "Añadir al mazo"/layout), `draft` skipped | vitest 83 files / 506 ✅; typecheck ✅; build ✅; e2e fake: combat 1/1 (con aserción de rotación), combat-human 1/1, combat-multiblock 1/1, spells 4/4, targeting 1/1, full-flow 1/1, cross-zone 2/2, stack-priority/keywords/mechanics/planeswalker/mutate/recorded(+combat.json)/reveal/chat/concede/defeat/multi-user/voting ✅; fixtures: `combat.json` grabado del server local + `recorded.test.ts` 4/4 (invariante `hasAttackingTappedCreature`) ✅; replay e2e de la frame real 4/4 ✅ |
| 2026-08-25 | Feature | **Proxy multi-tenant** (un proceso sirve a muchos usuarios): `Gateway` pasa de un único `ProxyClient handler` a `byConn` (`Map<WebSocket,ProxyClient>`) + `byAccount` (`host|username` → sesión); `ProxyClient` gana `attach(WebSocket,requestId)` (misma cuenta reusa sesión vía `isSameSession`) y registra/desregistra su `accountKey` en el `Gateway`. `Main` deja de crear un `ProxyClient` singleton/auto-connect; el shutdown itera `gateway.getSessions()`. `GatewayProtocolIntegrationTest` migrado al nuevo modelo. Añadido `scripts/multi-tenant-test.mjs` (Node: aislamiento entre cuentas + adjunte de misma cuenta) y `web/e2e/multi-user.spec.ts` (Playwright 2 contextos: ambos entran y uno ve la mesa del otro) | `mvn -pl Mage.Proxy -am test` 18/18 ✅; `node scripts/multi-tenant-test.mjs` 6/6 ✅; `self-test`+`human-test` ✅; `web/e2e/multi-user.spec.ts` (real, localhost) ✅ |
| 2026-08-25 | QA/Feature | **Detector reverse-drift de mecánicas**: `scripts/view-schema.mjs` extrae el conjunto exhaustivo de campos serializables de las clases `mage.view.*` del server (oráculo derivado de las reglas de reflexión de `JsonUtil`, no de partidas grabadas) → `web/fixtures/server-view-schema.json`. `web/src/state/mechanicsCoverage.test.ts` difumina ese oráculo contra los campos modelados en `contract.schema.json` y falla si el server emite un campo que el cliente no modela (anti-drift automático, sin enumeración manual). Al correrlo solo faltaban 16 campos de carta de datos/ayuda de render (split-cards, selección, arte) — ya añadidos a `contract.schema.json` + `types.generated.ts`. Hallazgo corregido: **`goad`/`goaded` SÍ se emite** — el motor añade la restricción "Goaded by X (must attack)" a `rules` y a un icono `OTHER_HAS_RESTRICTIONS` de `cardIcons` (`CardView.java:758-773`), así que el cliente lo muestra como badge de restricción (`CardIcons.tsx`); el campo `goadingPlayers` no va en el DTO pero su info llega por el icono. Para cubrir la *segunda* dimensión de drift (estado del motor no serializado en `mage.view.*`) se añadió el **tercer guard** `engineViewCoverage.test.ts` + `scripts/engine-view-schema.mjs` (baseline `engine-view-gap.baseline.json`) que rastrea esos gaps reales (p.ej. `harnessed`/`monstrous`/`renowned`/targeting de jugador). | `node scripts/view-schema.mjs` ✅; `mechanicsCoverage.test.ts` 6/6 ✅; `node scripts/engine-view-schema.mjs` ✅; `engineViewCoverage.test.ts` 3/3 ✅; `gen-types:validate` + `typecheck` ✅ |
| 2026-08-25 | QA/Refactor | Contract anti-drift: (1) Plan A (`9e91197a04`) — `gameViewSchema` generado desde `contract.schema.json` (`scripts/gen-zod.mjs` → `web/fixtures/schema.generated.ts`), consumido por `schema.ts`; centralizadas las constantes de test en `deck-names.ts`/`table-names.ts`/`fake-mode.ts` (`FAKE_MODE = process.env.E2E_BACKEND !== 'real'`), reemplazando literales en 16 specs de mazos, 19 escenarios/tablas y 15 `fakeOnly()`. (2) Drift fix (`4a2e8aac48`) — `types.generated.ts` regenerado para casar con el contrato: conserva `CardView.sourceCard` y `UsersView.avatarId` (el server XMage los sigue enviando; verificado en `StackAbilityView.java`/`UserDataView.java`) y elimina 11 campos muertos (`CardView.white/blue/black/red/green`, `isSecondCardFace/isFrontFace/isBackFace`, `GameEndInfo.matchId/result/games`); las banderas sintéticas de preview `isFrontFace/isSecondCardFace` se castean en `FloatingCardPreview.tsx`/`CardPreview.tsx`. CI añade `gen-types:validate` (junto a `gen-zod:validate`) para fallar ante futuro drift | tsc ✅; vitest 354/354 ✅; e2e fake 31/1 ✅; `gen-types:validate` + `gen-zod:validate` en sincronía ✅ |
| 2026-08-24 | Bug/Fix | Fixed pre-existing `mulligan.spec` fake failure: the dedicated MulliganDialog renamed the keep button to "✋ Mantener (N)", but the spec still clicked `/Mantener mano/i` — never matched, and with no `actionTimeout` configured the click hung until the 120s global timeout (the London test passed because its substring `/Mulligan/` still matched). Selector is now scoped to `.mulligan-dialog` + `/Mantener/` | e2e fake **27/27** ✅ (7.8m) |
| 2026-08-24 | Feature/Bugfix | Stack controller attribution (kills the "Desconocido" pill when spectating): fork `CardView`/`StackAbilityView` now populate & serialize `controllerId`/`controllerName` via reflection serializer (covers local-server games); web consumes them typed in `StackZone.getControllerInfo`; NEW pure module `web/src/game/stackAttribution.ts` infers controllers on unpatched public servers (beta.xmage.today builds its GameViews remotely without the patch): abilities attributed by locating `sourceCard.id` in per-player zones (battlefield/graveyard/exile), new-since-prev-frame spells attributed to previous `priorityPlayerName` (fallback `activePlayerName`), carry-forward across frames, never overwrites server-provided data; wired in `eventHandler` GAME_UPDATE/GAME_INIT path (fresh attribution per game). Schema updated + types hand-edited (`npm run gen-types` still drops live fields — known tooling gap). **Confirmed live by user watching beta.xmage.today** | vitest 349/349 ✅; typecheck ✅; build ✅; java `mvn -pl Mage.Proxy -am test` 34/34 ✅; e2e fake 26/27 → 27/27 after mulligan fix |
| 2026-08-24 | UI/Fix | Responsive Battlefield & Rotation Geometry Polish: (1) resolved left-edge card clipping/disappearing on tap by upgrading flex bands to `justify-content: safe center` with padding boundaries; (2) implemented conditional Command Zone collapsing in `PlayerZone` and `OpponentZone` via `hasCommandObjects` to free full 100% row width for creatures when no commander is in the zone; (3) expanded `CardSlot` to render distinct counter badges for Shield (🛡️), Stun (⚡), Oil (🛢️), Finality (⏳), +1/+1 and -1/-1 | vitest 309/309 ✅; typecheck ✅; build ✅; e2e 23/23 ✅ (7.4m) |
| 2026-08-24 | Bug/Fix | Game Log (ActionFeed) only shows match events now. Root cause: the single `log` store mixed chat, lobby join/leave (`g-dogg has joined`, `gabrielk12 has left XMage`) and game-log lines, and unmatched lines fell back to `type:'system'` (🏆). Fix: route each `CHATMESSAGE` by XMage `messageType` (`GAME`→game log, `TALK`→Chat tab, `STATUS`/`USER_INFO`→system noise) via a new `channel` field on `LogEntry`; `ActionFeed` filters to `channel==='game'` and `GameChat` to `channel==='chat'`. No proxy rebuild needed — `JsonUtil` already serializes the whole `ChatMessage` (incl. `messageType`); updated `Mage.Proxy/README.md` contract. Fallback heuristic keeps the fake test server (no `messageType`) working | vitest 310/310 ✅; typecheck ✅; e2e chat 3/3 ✅ |
| 2026-08-23 | F2/F3 | Advanced Stack & Priority Suite (Block B): implemented `holdPriority` setting and UI toggle in `GameScreen` with `Ctrl`/`Cmd` key support; connected `HOLD_PRIORITY`/`UNHOLD_PRIORITY` actions via WebSocket; updated `HelpWikiModal` with full Stack, Priority, APNAP trigger ordering, and Storm rules; created deterministic scenario & Playwright E2E covering (1) Hold Priority response loop with *Infernal Tutor* + *Lion's Eye Diamond*, (2) APNAP simultaneous trigger stacking (*Soul Warden* + *Impact Tremors*), and (3) Storm count copies with re-targeting (*Grapeshot*) | vitest 306/306 ✅; typecheck ✅; e2e stack-priority 1/1 ✅ (3.1s) |
| 2026-08-23 | UI/Polish | Resource Piles Top Card Rendering & Hover Preview: upgraded `ResourceBar` to display the HD card artwork of the top card in Graveyard, Exile, and Cross-Zone (Ray stack) with count badge and mini-lightning icon; added full hover card preview & Scryfall resolution on resource stacks; updated `CardSlot` data-attributes | vitest 306/306 ✅; typecheck ✅; e2e cross-zone 2/2 ✅ |
| 2026-09-03 | F3/F5 | Commander Small-Screen & Multiplayer UX Overhaul: (1) Implemented `micro` mode for `ResourceBar` in `compactPod` replacing 68x96px card stacks with sleek 26px chips (`[📚 84]`, `[🪦 12]`, `[🌀 2]`, `[⚡ 2]`), preventing card/resource overlap; (2) Implemented `land-group` cascade accordion grouping identical lands into a single fan with `×N` badge expanding smoothly on hover; (3) Restored equal 1fr/1fr height for opponents' lands and creatures; (4) Boosted player card size up to 175px on battlefield (`MAX_PLAYER_CARD_W`) and 156px in hand (`HAND_BAR_MAX_CARD_W`); (5) Constrained `HandBar` to left 50% in POD 2x2 with `row-reverse` for bottom-right opponent status bar, preventing avatar occlusion; (6) Fixed Partner / multi-commander layout by removing fixed 1-card container width (`bz-commander` `width: auto`) and adding `multi-commander`/`compact` classes; (7) Implemented "Hand Tool" (`useDragScroll`) on battlefield bands with mouse click-and-drag panning, click suppression on drag, vertical-to-horizontal wheel translation, and `grab`/`grabbing` cursors | vitest 97 files / 669 tests ✅; typecheck ✅; build ✅ |
| 2026-09-04 | Wizard/F1-3 | **Wizard Crear Mesa — cierre de gaps vs desktop `NewTableDialog` + `CustomOptionsDialog`** (auditoría `lobby_roadmap.md` §F, 10 gaps funcionales + 8 UX): **F1** numPlayers selector (min/max del `GameTypeView` → `MaxPlayers` del oráculo `server-state-schema.json`: Commander/FFA 3-10 ya configurable, derive `attackOption`/`range` gating como desktop); matriz de asientos `SeatConfig[]` por plaza (tipo+deck por plaza, collapse a chips en 2p, atajo global); **F2** validación `validateDeckGameCompatibility` port de `NewTableDialog.checkMatchOptions` (Commander↔gameType etc.) con banner y bloqueo del submit + `isLimitedDeckType` fix (`Freeform Unlimited Commander→limited=true` + `limited:true` al proxy); wins 1-5 (Bo7/Bo9), freeMulligans 0-5, persistencia `mage_createTable_v1` (lastSession); **F3** contrato proxy extendido `parseMatchOptions` con `mulliganType`/`customStartLife*`/`customStartHandSize*`/`planeChase`/`bannedUsers` y UI Custom Options en tab Timing (badge "Custom (n)" + summary pill); draft cubes/tournaments exhaustivos (21 `tournamentTypes` + 46 `draftCubes` del oráculo, `draftCubeName` visible si `Cube`); simDecks por plaza (no global único). Docs: `lobby_roadmap.md` §F detallado, `PROJECT.md` phase 6 in_progress→done, `site/content.json` phase 6. | vitest 101 files / 694 ✅; typecheck ✅; build ✅ (1.27MB); java `mvn -pl Mage.Proxy -am test` 27/27 ✅; `server-state-schema --validate` ✅; `build.mjs proxy` ✅ |
| 2026-08-23 | F2/F3 | Complex Casting Costs Suite & E2E Validation: implemented Phyrexian mana symbols (`{U/P}`, `{W/P}`, etc. with $\Phi$ icon badges and gradients), hybrid mana badges, Convoke/Improvise board tapping guidance in `mana-prompt-bar`, and dedicated E2E test scenario covering (1) Phyrexian life payment with live 20→18 HUD life update, (2) Kicker confirmation with 4-damage resolution to opponent (20→16), (3) Split cards (Fire // Ice) modal choice, (4) Adventure/MDFC (Bonecrusher Giant // Stomp) choice, and (5) Convoke creature tapping on the battlefield | vitest 303/303 ✅; typecheck ✅; e2e complex-costs 1/1 ✅ (5.8s) |
| 2026-08-22 | F2/F3 | Comprehensive MTG Interactions Suite & Visual Showcase: built E2E test scenario covering `GAME_ASK` (Shockland binary prompts), `GAME_CHOOSE_COLOR` (mana color picker), `GAME_CHOOSE_PILE` (Fact or Fiction split piles), `CardGrid` (Demonic Tutor search with Scryfall HD art), `LibraryOrderDialog` (Scry 3 Top/Bottom order), and Command Zone Commander Tax (+2 badge & cast); updated `GAME_ASK` fallback and added `skipAsks` flag to `HumanHelper` | vitest 286/286 ✅; typecheck ✅; e2e interactions 1/1 ✅ (5.7s) |
| 2026-08-22 | F2/F3 | Universal Playable Objects Audit & Mana Floating: audited and unified `canPlayObjects` resolution across all zones (Hand, Battlefield lands & activated abilities, Command Zone commanders & companions, Graveyard, Exile, Library top card); fixed land tapping during regular priority to float mana before casting spells | vitest 286/286 ✅; typecheck ✅; e2e combat 4/4 ✅ |
| 2026-08-22 | F2/F3 | Combat Visuals & Mechanics Polish: multi-blocker arrows convergence, damage assignment ordering (`LibraryOrderDialog` in blocker mode), numeric damage distribution, 90° tapping rotation on attack with Vigilance exception handling, vertical blocker orientation, and automated E2E screenshots | vitest 285/285 ✅; typecheck ✅; e2e combat 4/4 ✅ |
| 2026-08-22 | F2/F3 | Mechanics & Reminder Tray Widget: added dedicated 2x2 tab in right-panel for global states (The Ring 4-level progressive breakdown & bearer, Active Dungeons room flow & tracker, Day/Night banner & transition rules, Monarch, Initiative, City's Blessing, Speed) with responsive 250px layout | vitest 285/285 ✅; typecheck ✅; build ✅; e2e mechanics 1/1 ✅ |
| 2026-08-22 | F2/F3 | Comprehensive Mechanic Badges & HD Token Previews: added The Ring (level 1-4), active Dungeons, Day/Night, Curses, specialized designations (City's Blessing, Speed, Enduring Story) and connected interactive hover preview for all badges and counters via Scryfall | vitest 279/279 ✅; typecheck ✅; build ✅; e2e 18/18 ✅ |
| 2026-08-21 | T | Repaired e2e suite after lobby rework: best-of-3/5 cursor bugs (`waitFrame` has no `.index`; NaN cursors skipped every frame), fixture `END_GAME_INFO` aligned with real `GameEndView` (`<sim> won the match!`, "You need N more wins…" also after losing a game) | fake e2e 18/18 ✅ |
| 2026-08-21 | T | Fixed real-mode e2e blockers: (1) page+HumanHelper pointed at fake port instead of proxy WS 8787 (`PROXY_PORT` in `e2e/dual.ts`); (2) demo launcher lost the explicit `startMatch` — the server never auto-starts IA-vs-IA tables (the fake scenario was emulating it); (3) empty-lobby second "Crear Nueva Mesa" button broke `/Nueva mesa/i` selector; (4) spectator Log tab auto-switches to Stack on live games | full-flow + combat + lobby-chat green in `E2E_BACKEND=real` |
| 2026-08-21 | T | Port split fix: fake FixtureServer moved **8788→8789** — 8788 is the Java proxy's HTTP test page and collides whenever the stack is up (fake suite silently depended on the proxy being stopped) | fake 18/18 with stack up ✅ |
| 2026-08-21 | T | Real-mode triage: spells/targeting/cross-zone/combat-human/best-of-N/defeat marked **fake-only** (`test.skip(!FAKE_MODE)`): they depend on fixture-scripted windows; vs a live server the HumanHelper auto-pass races server timers (games sprint to turn ~110 before the test acts). Real suite = full-flow, combat, lobby-chat. Pending: helper redesign to HOLD priority in main when the test wants to act | `E2E_BACKEND=real`: 3 pass / 15 skipped ✅ |
| 2026-08-08 | F0 | Built `Mage.Proxy` (client, gateway, json, deck, main) + test harness page | Compiled via `mvn -pl Mage,Mage.Common,Mage.Sets,Mage.Server,Mage.Proxy -am package` |
| 2026-08-08 | F0 | Local server configured in `local-server/` with plugins and test mode | Login, lobby, chat, table creation, startMatch OK |
| 2026-08-08 | F0 | Verified complete event flow: `START_GAME`, `GAME_INIT`, `GAME_UPDATE`, `GAME_ASK` in JSON | Test harness at `http://localhost:8788/index.html` |
| 2026-08-08 | F0 | Repo hygiene: `.gitignore` (local-server, target, plugins), root `pom.xml` module registration | `git status` clean |
| 2026-08-08 | F1 | Master document & stack decisions established (React + Vite + TS + PixiJS) | Approved |
| 2026-08-08 | F1.1 | Environment: Node.js v24.19.0 and `web/` scaffolding initialized | `npm install` + `vite dev` OK |
| 2026-08-08 | F1 | Built web client (types, Gateway, store, lobby, Pixi board, cards, game log) | typecheck + build clean |
| 2026-08-08 | F1 | Spectator crash fix: `game.players` undefined guards & types | `vite.log` 0 page errors |
| 2026-08-08 | F1 | Proxy session thrash fix: idempotent connect logic | self-test 15/15, E2E 0 page errors |
| 2026-08-08 | F1 | Leaked ResizeObserver fix: proper disconnect in cleanup | Playwright 0 TypeError |
| 2026-08-08 | F1 | Vitest unit tests (zones, gameToScene, Gateway, store) + fixtures | 38/38 PASS |
| 2026-08-08 | F1 | Orchestration scripts (`test.mjs`, `install.mjs`, `ctl.mjs`) | `node scripts/test.mjs` all green |
| 2026-08-08 | F1 | Playwright E2E full-flow (login → lobby → spectator demo → board progression) | 1/1 PASS, 0 page errors |
| 2026-08-08 | Hardening | Local-first proxy security: loopback bind, origin allowlist, WS limits, canonical path traversal, per-connection auth | Maven proxy tests + WS integration ✅ |
| 2026-08-08 | Contract | Enforced `requestId`, `errorCode`, `protocolVersion`, and required `gameId` in game actions | 14 Java tests + 53 web tests + typecheck/build ✅ |
| 2026-08-09 | F2 | Corrected priority boolean semantics, `GAME_UPDATE_AND_INFORM.gameView`, typed `canPlayObjects` | 60 web tests + typecheck ✅ |
| 2026-08-09 | F2 | Explicit target feedback, player targeting, optional targets, special payments | Vitest + coverage ✅ |
| 2026-08-09 | F2 | Highlighted playable cards & click-to-dispatch `sendPlayerUUID`; extended `human-test` through *Lightning Bolt* resolution | Build + 14 Java tests ✅ |
| 2026-08-09 | F2 | Fixed boolean mulligan semantics (TRUE=mulligan/FALSE=keep) in `feedback.ts`, `store.ts`, and `human-test.mjs` | 62 web tests + typecheck + build ✅ |
| 2026-08-09 | F2 | Random starting player selection handling in `GAME_TARGET` | Empirically verified via event dumps |
| 2026-08-09 | F2 | London mulligan library bottoming resolution in `GAME_TARGET` | Confirmed server re-ask loops |
| 2026-08-09 | F2 | "Any target" selection logic pointing to opponent player UUID | Verified life drop 20→17 |
| 2026-08-09 | F2 | Complete `human-test.mjs` green: roll → mulligan → land → turns → *Lightning Bolt* → target → mana → resolution → quit | 26 checks PASS; full suite 7/7 ✅ |
| 2026-08-10 | Env | Rebuilt macOS development environment: Java 17.0.20 + Maven 3.9.16, regenerated `local-server/` with 27 plugins | `install.mjs` + `build.mjs proxy` ✅ |
| 2026-08-10 | Infra | Fixed macOS daemon spawning: absolute path resolution in `javaBin()` | ctl.mjs start/restart stable |
| 2026-08-10 | F2 | Visual targeting overlay in `BoardScene`: pulsating outlines, animated dotted lines from source to targets, player targeting rings | unit + typecheck ✅ |
| 2026-08-10 | F2 | UI mana payment: non-blocking `GAME_PLAY_MANA`, color pool buttons (`sendPlayerManaType`), board source tapping | unit + typecheck ✅ |
| 2026-08-10 | F2 | Human vs AI lobby workflows: creator auto-seat, AI seat joining | full-flow E2E ✅ |
| 2026-08-10 | F2 | Visual targeting E2E (`e2e/targeting.spec.ts`): browser-driven match with canvas pulse verification | 2× PASS in sequence |
| 2026-08-10 | F2 | Multi-target UX: `chosenTargets` parsed in `feedback.ts`, chosen targets highlighted in green with checkmark badge | unit 67/67 + typecheck ✅ |
| 2026-08-10 | F2 | `ADVANCED_DECK` (Blaze, Arc Trail, Boros Charm, Walking Ballista) + deck selector in `CreateTableDialog` | typecheck ✅ |
| 2026-08-15 | F2 | Verified Phase 2 close: 100-card AI deck, preferential land discarding, complete advanced scenario | `human-test` 83 checks ALL PASS ✅ |
| 2026-08-15 | F2 | Re-architected `e2e/spells.spec.ts` into 4 serial independent tests (Blaze, Arc Trail, Boros Charm, Ballista) | E2E 6/6 PASS ✅ |
| 2026-08-15 | F2 | Scene-driven E2E clicking: `window.__mageScene` exposes live card coordinates and playable state | Boros→Ballista misalignment eliminated ✅ |
| 2026-08-15 | F2 | Color-aware mana payment: `payMana` parses required mana strings and taps matching color sources | Clean payment without server re-asks ✅ |
| 2026-08-16 | F2 | Simulated Bot Opponent (`SIM` seat in proxy): `SimPlayer.java` with independent server session playing lands, spells, attacking, and blocking | Java tests 18 ✅; unit 90 ✅; combat/targeting/full-flow ✅ |
| 2026-08-16 | F2 | WS `HumanHelper` (`e2e/wshelper.ts`): node helper managing lands, discards, and priority yielding over WS | unit 90 ✅; typecheck ✅ |
| 2026-08-16 | Arq | Dual-mode deterministic testing: `FixtureServer` (`fixtures/fake.ts`), declarative scenarios, zod schema anti-drift testing | unit 95/95 + typecheck ✅; fake full-flow 9s ✅ |
| 2026-08-16 | Arq | Deterministic UI assertions: `BoardScene` publishes `__mageScene.targeting` replacing canvas byte-diffs | typecheck ✅ |
| 2026-08-16 | F2 | Resolved Sim victory bug: caused by degraded server state with orphaned proxy sessions; resolved by coupled restarts | targeting 12.9s ✅; spells 4/4 ✅ |
| 2026-08-16 | F2 | Resolved spectator freeze bug: `SimPlayer.tryCast` made color-aware to avoid infinite GAME_SELECT loops | trace verified; demo casts ✅ |
| 2026-08-17 | F2 | Modular E2E suite (`web/e2e/support/`): shared DSL libraries, fake scenarios in `fixtures/scenarios/` | fake 7/7 ✅; real 7/7 ✅; unit 97/97 |
| 2026-08-17 | Up | Upgraded to upstream XMage 1.4.61-V1: cleanly merged upstream tags, updated proxy parent POM, set default host to `beta.xmage.today` | full suite 8/8 PASS ✅; real E2E 7/7 PASS ✅ |
| 2026-08-17 | Fx | Handshake buffer for `SHOW_USERMESSAGE` in `ProxyClient.java`: buffers early server messages until connected | unit/typecheck/build/java/self-test PASS ✅ |
| 2026-08-18 | Arq | Complete Board UI layout redesign: CSS Grid macro layout, `SideLayout` zones with creature/other/land bands, easing repositioning | unit 122/122 ✅; typecheck ✅; build ✅ |
| 2026-08-20 | Arq | Store refactoring: split `store.ts` into 7 cohesive modules (`state`, `persistence`, `gameUtils`, `eventHandler`, `gateway`, `selectors`, `actions`) | unit 75/75 ✅; typecheck ✅; 0 breaking consumer changes |
| 2026-08-20 | Arq | Java→TS JSON Schema codegen pipeline: `contract.schema.json` with `gen-types.mjs` and CI validation | typecheck ✅; `--validate` PASS ✅ |
| 2026-08-20 | Fix | Non-modal floating combat prompt bar and step-gated blocker declarations | unit 75/75 ✅; typecheck ✅; build ✅ |
| 2026-08-20 | Fix | Verified human combat E2E 4/4 and restored `HumanGame.start()` in fixture server | vitest 104/104 ✅; combat-human 4/4 ✅ |
| 2026-08-21 | F2/F3 | Persistent connection & clean reconnect: 60s disconnect grace period, localStorage session caching, auto-rejoin on F5 | vitest 132/132 ✅; Java 18/18 ✅ |
| 2026-08-21 | F2/F3 | Token and Ability Art Resolution: Scryfall multi-tier token lookups, source card extraction for abilities, multi-face card tabs | vitest 140/140 ✅; typecheck ✅; build ✅ |
| 2026-08-21 | F2/F3 | Cascading LIFO Stack UI: vertical cascading stack with topmost active spell, thumbnail strips, and integrated Resolve button | vitest 145/145 ✅; StackZone.test.tsx ✅; build ✅ |
| 2026-08-21 | F2/F3 | Floating HD Card Preview & hover stabilization on hand and battlefield slots | vitest 149/149 ✅; FloatingCardPreview.test.tsx ✅; build ✅ |
| 2026-08-21 | F2/F3 | Floating action prompt bars (`GAME_PLAY_MANA`/`GAME_TARGET`) & `FormattedText` component for mana badges and entity decoding | vitest 154/154 ✅; FormattedText.test.tsx ✅; build ✅ |
| 2026-08-21 | F2/F3 | Comprehensive UI Modernization (Arena-grade): Reactive ActionButton with global Space key, unified PhaseBar with stop toggles, circular PlayerInfoBar with priority ring | vitest 154/154 ✅; typecheck ✅; build ✅; java ✅ |
| 2026-08-21 | Fix | Combat attacker/blocker toggle de-selection and tapped combatant state management | vitest 154/154 ✅; typecheck ✅; build ✅ |
| 2026-08-21 | F2/F3 | Vertical layout proportion fixes and expanded card-sized resource piles (~68x96px) | vitest 154/154 ✅; typecheck ✅; build ✅ |
| 2026-08-21 | Fase A1 | SVG Bézier Combat & Target Arrows overlay and player avatar target glow | vitest 156/156 ✅; typecheck ✅; build ✅ |
| 2026-08-21 | F2/F3 | Full Match HUD Parity: Player priority clocks, match win gems, revealed top library card, summon sickness spiral for creatures | vitest 156/156 ✅; typecheck ✅; build ✅ |
| 2026-08-21 | Fase A2 | Dedicated CommandZone: Commander card renders with tax badge (`+{tax}`) and Planeswalker emblem trays | vitest 159/159 ✅; typecheck ✅; build ✅ |
| 2026-08-21 | Fase A3 | Interactive Scry / Surveil / Library Order Dialog & revealed opponent hand cards | vitest 165/165 ✅; typecheck ✅; build ✅ |
| 2026-08-21 | Feature | Live Structured Action Feed (`ActionFeed.tsx` & `gameEventParser.ts`) with live game log subscriptions | vitest 183/183 ✅; typecheck ✅; build ✅ |
| 2026-08-21 | Fix | Game session isolation and stale packet drop guards | vitest 184/184 ✅; typecheck ✅; build ✅ |
| 2026-08-21 | Fase 3 | Redesigned Lobby and Login Screen: animated mana orbs, 3-column table grid, deck manager with text list import | vitest 194/194 ✅; typecheck ✅; build ✅ |
| 2026-08-21 | UI/Fix | Unified vertical Stack card template, compact 250px right panel, and global targeting arrows | vitest 195/195 ✅; typecheck ✅; build ✅ |
| 2026-08-21 | Fix | Transform / MDFC back face artwork resolution in card preview inspector | vitest 197/197 ✅; typecheck ✅; build ✅ |
| 2026-08-21 | Feature/Fix | Universal HTML/Text sanitizer in `FormattedText.tsx` & interactive card hover previews across all chats and logs | vitest 199/199 ✅; typecheck ✅; build ✅ |
| 2026-08-21 | Feature | Interactive Library Stack & Card Viewer with standard card back | vitest 203/203 ✅; typecheck ✅; build ✅ |
| 2026-08-21 | UI/UX | High-definition card modals (210px cards) for direct reading without zoom | vitest 203/203 ✅; typecheck ✅; build ✅ |
| 2026-08-21 | Feature/UX | Modern Lobby Table Filter Bar: Omnibox search, format chips, open seats filter, and collapsible advanced options | vitest 211/211 ✅; typecheck ✅; build ✅ |
| 2026-08-21 | Feature/UX | Interactive Spectator Staging Screen: live 1v1 and pod staging room for waiting tables | vitest 214/214 ✅; typecheck ✅; build ✅ |
| 2026-08-21 | Fix/UI | Sanitized global system messages in ChatBox and join/leave notification toggle | vitest 217/217 ✅; typecheck ✅; build ✅ |
| 2026-08-21 | Fix | Chat room isolation and automatic `leaveChat` cleanup on table exit | vitest 219/219 ✅; typecheck ✅; build ✅ |
| 2026-08-21 | Feature/UX | Quick Deck Selector on Join Table Dialog with inline text decklist importer | vitest 222/222 ✅; typecheck ✅; build ✅ |
| 2026-08-21 | Fix | Robust lobby user list extraction supporting Array of RoomUsersView and direct UsersView wire formats | vitest 226/226 ✅; typecheck ✅; build ✅; Playwright e2e ✅ |
| 2026-08-21 | Feature/UX | Advanced Table Creation Dialog with categorized tabs (General, Clocks & Rules, Security & Passwords, Bot Seats with Custom Decks) | vitest 230/230 ✅; typecheck ✅; build ✅; java ✅; Playwright e2e ✅ |
| 2026-08-22 | Feature/UX | MTG Keywords Hover Tooltips (Arena-style) & In-Client Rules Wiki Modal: dynamic rule parsing, bilingual glossary with live search, turn phases guide, keyboard shortcuts reference | vitest 293/293 ✅; typecheck ✅; build ✅; Playwright e2e ✅ |
| 2026-08-22 | Feature/UX | Fullscreen Web Mode (in-game sidebar & lobby top bar), Real-time Dynamic Priority Chess Clocks (`useTickingTimer`, hours formatting `1:55:08`), and Automated Extraction of 267 MTG Keywords from XMage rules engine | vitest 302/302 ✅; typecheck ✅; build ✅; Playwright e2e ✅ |
| 2026-08-24 | Feature/Fix | Priority & Stack Suite (Bloque B): Hold Priority toggle with Ctrl+Click, APNAP trigger ordering, Storm copy cascade stack view & Ability Tiles | vitest 307/307 ✅; typecheck ✅; Playwright E2E 23/23 ✅ |
| 2026-08-24 | Fix/UI | Battlefield Card Polish: Eliminated boolean-coercion text node "0" on undamaged creatures, vertical tab cascade for auras/attachments, top-left sickness badge relocation, and removed dead targeting overlay | vitest 309/309 ✅; typecheck ✅; Playwright E2E 23/23 ✅ |
| 2026-08-24 | Fix/UI | Modal & Arrow Layering: React Portal for `PileOverlay` (z-index 100), elevated feedback/grid backdrops (z-index 90), defeat overlay over arrows (z-index 30 vs 28), and filtered defeated player arrows | vitest 309/309 ✅; typecheck ✅; build ✅ |
| 2026-08-24 | Feature/UX | Stack Player Identification: Controller Ribbons with avatar, controller name, color-coded badges (cyan `[👤 Tú]` vs red `[🤖 Opponent]`) and left border accents across compact/expanded views | vitest 310/310 ✅; typecheck ✅; build ✅ |
| 2026-08-24 | UI/UX | HUD Corner Alignment & HD Card Preview: Anchored PlayerInfoBar to top-left (opponent) and bottom-left (player) corners, enlarged FloatingCardPreview to 320x448px with 240px keywords sidebar | vitest 310/310 ✅; typecheck ✅; build ✅ |
| 2026-08-24 | F2/F3 | Missing Server Prompts E2E Suite: new `missingPrompts` FixtureServer scenario + `missing-prompts.spec.ts` covering the 9 supported-but-untested `feedback.ts` prompts (`GAME_SELECT_PLAYER`, `GAME_CHOOSE_STRING` list + free-text, `GAME_CHOOSE_NUMBER`, `GAME_CHOOSE_ONE`, `GAME_CHOOSE_BETWEEN`, `GAME_CHOOSE_MODE`, `GAME_TARGET_AMOUNT`, `GAME_SELECT_CARDS` multi, `GAME_PLAY_XMANA`); fixed `FeedbackDialog` to render a free-text input for `GAME_CHOOSE_STRING` with no options; added 10 unit tests in `feedback.test.ts` | vitest 315/315 ✅; typecheck ✅; build ✅; e2e missing-prompts 1/1 ✅ (7.0s) · e2e 24/24 ✅ |
| 2026-08-24 | E2E/Tool | Global visual-verification screenshots: `autoShot` `auto:true` fixture in `e2e/fixtures.ts` captures a final `fullPage` PNG per test into `e2e/shots/<spec>__<title>.png` (and `-FAILED` on failure). Replaces the flaky `test.afterEach` approach (only fired for 1 test in combined runs); migrated `stack-priority.spec.ts` to import `test` from `./fixtures` so it inherits the fixture. All 24 E2E specs now emit a screenshot | e2e 24/24 ✅ (7.6m) · 24 screenshots generadas |
| 2026-08-24 | Fix/F2 | Callbacks perdidos del servidor: añadidos `GAME_CHOOSE_CARDS` y `GAME_TARGET_PLAYER` al parser `feedback.ts` (antes caían en `default`→`null` y se ignoraban en silencio). Eliminada la etiqueta `case 'GAME_SELECT':` redundante en el grupo de `GAME_SELECT_CARDS`/`GAME_SELECT_TARGETS` (código muerto inofensivo). Ampliado el escenario `missingPrompts`` y su E2E con los 2 prompts nuevos (flujo lineal: … CHOOSE_MODE → CHOOSE_CARDS → TARGET_PLAYER → TARGET_AMOUNT → SELECT_CARDS → PLAY_XMANA) | vitest 317/317 ✅; typecheck ✅; e2e missing-prompts 1/1 ✅ (8.0s) |
| 2026-08-24 | F2 | Callbacks críticos del servidor sin manejar: nuevo `UserRequestDialog.tsx` que renderiza `USER_REQUEST_DIALOG` (hasta 3 botones → `sendPlayerAction(PlayerAction)`), desbloqueando rollback / stop-until-* y otras confirmaciones que antes se ignoraban; `GAME_ERROR` ahora se muestra como error; `VIEW_LIMITED_DECK`/`VIEW_SIDEBOARD` abren `LimitedDeckDialog.tsx` (visor de cartas con `CardSlot`); `GAME_REDRAW_GUI` registrado. Nuevo estado `userRequest`/`viewer` en `state.ts`. Cubierto con `eventHandler.test.ts` (4 tests) y paso `USER_REQUEST_DIALOG` en `missing-prompts` E2E | vitest 321/321 ✅; typecheck ✅; e2e missing-prompts 1/1 ✅ (8.2s) |
| 2026-08-24 | Doc/Test | Registro de cobertura de interacciones: nueva matriz `web/INTERACTION_COVERAGE.md` (Tabla A: los 43 callbacks de `ClientCallbackMethod.java` con estado implementado/unit/E2E + ref; Tabla B: interacciones especiales como costes complejos/DFC/sagas/morph con su spec). Nueva guardia `web/src/state/callbackCoverage.test.ts` (capa `unit`) que lee el enum Java y falla si un callback no tiene `case` en `eventHandler.ts`/`feedback.ts` ni está en `KNOWN_UNHANDLED`, y si la matriz no lista todos los callbacks (anti-drift). `KNOWN_UNHANDLED` documenta Draft/Tournament/Replay como Slices A/B/C planeados | vitest 325/325 ✅; typecheck ✅ |
| 2026-08-24 | Doc | Catálogo completo de mecánicas de juego en `web/INTERACTION_COVERAGE.md`: la `Tabla B` se expandió a 13 categorías (A–M) cubriendo morfologías (MDFC/aventura/split/sagas/battles/tokens), adjuntos (aura/equipo/mutate), estados globales/contadores (Monarca/Anillo/Mazmorra/Día-Noche/Poison/Emblemas), keyword badges, info revelada, combate, stack/prioridad, maná/costes, elecciones/voting, biblioteca (scry/surveil/mill), planeswalkers, modos de juego (Commander/2HG) y miscelánea. Cada fila con estado ✅/⚠️/❌ auditado en `web/src` (CardPreview, CardSlot, OpponentZone, PlayerInfoBar, CommandZone, MechanicsTray, keywordExtractor, gameEventParser) y refs a specs E2E (mechanics, complex-costs, combat*, stack-priority). Documenta como pendientes: Mutate, Battles (spec), Sagas/Planeswalkers (solo render parcial), Scry/Surveil/Mill, Voting, Commander/2HG, Concede | typecheck ✅ |
| 2026-08-24 | Test | Cobertura E2E+unit de mecánicas implementadas sin test: enriquecido `fixtures/scenarios/mechanics.ts` (planeswalker con lealtad, token, battle, emblema y comandante en `commandList`, aura adjunta al oponente, `topCard` revelado, `game.revealed`) y ampliado `mechanics.spec.ts` (`@mechanics`) con aserciones de `.loyalty-badge`, `.defense-badge`, token, `.emblem-slot`, `.commander-slot`, `.attachment-subcard`, known hand y `.library-stack.has-top-revealed`. Añadido caso unitario en `feedback.test.ts` para el parser `GAME_CHOOSE_CARDS_ORDER`. Filas de `INTERACTION_COVERAGE.md` volteadas a ✅/✅ (tokens, auras/equipo, emblemas, poison/energy/city's blessing, revealed hand, lealtad PW, scry/surveil/mill, reorder, commander, battles) | vitest 326/326 ✅; typecheck ✅; e2e mechanics (fake) ✅ |
| 2026-08-24 | F2 | Concede real (T1): nueva `concedeGame(gameId)` en `actions.ts` que envía `PlayerAction.CONCEDE` al servidor (antes el botón solo hacía `returnToLobby`); usada por el botón `🏳️ Conceder` de `GameScreen` y el ítem `exit` de `Sidebar` (en espectador solo `returnToLobby`). Nuevo escenario `fixtures/scenarios/concede.ts` que responde a CONCEDE con `GAME_OVER`+`END_GAME_INFO`, y E2E `concede.spec.ts` (fake) + unit `concede.test.ts` que verifican el cableado y el retorno al lobby. Fila M (Concede) → ✅/✅ | vitest 328/328 ✅; typecheck ✅; build ✅; e2e concede (fake) ✅ |
| 2026-08-24 | F2/E2E | Mulligan (Keep + London): el auto-keep (`autoKeepMulligan`) impedía que la ventana de mulligan se pintara en los E2E; añadido gancho de debug `window.__mageStore.setSetting` y opción `autoKeepMulligan` en `startGame` para desactivarlo en test. Nuevo escenario estático `fixtures/scenarios/mulligan.ts` (no auto-avanza) que emite `GAME_ASK` de mulligan y, al elegir Mulligan, el `GAME_TARGET` de London-bottom. E2E `mulligan.spec.ts` (fake, 2 tests): (1) ventana visible + screenshot `shots/mulligan-01-window.png` + "Keep hand" → sigue la partida; (2) "Mulligan" → barra de target de London + clic en carta de mano → `sendPlayerUUID`. Los unit tests de mulligan ya existían (`feedback.test.ts`, `store.test.ts`). Fila M (Mulligan) → ref actualizada | vitest 328/328 ✅; typecheck ✅; build ✅; e2e mulligan (fake) 2/2 ✅ |
| 2026-08-24 | F2/UI | UI Mulligan dedicada (`MulliganDialog`): se añaden flags `isMulligan`/`isMulliganLondon` en `feedback.ts` (`prompt()` + GAME_ASK + GAME_TARGET con regex exacta `^select a card to put on the bottom of (your|the) library`) y `FeedbackPrompt`. `FeedbackDialog` enruta estos casos a `MulliganDialog` (nuevo), que reusa `HandZone`/`CardSlot` para mostrar la mano en abanico con arte real: en Keep/Mulligan dos botones grandes (verde "Mantener mano" / ámbar "Mulligan"); en London la mano es clicable para poner cartas al fondo (selección múltiple si `max>1`, contador). Reemplaza el modal genérico `.feedback-dialog` y la barra flotante `.action-prompt-bar.targeting-bar`. CSS en `MulliganDialog.css`. E2E `mulligan.spec.ts` actualizado a `.mulligan-dialog`. | vitest 328/328 ✅; typecheck ✅; build ✅; e2e mulligan (fake) 2/2 ✅ |
| 2026-08-24 | F2/Bug | Mulligan visible para el humano: el default `autoKeepMulligan` era `true`, así que `eventHandler` auto-contestaba el `GAME_ASK` de mulligan ("mantener") sin mostrar el diálogo → al crear mesa en localhost no aparecía la ventana. Cambios: (1) default `autoKeepMulligan: false` en `state.ts`; (2) guard de espectador en `eventHandler` (`isSpectator = !players.some(p => p.controlled)`) para que el auto-skip solo ocurra con el setting activo O siendo espectador (también en el ask de "jugador inicial"); (3) el helper E2E `startGame` ahora fuerza `autoKeepMulligan=true` salvo `false` explícito, preservando el resto de specs que dependían de auto-keep. El toggle en `GameScreen` sigue disponible. | vitest ✅; typecheck ✅; build ✅; e2e mulligan 2/2 + combat-multiblock ✅ |
| 2026-08-25 | F2 | Mecánica Mutate (fiel al protocolo real XMage): el server expone `PermanentView.mutateView` (tipo `MutateView` = `Record<id, CardView>`) con la composición de la pila (no solo el flag `mutated`). Añadido `MutateView`+`mutateView` al schema `contract.schema.json` y a `types.generated.ts` (edición manual, respetando la práctica de campos droppeados por `gen-types`). Render en `PlayerZone`/`OpponentZone`: rama `card-mutate-pile` con `.mutated-badge` (🧬) y sub-cards `.mutate-part` (constituyentes, arte Scryfall) detrás de la carta superior; se distingue de `.card-attachment-group` (auras/equipo). La activación de habilidades de la criatura fusionada ya funciona por el pipeline genérico (`canPlayObjects`→click→`sendPlayerUUID`→`GAME_CHOOSE_ABILITY`). Unit en `OpponentZone.test.tsx`; escenario `fixtures/scenarios/mutate.ts` + E2E `mutate.spec.ts` (`@mutate`, fake) que verifica pila/badge/parts y el click→`GAME_CHOOSE_ABILITY`. Fila Mutate en `INTERACTION_COVERAGE.md` → ✅/✅. Nota anti-drift: requiere verificar en modo real que el proxy serializa `mutateView` (Java `JsonUtil`); si no, pequeño fix en `Mage.Proxy`. | vitest 350/350 ✅; typecheck ✅; build ✅; e2e mutate (fake) 1/1 ✅ + regresión 12/12 ✅ |
| 2026-08-25 | F2/E2E-real | Verificación real de Mutate contra `beta.xmage.today`: el proxy (`Mage.Proxy`) conecta a beta (login OK, partida corre) y el mazo mutate IKO (formato `Constructed - Pioneer`, tierras `iko 272`) es aceptado por el servidor (criaturas + Forests en mano). **Hallazgo clave**: el proxy reenvía `mutateView` por reflexión (`JsonUtil.toJson` sobre el grafo de vistas XMage, `ProxyClient.java:317`); `PermanentView.mutateView` es `private final MutateView` (no static/transient) → llega al web intacto. **NO requiere cambio Java**. El play en vivo quedó BLOQUEADO por el modelo de sesión del proxy: el `HumanHelper` del harness abre un 2º WS con el mismo `username` que la página y `Mage.Proxy` rechaza sesiones duplicadas del mismo usuario (el `self-test` de Node sí funciona porque usa una sola conexión). Esto afecta a CUALQUIER E2E real que necesite el helper, no solo a mutate. Para cerrar el bucle real falta: (a) permitir la 2ª sesión en `Mage.Proxy`, o (b) grabar frames reales de una partida mutate (extender `scripts/self-test.mjs` o un recorder) y regenerar `fixtures/scenarios/mutate.ts` como fixture anti-drift. El render del cliente quedó verificado en fake; el forwarding del proxy, por inspección de código. | e2e mutate-real (real) corre contra beta pero el helper no conecta (sesión duplicada); fake mutate 1/1 ✅ |
| 2026-08-25 | F2 (Phase 2.5) | Match Chess Clock — cierre de paridad: el reloj de prioridad ya existía en `PlayerInfoBar` (`useTickingTimer` + badge `player-timer-badge`); se superficia `bufferTimeLeft` (campo del contrato hasta ahora sin usar) como subcadena `+MM:SS` y se añaden atajos de teclado **F4/F9** en `GameScreen` para alternar el "stop" de fin de turno (tu turno / turno del oponente) vía `updatePreferences`. Tests añadidos en `PlayerInfoBar.test.tsx` (badge con `priorityTimeLeftSecs`, clase `timer-low` ≤30s, buffer). `ROADMAP.md` §3 y `site/content.json` marcan "Match Clocks / Visible Timers" → ✅. Sin cambios de contrato → `callbackCoverage`/`mechanicsCoverage` siguen verdes. | vitest 352/352 (PlayerInfoBar + timer) ✅; typecheck ✅ |
| 2026-08-25 | F2 (Phase 2.5) | Cierre de DFC/Sagas y Card Selection Lists: (1) DFC/MDFC ya resuelven cara trasera en `cardImages.ts` (`isCardBackFace` considera `transformed`) y Sagas muestran contador `lore` (📖 + nº de capítulo) en `CardSlot.tsx:245`; (2) `GAME_CHOOSE_CARDS`/`GAME_SELECT_CARDS`/`GAME_SELECT_TARGETS` ahora enrutan a la grilla HD `CardGrid` (`FeedbackDialog.tsx`) en vez de la lista genérica de botones, con búsqueda y multi-select (`sendPlayerUUID`). Test añadido en `FeedbackDialog.test.tsx` (tutor → `CardGrid` → `sendPlayerUUID`). `ROADMAP.md` §3 y `site/content.json` marcan "Double-Faced Cards" y "Card Selection Lists" → ✅; `web/INTERACTION_COVERAGE.md` actualiza Sagas y añade fila de selección HD. | vitest 354/354 (FeedbackDialog + PlayerInfoBar + timer) ✅; typecheck ✅ |
| 2026-08-25 | F2 (Phase 2.5 follow-up) | Descarte interactivo desde reveal de mano (Thoughtseize): la mano revelada del oponente ya se muestra boca arriba en `OpponentZone` vía `game.revealed`+`game.opponentHands`; cuando el server pide descartar (`GAME_CHOOSE_CARDS`/`GAME_SELECT_TARGETS` con la mana ajena como `cardsView1`) la `FeedbackDialog` enruta a la grilla HD `CardGrid` y el clic envía `sendPlayerUUID` (descarte). Se añadió claridad de título: "Elige una carta para que descarte" cuando el mensaje coincide con `/descart|discard/i` (`feedback.ts`). Nuevo escenario fake `fixtures/scenarios/thoughtseize.ts` + spec E2E `e2e/reveal.spec.ts` (`@reveal`) que verifica diálogo, mano revelada, descarte y desaparición de la carta de la zona del oponente. `INTERACTION_COVERAGE.md` añade fila de descarte desde reveal. **Phase 2.5 queda completa.** | vitest 367/367 (feedback + FeedbackDialog + PlayerInfoBar + timer) ✅; typecheck ✅; e2e reveal 1/1 ✅ |
| 2026-08-25 | QA/E2E estability | Investigación y cierre de la cascada de fallos en la suite E2E fake (un spec lento/timbado envenenaba a los siguientes vía puerto compartido 8789). **Fix 1**: `missing-prompts.spec.ts` conducía `GAME_CHOOSE_CARDS`/`GAME_SELECT_CARDS` como `.feedback-dialog`/`.feedback-options`, pero `feedback.ts` los renderiza en `CardGrid` (`.card-grid-dialog`/`.card-grid-cell`) → los clics no aterrizaban. Corregido: `step()` genérico (`.feedback-dialog, .card-grid-dialog, .targeting-bar, .mana-prompt-bar`) y pasos 8/11 vía clic raw por `evaluate` sobre `.card-grid-cell` (el `.feedback-backdrop` intercepta el `.click()` de Playwright, igual que en `reveal.spec.ts`). **Fix 2**: `withFakeServer`/`FakeServer` ahora usan **puerto efímero por test** (`FakeServer.start(0)` + `getFakePort()/setFakePort()` en `e2e/support/fake-port.ts`) en vez del 8789 fijo; login y `HumanHelper` leen el puerto dinámico (`getFakePort()`), eliminando colisiones TIME_WAIT entre specs secuenciales. El server compartido por worker (`fixtures.ts`) sigue en 8789. | tsc ✅; vitest ✅; `npx playwright test` (suite fake completa) 32 passed / 0 failed / 2 skipped ✅; `missing-prompts` 3/3 ✅; `reveal` 20/20 ✅ |
| 2026-08-25 | UI/Refactor | Refactorizado simétrico del tablero (`BoardZone.tsx` + `BoardZone.css`): unifica la estructura de filas para jugador y oponente en 1vs1 y Commander (modo estándar y POD 2x2). La fila superior es el espejo exacto de la inferior (criaturas enfrentadas al divisor central, tierras al medio, barra de estado en el borde exterior). Corrige el recorte vertical de cartas (reemplazo de flex asimétrico 42%/58% por 50%/50% y corrección de `useZoneScale.ts` con cálculo exacto de altura segura). Animaciones espejo en manos (inferior hacia arriba, superior hacia abajo). `PlayerZone` y `OpponentZone` delegan a `BoardZone`. | vitest 381/381 ✅; typecheck ✅; build ✅; e2e 35/35 ✅ |
| 2026-08-26 | F5 (Stage 0) | Contrato Draft/Tournament: `contract.schema.json` añade `DraftView/DraftPickView/DraftClientMessage/DeckView/TableClientMessage/TournamentView/RoundView/TournamentPlayerView/TournamentGameView` + `SimpleCardView` extendido (expansionSetCode/cardNumber); `types.generated.ts` regen 414→430L. `ProxyClient.java` añade 12 actions (`createTournamentTable/joinTournamentTable/watchTournamentTable/getTournament/quitTournament/quitDraft/sendCardPick/sendCardMark/setBoosterLoaded/replayNext/Previous/SkipForward/stopReplay` + `getTournamentTypes/getDraftCubes` + `parseTournamentOptions`) y `state.ts` `draft/tournament/construct/replayViewer`. `eventHandler.ts` maneja `START_DRAFT/DRAFT_* (4)`, `CONSTRUCT`, `TOURNAMENT_* (5)`, `REPLAY_* (4)`; `callbackCoverage` vacía allowlist (todos manejados). | vitest 402 ✅; typecheck ✅; `mvn -pl Mage.Proxy -am test` 18/18 ✅ |
| 2026-08-26 | F5 Agent B | Draft & Sealed Construct: `DraftScreen.tsx` (booster grid SimpleCardsView + Scryfall, timeout live, `sendCardPick`/`setBoosterLoaded`/`sendCardMark`, picks tray) + `ConstructScreen.tsx` (pool 40 pool→deck con `ArenaCardStrip`, agrupación, timer auto-submit, `submitDeck`) + `CreateTableDialog.tsx` (LimitedDraftOptions + `MAX_COMMANDER=4`/`MAX_DRAFT=8`/`getEffectiveMaxPlayers`, UI draftSets/boosters/constructionTime). Fixture `scenarios/draft.ts` + `App.tsx` overlay. | vitest 409 ✅; typecheck ✅; build ✅; e2e draft 2/2 ✅ |
| 2026-08-26 | F5 Agent C | Torneo Swiss/Bracket: `TournamentBracket.tsx` (rounds bracket + standings sorted points) + `TournamentStandings.tsx` + `TournamentPanel.tsx` (overlay `state.tournament` con `TournamentBracket` compact) + `LobbyScreen.tsx` (`Ver bracket` + `getTournament` polling). Fixture `scenarios/tournament.ts` (sample 4 players/2 rounds). | vitest 417 ✅; typecheck ✅; e2e tournament 2/2 ✅ |
| 2026-08-26 | F5 Agent A | Commander Pod 4-max (sin tocar `BoardZone.css`): `PodBoard.tsx` wrapper `TwoHeadedBoard` (`clampedGame` `slice(0,4)` + `TurnOrderRing` + `CommanderDamageMatrix`) + `TurnOrderRing.tsx` (circular 3-4, pill 2) + `CommanderDamageMatrix.tsx` (Target×Commander 21 lethal) + `GameScreen.tsx` (`boardLayout==='pod' ? PodBoard : GameBoard`). `PodBoard.test.tsx` 12. `MAX_POD_PLAYERS=4` en `PodBoard/TurnOrderRing/CommanderDamageMatrix` + `CreateTableDialog.getEffectiveMaxPlayers` (XMage hard limit). `BoardZone.css` intacto verificado `git diff --stat`. | vitest 429 ✅; typecheck ✅; build ✅ |
| 2026-08-26 | F5 Integración | Fase 5 completa: todos los callbacks `INTERACTION_COVERAGE.md` Tabla A → ✅ (15 Slices A/B/C a log/UI), L/Tournament/Replay en `site/content.json` `done 2026-08-26` + `PROJECT.md` §2. Verif: `vitest 67 files 429` ✅; `typecheck` ✅; `vite build 193kB` ✅; `mvn Proxy 18` ✅; `playwright draft 2 + tournament 2 + PodBoard 12 unit` ✅; `git diff` sin `BoardZone.css` | — |
| 2026-08-28 | QA/Antidrift | **4º guard ServerState** (`scripts/server-state-schema.mjs` → `web/fixtures/server-state-schema.json` 17 gameTypes/52 deckTypes/4 playerTypes/21 tournamentTypes/46 cubes desde `Mage.Server/config/config.xml` + `MatchType` limits): `web/src/state/serverStateCoverage.test.ts` (7 tests: schema up-to-date, DEFAULT_GAME_TYPES/DEFAULT_DECK_TYPES válidos y exhaustivos, FakeServer válido). `CreateTableDialog.tsx` DEFAULTs expandidos a exhaustivos 1.4.61-V1 (`Two Player Duel`…`Custom Pillar`, `Constructed - Standard`…`Limited` 52), `fake.ts`/`draft.ts`/`tournament.ts` corregidos (`Three/Four Player`→`Free For All`/`Commander Free For All`, `Commander`→`Variant Magic - Commander`). `web/package.json` `gen-server-state[:validate]`, `web-ci.yml` step `Validate server-state schema`. `site/content.json` 4º guard `serverStateCoverage`. | vitest 68/436 ✅; typecheck ✅; build 715kB ✅; `gen-server-state:validate` ✅ |
| 2026-08-30 | UI/Commander | **Commander & Pod Layout Polish**: Desacoplados `TurnOrderRing` y `CommanderDamageMatrix` de `PodBoard` ganando 100% de altura de viewport en partidas de 4 jugadores con escalado de cartas dinámico (`clamp(80px, 6.8vw, 118px)`). `TurnOrderRing` integrado en la cabecera superior como cápsula de progresión de turno con avatares, prioridad y vidas. `CommanderDamageMatrix` movido a pestaña dedicada `👑 CMD` en el sidebar con diseño vertical en tarjetas de jugador (`.cdm-player-card`), barras de progreso codificadas por color (0-7, 8-14, 15-20, 21 letal), banner de alerta de peligro inminente y selector de vista `[📊 / ⊞]`. Rastreo exhaustivo de daño de comandante multizona (`battlefield`, `graveyard`, `commandList`, `counters`). | vitest 74 files / 461 tests ✅; typecheck ✅; build ✅; Playwright e2e ✅ |
| 2026-08-30 | Feature/Images | **XMage Image & Symbol Download Manager**: Implementado servicio de descarga persistente (`imageDownloader.ts`) y modal oficial de descarga (`DownloadImagesDialog.tsx`/`.css`). Soporta fuentes Scryfall (Normal ~10GB, Large ~15GB, Small ~1.5GB), alcances completos (`- ALL images` vía Scryfall Bulk Data, `- STANDARD`, `- MODERN`, `- COMMANDER`, `- MIS MAZOS`, `- TIERRAS BÁSICAS`, `- FICHAS`, o sets individuales), multihilo configurable (1-10 hilos), descarga de símbolos de maná y fases (`symbology` SVG), y caché persistente de navegador con `CacheStorage` (`xmage-card-images-v1`/`xmage-symbols-v1`) integrado con `cardImages.ts` para carga offline instantánea. Botón `📥 Imágenes` en la barra lateral del Lobby. | vitest 76 files / 466 tests ✅; typecheck ✅; build ✅ |
| 2026-08-30 | Feature/i18n | **Multi-Language (i18n) Engine & Card Artwork Localization**: Sistema tipado y reactivo de internacionalización (`web/src/i18n/`) con 9 idiomas completos de interfaz y cartas (`es`, `en`, `de`, `fr`, `it`, `pt`, `ru`, `ja`, `zhs`), hook `useTranslation()`, selector visual con bandera (`LanguageSelector.tsx`), selector en pantalla de Login y en Lobby, persistencia en `localStorage` (`nexus_lang`) y soporte para ilustraciones multilingües de Scryfall (`nexus_card_lang`) en `cardImages.ts` con fallback automático. | vitest 78 files / 473 tests ✅; typecheck ✅; build ✅ |
| 2026-09-02 | Fix/UX | **Saneamiento integral de diálogos y modales de juego**: Eliminación de metadatos de servidor (`queryType="ASK"`, `autoAnswerMessage`, UUIDs, nombres de cartas) colándose como opciones en `GAME_ASK` (ej. advertencia de maná abierto en pool) extrayendo limpiamente `UI.left.btn.text` (`true`) y `UI.right.btn.text` (`false`); rescate de `secondMessage` como `sourceName` (subtítulo de contexto); soporte para `choices` (`Set`/Array de Java) en `GAME_CHOOSE_CHOICE` cuando `keyChoices` es `{}` (resuelve `ChoiceColor` y `ChoiceBasicLandType`) evitando leak de campos DTO internos de Java; resolución de nombres de objetivo en `GAME_TARGET` para cartas en pila (`game.stack`), cementerios (`graveyard`), exilio (`exile`) y comandancia (`commandList`), eliminando etiquetas crípticas `Objetivo 1 (hex)`; y filtrado reactivo de opciones al escribir en el buscador de modo texto (`mode === 'string'`) en `FeedbackDialog`. | vitest 89 files / 581 tests ✅; typecheck ✅; build ✅; Playwright e2e 46/46 ✅ |
| 2026-09-02 | Feature/Lobby | **Ready Check interactivo (🟢/🟡) y Cambiar Baraja en la Sala de Espera**: (1) Sistema de confirmación de preparación por jugador: botón interactivo `staging-toggle-ready` con etiquetas dinámicas `No estoy listo` / `Estoy listo`, badges de estado (`🟢 Conectado / Listo` vs `🟡 Preparándose…` con animación pulsante), propagación en tiempo real a través del chat de la sala con eventos formateados (`🟢 [Usuario] está listo para jugar.` / `🟡 [Usuario] aún no está listo`), y bloqueo preventivo del botón de inicio del anfitrión hasta que todos los jugadores humanos hayan confirmado. (2) Botón `🃏 Cambiar baraja` en la barra de acciones de la sala de espera: apertura de modal selector de barajas compatibles (`JoinTableDialog` reutilizable con títulos y etiquetas customizables) que re-conecta fluidamente al invitado (`leaveTable` + `joinTable`) o advierte y recrea la mesa si es el anfitrión. (3) Corrección en la extracción del anfitrión para soportar cadenas `controllerName` del servidor XMage con comas (`split(',')[0].trim()`). Soporte completo en los 9 idiomas. | vitest 89 files / 591 tests ✅; typecheck ✅; build ✅; Playwright e2e staging 4/4 ✅ |
| 2026-09-03 | Feature/Audio | **Sistema completo de audio y efectos de sonido táctiles**: Motor de audio nativo Web Audio API (`soundManager.ts`) con 3 buses (`masterGain`, `sfxGain`, `uiGain`), desbloqueo automático de autoplay (`click`, `pointerdown`, `keydown`, `touchstart` + buffer silencioso de wake-up), anti-amontonamiento y jitter orgánico de pitch (±4%); sintetizador foley de alta fidelidad (`soundSynthesizer.ts`) para 15 eventos (`tap`, `play_card`, `draw` cálido filtrado con filtro paso bajo de 2 polos sin estática, `stack_cast`, `priority`, `combat_hit`, `life_gain`, `life_loss`, `destroy`, `game_start`, `victory`, `defeat`, `timer_tick`, `ui_click`, `whisper`); despacho en tiempo real (`gameSoundDispatcher.ts` y `flightManager.ts`) ante transiciones visuales de cartas, clics en `CardSlot.tsx`, variaciones de vidas y entrada de hechizos a la pila; controles de volumen y toggle en el popover de ajustes del `Sidebar.tsx` con persistencia en `localStorage` (`mage-web-audio`); traducciones en los 9 idiomas. | vitest 91 files / 630 tests ✅; typecheck ✅; build ✅ |
| 2026-09-03 | Feature/UI | **Rollback in-game, Inspector de mazos, Curva de maná y Localización del Stack**: (1) Diálogo interactivo de Rollback (`RollbackDialog.tsx`) accesible desde barra superior y sidebar con selector de turnos a retroceder y confirmación. (2) Apertura del inspector de barajas (`DeckInspectorModal.tsx`) mediante doble clic en la galería de mazos (`DecksGallery.tsx`) y en el editor de mazos (`DeckBuilder.tsx`/`ArenaDeckHeader.tsx`). (3) Curva de maná estilo Arena (`CurveChart.tsx`) excluyendo tierras del CMC de hechizos, cuadrícula de métricas del mazo (Tierras, Criaturas, Hechizos, Avg CMC) y desglose de símbolos de color SVG oficiales. (4) Rediseño de ancho completo y responsive del modal de Clasificación y Perfil (`LeaderboardModal.tsx`). (5) Traducción exhaustiva de rangos y descripciones en 9 idiomas. (6) Localización dinámica de nombres de cartas y habilidades en el Stack (`cardLocalization.ts`/`StackZone.tsx`) vía Scryfall `printed_name` con caché local. | vitest 95 files / 651 tests ✅; typecheck ✅; build ✅ |
| 2026-09-03 | UI/i18n | **Dock de acciones en panel derecho, iconos contextuales y traducción integral de mensajes de servidor**: (1) Reubicación de la barra de acciones (`.action-prompt-bar`) al dock derecho fijo sobre el botón de prioridad/resolución sin obstruir zonas de juego. (2) Sorteo de jugador inicial directamente seleccionable en los avatares del tablero con destello dorado y descarte interactivo en HandBar (+24px y pulso de objetivo). (3) Asignación de iconos precisos por contexto (🎯 objetivos, 🎲 sorteo, 🗑️ descarte, ⚡ maná, ⚔️ combate, ✨ modo, 🎨 color, 🏷️ nombre, 🔢 número, 📊 cantidades, 👤 jugador, 📦 pila, ❓ confirmación). (4) Normalizador y traductor de mensajes del servidor XMage (`serverMessageTranslation.ts`) para eliminar fugas de inglés (Pay costs, Select target, Choose mode/color/player, Take a mulligan, Discard, Yes/No option buttons, etc.) en los 9 idiomas. (5) Localización de paso de turno superior (`Turno N`), nombres de fases de `PhaseBar` en tooltips y estados ON/OFF. | vitest 96 files / 663 tests ✅; typecheck ✅; build ✅ |
| 2026-09-03 | Feature/i18n | **Soporte Multilingüe Integral en el Deck Builder (9 idiomas)**: (1) Motor de búsqueda Scryfall multilingüe (`scryfallSearch.ts`): queries con `lang:<lang>` y fallback suave a `lang:any`, soporte para `printed_name`, `printed_type_line` e ilustraciones localizadas. (2) Selector visual de idioma de cartas en la barra de búsqueda del constructor (`ArenaFilterBar.tsx`) y en la cabecera del mazo (`ArenaDeckHeader.tsx`), sincronizado con la configuración global de idioma de cartas. (3) Localización de fichas de tipo de carta (Criatura, Instantáneo, Conjuro, etc.) en los 9 idiomas. (4) Tiras de cartas (`ArenaCardStrip.tsx`), cuadrícula (`ArenaCardGrid.tsx`), inspector (`DeckInspectorModal.tsx`) e inspector de impresiones mostrando `displayName` (`printed_name`) local con tooltip con nombre original en inglés para claridad. (5) Barra de tierras básicas (`BasicLandAdder.tsx` y `deckUtils.ts`) con etiquetas traducidas a los 9 idiomas. (6) Importador universal de MTG Arena (`parseDck.ts`): reconocimiento de encabezados en cualquier idioma (`Mazo`, `Banquillo`, `Колода`, `デッキ`, `套牌`, etc.) y normalización transparente de nombres de tierras básicas extranjeras a inglés canónico para compatibilidad estricta con el servidor Java XMage. | vitest 98 files / 677 tests ✅; typecheck ✅; build ✅ |
| 2026-09-03 | UI/DeckBuilder | **Deck Builder Full-Width & Header Polish**: (1) Eliminada la restricción de ancho fijo `max-width: 1920px` y márgenes laterales centrados en `.deck-builder-body`, tomando ahora el 100% del ancho del contenedor en pantallas anchas y ultrapanorámicas. (2) Reorganización y espaciado de `.arena-deck-header`: portada compacta (38px), clúster derecho independiente para mini-curva, botón de estadísticas 📊 y botón de layout, evitando cualquier colisión o pisado entre controles. (3) Selector de idiomas reubicado fuera de la cabecera con `overflow:hidden` hacia la barra superior (`arena-nav-right`) y barra de búsqueda (`ArenaFilterBar`). (4) Adaptación de `.lobby-main.has-deck-builder` sin márgenes residuales (`padding: 0`) y ensanchamiento responsive de la columna del mazo (380px-420px) para máxima legibilidad. | vitest 98 files / 677 tests ✅; typecheck ✅; build ✅ |
| 2026-09-03 | UI/ManaCurve | **Curva de Maná Dedicada y Colapsable en el Panel del Mazo**: (1) Eliminada la mini-curva encogida de 28px de la cabecera del mazo (`ArenaDeckHeader`), dejando la cabecera limpia, espaciosa y sin congestión. (2) Nuevo bloque dedicado y colapsable `.deck-curve-panel` ubicado entre la cabecera y las tierras básicas, integrado con `CurveChart.tsx` (barras de 70px de alto, anchos de 24px, conteo numérico dorado por coste CMC 0..7+, 4 métricas de resumen: Tierras, Criaturas, Hechizos y Coste medio, y desglose de símbolos SVG oficiales de maná W/U/B/R/G/C). (3) Control bidireccional de plegado/desplegado vía botón `▲ / ▼` en el panel o botón `📊` en la cabecera, con memoria en `localStorage` (`nexus_deck_show_curve`). (4) Acceso directo a detalles completos del inspector modal (`🔍 📊`). | vitest 98 files / 677 tests ✅; typecheck ✅; build ✅ |
| 2026-09-03 | UI/HoverPreview | **Ampliación HD del Hover Preview en el Deck Builder**: (1) Aumentado el ancho de previsualización flotante de 250px a **350px** (y 490px de alto), proporcionando texto de reglas grande, nítido y legible sin esfuerzo visual. (2) Soporte DFC con ancho ampliado adaptativo (hasta 714px para cartas de doble cara cara frontal + cara trasera). (3) Cálculo de coordenadas en pantalla centrado verticalmente respecto a la carta sobre la que se pasa el cursor y con márgenes de seguridad para no salirse de los bordes del viewport. (4) Badges de cara frontal/trasera estilizados y sombra de relieve profunda con halo dorado. | vitest 98 files / 677 tests ✅; typecheck ✅; build ✅ |
| 2026-09-05 | UI/PodLayout | **Fix mano POD a 3 jugadores + refactor medio de layouts**: la `HandBar` estaba constreñida al 50% izquierdo (`TwoHeadedBoard.css`) asumiendo 4 jugadores; con 2-3 la fila inferior colapsa a ancho completo y la mano quedaba pegada a la izquierda. `TwoHeadedBoard` añade clases `pod-board--bottom-full/--top-full` y la mano pasa a 100% cuando `botRight` está vacío; divisores verticales huérfanos solo se renderizan si ambas celdas existen; celdas vacías ya no montan `BoardZone` con `player undefined`. Refactor: nuevo `board/boardShared.ts` (`BoardProps` único, `MAX_BOARD_PLAYERS=4`, `useSpectatorBottomHand`) adoptado por `GameBoard/ArenaBoard/PodBoard/TwoHeadedBoard`, `TurnOrderRing` y `CommanderDamageMatrix`; `GameScreen` construye un solo `boardProps`; espectador unificado con `useBoardPlayers(game, true)`; eliminado `game/layout-new/constants.ts` muerto. | vitest 105 files / 751 tests ✅ (3 regresión POD nuevos); typecheck ✅ |
| 2026-09-05 | UI/PodLayout | **División estática criaturas/tierras en POD (sin partición dinámica)**: en `compactPod` la banda vacía colapsaba (`display:none` + track `0px`), así que cada primera tierra/criatura "partía" el tablero y re-escalaba todas las cartas (`useZoneScale` calculaba `cardRows=1`). `BoardZone.css` reserva siempre ambas bandas (`auto 1fr 1fr` / `1fr 1fr auto` como el estándar) y la banda vacía muestra un slot punteado; `useZoneScale` fija `cardRows=2` siempre. Regla global `zone-empty` intacta. Nuevo spec e2e fake `pod-bands.spec.ts` (2 tests: banda visible con altura aunque vacía) verificado rojo→verde contra el CSS anterior. Aplica también a Arena (comparte `compactPod`). | vitest 751 ✅; typecheck ✅; playwright `pod-bands` 2/2 ✅ (2/2 rojo sin el fix) |
| 2026-09-05 | UI/Commander | **Marca de turno en la zona (vista enfocada Commander)**: la cabecera no era el sitio (revertido el anillo 1v1 en `GameScreen` + borrado su spec). `PlayerInfoBar` detecta jugador activo (`game.activePlayerId`, fallback `player.isActive`) y muestra glow dorado `is-turn` + etiqueta `▶ Activo` (clave `board.turn_active_badge` existente, sin i18n nuevo); suprimida en derrotados; distinguible de la prioridad (verde/naranja). Spec e2e fake `commander-turn-mark.spec.ts` (marca en rival activo y cambia al pasar el turno). | vitest 754 ✅ (3 tests `PlayerInfoBar` nuevos); typecheck ✅; playwright `commander-turn-mark` 1/1 ✅ |
| 2026-09-05 | UI/Switcher | **Switcher con orden implícito en estándar multi**: `OpponentSwitcherBar` pasa de paginador a tira de orden — props `players` (todos, en orden de turno, posiciones estables) + `controlledId`; mi píldora visible pero desactivada (`is-self`, sin clic); derrotados atenuados y fuera del ciclo ‹ ›; flechas `→` entre píldoras + `↺` de cierre circular, con `is-active-edge` dorada tras el jugador activo (mismo lenguaje que el anillo, sin su volumen). Regla `null` con ≤1 enfocable intacta (1v1 sin switcher). Tests unitarios ampliados + spec e2e fake `switcher-order.spec.ts`. | vitest 757 ✅; typecheck ✅; playwright `switcher-order` + `turn-mark` + `pod-bands` 4/4 ✅ |
| 2026-09-05 | Parity/U11-U13 | **Auditoría jugabilidad desktop↔web (U11/U12/U13) en `COMPONENT_PARITY.md`**: 9 gaps (G11-1 skips F5/F6/F7/F10/F11/F3, G11-2 trigger-order, G11-3 auto-answers, G11-4 macros, G12-1 permisos de mano, G12-2 visores looked-at/companion/sideboard, G12-3 menú contextual sin cablear, G12-4 phased-out, G13-1 prefs auto-pago maná); resto ✅. Evidencia por grep (ausencias) + `commands.test.ts:51` (transporte soporta `PASS_PRIORITY_UNTIL_*`). | docs-only (sin suite requerida) ✅ |
| 2026-09-05 | UI/GameShell+Skips | **Pantalla de partida centrada en el juego + skips one-shot (cierra G11-1, dual fake/real)**: header slim de solo-estado (turno, `PhaseBar`, ring/switcher; relojes fuera — ya viven en la cara del jugador) + menú ⋯ único (`GameMenu` con sección Vista de mesa: radios nombradas Estándar/Pod/Arena sin descripción — Arena solo en multi; Sidebar eliminado); `ActionButton` split-button `Pasar ▾` con `PassMenu` (6 skips F4/F5/F7/F9/F10/F11, F3 cancela solo con skip activo, hold-priority, auto-pass; stops solo en la `PhaseBar`); auto-mulligan contextual en `MulliganDialog`; skip activo con borde dorado + `skip_active_to` desde flags `passed*`; i18n 12 claves ×9 locales + wiki. Sin F6 (desktop lo tiene desactivado). Mano pod 1v1 al 100% (decisión A; spec `hand-bar` actualizado). Real: ok proxy a las 7 acciones + eco `passedAllTurns` + limpieza F3 permanente vía `HumanHelper.sendPlayerAction` (doctrina WS). Lecciones e2e-real: usernames ≤14 chars (mismatch página/helper cuelga `start()`), `isEnabled()` espera (sonda con `toBeEnabled` acotado), cursores inútiles con buffer-anillo de 500 (polls sobre última vista a 100ms), `startMatch`/`waitTableReady` tolerantes a staging, click forzado en lista viva. | vitest 772 ✅; typecheck ✅; `skips`+`concede` fake ✅; `skips` real 2× ✅; fake-full 59/59 ✅ |
| 2026-09-05 | UI/Density | **Densidad pod/arena (F1–F4)**: maná inline (6 mini-pips siempre visibles, sin popup en compacto) + pilas micro a grid 2×2 (`ResourceBar`); dorsos enemigos colapsados a stack ×N con badge 👁️ solo si hay conocidas que ver (`HandZone`); visor de mano rival (`HandViewer`: conocidas clicables para targeting + dorsos acotados a 12, Escape) cableado en `BoardZone` (G12-2 parcial: mano ✅); i18n 2 claves ×9. | vitest 783 ✅; typecheck ✅; e2e `pod-bands` +2 (stack/visor) ✅; `thoughtseize/reveal/hand-bar/arena` ✅ |
| 2026-09-05 | UI/PodArenaFix | **Fix espejo POD + ancho status compacta + sin badge de turno**: (1) `BoardZone` desacopla posición (`zone-top/bottom`) de rol (`opponent/player-zone`) — el rival de la esquina inferior era `player-zone` y el `row-reverse` del espejo nunca aplicaba (avatar al revés); selector POD ahora por `.mirrored`. (2) Tira de maná micro a grid 3×2 (pips 12px) + status compacta con `wrap` a 2 líneas (`max-height` 56→92, `ResourceBar.micro` altura auto) para que quepa en Arena 3-4j; `useZoneScale` con `defaultStatusH` 64 en compacto + observa la status row (sin salto al jugar la primera carta). (3) Fuera el badge `▶ Activo` de `PlayerInfoBar` (queda el glow dorado `is-turn` de la caja); CSS `.status-turn` eliminado, clave i18n conservada (la usa `TurnOrderRing`). | vitest 784 ✅; typecheck ✅; e2e `pod-bands`+`arena-board`+`hand-bar` 7/7 ✅ |
| 2026-09-05 | UI/PodArenaFix2 | **Anclaje status en vacío + abajo-der como el jugador + rayo siempre visible**: (1) `zone-empty` ocultaba las bandas con `display:none` y el auto-placement metía la status (3ª en DOM) en la fila 1 de 0px — visible por `overflow:visible` pero pegada al divisor hasta jugar carta; anclaje explícito `grid-row: 1/3` en `BoardZone.css`. (2) Fuera el `row-reverse` POD: abajo-derecha con cara izquierda y recursos derecha, igual que el jugador. (3) El chip ⚡ micro solo salía con jugables (`crossZone.length>0`) mientras el `ray-stack` normal sale siempre — ahora el `ray-chip` sale siempre en mi lado con contador (0 sin dot) y se oculta en rivales; 2 tests nuevos. | vitest 786 ✅; typecheck ✅; e2e tableros 7/7 ✅ |
| 2026-09-05 | UI/HandTarget | **Revert mano + clic siempre + objetivo legible en la PILA**: (1) Revertida la expansión de dorsos objetivo (`HandZone` vuelve al stack ×N único); a cambio el stack es **siempre clicable** y abre el visor aunque todo esté oculto (dorsos + contador; anillo azul + 👁️ solo con conocidas). (2) Cada entrada de la pila muestra siempre su objetivo inline `🎯 → 👤 jugador, permanente` (resuelve ids contra jugadores/campo/pila/cementerio/exilio/comando, mismo criterio que las flechas; sin i18n nuevo). | vitest 788 ✅ (StackZone 2 nuevos); typecheck ✅; e2e tableros+pila 8/8 ✅ |
| 2026-09-05 | UI/HandTarget2 | **Visor a tamaño carta + zonas vacías siempre visibles**: (1) Los dorsos del `HandViewer` medían 34px (contenedor fijo + inner 100%) frente a 210px de las conocidas — primero a 210px y tras ver captura, a **88px medianos** en fila con wrap (7 clónicos a tamaño carta llenaban el modal sin informar); las conocidas siguen a 210px. El spec `pod-bands` de visor pasa a aserciones por `data-card-name` (con red las imágenes Scryfall cargan y el nombre solo vive en el `alt`). (2) Quitado el colapso `zone-empty` (`auto 0px 0px` + `display:none` en `BoardZone.css`): cada jugador pinta sus dos bandas punteadas desde el turno 0, sin salto de escala al jugar la primera carta (alinea CSS con `useZoneScale cardRows=2`); placeholder punteado generalizado a todos los layouts, sin texto. | vitest 790 ✅ (2 tests nuevos); typecheck ✅; e2e `pod-bands`+`arena-board`+`hand-bar`+`stack-priority` 8/8 ✅ |
| 2026-09-05 | UI/HandTarget3 | **Visor de mano con el mismo cuerpo que el grimorio**: las ocultas se pintan como entradas `.pile-card-wrapper` con badge `#N` (numeración continua tras las conocidas), sin cap ni contador y en fila única con scroll horizontal — mismo markup y CSS que `PileOverlay`; eliminado `HandViewer.css` (el tamaño lo da `.pile-card`). Se mantienen header `Ver mano (X de Y)` y clics de targeting en conocidas. Spec `pod-bands` con `data-testid="hand-viewer-back"` + badge `#3`. | vitest 790 ✅; typecheck ✅; e2e `pod-bands`+`hand-bar` 6/6 ✅ |
| 2026-09-05 | UI/TurnOrder | **La barra superior mostraba el orden de turnos al revés**: `GameView` vuelca `players` en orden de mapa (`table added order`, `GameState.java:65`), pero los turnos avanzan en orden inverso porque `PlayerList` se construye con `CircularList.add()` que inserta en cabeza (`GameState.java:740-748`, verificado). `TurnOrderRing` y `OpponentSwitcherBar` iteran ahora una copia invertida (flecha dorada al verdadero siguiente; `‹ ›` navega el orden mostrado). Solo header (las celdas Pod son asientos). Límite: `isTurnOrderReversed` no se serializa, no se detectan inversiones mid-game. De paso: spec `commander-turn-mark` pedía el badge `status-turn` eliminado en F5 (roto desde entonces) — pasado a glow-only `is-turn`. | vitest 790 ✅; typecheck ✅; e2e `switcher-order`+`commander-turn-mark`+`pod-bands`+`arena-board` 7/7 ✅ |
| 2026-09-05 | UI/G11-2 | **Trigger order (G11-2 cerrado)**: el prompt `PICK_ABILITY` del servidor ya no cae en el grid genérico — nuevo `TriggerOrderDialog` (1 fila por trigger con arte+regla, Elegir + ⏫/⏬ con alcance por carta o por texto + ↺ reset + ayuda LIFO) vía flag `isTriggerOrder` (`detect.ts` por `queryType`/mensaje, `parse.ts`, routing en `FeedbackDialog`) + `sendTriggerAutoOrder` en `commands.ts` (mismas `PlayerAction` que desktop). Hallazgo: el proxy pasaba `data` como String y el servidor la metía en la rama NAME — `JsonArgs.parseActionData(data, action)` convierte a UUID en `TRIGGER_AUTO_ORDER_ABILITY_*` (+`JsonArgsTest` 5 tests). Reset también en `GameMenu` ⋯. Regla por texto aproximada de `CardView.rules` (`{this}`→nombre); documentado. | vitest 801 ✅; typecheck ✅; java 147 ✅ (142+5); jar rebuilt + proxy restarted; e2e `trigger-order` 2/2 ✅ |
| 2026-09-05 | UI/G11-3 | **Auto-respuestas (G11-3 cerrado, solo-cliente)**: casilla *"Responder siempre así"* en diálogos Sí/No (`GAME_ASK` booleano; nunca mulligan/voting/starting) → reglas `{texto exacto→Sí/No}` (`game/autoAnswers.ts`, match normalizado sin comodines como desktop) persistidas en `localStorage` (`mage-web-auto-answers` vía slice `settings`, sobreviven entre partidas); intercepción en `prompts.ts handleGameAsk` con rastro `auto:` en el log; gestión/olvido en `GameMenu` ⋯ (lista+badges+✕+borrar todas). Cero Java/proxy. | vitest 808 ✅; typecheck ✅; build ✅; e2e `auto-answers` 1/1 ✅ |
| 2026-09-05 | UI/G13-1 | **Prefs pago de maná (G13-1 cerrado, último gap gameplay)**: sección *"Pago de maná"* en `GameMenu` ⋯ (Automatically def ON / pool-manual def ON / primera habilidad def OFF + confirmar pool def ON, con tooltips de `PlayAreaPanel.java:227-274`); cada toggle → `sendPlayerAction` (`MANA_AUTO_PAYMENT_ON/OFF`, `MANA_AUTO_PAYMENT_RESTRICTED_ON/OFF`, `USE_FIRST_MANA_ABILITY_ON/OFF`, `data=null`, proxy genérico sin Java) + re-emisión de las 3 de servidor al `GAME_INIT` (`events/game.ts`, solo si hay jugador controlado); pips del pool propio clicables (`ResourceBar` inline+breakdown, `PlayerResourcePanel`) → `sendPlayerManaType` (`WHITE/…` para `ManaType.valueOf`); pasar (botón/Espacio/F-keys, no F3-cancel) con pool>0 → diálogo local (ver fix 2026-09-06 abajo). `useFirstManaAbility` sin lector en motor (paridad de envío, efecto cliente). Spec fake-only como el resto de specs humanos (`fakeOnly`). | vitest 813 ✅; typecheck ✅; build ✅; e2e `mana-payment` 3/3 fake ✅ |
| 2026-09-05 | UI/G13-1-real | **Contrato real G13-1 pendiente (deuda anti-drift, 6 intentos)**: (a) en local, la sesión página+helper (mismo usuario) muere con `Ping failed` ~15-30s tras conectar (`proxy*.log`: `SessionImpl.ping MageException`; el servidor registra doble `connected` + `Disconnecting another user instance (same host)`) y el `startMatch` nunca llega — single-connection sí funciona (self-test 15/15 en caliente tras `restart all`); (b) en beta, el SIM se une a mesas viejas de runs anteriores (nombres fijos colisionan en servidor compartido) y con nombre único la mesa 0/2 nunca recibe al SIM en 20s. Queda `helper.payManaType()` en `wshelper.ts` para el futuro test real estilo multi-user. | self-test local 15/15 ✅ (1ª en frío falla WATCHGAME = flake conocido); real `@manapayment` ⏳ |
| 2026-09-06 | UI/G13-1-fix | **Corrección con capturas del usuario: el confirm de pool lo emite el servidor (diálogo local eliminado)**. Con la pref ON salían DOS modales; con OFF seguía saliendo el del servidor (`GAME_ASK` "…it will be lost. Pass anyway?", que además ya soportaba auto-respuesta G11-3). Causa: `HumanPlayer.passWithManaPoolCheck` (`Mage.Server.Plugins/Mage.Player.Human`, fuera de `Mage/` — el grep inicial lo falló) pregunta si `userData.confirmEmptyManaPool()` (default true; la web nunca lo enviaba). Fix: eliminado `ManaPoolConfirmDialog` + interceptación en `GameScreen` (`poolTotal`/`shouldConfirmEmptyPool` fuera); el 4º check envía `updatePreferences {confirmEmptyManaPool}` (proxy `GameCommands.userDataFromPreferences` + `GameCommandsTest` 3 tests; `User.setUserData` muta in-place vía `update()` → aplica en vivo incluso mid-game) + re-emisión al `GAME_INIT`. Lección: auditar flags de `UserData` también en `Mage.Server.Plugins`. | java 8 ✅; vitest ✅; typecheck ✅; build ✅; e2e `mana-payment` 3/3 fake ✅; self-test local 15/15 ✅ (jar rebuilt + `restart all`) |
| 2026-09-06 | UI/U2 | **Lobby mesas (U2 cerrada)**: réplica desktop en `TableFilterBar` — orden default libres-primero+recientes (`TablesPanel.java:305-367`) con selector Recientes/Antiguas; toggles simétricos solo-rated/solo-no-rated y ocultar-privadas/solo-privadas; sub-filtro torneo Construidos/Limitados (regex desktop `Booster|Sealed|Jumpstart`); ocultar mesas de ignorados con toggle (OFF por defecto) + aviso al unirse/espectar (`useTableActions`); doble-clic unirse/espectar (`TableCard`); la búsqueda por texto propia se conserva (el desktop no la tiene). i18n 11 claves × 9 locales. | vitest `TableFilterBar` 13/13 ✅; typecheck ✅ |
| 2026-09-06 | UI/Icons | **Adiós emojis cutres: set abierto Lucide (`lucide-react` ISC) en `ui/Icon` (~70 nombres semánticos)**. El `Icon` artesanal ya cubría partida/tablero — se extendió con Lucide en vez de duplicar. Barrido completo lobby+mazos+partida+sistema+9 locales (kickers de feedback, feed de acciones, badges, ranks por tier, wizards, diálogos, kickers wiki por categoría). Se conservan: símbolos de maná (`ArenaManaSymbols`), banderas de país, ✓/✕/×/→ y glifos tipográficos, emojis que envían los usuarios (chat/reactions) y el campo `icon` de datos wiki (invisible, iconos ahora por categoría). | vitest 815 ✅; typecheck ✅; build ✅; e2e fake staging 4/4 + full-flow/spells 5/5 ✅ |
| 2026-09-06 | UI/Zoom | **Zoom continuo estilo navegador sustituye a los presets discretos**: `appearance/zoom.ts` (0.5–2.0, paso 0.1, redondeo a 2 decimales para no romper el preset 1.15, clamp), atajos globales `Cmd/Ctrl + = / − / 0` con `preventDefault` (sin conflicto: Ctrl/Meta solo se usaba en clic), mecanismo `zoom` CSS en `<html>` (escala layout px + cartas, no solo texto rem) manteniendo `--cjk-boost` tipográfico. Los presets conviven como paradas (Aa/A+/A++ en header + stepper `− % +` y stops en Configuración, % en vivo con clic-a-100%). **Fix CJK**: `Noto Sans JP/SC` cargadas desde Google Fonts (se referenciaban pero nunca se cargaban). `UiScale` → `ZoomLevel = number` con migración automática de valores guardados. | vitest 826 ✅; typecheck ✅; build ✅; e2e fake `zoom.spec` 2/2 + `settings.spec` 1/1 ✅ |
| 2026-09-06 | UI/Settings | **Configuración global unificada (`settings/SettingsModal`) accesible desde login y header del lobby**: sidebar de texto sin iconos (Idioma / Interfaz / Tablero / Sonido y efectos / Partida) + panel de sección; responsive a pestañas horizontales en móvil. Reutiliza controles existentes sin duplicar: stepper/stops de zoom, fundas, sonidos/FX extraídos a `SoundFxControls` compartido con `GameMenu` (DOM idéntico, `GameMenu.test` intacto). Idiomas como listas de radio de texto (las banderas emoji del dropdown se quedan fuera del modal); layout sin glifos `▭⊞⬒`. Sin sección Perfil: el avatar se elige en el login por diseño (viaja en el handshake de conexión). i18n 5 claves × 9 locales. | vitest 826 ✅; typecheck ✅; build ✅; e2e fake `settings.spec` 1/1 ✅ |
| 2026-09-06 | Lobby/U3 | **Wizard crear mesa cerrado (U3 ✅)**: la auditoría §F 2026-09-04 estaba desactualizada (commits `2dde1a731b`+`3d520b7808` habían cerrado F1/F3/F4/F5/F8/F10/U1/U3/U4/U6/U7 sin documentar) → reescrita como delta verificado. Restante implementado: skill por plaza 1-10 default 2 (`SeatConfig.skill`, `seatSkills` en `createTable` → `SimManager.seatSkillAt` → `SimPlayer`, antes skill 0 fijo) + skill propia (antes 1 fijo, desktop default 2), input de vetados en SecurityTab (proxy ya parseaba `bannedUsers`), rondas de torneo con UI (0=auto, proxy ya lo aceptaba), range/attack por flags del servidor (`GameTypeView.useRange/useAttackOption`, fallback a la heurística), `goNext` bloquea con aviso (nombre+compatibilidad+ocupantes), hint vivo por plaza (cartas del mazo + aviso si vacío). F6 emblemas `.dck` declarado fuera de alcance (experimental desktop, 3 capas). i18n 8 claves × 9 locales. | vitest 839 ✅ (`CreateTableDialog` 7/7); typecheck ✅; build ✅; java `SimPlayerTest` 5/5 ✅ (jar rebuilt + proxy restarted); e2e fake `wizard.spec.ts` 2/2 ✅ (incl. aserción del frame WS `createTable` con `bannedUsers`+`seatSkills`) |
| 2026-09-06 | Lobby/ChatOverlap | **El chat flotante tapaba botones del contenido (regresión del rediseño)**: `decks-gallery` fallaba (click en "Importar Mazo" colgado — el panel cubría la columna derecha del builder). Causa: panel abierto por defecto (`useState(true)`) sin reserva de espacio (antes el aside era in-flow). Fix: panel **cerrado por defecto** (FAB con badge de no-leídos) + preferencia persistida (`floating_chat_open`) + `.lobby-main` reserva 380px vía `:has(> aside.floating-chat)` cuando está abierto (solo desktop). Specs actualizados (`chat.spec` abre vía FAB; `decks-gallery` verifica panel visible junto al builder). | vitest lobby 96 ✅; e2e `decks-gallery`+`chat`+`wizard` 9/9 ✅ |
| 2026-09-06 | Lobby/FullFlow | **Spec `full-flow` roto por el rediseño (colgaba la capa e2e)**: clicaba `.hero-create-btn` de la sidebar desktop eliminada → esperaba 120s y el presupuesto de 600s de la capa mataba la suite. Fix: entra por el topstrip ("Nueva Mesa" → paso Dev → demo IA vs IA). | e2e `full-flow` 1/1 ✅ (5.8s) |
| 2026-09-06 | E2E-real/AttachLocalhost | **Todos los e2e reales con helper morían en el login (sesión expulsada, `ping failed`)**: el proxy normalizaba `localhost→127.0.0.1` en `ProxyClient.connect` pero la clave `host\|username` de `Gateway.handleConnect` usaba el host crudo → el 2º connect (helper WS, misma cuenta) no adjuntaba sino que duplicaba sesión y el servidor expulsaba a la primera (`Disconnecting another user instance (same host)`). Fix: `ProxyClient.normalizeHost()` compartido (Gateway + connect) + logs attach/NEW + test `normalizeHostMapsLocalhostToLoopback`. Con beta no pasaba (sin normalización). | java ✅ (incl. test nuevo); e2e real: combat/skips/full-flow/multi-user/staging/wizard vuelven a pasar |
| 2026-09-06 | E2E-real/Puertos | **`decks-gallery` y `deckvalidation` apuntaban en real al puerto muerto del fake** (`BACKEND_PORT` 8789 en vez de 8787): `dual.proxyPort()` dinámico (`getFakePort()` en fake — cada `withFakeServer` usa puerto efímero —, 8787 en real). | gallery 4/4 ✅ real; deckvalidation builder ✅ real y fake |
| 2026-09-06 | E2E-real/Alcance | **12 specs estructuralmente fake-only ahora usan `fakeOnly()`** (convención existente de `e2e/support/fake-mode.ts`): auto-answers, keywords, mana-payment, mechanics, mutate, mutate-recorded, planeswalker, recorded, stack-priority, trigger-order, voting (+ deckvalidation join-flow ×2 con skip explícito: el dueño no puede abandonar sin que el servidor borre la mesa — `TableController.leaveTable` — y `mutate.real` solo con host beta). En real esos tests no pueden pasar (cartas/situaciones solo del escenario). | fake 74 ✅; real pasa de 26 failed a solo flakes de carga |
| 2026-09-06 | Lobby/U4 | **Auditoría y cierre U4 (unirse/espera/staging)**: comparativa fina `JoinTableDialog`+`TableWaitingDialog` vs web → 1 gap funcional puro + 2 paths torneo + polish. Implementado: (A) `swapSeats` (proxy `TableCommands` + `SessionImpl.swapSeats`, botones ↑/↓ por asiento solo dueño en READY); (B) torneo (`joinTournamentTable` directo sin diálogo en limitado no-construido sin password como `TablesPanel`, `startTournament` en proxy + `startStagedMatch` por `isTournament`/`flag` de `JOINED_TABLE` → `stagingIsTournament`, start con `confirm` si falta ready — aviso, no bloqueo); (C) roster rico (rating limited/constructed + history + `CountryFlag` por asiento; ratings añadidos al contrato `SeatView` + regen `gen-types`/`gen-zod`). Stretch: caché password, sonidos, chat por mesa. Docs: paridad U4 ✅ + § auditoría | `test.mjs` 7/9 (unit 119 files/857 ✅, coverage/typecheck/build/java/self-test/i18n ✅, human-test 79/79 en re-run ✅); e2e fake full **79 pass / 2 skip / 0 fail** (12m, `staging.spec` 8/8); e2e layer de `test.mjs` muere por su timeout de 600s (la suite fake ya tarda ~720s — subir el timeout del harness) |
| 2026-09-06 | E2E-real/U4 | **Fallo pre-existente, NO regresión U4** (verificado con `git stash`: falla idéntico en HEAD limpio): `multi-user.spec.ts` real no pasa del wizard — el mazo humano por defecto `DEFAULT_DECK` (`decks.ts:28`, 4+12=16 Lightning Bolt, así desde el 2026-08-25) es ilegal en Modern y el servidor rechaza el join (`Too many: 16`). Pendiente (fuera de U4): que el helper e2e elija un mazo legal o que el wizard ofrezca el `fixedDeck` en ese caso. `mutate.real` sigue siendo solo-beta (best-effort) | stash-test: mismo fallo sin mis cambios ✅ (mi código no lo causa) |
| 2026-09-06 | Lobby/U3-HUMAN | **Plazas HUMAN en espera en el creador (F11, único gap real tras el cierre U3)**: el wizard solo vendía bots por plaza; el desktop (`TablePlayerPanel=HUMAN`) deja sillas vacías esperando rivales y el protocolo ya lo soportaba (`MatchOptionsParser` pasa `HUMAN`, `SimManager` lo salta — sin cambio Java). Implementado solo-web: opción `Humano — espera rival` primera en el desplegable por plaza + chip global, skill oculta y sin mazo/validación para esas plazas (`simDecks` ausente si no hay SIM), pill `👤 +N en espera` en el resumen, header con desglose espera/bots, error `create_err_no_seats` neutro (1 clave × 9 locales); tipos normalizados a nombres canónicos del enum (`constants.normalizeSeatType`: `Computer - mad`→`COMPUTER_MAD`, `COMPUTER_DRAFT`→`COMPUTER_DRAFT_BOT` — este último antes rompía `PlayerType.valueOf` en el proxy). §F sincronizada (F2/F7/F9/U2/U5/U8 pasaban como pendientes estando hechos). | vitest 844 ✅ (`CreateTableDialog` 8/8 + `constants.test` 4/4); typecheck ✅; e2e fake `wizard.spec.ts` 3/3 ✅ (nuevo HUMAN: `playerTypes=['HUMAN','HUMAN']` sin `simDecks`) |
| 2026-09-06 | E2E-real/Flakes | **Flakes de carga en suite real completa**: en runs largos `skips` (fila sin aparecer) y esporádicamente `combat`/`multi-user` fallan; solos o por parejas pasan siempre. Causas: hilos de juego huérfanos acumulados (`maxGameThreads=10`, el afterEach solo limpia mesas) + **interferencia manual confirmada** (sesiones `player1` del navegador del usuario arrancando mesas durante los runs, p.ej. `skips-test` a las 19:15). Pendiente: limpieza que mate partidas + nombres de mesa únicos por run. El proxy además murió una vez sin excepción (18:27–18:40, causa desconocida) — vigilar. | — |
| 2026-09-06 | Lobby/U4-StagingRedesign | **Rediseño sala de espera por fases (pedido usuario, verificado con capturas)**: F1 full-bleed (fuera `max-width:1400px`, grid `1fr 400px`, arena con scroll interno + barra de acciones sticky), vacíos compactos (chip `＋ Plaza #N · Disponible` en parrilla / fantasma numerado en anillo), insignia de orden ①-⑩ + resaltado dorado "mi asiento"; F2 stepper `Mesa ✓ → Jugadores N/M → Listos → ¡A jugar!` (5 claves i18n ×9); F3 anillo pod 3-6 (posicionamiento angular, centro N/M, cae a lista vertical <700px) + parrilla compacta 7-10; F4 swap siempre visible al dueño (↺↻ en anillo, ↑↓ en lineal/parrilla, deshabilitado con tooltip `staging_seat_move_locked` si no READY — antes invisible y parecía bug) + highlight `seat-just-moved` (el anillo además transiciona `left/top`); F5 drag&drop dueño-en-READY (HTML5 DnD, `handleSwapPair` directo par arbitrario, highlight `seat-drop-target`, flechas como fallback táctil/teclado). Corrección docs: el "max 4 Commander" es clamp de pintado web (`MAX_BOARD_PLAYERS`), el servidor soporta FFA 3-10 (`CommanderFreeForAllType.java`) — `INTERACTION_COVERAGE.md` + `site/content.json` actualizados. | vitest 119 files/858 ✅ (18/18 staging incl. DnD nuevo); typecheck ✅; build ✅; e2e fake `staging.spec` 8/8 ✅; capturas duelo+anillo verificadas |
| 2026-09-07 | Lobby/U4-11 | **Chat por mesa en la sala (paridad desktop, pedido usuario)**: proxy `getTableChatId` (`InfoCommands`, `SessionImpl.getTableChatId`) + test 2 nuevos (`TableStagingCommandsTest` 7/7); web `getTableChatId` + `tableChatId/tableChatTableId` en slice lobby + `enterTableChat/exitTableChat` (join al entrar por `openStagingTable`/`JOINED_TABLE`/espectar, leave al salir/arrancar/desconectar, no-op si misma mesa, degradado a global si no hay chat) + routing `events/chat.ts` acepta el canal mesa + `ChatBox chatIdOverride` + marcas `[NEXUS_READY]` a la mesa (filtradas por canal) + 3 claves i18n ×9; fake `getTableChatId/joinChat/leaveChat/sendChatMessage` con eco. **Hallazgo real**: el `ChatMessage` del servidor NO trae `chatId` en `data` (solo `objectId` del envelope) — el proxy lo inyecta ahora (`ProxyClient.processCallback`, README actualizado), alineando real con el contrato `ChatMessageEvent.chatId` requerido y el fake. | java `TableStagingCommandsTest` 7/7 ✅ (jar rebuilt + proxy restarted); vitest 865 ✅ (`store.test` +7); typecheck ✅; e2e fake `staging-chat.spec` 2/2 + `staging.spec` 8/8 + `chat.spec` ✅; verificación real 11/11 (2 sesiones misma mesa ven mensajes, mesas distintas aisladas, espectador recibe, `data.chatId` presente) |
| 2026-09-07 | Lobby/U5 | **Auditoría y cierre U5 (chat)**: comparativa `ChatPanelBasic/Separated` + `PlayersChatPanel` + `LocalCommands` + comandos de servidor (`ChatManagerImpl`: `/w`, `/history`, `/me`, `/card`, `/list`) vs `ChatBox`/`GameChat`/`FloatingChat`/`ignoreList` → sin gaps funcionales (whisper, comandos, card-hover, split Talk/System ≈ canales, usuarios, ventana ≈ flotante arrastrable). Delta implementado: hora tenue por mensaje (U5-2, del `time` del evento), mensaje propio en verde (U5-4) en sala/mesa y partida, límite 500 con aviso i18n `chat_too_long` (U5-3; `GameChat` ya tenía `maxLength`); `time?` añadido a `ChatMessageEvent`, `time` en el eco del fake. Diferencias declaradas por el usuario: ignore solo-cliente (sin bloqueo de joins), profanity fuera de alcance. | vitest `ChatBox.test` 7/7 (+3) ✅; typecheck ✅; build ✅; e2e fake `chat.spec` + `staging-chat.spec` (asserts de hora) ✅; verificación real 8/8 (`WHISPER_FROM/TO` A↔B, `/history` y `/card` responden `USER_INFO`, el servidor acepta >500 — el límite es solo UX) |
| 2026-09-07 | Decks/U6 | **Auditoría y cierre U6 (editor mazos)**: comparativa `DeckEditorPanel/CardSelector/DeckArea/DeckLegalityPanel` + importers/exporters + `AddLandDialog` + `collection/viewer` vs `decks/*` (69 ficheros). Delta: **U6-1** selector 6 órdenes + dirección (`order`/`dir` Scryfall, i18n ×9) + slider tamaño carta 90–190px + fix doble-fetch pág≥2 con idioma; **U6-2** Mana Analyser (fuentes tierras/no-tierras, donut 6 básicas + no-básicas, barras pips×CMC; `oracleText` en el meta); **U6-3** +4 formatos (Oathbreaker `oathbreaker`, PD Commander `penny`, Highlander EU/CA sin clave Scryfall) + Moxfield oathbreaker; **U6-4/5** import `.dek` XML MTGO + `.cod` + `.o8d` (DOMParser) + export `.dek` + `.dek` en pickers + `exportDck` tolera cartas sin impresión. No-aplica declarado: MageBook/colección local, Open Booster, Bling, Draft Rating, LAYOUT posicional, `dck_info`; resto import (`.mwdeck/.draft/.json`) → U7; Generate → U8. Incidencia: el 7º botón del footer pasó a 3 filas y tapaba la tira en el e2e → footer a 4/fila. | vitest 883 ✅ (+15 U6); typecheck ✅; build ✅; e2e fake `decks-gallery` 5/5 (test U6 nuevo: sort/slider/`.cod`/`.dek`) + `deckvalidation` 3/3 ✅ |
| 2026-09-07 | Decks/U7 | **Auditoría y cierre U7 (import/export/sample)**: comparativa `DeckImportClipboardDialog/DeckExportClipboardDialog` + `DeckImporter` (9 ext) + `Txt/Mtga/MWS/DraftLog/Mtgjson` importers + 4 exportadores vs `DeckImportModal/parseDck/downloadDeckFile/SampleHandModal/onlineDeckService`. Delta: **U7-1** `parseDraftLog` (sets por sección); **U7-2** `parseMtgjson` + `.json` en galería desambiguando backup vs mazo; **U7-3** variante `N [SET] Nombre`, switch a banquillo por primera línea vacía MTGO (el propio `.dek` ya reimporta bien), comentarios `#`, cabeceras con conteo; **U7-4** `.mtga/.mwdeck/.draft/.json` en pickers + etiqueta formatos ×9; **U7-5** botón pegar-portapapeles; **U7-6** Commander→principal en cabeza (=cover→zona 👑) + Maybeboard→banquillo (divergencia justificada: el desktop no muestra zona); **U7-7** Partner con 2 coronas + unión de identidad detectada por datos del oráculo (`keywords`, sin mantenimiento manual). | vitest 894 ✅ (+11 U7); typecheck ✅; build ✅; e2e fake `decks-gallery` 6/6 (test U7: commander/maybeboard/`.draft`/pegado) + `deckvalidation` 3/3 ✅ |
| 2026-09-08 | Draft/U9 | **Auditoría y cierre U9 (draft)**: comparativa `DraftPanel/DraftGrid/DraftPane/DraftPickLogger` vs `DraftScreen`. Delta: **U9-1** ocultar pickeadas (botón 👁 + F9 + enlace ocultas + `hiddenCards` en el pick, el proxy ya lo aceptaba); **U9-2** confirm quit (i18n ×9); **U9-3** tira mesa/asientos + dirección ←/→ (campo `players` antes ignorado); **U9-4** timer naranja ≤30s + `timer_tick` a 6s + aviso con pestaña oculta; **U9-5** anti-doble-pick 1.5s; **U9-6** `draftLog.ts` + botón descarga `.draft` reimportable por U7-1 (test roundtrip); **U9-7** sobre ordenado por rareza (verificado: el servidor no ordena, `SimpleCardView` sin rareza; ranking = `Rarity.getSorting()`). Fix colateral: `DraftScreen`+`ConstructScreen` montados 2× (`App`+`GameScreen`) → solo `App`; el preview flotante del draft interceptaba el puntero (sin regla CSS) → `pointer-events:none`. | vitest 903 ✅ (+9 U9); typecheck ✅; build ✅; e2e fake `draft.spec` 3/3 (test U9: mesa/ocultar/F9/log) ✅ |
| 2026-09-08 | Tourn/U10-A | **U10 Fase A (quick wins)**: **T1** ojo por match clicable (`bracket-watch` + `watchTournamentMatch`→`watchTournamentTable(matchTableId)`, cableado en modal lobby y panel de partida; semántica verificada en fuente: `roomWatchTournament`→`tableManager().watchTable`, igual que desktop); **T2** flags de creación torneo (skill/rated/rollback/clocks/minRating/quitRatio/bans al `tArgs` + `rated/bannedUsers/rollbackTurnsAllowed` en `parseTournamentOptions` + checkbox `isSingleMultiplayerGame` i18n ×9); **T3** confirm al salir (×9, lobby+partida), fechas inicio/fin, cuenta atrás de construcción en estado Constructing, nº rondas solo en tipos Swiss. Jar del proxy reconstruido + reiniciado. Pendiente Fase B (chat torneo, RandomPacksSelector, timing draft) + verificación real de ver un match vivo. | vitest 912 ✅ (+9); typecheck ✅; build ✅; java proxy 45 ✅ (+1); e2e fake `tournament.spec` 2/2 (T1: panel + modal vía WS) ✅ |
| 2026-09-08 | Tourn/U10-B | **U10 Fase B + cierre (chat, packs, timing)**: **T4** chat de torneo solo en `TournamentPanel` (decisión: el id correcto solo existe ahí) — `getTournamentChatId` + `enter/exitTournamentChat` + `<ChatBox chatIdOverride>` + allowlist `chat.ts`; salida en unmount/`TOURNAMENT_OVER`/lobby/disconnect; `FakeServer` + escenario con `getTournamentChatId` y eco. **T5** `RandomPacksSelector` fiel al desktop — comando proxy `getExpansionsWithBoosters` (solo proxy, sin fork: `ExpansionRepository.instance`, `[]` si la BD no está lista; documentado en README) + diálogo (todos marcados, all/none, shuffle+truncado `3×(j+1)`/RichMan 36, `;`-join; `parseLimitedSetCodes` parte `;`). **T6** timing de draft — select BEGINNER/REGULAR/PROFESSIONAL (def. REGULAR) solo tipos con `Draft` + `timing` en `limitedOptions` + `DraftOptions` en `MatchOptionsParser` (sin fork). Jar reconstruido + proxy reiniciado ×2. Fila U10→✅ + §U10 en `COMPONENT_PARITY.md`. | vitest 925 ✅ (+13: T6 1, T4 5, T5 7); typecheck ✅; build ✅; java proxy 48 ✅ (+3: timing 2, expansions 1); e2e fake `tournament.spec` 4/4 (T4 chat con eco, T5 diálogo→input) ✅ |
| 2026-09-08 | E2E pendientes | **Cierre de los 2 pendientes conocidos**: **multi-user real** — causa confirmada en captura (el servidor rechaza el join humano: `Lightning Bolt: Too many: 16` en Modern); fix quirúrgico `createTable(..., { deck: 'Mage Web starter' })` en el spec (sin tocar `DEFAULT_DECK`, load-bearing en fake) → real 1/1 ✅. **Ver match vivo de torneo en real** — nuevo `scripts/verify-tournament-watch.mjs` (headless WS, Constructed Elimination 2×COMPUTER_MAD: los HUMAN/SIM no sirven — el proxy no expone `joinTournament`): **13/13 PASS** (`WATCHGAME` + `GAME_INIT` 2 jugadores tras `watchTournamentTable(matchTableId)` — el ojo T1 verificado de punta a punta). Hallazgos de robustez del parser (NPEs del servidor si faltan claves): `password`→`''` y `LimitedOptions` por defecto siempre (con test). | multi-user real 1/1 ✅; verify-tournament-watch 13/13 ✅; java proxy 49 ✅ (+1) |
| 2026-09-08 | Game/U12 | **Auditoría y cierre U12 (zonas y jugador)**: **G12-1** menú clic-derecho en `PlayerInfoBar` → `PlayerContextMenu` (portal, items por bando en `playerMenu.ts` puro) + 5 wrappers `commands.ts` + `allowHandRequests` persistido + `UserRequestDialog` envía `relatedUserId` como `data` + Switch Hands (toggle ⟲ en zona propia con `opponentHands`); **G12-2** disparadores `VIEW_SIDEBOARD/VIEW_LIMITED_DECK` (ya pintaban) + `InfoWindows.tsx` auto por presencia en vista; **G12-3** `CARD_CONTEXT_ITEMS` eliminado (sin menú de carta en desktop para gameplay; gesto cubierto a nivel jugador); **G12-4** filtro `phasedIn !== false` en `BoardZone`. Fixes colaterales con test: proxy enviaba `allowRequestShowHandCards=false` (default desktop true, 1 línea en `ProxyClient`) y `JsonArgs` no convertía String→UUID en las 4 acciones (grant silencioso sin esto); `?? []` dentro del selector de `useStore` = loop infinito (cazado en e2e). i18n 9 claves ×9. | vitest 127 files/951 ✅; typecheck ✅; build ✅; java 167 ✅; self-test ✅; human-test ✅; e2e fake **89 pass/2 skip** ✅ (player-menu 2/2, info-windows 1/1); `test.mjs` 7 pass/2 fail solo por timeout 600s del harness e2e + coverage (re-verde tras añadir tests de commands: 80.4%/71.3%); `verify-hand-permission.mjs` **19/19 ×2 en real** |


---

## 10. Execution Notes

- Rule: At the completion of each step, update Section 6 (checklist) and Section 9 (changelog) with verified reality.
- Verification must always be performed against the live environment (local server + proxy), never in theory.
- Document any discoveries in Section 5 (lessons learned).
